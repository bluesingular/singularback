import "./chunk-OS7ZSSJM.js";

// ../node_modules/.pnpm/hermes-paperclip-adapter@0.2.0/node_modules/hermes-paperclip-adapter/dist/shared/constants.js
var DEFAULT_TIMEOUT_SEC = 300;
var TOOL_OUTPUT_PREFIX = "┊";

// ../node_modules/.pnpm/hermes-paperclip-adapter@0.2.0/node_modules/hermes-paperclip-adapter/dist/ui/parse-stdout.js
function stripKaomoji(text) {
  return text.replace(/[(][^()]{2,20}[)]\s*/gu, "").trim();
}
function isAssistantToolLine(stripped) {
  return /^┊\s*💬/.test(stripped);
}
function extractAssistantText(line) {
  return line.replace(/^[\s┊]*💬\s*/, "").trim();
}
function parseToolCompletionLine(line) {
  let cleaned = line.trim().replace(/^\[done\]\s*/, "");
  if (!cleaned.startsWith(TOOL_OUTPUT_PREFIX))
    return null;
  cleaned = cleaned.slice(TOOL_OUTPUT_PREFIX.length);
  cleaned = stripKaomoji(cleaned).trim();
  const durationMatch = cleaned.match(/([\d.]+s)\s*(?:\([\d.]+s\))?\s*$/);
  const duration = durationMatch ? durationMatch[1] : "";
  let verbAndDetail = durationMatch ? cleaned.slice(0, cleaned.lastIndexOf(durationMatch[0])).trim() : cleaned;
  const hasError = /\[(?:exit \d+|error|full)\]/.test(verbAndDetail) || /\[error\]\s*$/.test(cleaned);
  const parts = verbAndDetail.match(/^(\S+)\s+(.*)/);
  if (!parts) {
    return { name: "tool", detail: verbAndDetail, duration, hasError };
  }
  const verb = parts[1];
  const detail = parts[2].trim();
  const nameMap = {
    "$": "shell",
    "exec": "shell",
    "terminal": "shell",
    "search": "search",
    "fetch": "fetch",
    "crawl": "crawl",
    "navigate": "browser",
    "snapshot": "browser",
    "click": "browser",
    "type": "browser",
    "scroll": "browser",
    "back": "browser",
    "press": "browser",
    "close": "browser",
    "images": "browser",
    "vision": "browser",
    "read": "read",
    "write": "write",
    "patch": "patch",
    "grep": "search",
    "find": "search",
    "plan": "plan",
    "recall": "recall",
    "proc": "process",
    "delegate": "delegate",
    "todo": "todo",
    "memory": "memory",
    "clarify": "clarify",
    "session_search": "recall",
    "code": "execute",
    "execute": "execute",
    "web_search": "search",
    "web_extract": "fetch",
    "browser_navigate": "browser",
    "browser_click": "browser",
    "browser_type": "browser",
    "browser_snapshot": "browser",
    "browser_vision": "browser",
    "browser_scroll": "browser",
    "browser_press": "browser",
    "browser_back": "browser",
    "browser_close": "browser",
    "browser_get_images": "browser",
    "read_file": "read",
    "write_file": "write_file",
    "search_files": "search",
    "patch_file": "patch",
    "execute_code": "execute"
  };
  const name = nameMap[verb.toLowerCase()] || verb;
  return { name, detail, duration, hasError };
}
var toolCallCounter = 0;
function syntheticToolUseId() {
  return `hermes-tool-${++toolCallCounter}`;
}
function isThinkingLine(line) {
  return line.includes("💭") || line.startsWith("<thinking>") || line.startsWith("</thinking>") || line.startsWith("Thinking:");
}
function parseHermesStdoutLine(line, ts) {
  const trimmed = line.trim();
  if (!trimmed)
    return [];
  if (trimmed.startsWith("[hermes]") || trimmed.startsWith("[paperclip]")) {
    return [{ kind: "system", ts, text: trimmed }];
  }
  if (trimmed.startsWith("[tool]")) {
    return [];
  }
  if (/^\[\d{4}-\d{2}-\d{2}T/.test(trimmed)) {
    return [{ kind: "stderr", ts, text: trimmed }];
  }
  if (new RegExp("^\\p{Emoji_Presentation}\\s*(Completed|Running|Error)?\\s*$", "u").test(trimmed)) {
    return [];
  }
  if (trimmed.startsWith("session_id:")) {
    return [{ kind: "system", ts, text: trimmed }];
  }
  if (trimmed.includes(TOOL_OUTPUT_PREFIX)) {
    if (isAssistantToolLine(trimmed)) {
      return [{ kind: "assistant", ts, text: extractAssistantText(trimmed) }];
    }
    const toolInfo = parseToolCompletionLine(trimmed);
    if (toolInfo) {
      const id = syntheticToolUseId();
      const detailText = toolInfo.duration ? `${toolInfo.detail}  ${toolInfo.duration}` : toolInfo.detail;
      return [
        {
          kind: "tool_call",
          ts,
          name: toolInfo.name,
          input: { detail: toolInfo.detail },
          toolUseId: id
        },
        {
          kind: "tool_result",
          ts,
          toolUseId: id,
          content: detailText,
          isError: toolInfo.hasError
        }
      ];
    }
    const stripped = trimmed.replace(/^\[done\]\s*/, "").replace(new RegExp(`^${TOOL_OUTPUT_PREFIX.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}\\s*`), "").trim();
    return [{ kind: "stdout", ts, text: stripped }];
  }
  if (isThinkingLine(trimmed)) {
    return [
      {
        kind: "thinking",
        ts,
        text: trimmed.replace(/^💭\s*/, "")
      }
    ];
  }
  if (trimmed.startsWith("Error:") || trimmed.startsWith("ERROR:") || trimmed.startsWith("Traceback")) {
    return [{ kind: "stderr", ts, text: trimmed }];
  }
  return [{ kind: "assistant", ts, text: trimmed }];
}

// ../node_modules/.pnpm/hermes-paperclip-adapter@0.2.0/node_modules/hermes-paperclip-adapter/dist/ui/build-config.js
function buildHermesConfig(v) {
  const ac = {};
  if (v.model.trim()) {
    ac.model = v.model.trim();
  }
  ac.timeoutSec = DEFAULT_TIMEOUT_SEC;
  ac.persistSession = true;
  if (v.cwd) {
    ac.cwd = v.cwd;
  }
  if (v.command) {
    ac.hermesCommand = v.command;
  }
  if (v.extraArgs) {
    ac.extraArgs = v.extraArgs.split(/\s+/).filter(Boolean);
  }
  if (v.thinkingEffort) {
    const existing = ac.extraArgs || [];
    existing.push("--reasoning-effort", String(v.thinkingEffort));
    ac.extraArgs = existing;
  }
  if (v.promptTemplate) {
    ac.promptTemplate = v.promptTemplate;
  }
  return ac;
}
export {
  buildHermesConfig,
  parseHermesStdoutLine
};
//# sourceMappingURL=hermes-paperclip-adapter_ui.js.map
