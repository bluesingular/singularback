import {
  __export,
  __publicField
} from "./chunk-OS7ZSSJM.js";

// ../node_modules/.pnpm/lexical@0.35.0/node_modules/lexical/Lexical.mjs
var Lexical_exports = {};
__export(Lexical_exports, {
  $addUpdateTag: () => $addUpdateTag2,
  $applyNodeReplacement: () => $applyNodeReplacement2,
  $caretFromPoint: () => $caretFromPoint2,
  $caretRangeFromSelection: () => $caretRangeFromSelection2,
  $cloneWithProperties: () => $cloneWithProperties2,
  $comparePointCaretNext: () => $comparePointCaretNext2,
  $copyNode: () => $copyNode2,
  $create: () => $create2,
  $createLineBreakNode: () => $createLineBreakNode2,
  $createNodeSelection: () => $createNodeSelection2,
  $createParagraphNode: () => $createParagraphNode2,
  $createPoint: () => $createPoint2,
  $createRangeSelection: () => $createRangeSelection2,
  $createRangeSelectionFromDom: () => $createRangeSelectionFromDom2,
  $createTabNode: () => $createTabNode2,
  $createTextNode: () => $createTextNode2,
  $extendCaretToRange: () => $extendCaretToRange2,
  $getAdjacentChildCaret: () => $getAdjacentChildCaret2,
  $getAdjacentNode: () => $getAdjacentNode2,
  $getAdjacentSiblingOrParentSiblingCaret: () => $getAdjacentSiblingOrParentSiblingCaret2,
  $getCaretInDirection: () => $getCaretInDirection2,
  $getCaretRange: () => $getCaretRange2,
  $getCaretRangeInDirection: () => $getCaretRangeInDirection2,
  $getCharacterOffsets: () => $getCharacterOffsets2,
  $getChildCaret: () => $getChildCaret2,
  $getChildCaretAtIndex: () => $getChildCaretAtIndex2,
  $getChildCaretOrSelf: () => $getChildCaretOrSelf2,
  $getCollapsedCaretRange: () => $getCollapsedCaretRange2,
  $getCommonAncestor: () => $getCommonAncestor2,
  $getCommonAncestorResultBranchOrder: () => $getCommonAncestorResultBranchOrder2,
  $getEditor: () => $getEditor2,
  $getNearestNodeFromDOMNode: () => $getNearestNodeFromDOMNode2,
  $getNearestRootOrShadowRoot: () => $getNearestRootOrShadowRoot2,
  $getNodeByKey: () => $getNodeByKey2,
  $getNodeByKeyOrThrow: () => $getNodeByKeyOrThrow2,
  $getPreviousSelection: () => $getPreviousSelection2,
  $getRoot: () => $getRoot2,
  $getSelection: () => $getSelection2,
  $getSiblingCaret: () => $getSiblingCaret2,
  $getState: () => $getState2,
  $getStateChange: () => $getStateChange2,
  $getTextContent: () => $getTextContent2,
  $getTextNodeOffset: () => $getTextNodeOffset2,
  $getTextPointCaret: () => $getTextPointCaret2,
  $getTextPointCaretSlice: () => $getTextPointCaretSlice2,
  $getWritableNodeState: () => $getWritableNodeState2,
  $hasAncestor: () => $hasAncestor2,
  $hasUpdateTag: () => $hasUpdateTag2,
  $insertNodes: () => $insertNodes2,
  $isBlockElementNode: () => $isBlockElementNode2,
  $isChildCaret: () => $isChildCaret2,
  $isDecoratorNode: () => $isDecoratorNode2,
  $isElementNode: () => $isElementNode2,
  $isExtendableTextPointCaret: () => $isExtendableTextPointCaret2,
  $isInlineElementOrDecoratorNode: () => $isInlineElementOrDecoratorNode2,
  $isLeafNode: () => $isLeafNode2,
  $isLineBreakNode: () => $isLineBreakNode2,
  $isNodeCaret: () => $isNodeCaret2,
  $isNodeSelection: () => $isNodeSelection2,
  $isParagraphNode: () => $isParagraphNode2,
  $isRangeSelection: () => $isRangeSelection2,
  $isRootNode: () => $isRootNode2,
  $isRootOrShadowRoot: () => $isRootOrShadowRoot2,
  $isSiblingCaret: () => $isSiblingCaret2,
  $isTabNode: () => $isTabNode2,
  $isTextNode: () => $isTextNode2,
  $isTextPointCaret: () => $isTextPointCaret2,
  $isTextPointCaretSlice: () => $isTextPointCaretSlice2,
  $isTokenOrSegmented: () => $isTokenOrSegmented2,
  $isTokenOrTab: () => $isTokenOrTab2,
  $nodesOfType: () => $nodesOfType2,
  $normalizeCaret: () => $normalizeCaret2,
  $normalizeSelection__EXPERIMENTAL: () => $normalizeSelection__EXPERIMENTAL,
  $onUpdate: () => $onUpdate2,
  $parseSerializedNode: () => $parseSerializedNode2,
  $removeTextFromCaretRange: () => $removeTextFromCaretRange2,
  $rewindSiblingCaret: () => $rewindSiblingCaret2,
  $selectAll: () => $selectAll2,
  $setCompositionKey: () => $setCompositionKey2,
  $setPointFromCaret: () => $setPointFromCaret2,
  $setSelection: () => $setSelection2,
  $setSelectionFromCaretRange: () => $setSelectionFromCaretRange2,
  $setState: () => $setState2,
  $splitAtPointCaretNext: () => $splitAtPointCaretNext2,
  $splitNode: () => $splitNode2,
  $updateRangeSelectionFromCaretRange: () => $updateRangeSelectionFromCaretRange2,
  ArtificialNode__DO_NOT_USE: () => ArtificialNode__DO_NOT_USE2,
  BLUR_COMMAND: () => BLUR_COMMAND2,
  CAN_REDO_COMMAND: () => CAN_REDO_COMMAND2,
  CAN_UNDO_COMMAND: () => CAN_UNDO_COMMAND2,
  CLEAR_EDITOR_COMMAND: () => CLEAR_EDITOR_COMMAND2,
  CLEAR_HISTORY_COMMAND: () => CLEAR_HISTORY_COMMAND2,
  CLICK_COMMAND: () => CLICK_COMMAND2,
  COLLABORATION_TAG: () => COLLABORATION_TAG2,
  COMMAND_PRIORITY_CRITICAL: () => COMMAND_PRIORITY_CRITICAL2,
  COMMAND_PRIORITY_EDITOR: () => COMMAND_PRIORITY_EDITOR2,
  COMMAND_PRIORITY_HIGH: () => COMMAND_PRIORITY_HIGH2,
  COMMAND_PRIORITY_LOW: () => COMMAND_PRIORITY_LOW2,
  COMMAND_PRIORITY_NORMAL: () => COMMAND_PRIORITY_NORMAL2,
  CONTROLLED_TEXT_INSERTION_COMMAND: () => CONTROLLED_TEXT_INSERTION_COMMAND2,
  COPY_COMMAND: () => COPY_COMMAND2,
  CUT_COMMAND: () => CUT_COMMAND2,
  DELETE_CHARACTER_COMMAND: () => DELETE_CHARACTER_COMMAND2,
  DELETE_LINE_COMMAND: () => DELETE_LINE_COMMAND2,
  DELETE_WORD_COMMAND: () => DELETE_WORD_COMMAND2,
  DRAGEND_COMMAND: () => DRAGEND_COMMAND2,
  DRAGOVER_COMMAND: () => DRAGOVER_COMMAND2,
  DRAGSTART_COMMAND: () => DRAGSTART_COMMAND2,
  DROP_COMMAND: () => DROP_COMMAND2,
  DecoratorNode: () => DecoratorNode2,
  ElementNode: () => ElementNode2,
  FOCUS_COMMAND: () => FOCUS_COMMAND2,
  FORMAT_ELEMENT_COMMAND: () => FORMAT_ELEMENT_COMMAND2,
  FORMAT_TEXT_COMMAND: () => FORMAT_TEXT_COMMAND2,
  HISTORIC_TAG: () => HISTORIC_TAG2,
  HISTORY_MERGE_TAG: () => HISTORY_MERGE_TAG2,
  HISTORY_PUSH_TAG: () => HISTORY_PUSH_TAG2,
  INDENT_CONTENT_COMMAND: () => INDENT_CONTENT_COMMAND2,
  INSERT_LINE_BREAK_COMMAND: () => INSERT_LINE_BREAK_COMMAND2,
  INSERT_PARAGRAPH_COMMAND: () => INSERT_PARAGRAPH_COMMAND2,
  INSERT_TAB_COMMAND: () => INSERT_TAB_COMMAND2,
  INTERNAL_$isBlock: () => INTERNAL_$isBlock2,
  IS_ALL_FORMATTING: () => IS_ALL_FORMATTING2,
  IS_BOLD: () => IS_BOLD2,
  IS_CODE: () => IS_CODE2,
  IS_HIGHLIGHT: () => IS_HIGHLIGHT2,
  IS_ITALIC: () => IS_ITALIC2,
  IS_STRIKETHROUGH: () => IS_STRIKETHROUGH2,
  IS_SUBSCRIPT: () => IS_SUBSCRIPT2,
  IS_SUPERSCRIPT: () => IS_SUPERSCRIPT2,
  IS_UNDERLINE: () => IS_UNDERLINE2,
  KEY_ARROW_DOWN_COMMAND: () => KEY_ARROW_DOWN_COMMAND2,
  KEY_ARROW_LEFT_COMMAND: () => KEY_ARROW_LEFT_COMMAND2,
  KEY_ARROW_RIGHT_COMMAND: () => KEY_ARROW_RIGHT_COMMAND2,
  KEY_ARROW_UP_COMMAND: () => KEY_ARROW_UP_COMMAND2,
  KEY_BACKSPACE_COMMAND: () => KEY_BACKSPACE_COMMAND2,
  KEY_DELETE_COMMAND: () => KEY_DELETE_COMMAND2,
  KEY_DOWN_COMMAND: () => KEY_DOWN_COMMAND2,
  KEY_ENTER_COMMAND: () => KEY_ENTER_COMMAND2,
  KEY_ESCAPE_COMMAND: () => KEY_ESCAPE_COMMAND2,
  KEY_MODIFIER_COMMAND: () => KEY_MODIFIER_COMMAND2,
  KEY_SPACE_COMMAND: () => KEY_SPACE_COMMAND2,
  KEY_TAB_COMMAND: () => KEY_TAB_COMMAND2,
  LineBreakNode: () => LineBreakNode2,
  MOVE_TO_END: () => MOVE_TO_END2,
  MOVE_TO_START: () => MOVE_TO_START2,
  NODE_STATE_KEY: () => NODE_STATE_KEY2,
  OUTDENT_CONTENT_COMMAND: () => OUTDENT_CONTENT_COMMAND2,
  PASTE_COMMAND: () => PASTE_COMMAND2,
  PASTE_TAG: () => PASTE_TAG2,
  ParagraphNode: () => ParagraphNode2,
  REDO_COMMAND: () => REDO_COMMAND2,
  REMOVE_TEXT_COMMAND: () => REMOVE_TEXT_COMMAND2,
  RootNode: () => RootNode2,
  SELECTION_CHANGE_COMMAND: () => SELECTION_CHANGE_COMMAND2,
  SELECTION_INSERT_CLIPBOARD_NODES_COMMAND: () => SELECTION_INSERT_CLIPBOARD_NODES_COMMAND2,
  SELECT_ALL_COMMAND: () => SELECT_ALL_COMMAND2,
  SKIP_COLLAB_TAG: () => SKIP_COLLAB_TAG2,
  SKIP_DOM_SELECTION_TAG: () => SKIP_DOM_SELECTION_TAG2,
  SKIP_SCROLL_INTO_VIEW_TAG: () => SKIP_SCROLL_INTO_VIEW_TAG2,
  TEXT_TYPE_TO_FORMAT: () => TEXT_TYPE_TO_FORMAT2,
  TabNode: () => TabNode2,
  TextNode: () => TextNode2,
  UNDO_COMMAND: () => UNDO_COMMAND2,
  buildImportMap: () => buildImportMap2,
  createCommand: () => createCommand2,
  createEditor: () => createEditor2,
  createSharedNodeState: () => createSharedNodeState2,
  createState: () => createState2,
  flipDirection: () => flipDirection2,
  getDOMOwnerDocument: () => getDOMOwnerDocument2,
  getDOMSelection: () => getDOMSelection2,
  getDOMSelectionFromTarget: () => getDOMSelectionFromTarget2,
  getDOMTextNode: () => getDOMTextNode2,
  getEditorPropertyFromDOMNode: () => getEditorPropertyFromDOMNode2,
  getNearestEditorFromDOMNode: () => getNearestEditorFromDOMNode2,
  getRegisteredNode: () => getRegisteredNode2,
  getRegisteredNodeOrThrow: () => getRegisteredNodeOrThrow2,
  getStaticNodeConfig: () => getStaticNodeConfig2,
  isBlockDomNode: () => isBlockDomNode2,
  isCurrentlyReadOnlyMode: () => isCurrentlyReadOnlyMode2,
  isDOMDocumentNode: () => isDOMDocumentNode2,
  isDOMNode: () => isDOMNode2,
  isDOMTextNode: () => isDOMTextNode2,
  isDOMUnmanaged: () => isDOMUnmanaged2,
  isDocumentFragment: () => isDocumentFragment2,
  isExactShortcutMatch: () => isExactShortcutMatch2,
  isHTMLAnchorElement: () => isHTMLAnchorElement2,
  isHTMLElement: () => isHTMLElement2,
  isInlineDomNode: () => isInlineDomNode2,
  isLexicalEditor: () => isLexicalEditor2,
  isModifierMatch: () => isModifierMatch2,
  isSelectionCapturedInDecoratorInput: () => isSelectionCapturedInDecoratorInput2,
  isSelectionWithinEditor: () => isSelectionWithinEditor2,
  makeStepwiseIterator: () => makeStepwiseIterator2,
  removeFromParent: () => removeFromParent2,
  resetRandomKey: () => resetRandomKey2,
  setDOMUnmanaged: () => setDOMUnmanaged2,
  setNodeIndentFromDOM: () => setNodeIndentFromDOM2
});

// ../node_modules/.pnpm/lexical@0.35.0/node_modules/lexical/Lexical.dev.mjs
var Lexical_dev_exports = {};
__export(Lexical_dev_exports, {
  $addUpdateTag: () => $addUpdateTag,
  $applyNodeReplacement: () => $applyNodeReplacement,
  $caretFromPoint: () => $caretFromPoint,
  $caretRangeFromSelection: () => $caretRangeFromSelection,
  $cloneWithProperties: () => $cloneWithProperties,
  $comparePointCaretNext: () => $comparePointCaretNext,
  $copyNode: () => $copyNode,
  $create: () => $create,
  $createLineBreakNode: () => $createLineBreakNode,
  $createNodeSelection: () => $createNodeSelection,
  $createParagraphNode: () => $createParagraphNode,
  $createPoint: () => $createPoint,
  $createRangeSelection: () => $createRangeSelection,
  $createRangeSelectionFromDom: () => $createRangeSelectionFromDom,
  $createTabNode: () => $createTabNode,
  $createTextNode: () => $createTextNode,
  $extendCaretToRange: () => $extendCaretToRange,
  $getAdjacentChildCaret: () => $getAdjacentChildCaret,
  $getAdjacentNode: () => $getAdjacentNode,
  $getAdjacentSiblingOrParentSiblingCaret: () => $getAdjacentSiblingOrParentSiblingCaret,
  $getCaretInDirection: () => $getCaretInDirection,
  $getCaretRange: () => $getCaretRange,
  $getCaretRangeInDirection: () => $getCaretRangeInDirection,
  $getCharacterOffsets: () => $getCharacterOffsets,
  $getChildCaret: () => $getChildCaret,
  $getChildCaretAtIndex: () => $getChildCaretAtIndex,
  $getChildCaretOrSelf: () => $getChildCaretOrSelf,
  $getCollapsedCaretRange: () => $getCollapsedCaretRange,
  $getCommonAncestor: () => $getCommonAncestor,
  $getCommonAncestorResultBranchOrder: () => $getCommonAncestorResultBranchOrder,
  $getEditor: () => $getEditor,
  $getNearestNodeFromDOMNode: () => $getNearestNodeFromDOMNode,
  $getNearestRootOrShadowRoot: () => $getNearestRootOrShadowRoot,
  $getNodeByKey: () => $getNodeByKey,
  $getNodeByKeyOrThrow: () => $getNodeByKeyOrThrow,
  $getPreviousSelection: () => $getPreviousSelection,
  $getRoot: () => $getRoot,
  $getSelection: () => $getSelection,
  $getSiblingCaret: () => $getSiblingCaret,
  $getState: () => $getState,
  $getStateChange: () => $getStateChange,
  $getTextContent: () => $getTextContent,
  $getTextNodeOffset: () => $getTextNodeOffset,
  $getTextPointCaret: () => $getTextPointCaret,
  $getTextPointCaretSlice: () => $getTextPointCaretSlice,
  $getWritableNodeState: () => $getWritableNodeState,
  $hasAncestor: () => $hasAncestor,
  $hasUpdateTag: () => $hasUpdateTag,
  $insertNodes: () => $insertNodes,
  $isBlockElementNode: () => $isBlockElementNode,
  $isChildCaret: () => $isChildCaret,
  $isDecoratorNode: () => $isDecoratorNode,
  $isElementNode: () => $isElementNode,
  $isExtendableTextPointCaret: () => $isExtendableTextPointCaret,
  $isInlineElementOrDecoratorNode: () => $isInlineElementOrDecoratorNode,
  $isLeafNode: () => $isLeafNode,
  $isLineBreakNode: () => $isLineBreakNode,
  $isNodeCaret: () => $isNodeCaret,
  $isNodeSelection: () => $isNodeSelection,
  $isParagraphNode: () => $isParagraphNode,
  $isRangeSelection: () => $isRangeSelection,
  $isRootNode: () => $isRootNode,
  $isRootOrShadowRoot: () => $isRootOrShadowRoot,
  $isSiblingCaret: () => $isSiblingCaret,
  $isTabNode: () => $isTabNode,
  $isTextNode: () => $isTextNode,
  $isTextPointCaret: () => $isTextPointCaret,
  $isTextPointCaretSlice: () => $isTextPointCaretSlice,
  $isTokenOrSegmented: () => $isTokenOrSegmented,
  $isTokenOrTab: () => $isTokenOrTab,
  $nodesOfType: () => $nodesOfType,
  $normalizeCaret: () => $normalizeCaret,
  $normalizeSelection__EXPERIMENTAL: () => $normalizeSelection,
  $onUpdate: () => $onUpdate,
  $parseSerializedNode: () => $parseSerializedNode,
  $removeTextFromCaretRange: () => $removeTextFromCaretRange,
  $rewindSiblingCaret: () => $rewindSiblingCaret,
  $selectAll: () => $selectAll,
  $setCompositionKey: () => $setCompositionKey,
  $setPointFromCaret: () => $setPointFromCaret,
  $setSelection: () => $setSelection,
  $setSelectionFromCaretRange: () => $setSelectionFromCaretRange,
  $setState: () => $setState,
  $splitAtPointCaretNext: () => $splitAtPointCaretNext,
  $splitNode: () => $splitNode,
  $updateRangeSelectionFromCaretRange: () => $updateRangeSelectionFromCaretRange,
  ArtificialNode__DO_NOT_USE: () => ArtificialNode__DO_NOT_USE,
  BLUR_COMMAND: () => BLUR_COMMAND,
  CAN_REDO_COMMAND: () => CAN_REDO_COMMAND,
  CAN_UNDO_COMMAND: () => CAN_UNDO_COMMAND,
  CLEAR_EDITOR_COMMAND: () => CLEAR_EDITOR_COMMAND,
  CLEAR_HISTORY_COMMAND: () => CLEAR_HISTORY_COMMAND,
  CLICK_COMMAND: () => CLICK_COMMAND,
  COLLABORATION_TAG: () => COLLABORATION_TAG,
  COMMAND_PRIORITY_CRITICAL: () => COMMAND_PRIORITY_CRITICAL,
  COMMAND_PRIORITY_EDITOR: () => COMMAND_PRIORITY_EDITOR,
  COMMAND_PRIORITY_HIGH: () => COMMAND_PRIORITY_HIGH,
  COMMAND_PRIORITY_LOW: () => COMMAND_PRIORITY_LOW,
  COMMAND_PRIORITY_NORMAL: () => COMMAND_PRIORITY_NORMAL,
  CONTROLLED_TEXT_INSERTION_COMMAND: () => CONTROLLED_TEXT_INSERTION_COMMAND,
  COPY_COMMAND: () => COPY_COMMAND,
  CUT_COMMAND: () => CUT_COMMAND,
  DELETE_CHARACTER_COMMAND: () => DELETE_CHARACTER_COMMAND,
  DELETE_LINE_COMMAND: () => DELETE_LINE_COMMAND,
  DELETE_WORD_COMMAND: () => DELETE_WORD_COMMAND,
  DRAGEND_COMMAND: () => DRAGEND_COMMAND,
  DRAGOVER_COMMAND: () => DRAGOVER_COMMAND,
  DRAGSTART_COMMAND: () => DRAGSTART_COMMAND,
  DROP_COMMAND: () => DROP_COMMAND,
  DecoratorNode: () => DecoratorNode,
  ElementNode: () => ElementNode,
  FOCUS_COMMAND: () => FOCUS_COMMAND,
  FORMAT_ELEMENT_COMMAND: () => FORMAT_ELEMENT_COMMAND,
  FORMAT_TEXT_COMMAND: () => FORMAT_TEXT_COMMAND,
  HISTORIC_TAG: () => HISTORIC_TAG,
  HISTORY_MERGE_TAG: () => HISTORY_MERGE_TAG,
  HISTORY_PUSH_TAG: () => HISTORY_PUSH_TAG,
  INDENT_CONTENT_COMMAND: () => INDENT_CONTENT_COMMAND,
  INSERT_LINE_BREAK_COMMAND: () => INSERT_LINE_BREAK_COMMAND,
  INSERT_PARAGRAPH_COMMAND: () => INSERT_PARAGRAPH_COMMAND,
  INSERT_TAB_COMMAND: () => INSERT_TAB_COMMAND,
  INTERNAL_$isBlock: () => INTERNAL_$isBlock,
  IS_ALL_FORMATTING: () => IS_ALL_FORMATTING,
  IS_BOLD: () => IS_BOLD,
  IS_CODE: () => IS_CODE,
  IS_HIGHLIGHT: () => IS_HIGHLIGHT,
  IS_ITALIC: () => IS_ITALIC,
  IS_STRIKETHROUGH: () => IS_STRIKETHROUGH,
  IS_SUBSCRIPT: () => IS_SUBSCRIPT,
  IS_SUPERSCRIPT: () => IS_SUPERSCRIPT,
  IS_UNDERLINE: () => IS_UNDERLINE,
  KEY_ARROW_DOWN_COMMAND: () => KEY_ARROW_DOWN_COMMAND,
  KEY_ARROW_LEFT_COMMAND: () => KEY_ARROW_LEFT_COMMAND,
  KEY_ARROW_RIGHT_COMMAND: () => KEY_ARROW_RIGHT_COMMAND,
  KEY_ARROW_UP_COMMAND: () => KEY_ARROW_UP_COMMAND,
  KEY_BACKSPACE_COMMAND: () => KEY_BACKSPACE_COMMAND,
  KEY_DELETE_COMMAND: () => KEY_DELETE_COMMAND,
  KEY_DOWN_COMMAND: () => KEY_DOWN_COMMAND,
  KEY_ENTER_COMMAND: () => KEY_ENTER_COMMAND,
  KEY_ESCAPE_COMMAND: () => KEY_ESCAPE_COMMAND,
  KEY_MODIFIER_COMMAND: () => KEY_MODIFIER_COMMAND,
  KEY_SPACE_COMMAND: () => KEY_SPACE_COMMAND,
  KEY_TAB_COMMAND: () => KEY_TAB_COMMAND,
  LineBreakNode: () => LineBreakNode,
  MOVE_TO_END: () => MOVE_TO_END,
  MOVE_TO_START: () => MOVE_TO_START,
  NODE_STATE_KEY: () => NODE_STATE_KEY,
  OUTDENT_CONTENT_COMMAND: () => OUTDENT_CONTENT_COMMAND,
  PASTE_COMMAND: () => PASTE_COMMAND,
  PASTE_TAG: () => PASTE_TAG,
  ParagraphNode: () => ParagraphNode,
  REDO_COMMAND: () => REDO_COMMAND,
  REMOVE_TEXT_COMMAND: () => REMOVE_TEXT_COMMAND,
  RootNode: () => RootNode,
  SELECTION_CHANGE_COMMAND: () => SELECTION_CHANGE_COMMAND,
  SELECTION_INSERT_CLIPBOARD_NODES_COMMAND: () => SELECTION_INSERT_CLIPBOARD_NODES_COMMAND,
  SELECT_ALL_COMMAND: () => SELECT_ALL_COMMAND,
  SKIP_COLLAB_TAG: () => SKIP_COLLAB_TAG,
  SKIP_DOM_SELECTION_TAG: () => SKIP_DOM_SELECTION_TAG,
  SKIP_SCROLL_INTO_VIEW_TAG: () => SKIP_SCROLL_INTO_VIEW_TAG,
  TEXT_TYPE_TO_FORMAT: () => TEXT_TYPE_TO_FORMAT,
  TabNode: () => TabNode,
  TextNode: () => TextNode,
  UNDO_COMMAND: () => UNDO_COMMAND,
  buildImportMap: () => buildImportMap,
  createCommand: () => createCommand,
  createEditor: () => createEditor,
  createSharedNodeState: () => createSharedNodeState,
  createState: () => createState,
  flipDirection: () => flipDirection,
  getDOMOwnerDocument: () => getDOMOwnerDocument,
  getDOMSelection: () => getDOMSelection,
  getDOMSelectionFromTarget: () => getDOMSelectionFromTarget,
  getDOMTextNode: () => getDOMTextNode,
  getEditorPropertyFromDOMNode: () => getEditorPropertyFromDOMNode,
  getNearestEditorFromDOMNode: () => getNearestEditorFromDOMNode,
  getRegisteredNode: () => getRegisteredNode,
  getRegisteredNodeOrThrow: () => getRegisteredNodeOrThrow,
  getStaticNodeConfig: () => getStaticNodeConfig,
  isBlockDomNode: () => isBlockDomNode,
  isCurrentlyReadOnlyMode: () => isCurrentlyReadOnlyMode,
  isDOMDocumentNode: () => isDOMDocumentNode,
  isDOMNode: () => isDOMNode,
  isDOMTextNode: () => isDOMTextNode,
  isDOMUnmanaged: () => isDOMUnmanaged,
  isDocumentFragment: () => isDocumentFragment,
  isExactShortcutMatch: () => isExactShortcutMatch,
  isHTMLAnchorElement: () => isHTMLAnchorElement,
  isHTMLElement: () => isHTMLElement,
  isInlineDomNode: () => isInlineDomNode,
  isLexicalEditor: () => isLexicalEditor,
  isModifierMatch: () => isModifierMatch,
  isSelectionCapturedInDecoratorInput: () => isSelectionCapturedInDecoratorInput,
  isSelectionWithinEditor: () => isSelectionWithinEditor,
  makeStepwiseIterator: () => makeStepwiseIterator,
  removeFromParent: () => removeFromParent,
  resetRandomKey: () => resetRandomKey,
  setDOMUnmanaged: () => setDOMUnmanaged,
  setNodeIndentFromDOM: () => setNodeIndentFromDOM
});
function formatDevErrorMessage(message) {
  throw new Error(message);
}
var CAN_USE_DOM = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
var documentMode = CAN_USE_DOM && "documentMode" in document ? document.documentMode : null;
var IS_APPLE = CAN_USE_DOM && /Mac|iPod|iPhone|iPad/.test(navigator.platform);
var IS_FIREFOX = CAN_USE_DOM && /^(?!.*Seamonkey)(?=.*Firefox).*/i.test(navigator.userAgent);
var CAN_USE_BEFORE_INPUT = CAN_USE_DOM && "InputEvent" in window && !documentMode ? "getTargetRanges" in new window.InputEvent("input") : false;
var IS_SAFARI = CAN_USE_DOM && /Version\/[\d.]+.*Safari/.test(navigator.userAgent);
var IS_IOS = CAN_USE_DOM && /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
var IS_ANDROID = CAN_USE_DOM && /Android/.test(navigator.userAgent);
var IS_CHROME = CAN_USE_DOM && /^(?=.*Chrome).*/i.test(navigator.userAgent);
var IS_ANDROID_CHROME = CAN_USE_DOM && IS_ANDROID && IS_CHROME;
var IS_APPLE_WEBKIT = CAN_USE_DOM && /AppleWebKit\/[\d.]+/.test(navigator.userAgent) && !IS_CHROME;
function normalizeClassNames(...classNames) {
  const rval = [];
  for (const className of classNames) {
    if (className && typeof className === "string") {
      for (const [s2] of className.matchAll(/\S+/g)) {
        rval.push(s2);
      }
    }
  }
  return rval;
}
var DOM_ELEMENT_TYPE = 1;
var DOM_TEXT_TYPE = 3;
var DOM_DOCUMENT_TYPE = 9;
var DOM_DOCUMENT_FRAGMENT_TYPE = 11;
var NO_DIRTY_NODES = 0;
var HAS_DIRTY_NODES = 1;
var FULL_RECONCILE = 2;
var IS_NORMAL = 0;
var IS_TOKEN = 1;
var IS_SEGMENTED = 2;
var IS_BOLD = 1;
var IS_ITALIC = 1 << 1;
var IS_STRIKETHROUGH = 1 << 2;
var IS_UNDERLINE = 1 << 3;
var IS_CODE = 1 << 4;
var IS_SUBSCRIPT = 1 << 5;
var IS_SUPERSCRIPT = 1 << 6;
var IS_HIGHLIGHT = 1 << 7;
var IS_LOWERCASE = 1 << 8;
var IS_UPPERCASE = 1 << 9;
var IS_CAPITALIZE = 1 << 10;
var IS_ALL_FORMATTING = IS_BOLD | IS_ITALIC | IS_STRIKETHROUGH | IS_UNDERLINE | IS_CODE | IS_SUBSCRIPT | IS_SUPERSCRIPT | IS_HIGHLIGHT | IS_LOWERCASE | IS_UPPERCASE | IS_CAPITALIZE;
var IS_DIRECTIONLESS = 1;
var IS_UNMERGEABLE = 1 << 1;
var IS_ALIGN_LEFT = 1;
var IS_ALIGN_CENTER = 2;
var IS_ALIGN_RIGHT = 3;
var IS_ALIGN_JUSTIFY = 4;
var IS_ALIGN_START = 5;
var IS_ALIGN_END = 6;
var NON_BREAKING_SPACE = " ";
var ZERO_WIDTH_SPACE = "​";
var COMPOSITION_SUFFIX = IS_SAFARI || IS_IOS || IS_APPLE_WEBKIT ? NON_BREAKING_SPACE : ZERO_WIDTH_SPACE;
var DOUBLE_LINE_BREAK = "\n\n";
var COMPOSITION_START_CHAR = IS_FIREFOX ? NON_BREAKING_SPACE : COMPOSITION_SUFFIX;
var TEXT_TYPE_TO_FORMAT = {
  bold: IS_BOLD,
  capitalize: IS_CAPITALIZE,
  code: IS_CODE,
  highlight: IS_HIGHLIGHT,
  italic: IS_ITALIC,
  lowercase: IS_LOWERCASE,
  strikethrough: IS_STRIKETHROUGH,
  subscript: IS_SUBSCRIPT,
  superscript: IS_SUPERSCRIPT,
  underline: IS_UNDERLINE,
  uppercase: IS_UPPERCASE
};
var DETAIL_TYPE_TO_DETAIL = {
  directionless: IS_DIRECTIONLESS,
  unmergeable: IS_UNMERGEABLE
};
var ELEMENT_TYPE_TO_FORMAT = {
  center: IS_ALIGN_CENTER,
  end: IS_ALIGN_END,
  justify: IS_ALIGN_JUSTIFY,
  left: IS_ALIGN_LEFT,
  right: IS_ALIGN_RIGHT,
  start: IS_ALIGN_START
};
var ELEMENT_FORMAT_TO_TYPE = {
  [IS_ALIGN_CENTER]: "center",
  [IS_ALIGN_END]: "end",
  [IS_ALIGN_JUSTIFY]: "justify",
  [IS_ALIGN_LEFT]: "left",
  [IS_ALIGN_RIGHT]: "right",
  [IS_ALIGN_START]: "start"
};
var TEXT_MODE_TO_TYPE = {
  normal: IS_NORMAL,
  segmented: IS_SEGMENTED,
  token: IS_TOKEN
};
var TEXT_TYPE_TO_MODE = {
  [IS_NORMAL]: "normal",
  [IS_SEGMENTED]: "segmented",
  [IS_TOKEN]: "token"
};
var NODE_STATE_KEY = "$";
var PROTOTYPE_CONFIG_METHOD = "$config";
function $garbageCollectDetachedDecorators(editor, pendingEditorState) {
  const currentDecorators = editor._decorators;
  const pendingDecorators = editor._pendingDecorators;
  let decorators = pendingDecorators || currentDecorators;
  const nodeMap = pendingEditorState._nodeMap;
  let key;
  for (key in decorators) {
    if (!nodeMap.has(key)) {
      if (decorators === currentDecorators) {
        decorators = cloneDecorators(editor);
      }
      delete decorators[key];
    }
  }
}
function $garbageCollectDetachedDeepChildNodes(node, parentKey, prevNodeMap, nodeMap, nodeMapDelete, dirtyNodes) {
  let child = node.getFirstChild();
  while (child !== null) {
    const childKey = child.__key;
    if (child.__parent === parentKey) {
      if ($isElementNode(child)) {
        $garbageCollectDetachedDeepChildNodes(child, childKey, prevNodeMap, nodeMap, nodeMapDelete, dirtyNodes);
      }
      if (!prevNodeMap.has(childKey)) {
        dirtyNodes.delete(childKey);
      }
      nodeMapDelete.push(childKey);
    }
    child = child.getNextSibling();
  }
}
function $garbageCollectDetachedNodes(prevEditorState, editorState, dirtyLeaves, dirtyElements) {
  const prevNodeMap = prevEditorState._nodeMap;
  const nodeMap = editorState._nodeMap;
  const nodeMapDelete = [];
  for (const [nodeKey] of dirtyElements) {
    const node = nodeMap.get(nodeKey);
    if (node !== void 0) {
      if (!node.isAttached()) {
        if ($isElementNode(node)) {
          $garbageCollectDetachedDeepChildNodes(node, nodeKey, prevNodeMap, nodeMap, nodeMapDelete, dirtyElements);
        }
        if (!prevNodeMap.has(nodeKey)) {
          dirtyElements.delete(nodeKey);
        }
        nodeMapDelete.push(nodeKey);
      }
    }
  }
  for (const nodeKey of nodeMapDelete) {
    nodeMap.delete(nodeKey);
  }
  for (const nodeKey of dirtyLeaves) {
    const node = nodeMap.get(nodeKey);
    if (node !== void 0 && !node.isAttached()) {
      if (!prevNodeMap.has(nodeKey)) {
        dirtyLeaves.delete(nodeKey);
      }
      nodeMap.delete(nodeKey);
    }
  }
}
var TEXT_MUTATION_VARIANCE = 100;
var isProcessingMutations = false;
var lastTextEntryTimeStamp = 0;
function getIsProcessingMutations() {
  return isProcessingMutations;
}
function updateTimeStamp(event) {
  lastTextEntryTimeStamp = event.timeStamp;
}
function initTextEntryListener(editor) {
  if (lastTextEntryTimeStamp === 0) {
    getWindow(editor).addEventListener("textInput", updateTimeStamp, true);
  }
}
function isManagedLineBreak(dom, target, editor) {
  const isBR = dom.nodeName === "BR";
  const lexicalLineBreak = target.__lexicalLineBreak;
  return lexicalLineBreak && (dom === lexicalLineBreak || isBR && dom.previousSibling === lexicalLineBreak) || isBR && getNodeKeyFromDOMNode(dom, editor) !== void 0;
}
function getLastSelection(editor) {
  return editor.getEditorState().read(() => {
    const selection = $getSelection();
    return selection !== null ? selection.clone() : null;
  });
}
function $handleTextMutation(target, node, editor) {
  const domSelection = getDOMSelection(getWindow(editor));
  let anchorOffset = null;
  let focusOffset = null;
  if (domSelection !== null && domSelection.anchorNode === target) {
    anchorOffset = domSelection.anchorOffset;
    focusOffset = domSelection.focusOffset;
  }
  const text = target.nodeValue;
  if (text !== null) {
    $updateTextNodeFromDOMContent(node, text, anchorOffset, focusOffset, false);
  }
}
function shouldUpdateTextNodeFromMutation(selection, targetDOM, targetNode) {
  if ($isRangeSelection(selection)) {
    const anchorNode = selection.anchor.getNode();
    if (anchorNode.is(targetNode) && selection.format !== anchorNode.getFormat()) {
      return false;
    }
  }
  return isDOMTextNode(targetDOM) && targetNode.isAttached();
}
function $getNearestManagedNodePairFromDOMNode(startingDOM, editor, editorState, rootElement) {
  for (let dom = startingDOM; dom && !isDOMUnmanaged(dom); dom = getParentElement(dom)) {
    const key = getNodeKeyFromDOMNode(dom, editor);
    if (key !== void 0) {
      const node = $getNodeByKey(key, editorState);
      if (node) {
        return $isDecoratorNode(node) || !isHTMLElement(dom) ? void 0 : [dom, node];
      }
    } else if (dom === rootElement) {
      return [rootElement, internalGetRoot(editorState)];
    }
  }
}
function flushMutations(editor, mutations, observer) {
  isProcessingMutations = true;
  const shouldFlushTextMutations = performance.now() - lastTextEntryTimeStamp > TEXT_MUTATION_VARIANCE;
  try {
    updateEditorSync(editor, () => {
      const selection = $getSelection() || getLastSelection(editor);
      const badDOMTargets = /* @__PURE__ */ new Map();
      const rootElement = editor.getRootElement();
      const currentEditorState = editor._editorState;
      const blockCursorElement = editor._blockCursorElement;
      let shouldRevertSelection = false;
      let possibleTextForFirefoxPaste = "";
      for (let i2 = 0; i2 < mutations.length; i2++) {
        const mutation = mutations[i2];
        const type = mutation.type;
        const targetDOM = mutation.target;
        const pair = $getNearestManagedNodePairFromDOMNode(targetDOM, editor, currentEditorState, rootElement);
        if (!pair) {
          continue;
        }
        const [nodeDOM, targetNode] = pair;
        if (type === "characterData") {
          if (
            // TODO there is an edge case here if a mutation happens too quickly
            //      after text input, it may never be handled since we do not
            //      track the ignored mutations in any way
            shouldFlushTextMutations && $isTextNode(targetNode) && isDOMTextNode(targetDOM) && shouldUpdateTextNodeFromMutation(selection, targetDOM, targetNode)
          ) {
            $handleTextMutation(targetDOM, targetNode, editor);
          }
        } else if (type === "childList") {
          shouldRevertSelection = true;
          const addedDOMs = mutation.addedNodes;
          for (let s2 = 0; s2 < addedDOMs.length; s2++) {
            const addedDOM = addedDOMs[s2];
            const node = $getNodeFromDOMNode(addedDOM);
            const parentDOM = addedDOM.parentNode;
            if (parentDOM != null && addedDOM !== blockCursorElement && node === null && !isManagedLineBreak(addedDOM, parentDOM, editor)) {
              if (IS_FIREFOX) {
                const possibleText = (isHTMLElement(addedDOM) ? addedDOM.innerText : null) || addedDOM.nodeValue;
                if (possibleText) {
                  possibleTextForFirefoxPaste += possibleText;
                }
              }
              parentDOM.removeChild(addedDOM);
            }
          }
          const removedDOMs = mutation.removedNodes;
          const removedDOMsLength = removedDOMs.length;
          if (removedDOMsLength > 0) {
            let unremovedBRs = 0;
            for (let s2 = 0; s2 < removedDOMsLength; s2++) {
              const removedDOM = removedDOMs[s2];
              if (isManagedLineBreak(removedDOM, targetDOM, editor) || blockCursorElement === removedDOM) {
                targetDOM.appendChild(removedDOM);
                unremovedBRs++;
              }
            }
            if (removedDOMsLength !== unremovedBRs) {
              badDOMTargets.set(nodeDOM, targetNode);
            }
          }
        }
      }
      if (badDOMTargets.size > 0) {
        for (const [nodeDOM, targetNode] of badDOMTargets) {
          targetNode.reconcileObservedMutation(nodeDOM, editor);
        }
      }
      const records = observer.takeRecords();
      if (records.length > 0) {
        for (let i2 = 0; i2 < records.length; i2++) {
          const record = records[i2];
          const addedNodes = record.addedNodes;
          const target = record.target;
          for (let s2 = 0; s2 < addedNodes.length; s2++) {
            const addedDOM = addedNodes[s2];
            const parentDOM = addedDOM.parentNode;
            if (parentDOM != null && addedDOM.nodeName === "BR" && !isManagedLineBreak(addedDOM, target, editor)) {
              parentDOM.removeChild(addedDOM);
            }
          }
        }
        observer.takeRecords();
      }
      if (selection !== null) {
        if (shouldRevertSelection) {
          $setSelection(selection);
        }
        if (IS_FIREFOX && isFirefoxClipboardEvents(editor)) {
          selection.insertRawText(possibleTextForFirefoxPaste);
        }
      }
    });
  } finally {
    isProcessingMutations = false;
  }
}
function flushRootMutations(editor) {
  const observer = editor._observer;
  if (observer !== null) {
    const mutations = observer.takeRecords();
    flushMutations(editor, mutations, observer);
  }
}
function initMutationObserver(editor) {
  initTextEntryListener(editor);
  editor._observer = new MutationObserver((mutations, observer) => {
    flushMutations(editor, mutations, observer);
  });
}
var StateConfig = class {
  /** The string key used when serializing this state to JSON */
  /** The parse function from the StateValueConfig passed to createState */
  /**
   * The unparse function from the StateValueConfig passed to createState,
   * with a default that is simply a pass-through that assumes the value is
   * JSON serializable.
   */
  /**
   * An equality function from the StateValueConfig, with a default of
   * Object.is.
   */
  /**
   * The result of `stateValueConfig.parse(undefined)`, which is computed only
   * once and used as the default value. When the current value `isEqual` to
   * the `defaultValue`, it will not be serialized to JSON.
   */
  constructor(key, stateValueConfig) {
    this.key = key;
    this.parse = stateValueConfig.parse.bind(stateValueConfig);
    this.unparse = (stateValueConfig.unparse || coerceToJSON).bind(stateValueConfig);
    this.isEqual = (stateValueConfig.isEqual || Object.is).bind(stateValueConfig);
    this.defaultValue = this.parse(void 0);
  }
};
function createState(key, valueConfig) {
  return new StateConfig(key, valueConfig);
}
function $getState(node, stateConfig, version = "latest") {
  const latestOrDirectNode = version === "latest" ? node.getLatest() : node;
  const state = latestOrDirectNode.__state;
  if (state) {
    $checkCollision(node, stateConfig, state);
    return state.getValue(stateConfig);
  }
  return stateConfig.defaultValue;
}
function $getStateChange(node, prevNode, stateConfig) {
  const value = $getState(node, stateConfig, "direct");
  const prevValue = $getState(prevNode, stateConfig, "direct");
  return stateConfig.isEqual(value, prevValue) ? null : [value, prevValue];
}
function $setState(node, stateConfig, valueOrUpdater) {
  errorOnReadOnly();
  let value;
  if (typeof valueOrUpdater === "function") {
    const latest = node.getLatest();
    const prevValue = $getState(latest, stateConfig);
    value = valueOrUpdater(prevValue);
    if (stateConfig.isEqual(prevValue, value)) {
      return latest;
    }
  } else {
    value = valueOrUpdater;
  }
  const writable = node.getWritable();
  const state = $getWritableNodeState(writable);
  $checkCollision(node, stateConfig, state);
  state.updateFromKnown(stateConfig, value);
  return writable;
}
function $checkCollision(node, stateConfig, state) {
  {
    const collision = state.sharedNodeState.sharedConfigMap.get(stateConfig.key);
    if (collision !== void 0 && collision !== stateConfig) {
      {
        formatDevErrorMessage(`$setState: State key collision ${JSON.stringify(stateConfig.key)} detected in ${node.constructor.name} node with type ${node.getType()} and key ${node.getKey()}. Only one StateConfig with a given key should be used on a node.`);
      }
    }
  }
}
function createSharedNodeState(nodeConfig) {
  const sharedConfigMap = /* @__PURE__ */ new Map();
  const flatKeys = /* @__PURE__ */ new Set();
  for (let klass = typeof nodeConfig === "function" ? nodeConfig : nodeConfig.replace; klass.prototype && klass.prototype.getType !== void 0; klass = Object.getPrototypeOf(klass)) {
    const {
      ownNodeConfig
    } = getStaticNodeConfig(klass);
    if (ownNodeConfig && ownNodeConfig.stateConfigs) {
      for (const requiredStateConfig of ownNodeConfig.stateConfigs) {
        let stateConfig;
        if ("stateConfig" in requiredStateConfig) {
          stateConfig = requiredStateConfig.stateConfig;
          if (requiredStateConfig.flat) {
            flatKeys.add(stateConfig.key);
          }
        } else {
          stateConfig = requiredStateConfig;
        }
        sharedConfigMap.set(stateConfig.key, stateConfig);
      }
    }
  }
  return {
    flatKeys,
    sharedConfigMap
  };
}
var NodeState = class _NodeState {
  /**
   * @internal
   *
   * Track the (versioned) node that this NodeState was created for, to
   * facilitate copy-on-write for NodeState. When a LexicalNode is cloned,
   * it will *reference* the NodeState from its prevNode. From the nextNode
   * you can continue to read state without copying, but the first $setState
   * will trigger a copy of the prevNode's NodeState with the node property
   * updated.
   */
  /**
   * @internal
   *
   * State that has already been parsed in a get state, so it is safe. (can be returned with
   * just a cast since the proof was given before).
   *
   * Note that it uses StateConfig, so in addition to (1) the CURRENT VALUE, it has access to
   * (2) the State key (3) the DEFAULT VALUE and (4) the PARSE FUNCTION
   */
  /**
   * @internal
   *
   * A copy of serializedNode[NODE_STATE_KEY] that is made when JSON is
   * imported but has not been parsed yet.
   *
   * It stays here until a get state requires us to parse it, and since we
   * then know the value is safe we move it to knownState.
   *
   * Note that since only string keys are used here, we can only allow this
   * state to pass-through on export or on the next version since there is
   * no known value configuration. This pass-through is to support scenarios
   * where multiple versions of the editor code are working in parallel so
   * an old version of your code doesnt erase metadata that was
   * set by a newer version of your code.
   */
  /**
   * @internal
   *
   * This sharedNodeState is preserved across all instances of a given
   * node type in an editor and remains writable. It is how keys are resolved
   * to configuration.
   */
  /**
   * @internal
   *
   * The count of known or unknown keys in this state, ignoring the
   * intersection between the two sets.
   */
  /**
   * @internal
   */
  constructor(node, sharedNodeState, unknownState = void 0, knownState = /* @__PURE__ */ new Map(), size = void 0) {
    this.node = node;
    this.sharedNodeState = sharedNodeState;
    this.unknownState = unknownState;
    this.knownState = knownState;
    const {
      sharedConfigMap
    } = this.sharedNodeState;
    const computedSize = size !== void 0 ? size : computeSize(sharedConfigMap, unknownState, knownState);
    {
      if (!(size === void 0 || computedSize === size)) {
        formatDevErrorMessage(`NodeState: size != computedSize (${String(size)} != ${String(computedSize)})`);
      }
      for (const stateConfig of knownState.keys()) {
        if (!sharedConfigMap.has(stateConfig.key)) {
          formatDevErrorMessage(`NodeState: sharedConfigMap missing knownState key ${stateConfig.key}`);
        }
      }
    }
    this.size = computedSize;
  }
  /**
   * @internal
   *
   * Get the value from knownState, or parse it from unknownState
   * if it contains the given key.
   *
   * Updates the sharedConfigMap when no known state is found.
   * Updates unknownState and knownState when an unknownState is parsed.
   */
  getValue(stateConfig) {
    const known = this.knownState.get(stateConfig);
    if (known !== void 0) {
      return known;
    }
    this.sharedNodeState.sharedConfigMap.set(stateConfig.key, stateConfig);
    let parsed = stateConfig.defaultValue;
    if (this.unknownState && stateConfig.key in this.unknownState) {
      const jsonValue = this.unknownState[stateConfig.key];
      if (jsonValue !== void 0) {
        parsed = stateConfig.parse(jsonValue);
      }
      this.updateFromKnown(stateConfig, parsed);
    }
    return parsed;
  }
  /**
   * @internal
   *
   * Used only for advanced use cases, such as collab. The intent here is to
   * allow you to diff states with a more stable interface than the properties
   * of this class.
   */
  getInternalState() {
    return [this.unknownState, this.knownState];
  }
  /**
   * Encode this NodeState to JSON in the format that its node expects.
   * This returns `{[NODE_STATE_KEY]?: UnknownStateRecord}` rather than
   * `UnknownStateRecord | undefined` so that we can support flattening
   * specific entries in the future when nodes can declare what
   * their required StateConfigs are.
   */
  toJSON() {
    const state = {
      ...this.unknownState
    };
    const flatState = {};
    for (const [stateConfig, v2] of this.knownState) {
      if (stateConfig.isEqual(v2, stateConfig.defaultValue)) {
        delete state[stateConfig.key];
      } else {
        state[stateConfig.key] = stateConfig.unparse(v2);
      }
    }
    for (const key of this.sharedNodeState.flatKeys) {
      if (key in state) {
        flatState[key] = state[key];
        delete state[key];
      }
    }
    if (undefinedIfEmpty(state)) {
      flatState[NODE_STATE_KEY] = state;
    }
    return flatState;
  }
  /**
   * @internal
   *
   * A NodeState is writable when the node to update matches
   * the node associated with the NodeState. This basically
   * mirrors how the EditorState NodeMap works, but in a
   * bottom-up organization rather than a top-down organization.
   *
   * This allows us to implement the same "copy on write"
   * pattern for state, without having the state version
   * update every time the node version changes (e.g. when
   * its parent or siblings change).
   *
   * @param node The node to associate with the state
   * @returns The next writable state
   */
  getWritable(node) {
    if (this.node === node) {
      return this;
    }
    const {
      sharedNodeState,
      unknownState
    } = this;
    const nextKnownState = new Map(this.knownState);
    return new _NodeState(node, sharedNodeState, parseAndPruneNextUnknownState(sharedNodeState.sharedConfigMap, nextKnownState, unknownState), nextKnownState, this.size);
  }
  /** @internal */
  updateFromKnown(stateConfig, value) {
    const key = stateConfig.key;
    this.sharedNodeState.sharedConfigMap.set(key, stateConfig);
    const {
      knownState,
      unknownState
    } = this;
    if (!(knownState.has(stateConfig) || unknownState && key in unknownState)) {
      if (unknownState) {
        delete unknownState[key];
        this.unknownState = undefinedIfEmpty(unknownState);
      }
      this.size++;
    }
    knownState.set(stateConfig, value);
  }
  /**
   * @internal
   *
   * This is intended for advanced use cases only, such
   * as collab or dev tools.
   *
   * Update a single key value pair from unknown state,
   * parsing it if the key is known to this node. This is
   * basically like updateFromJSON, but the effect is
   * isolated to a single entry.
   *
   * @param k The string key from an UnknownStateRecord
   * @param v The unknown value from an UnknownStateRecord
   */
  updateFromUnknown(k, v2) {
    const stateConfig = this.sharedNodeState.sharedConfigMap.get(k);
    if (stateConfig) {
      this.updateFromKnown(stateConfig, stateConfig.parse(v2));
    } else {
      this.unknownState = this.unknownState || {};
      if (!(k in this.unknownState)) {
        this.size++;
      }
      this.unknownState[k] = v2;
    }
  }
  /**
   * @internal
   *
   * Reset all existing state to default or empty values,
   * and perform any updates from the given unknownState.
   *
   * This is used when initializing a node's state from JSON,
   * or when resetting a node's state from JSON.
   *
   * @param unknownState The new state in serialized form
   */
  updateFromJSON(unknownState) {
    const {
      knownState
    } = this;
    for (const stateConfig of knownState.keys()) {
      knownState.set(stateConfig, stateConfig.defaultValue);
    }
    this.size = knownState.size;
    this.unknownState = void 0;
    if (unknownState) {
      for (const [k, v2] of Object.entries(unknownState)) {
        this.updateFromUnknown(k, v2);
      }
    }
  }
};
function $getWritableNodeState(node) {
  const writable = node.getWritable();
  const state = writable.__state ? writable.__state.getWritable(writable) : new NodeState(writable, $getSharedNodeState(writable));
  writable.__state = state;
  return state;
}
function $getSharedNodeState(node) {
  return node.__state ? node.__state.sharedNodeState : getRegisteredNodeOrThrow($getEditor(), node.getType()).sharedNodeState;
}
function $updateStateFromJSON(node, serialized) {
  const writable = node.getWritable();
  const unknownState = serialized[NODE_STATE_KEY];
  let parseState = unknownState;
  for (const k of $getSharedNodeState(writable).flatKeys) {
    if (k in serialized) {
      if (parseState === void 0 || parseState === unknownState) {
        parseState = {
          ...unknownState
        };
      }
      parseState[k] = serialized[k];
    }
  }
  if (writable.__state || parseState) {
    $getWritableNodeState(node).updateFromJSON(parseState);
  }
  return writable;
}
function nodeStatesAreEquivalent(a2, b2) {
  if (a2 === b2) {
    return true;
  }
  if (a2 && b2 && a2.size !== b2.size) {
    return false;
  }
  const keys = /* @__PURE__ */ new Set();
  return !(a2 && hasUnequalMapEntry(keys, a2, b2) || b2 && hasUnequalMapEntry(keys, b2, a2) || a2 && hasUnequalRecordEntry(keys, a2, b2) || b2 && hasUnequalRecordEntry(keys, b2, a2));
}
function computeSize(sharedConfigMap, unknownState, knownState) {
  let size = knownState.size;
  if (unknownState) {
    for (const k in unknownState) {
      const sharedConfig = sharedConfigMap.get(k);
      if (!sharedConfig || !knownState.has(sharedConfig)) {
        size++;
      }
    }
  }
  return size;
}
function undefinedIfEmpty(obj) {
  if (obj) {
    for (const key in obj) {
      return obj;
    }
  }
  return void 0;
}
function coerceToJSON(v2) {
  return v2;
}
function parseAndPruneNextUnknownState(sharedConfigMap, nextKnownState, unknownState) {
  let nextUnknownState = void 0;
  if (unknownState) {
    for (const [k, v2] of Object.entries(unknownState)) {
      const stateConfig = sharedConfigMap.get(k);
      if (stateConfig) {
        if (!nextKnownState.has(stateConfig)) {
          nextKnownState.set(stateConfig, stateConfig.parse(v2));
        }
      } else {
        nextUnknownState = nextUnknownState || {};
        nextUnknownState[k] = v2;
      }
    }
  }
  return nextUnknownState;
}
function hasUnequalMapEntry(keys, sourceState, otherState) {
  for (const [stateConfig, value] of sourceState.knownState) {
    if (keys.has(stateConfig.key)) {
      continue;
    }
    keys.add(stateConfig.key);
    const otherValue = otherState ? otherState.getValue(stateConfig) : stateConfig.defaultValue;
    if (otherValue !== value && !stateConfig.isEqual(otherValue, value)) {
      return true;
    }
  }
  return false;
}
function hasUnequalRecordEntry(keys, sourceState, otherState) {
  const {
    unknownState
  } = sourceState;
  const otherUnknownState = otherState ? otherState.unknownState : void 0;
  if (unknownState) {
    for (const [key, value] of Object.entries(unknownState)) {
      if (keys.has(key)) {
        continue;
      }
      keys.add(key);
      const otherValue = otherUnknownState ? otherUnknownState[key] : void 0;
      if (value !== otherValue) {
        return true;
      }
    }
  }
  return false;
}
function $cloneNodeState(from, to2) {
  const state = from.__state;
  return state && state.node === from ? state.getWritable(to2) : state;
}
function $canSimpleTextNodesBeMerged(node1, node2) {
  const node1Mode = node1.__mode;
  const node1Format = node1.__format;
  const node1Style = node1.__style;
  const node2Mode = node2.__mode;
  const node2Format = node2.__format;
  const node2Style = node2.__style;
  const node1State = node1.__state;
  const node2State = node2.__state;
  return (node1Mode === null || node1Mode === node2Mode) && (node1Format === null || node1Format === node2Format) && (node1Style === null || node1Style === node2Style) && (node1.__state === null || node1State === node2State || nodeStatesAreEquivalent(node1State, node2State));
}
function $mergeTextNodes(node1, node2) {
  const writableNode1 = node1.mergeWithSibling(node2);
  const normalizedNodes = getActiveEditor()._normalizedNodes;
  normalizedNodes.add(node1.__key);
  normalizedNodes.add(node2.__key);
  return writableNode1;
}
function $normalizeTextNode(textNode) {
  let node = textNode;
  if (node.__text === "" && node.isSimpleText() && !node.isUnmergeable()) {
    node.remove();
    return;
  }
  let previousNode;
  while ((previousNode = node.getPreviousSibling()) !== null && $isTextNode(previousNode) && previousNode.isSimpleText() && !previousNode.isUnmergeable()) {
    if (previousNode.__text === "") {
      previousNode.remove();
    } else if ($canSimpleTextNodesBeMerged(previousNode, node)) {
      node = $mergeTextNodes(previousNode, node);
      break;
    } else {
      break;
    }
  }
  let nextNode;
  while ((nextNode = node.getNextSibling()) !== null && $isTextNode(nextNode) && nextNode.isSimpleText() && !nextNode.isUnmergeable()) {
    if (nextNode.__text === "") {
      nextNode.remove();
    } else if ($canSimpleTextNodesBeMerged(node, nextNode)) {
      node = $mergeTextNodes(node, nextNode);
      break;
    } else {
      break;
    }
  }
}
function $normalizeSelection(selection) {
  $normalizePoint(selection.anchor);
  $normalizePoint(selection.focus);
  return selection;
}
function $normalizePoint(point) {
  while (point.type === "element") {
    const node = point.getNode();
    const offset = point.offset;
    let nextNode;
    let nextOffsetAtEnd;
    if (offset === node.getChildrenSize()) {
      nextNode = node.getChildAtIndex(offset - 1);
      nextOffsetAtEnd = true;
    } else {
      nextNode = node.getChildAtIndex(offset);
      nextOffsetAtEnd = false;
    }
    if ($isTextNode(nextNode)) {
      point.set(nextNode.__key, nextOffsetAtEnd ? nextNode.getTextContentSize() : 0, "text", true);
      break;
    } else if (!$isElementNode(nextNode)) {
      break;
    }
    point.set(nextNode.__key, nextOffsetAtEnd ? nextNode.getChildrenSize() : 0, "element", true);
  }
}
var subTreeTextContent = "";
var subTreeTextFormat = null;
var subTreeTextStyle = "";
var editorTextContent = "";
var activeEditorConfig;
var activeEditor$1;
var activeEditorNodes;
var treatAllNodesAsDirty = false;
var activeEditorStateReadOnly = false;
var activeMutationListeners;
var activeDirtyElements;
var activeDirtyLeaves;
var activePrevNodeMap;
var activeNextNodeMap;
var activePrevKeyToDOMMap;
var mutatedNodes;
function destroyNode(key, parentDOM) {
  const node = activePrevNodeMap.get(key);
  if (parentDOM !== null) {
    const dom = getPrevElementByKeyOrThrow(key);
    if (dom.parentNode === parentDOM) {
      parentDOM.removeChild(dom);
    }
  }
  if (!activeNextNodeMap.has(key)) {
    activeEditor$1._keyToDOMMap.delete(key);
  }
  if ($isElementNode(node)) {
    const children = createChildrenArray(node, activePrevNodeMap);
    destroyChildren(children, 0, children.length - 1, null);
  }
  if (node !== void 0) {
    setMutatedNode(mutatedNodes, activeEditorNodes, activeMutationListeners, node, "destroyed");
  }
}
function destroyChildren(children, _startIndex, endIndex, dom) {
  let startIndex = _startIndex;
  for (; startIndex <= endIndex; ++startIndex) {
    const child = children[startIndex];
    if (child !== void 0) {
      destroyNode(child, dom);
    }
  }
}
function setTextAlign(domStyle, value) {
  domStyle.setProperty("text-align", value);
}
var DEFAULT_INDENT_VALUE = "40px";
function setElementIndent(dom, indent) {
  const indentClassName = activeEditorConfig.theme.indent;
  if (typeof indentClassName === "string") {
    const elementHasClassName = dom.classList.contains(indentClassName);
    if (indent > 0 && !elementHasClassName) {
      dom.classList.add(indentClassName);
    } else if (indent < 1 && elementHasClassName) {
      dom.classList.remove(indentClassName);
    }
  }
  const indentationBaseValue = getComputedStyle(dom).getPropertyValue("--lexical-indent-base-value") || DEFAULT_INDENT_VALUE;
  dom.style.setProperty("padding-inline-start", indent === 0 ? "" : `calc(${indent} * ${indentationBaseValue})`);
}
function setElementFormat(dom, format) {
  const domStyle = dom.style;
  if (format === 0) {
    setTextAlign(domStyle, "");
  } else if (format === IS_ALIGN_LEFT) {
    setTextAlign(domStyle, "left");
  } else if (format === IS_ALIGN_CENTER) {
    setTextAlign(domStyle, "center");
  } else if (format === IS_ALIGN_RIGHT) {
    setTextAlign(domStyle, "right");
  } else if (format === IS_ALIGN_JUSTIFY) {
    setTextAlign(domStyle, "justify");
  } else if (format === IS_ALIGN_START) {
    setTextAlign(domStyle, "start");
  } else if (format === IS_ALIGN_END) {
    setTextAlign(domStyle, "end");
  }
}
function $getReconciledDirection(node) {
  const direction = node.__dir;
  if (direction !== null) {
    return direction;
  }
  if ($isRootNode(node)) {
    return null;
  }
  const parent = node.getParentOrThrow();
  if (!$isRootNode(parent) || parent.__dir !== null) {
    return null;
  }
  return "auto";
}
function $setElementDirection(dom, node) {
  const direction = $getReconciledDirection(node);
  if (direction !== null) {
    dom.dir = direction;
  } else {
    dom.removeAttribute("dir");
  }
}
function $createNode(key, slot) {
  const node = activeNextNodeMap.get(key);
  if (node === void 0) {
    {
      formatDevErrorMessage(`createNode: node does not exist in nodeMap`);
    }
  }
  const dom = node.createDOM(activeEditorConfig, activeEditor$1);
  storeDOMWithKey(key, dom, activeEditor$1);
  if ($isTextNode(node)) {
    dom.setAttribute("data-lexical-text", "true");
  } else if ($isDecoratorNode(node)) {
    dom.setAttribute("data-lexical-decorator", "true");
  }
  if ($isElementNode(node)) {
    const indent = node.__indent;
    const childrenSize = node.__size;
    $setElementDirection(dom, node);
    if (indent !== 0) {
      setElementIndent(dom, indent);
    }
    if (childrenSize !== 0) {
      const endIndex = childrenSize - 1;
      const children = createChildrenArray(node, activeNextNodeMap);
      $createChildren(children, node, 0, endIndex, node.getDOMSlot(dom));
    }
    const format = node.__format;
    if (format !== 0) {
      setElementFormat(dom, format);
    }
    if (!node.isInline()) {
      reconcileElementTerminatingLineBreak(null, node, dom);
    }
    if ($textContentRequiresDoubleLinebreakAtEnd(node)) {
      subTreeTextContent += DOUBLE_LINE_BREAK;
      editorTextContent += DOUBLE_LINE_BREAK;
    }
  } else {
    const text = node.getTextContent();
    if ($isDecoratorNode(node)) {
      const decorator = node.decorate(activeEditor$1, activeEditorConfig);
      if (decorator !== null) {
        reconcileDecorator(key, decorator);
      }
      dom.contentEditable = "false";
    }
    subTreeTextContent += text;
    editorTextContent += text;
  }
  if (slot !== null) {
    slot.insertChild(dom);
  }
  {
    Object.freeze(node);
  }
  setMutatedNode(mutatedNodes, activeEditorNodes, activeMutationListeners, node, "created");
  return dom;
}
function $createChildren(children, element, _startIndex, endIndex, slot) {
  const previousSubTreeTextContent = subTreeTextContent;
  subTreeTextContent = "";
  let startIndex = _startIndex;
  for (; startIndex <= endIndex; ++startIndex) {
    $createNode(children[startIndex], slot);
    const node = activeNextNodeMap.get(children[startIndex]);
    if (node !== null && $isTextNode(node)) {
      if (subTreeTextFormat === null) {
        subTreeTextFormat = node.getFormat();
      }
      if (subTreeTextStyle === "") {
        subTreeTextStyle = node.getStyle();
      }
    }
  }
  if ($textContentRequiresDoubleLinebreakAtEnd(element)) {
    subTreeTextContent += DOUBLE_LINE_BREAK;
  }
  const dom = slot.element;
  dom.__lexicalTextContent = subTreeTextContent;
  subTreeTextContent = previousSubTreeTextContent + subTreeTextContent;
}
function isLastChildLineBreakOrDecorator(element, nodeMap) {
  if (element) {
    const lastKey = element.__last;
    if (lastKey) {
      const node = nodeMap.get(lastKey);
      if (node) {
        return $isLineBreakNode(node) ? "line-break" : $isDecoratorNode(node) && node.isInline() ? "decorator" : null;
      }
    }
    return "empty";
  }
  return null;
}
function reconcileElementTerminatingLineBreak(prevElement, nextElement, dom) {
  const prevLineBreak = isLastChildLineBreakOrDecorator(prevElement, activePrevNodeMap);
  const nextLineBreak = isLastChildLineBreakOrDecorator(nextElement, activeNextNodeMap);
  if (prevLineBreak !== nextLineBreak) {
    nextElement.getDOMSlot(dom).setManagedLineBreak(nextLineBreak);
  }
}
function reconcileTextFormat(element) {
  if (subTreeTextFormat != null && subTreeTextFormat !== element.__textFormat && !activeEditorStateReadOnly) {
    element.setTextFormat(subTreeTextFormat);
  }
}
function reconcileTextStyle(element) {
  if (subTreeTextStyle !== "" && subTreeTextStyle !== element.__textStyle && !activeEditorStateReadOnly) {
    element.setTextStyle(subTreeTextStyle);
  }
}
function $reconcileChildrenWithDirection(prevElement, nextElement, dom) {
  subTreeTextFormat = null;
  subTreeTextStyle = "";
  $reconcileChildren(prevElement, nextElement, nextElement.getDOMSlot(dom));
  reconcileTextFormat(nextElement);
  reconcileTextStyle(nextElement);
}
function createChildrenArray(element, nodeMap) {
  const children = [];
  let nodeKey = element.__first;
  while (nodeKey !== null) {
    const node = nodeMap.get(nodeKey);
    if (node === void 0) {
      {
        formatDevErrorMessage(`createChildrenArray: node does not exist in nodeMap`);
      }
    }
    children.push(nodeKey);
    nodeKey = node.__next;
  }
  return children;
}
function $reconcileChildren(prevElement, nextElement, slot) {
  const previousSubTreeTextContent = subTreeTextContent;
  const prevChildrenSize = prevElement.__size;
  const nextChildrenSize = nextElement.__size;
  subTreeTextContent = "";
  const dom = slot.element;
  if (prevChildrenSize === 1 && nextChildrenSize === 1) {
    const prevFirstChildKey = prevElement.__first;
    const nextFirstChildKey = nextElement.__first;
    if (prevFirstChildKey === nextFirstChildKey) {
      $reconcileNode(prevFirstChildKey, dom);
    } else {
      const lastDOM = getPrevElementByKeyOrThrow(prevFirstChildKey);
      const replacementDOM = $createNode(nextFirstChildKey, null);
      try {
        dom.replaceChild(replacementDOM, lastDOM);
      } catch (error) {
        if (typeof error === "object" && error != null) {
          const msg = `${error.toString()} Parent: ${dom.tagName}, new child: {tag: ${replacementDOM.tagName} key: ${nextFirstChildKey}}, old child: {tag: ${lastDOM.tagName}, key: ${prevFirstChildKey}}.`;
          throw new Error(msg);
        } else {
          throw error;
        }
      }
      destroyNode(prevFirstChildKey, null);
    }
    const nextChildNode = activeNextNodeMap.get(nextFirstChildKey);
    if ($isTextNode(nextChildNode)) {
      if (subTreeTextFormat === null) {
        subTreeTextFormat = nextChildNode.getFormat();
      }
      if (subTreeTextStyle === "") {
        subTreeTextStyle = nextChildNode.getStyle();
      }
    }
  } else {
    const prevChildren = createChildrenArray(prevElement, activePrevNodeMap);
    const nextChildren = createChildrenArray(nextElement, activeNextNodeMap);
    if (!(prevChildren.length === prevChildrenSize)) {
      formatDevErrorMessage(`$reconcileChildren: prevChildren.length !== prevChildrenSize`);
    }
    if (!(nextChildren.length === nextChildrenSize)) {
      formatDevErrorMessage(`$reconcileChildren: nextChildren.length !== nextChildrenSize`);
    }
    if (prevChildrenSize === 0) {
      if (nextChildrenSize !== 0) {
        $createChildren(nextChildren, nextElement, 0, nextChildrenSize - 1, slot);
      }
    } else if (nextChildrenSize === 0) {
      if (prevChildrenSize !== 0) {
        const canUseFastPath = slot.after == null && slot.before == null && slot.element.__lexicalLineBreak == null;
        destroyChildren(prevChildren, 0, prevChildrenSize - 1, canUseFastPath ? null : dom);
        if (canUseFastPath) {
          dom.textContent = "";
        }
      }
    } else {
      $reconcileNodeChildren(nextElement, prevChildren, nextChildren, prevChildrenSize, nextChildrenSize, slot);
    }
  }
  if ($textContentRequiresDoubleLinebreakAtEnd(nextElement)) {
    subTreeTextContent += DOUBLE_LINE_BREAK;
  }
  dom.__lexicalTextContent = subTreeTextContent;
  subTreeTextContent = previousSubTreeTextContent + subTreeTextContent;
}
function $reconcileNode(key, parentDOM) {
  const prevNode = activePrevNodeMap.get(key);
  let nextNode = activeNextNodeMap.get(key);
  if (prevNode === void 0 || nextNode === void 0) {
    {
      formatDevErrorMessage(`reconcileNode: prevNode or nextNode does not exist in nodeMap`);
    }
  }
  const isDirty = treatAllNodesAsDirty || activeDirtyLeaves.has(key) || activeDirtyElements.has(key);
  const dom = getElementByKeyOrThrow(activeEditor$1, key);
  if (prevNode === nextNode && !isDirty) {
    if ($isElementNode(prevNode)) {
      const previousSubTreeTextContent = dom.__lexicalTextContent;
      if (previousSubTreeTextContent !== void 0) {
        subTreeTextContent += previousSubTreeTextContent;
        editorTextContent += previousSubTreeTextContent;
      }
    } else {
      const text = prevNode.getTextContent();
      editorTextContent += text;
      subTreeTextContent += text;
    }
    return dom;
  }
  if (prevNode !== nextNode && isDirty) {
    setMutatedNode(mutatedNodes, activeEditorNodes, activeMutationListeners, nextNode, "updated");
  }
  if (nextNode.updateDOM(prevNode, dom, activeEditorConfig)) {
    const replacementDOM = $createNode(key, null);
    if (parentDOM === null) {
      {
        formatDevErrorMessage(`reconcileNode: parentDOM is null`);
      }
    }
    parentDOM.replaceChild(replacementDOM, dom);
    destroyNode(key, null);
    return replacementDOM;
  }
  if ($isElementNode(prevNode) && $isElementNode(nextNode)) {
    const nextIndent = nextNode.__indent;
    if (treatAllNodesAsDirty || nextIndent !== prevNode.__indent) {
      setElementIndent(dom, nextIndent);
    }
    const nextFormat = nextNode.__format;
    if (treatAllNodesAsDirty || nextFormat !== prevNode.__format) {
      setElementFormat(dom, nextFormat);
    }
    if (isDirty) {
      $reconcileChildrenWithDirection(prevNode, nextNode, dom);
      if (!$isRootNode(nextNode) && !nextNode.isInline()) {
        reconcileElementTerminatingLineBreak(prevNode, nextNode, dom);
      }
    }
    if ($textContentRequiresDoubleLinebreakAtEnd(nextNode)) {
      subTreeTextContent += DOUBLE_LINE_BREAK;
      editorTextContent += DOUBLE_LINE_BREAK;
    }
    if (treatAllNodesAsDirty || nextNode.__dir !== prevNode.__dir) {
      $setElementDirection(dom, nextNode);
      if (
        // Root node direction changing from set to unset (or vice versa)
        // changes how children's direction is calculated.
        $isRootNode(nextNode) && // Can skip if all children already reconciled.
        !treatAllNodesAsDirty
      ) {
        for (const child of nextNode.getChildren()) {
          if ($isElementNode(child)) {
            const childDom = getElementByKeyOrThrow(activeEditor$1, child.getKey());
            $setElementDirection(childDom, child);
          }
        }
      }
    }
  } else {
    const text = nextNode.getTextContent();
    if ($isDecoratorNode(nextNode)) {
      const decorator = nextNode.decorate(activeEditor$1, activeEditorConfig);
      if (decorator !== null) {
        reconcileDecorator(key, decorator);
      }
    }
    subTreeTextContent += text;
    editorTextContent += text;
  }
  if (!activeEditorStateReadOnly && $isRootNode(nextNode) && nextNode.__cachedText !== editorTextContent) {
    const nextRootNode = nextNode.getWritable();
    nextRootNode.__cachedText = editorTextContent;
    nextNode = nextRootNode;
  }
  {
    Object.freeze(nextNode);
  }
  return dom;
}
function reconcileDecorator(key, decorator) {
  let pendingDecorators = activeEditor$1._pendingDecorators;
  const currentDecorators = activeEditor$1._decorators;
  if (pendingDecorators === null) {
    if (currentDecorators[key] === decorator) {
      return;
    }
    pendingDecorators = cloneDecorators(activeEditor$1);
  }
  pendingDecorators[key] = decorator;
}
function getNextSibling(element) {
  let nextSibling = element.nextSibling;
  if (nextSibling !== null && nextSibling === activeEditor$1._blockCursorElement) {
    nextSibling = nextSibling.nextSibling;
  }
  return nextSibling;
}
function $reconcileNodeChildren(nextElement, prevChildren, nextChildren, prevChildrenLength, nextChildrenLength, slot) {
  const prevEndIndex = prevChildrenLength - 1;
  const nextEndIndex = nextChildrenLength - 1;
  let prevChildrenSet;
  let nextChildrenSet;
  let siblingDOM = slot.getFirstChild();
  let prevIndex = 0;
  let nextIndex = 0;
  while (prevIndex <= prevEndIndex && nextIndex <= nextEndIndex) {
    const prevKey = prevChildren[prevIndex];
    const nextKey = nextChildren[nextIndex];
    if (prevKey === nextKey) {
      siblingDOM = getNextSibling($reconcileNode(nextKey, slot.element));
      prevIndex++;
      nextIndex++;
    } else {
      if (prevChildrenSet === void 0) {
        prevChildrenSet = new Set(prevChildren);
      }
      if (nextChildrenSet === void 0) {
        nextChildrenSet = new Set(nextChildren);
      }
      const nextHasPrevKey = nextChildrenSet.has(prevKey);
      const prevHasNextKey = prevChildrenSet.has(nextKey);
      if (!nextHasPrevKey) {
        siblingDOM = getNextSibling(getPrevElementByKeyOrThrow(prevKey));
        destroyNode(prevKey, slot.element);
        prevIndex++;
      } else if (!prevHasNextKey) {
        $createNode(nextKey, slot.withBefore(siblingDOM));
        nextIndex++;
      } else {
        const childDOM = getElementByKeyOrThrow(activeEditor$1, nextKey);
        if (childDOM === siblingDOM) {
          siblingDOM = getNextSibling($reconcileNode(nextKey, slot.element));
        } else {
          slot.withBefore(siblingDOM).insertChild(childDOM);
          $reconcileNode(nextKey, slot.element);
        }
        prevIndex++;
        nextIndex++;
      }
    }
    const node = activeNextNodeMap.get(nextKey);
    if (node !== null && $isTextNode(node)) {
      if (subTreeTextFormat === null) {
        subTreeTextFormat = node.getFormat();
      }
      if (subTreeTextStyle === "") {
        subTreeTextStyle = node.getStyle();
      }
    }
  }
  const appendNewChildren = prevIndex > prevEndIndex;
  const removeOldChildren = nextIndex > nextEndIndex;
  if (appendNewChildren && !removeOldChildren) {
    const previousNode = nextChildren[nextEndIndex + 1];
    const insertDOM = previousNode === void 0 ? null : activeEditor$1.getElementByKey(previousNode);
    $createChildren(nextChildren, nextElement, nextIndex, nextEndIndex, slot.withBefore(insertDOM));
  } else if (removeOldChildren && !appendNewChildren) {
    destroyChildren(prevChildren, prevIndex, prevEndIndex, slot.element);
  }
}
function $reconcileRoot(prevEditorState, nextEditorState, editor, dirtyType, dirtyElements, dirtyLeaves) {
  subTreeTextContent = "";
  editorTextContent = "";
  treatAllNodesAsDirty = dirtyType === FULL_RECONCILE;
  activeEditor$1 = editor;
  activeEditorConfig = editor._config;
  activeEditorNodes = editor._nodes;
  activeMutationListeners = activeEditor$1._listeners.mutation;
  activeDirtyElements = dirtyElements;
  activeDirtyLeaves = dirtyLeaves;
  activePrevNodeMap = prevEditorState._nodeMap;
  activeNextNodeMap = nextEditorState._nodeMap;
  activeEditorStateReadOnly = nextEditorState._readOnly;
  activePrevKeyToDOMMap = new Map(editor._keyToDOMMap);
  const currentMutatedNodes = /* @__PURE__ */ new Map();
  mutatedNodes = currentMutatedNodes;
  $reconcileNode("root", null);
  activeEditor$1 = void 0;
  activeEditorNodes = void 0;
  activeDirtyElements = void 0;
  activeDirtyLeaves = void 0;
  activePrevNodeMap = void 0;
  activeNextNodeMap = void 0;
  activeEditorConfig = void 0;
  activePrevKeyToDOMMap = void 0;
  mutatedNodes = void 0;
  return currentMutatedNodes;
}
function storeDOMWithKey(key, dom, editor) {
  const keyToDOMMap = editor._keyToDOMMap;
  setNodeKeyOnDOMNode(dom, editor, key);
  keyToDOMMap.set(key, dom);
}
function getPrevElementByKeyOrThrow(key) {
  const element = activePrevKeyToDOMMap.get(key);
  if (element === void 0) {
    {
      formatDevErrorMessage(`Reconciliation: could not find DOM element for node key ${key}`);
    }
  }
  return element;
}
function warnOnlyOnce(message) {
  {
    let run = false;
    return () => {
      if (!run) {
        console.warn(message);
      }
      run = true;
    };
  }
}
function createCommand(type) {
  return {
    type
  };
}
var SELECTION_CHANGE_COMMAND = createCommand("SELECTION_CHANGE_COMMAND");
var SELECTION_INSERT_CLIPBOARD_NODES_COMMAND = createCommand("SELECTION_INSERT_CLIPBOARD_NODES_COMMAND");
var CLICK_COMMAND = createCommand("CLICK_COMMAND");
var DELETE_CHARACTER_COMMAND = createCommand("DELETE_CHARACTER_COMMAND");
var INSERT_LINE_BREAK_COMMAND = createCommand("INSERT_LINE_BREAK_COMMAND");
var INSERT_PARAGRAPH_COMMAND = createCommand("INSERT_PARAGRAPH_COMMAND");
var CONTROLLED_TEXT_INSERTION_COMMAND = createCommand("CONTROLLED_TEXT_INSERTION_COMMAND");
var PASTE_COMMAND = createCommand("PASTE_COMMAND");
var REMOVE_TEXT_COMMAND = createCommand("REMOVE_TEXT_COMMAND");
var DELETE_WORD_COMMAND = createCommand("DELETE_WORD_COMMAND");
var DELETE_LINE_COMMAND = createCommand("DELETE_LINE_COMMAND");
var FORMAT_TEXT_COMMAND = createCommand("FORMAT_TEXT_COMMAND");
var UNDO_COMMAND = createCommand("UNDO_COMMAND");
var REDO_COMMAND = createCommand("REDO_COMMAND");
var KEY_DOWN_COMMAND = createCommand("KEYDOWN_COMMAND");
var KEY_ARROW_RIGHT_COMMAND = createCommand("KEY_ARROW_RIGHT_COMMAND");
var MOVE_TO_END = createCommand("MOVE_TO_END");
var KEY_ARROW_LEFT_COMMAND = createCommand("KEY_ARROW_LEFT_COMMAND");
var MOVE_TO_START = createCommand("MOVE_TO_START");
var KEY_ARROW_UP_COMMAND = createCommand("KEY_ARROW_UP_COMMAND");
var KEY_ARROW_DOWN_COMMAND = createCommand("KEY_ARROW_DOWN_COMMAND");
var KEY_ENTER_COMMAND = createCommand("KEY_ENTER_COMMAND");
var KEY_SPACE_COMMAND = createCommand("KEY_SPACE_COMMAND");
var KEY_BACKSPACE_COMMAND = createCommand("KEY_BACKSPACE_COMMAND");
var KEY_ESCAPE_COMMAND = createCommand("KEY_ESCAPE_COMMAND");
var KEY_DELETE_COMMAND = createCommand("KEY_DELETE_COMMAND");
var KEY_TAB_COMMAND = createCommand("KEY_TAB_COMMAND");
var INSERT_TAB_COMMAND = createCommand("INSERT_TAB_COMMAND");
var INDENT_CONTENT_COMMAND = createCommand("INDENT_CONTENT_COMMAND");
var OUTDENT_CONTENT_COMMAND = createCommand("OUTDENT_CONTENT_COMMAND");
var DROP_COMMAND = createCommand("DROP_COMMAND");
var FORMAT_ELEMENT_COMMAND = createCommand("FORMAT_ELEMENT_COMMAND");
var DRAGSTART_COMMAND = createCommand("DRAGSTART_COMMAND");
var DRAGOVER_COMMAND = createCommand("DRAGOVER_COMMAND");
var DRAGEND_COMMAND = createCommand("DRAGEND_COMMAND");
var COPY_COMMAND = createCommand("COPY_COMMAND");
var CUT_COMMAND = createCommand("CUT_COMMAND");
var SELECT_ALL_COMMAND = createCommand("SELECT_ALL_COMMAND");
var CLEAR_EDITOR_COMMAND = createCommand("CLEAR_EDITOR_COMMAND");
var CLEAR_HISTORY_COMMAND = createCommand("CLEAR_HISTORY_COMMAND");
var CAN_REDO_COMMAND = createCommand("CAN_REDO_COMMAND");
var CAN_UNDO_COMMAND = createCommand("CAN_UNDO_COMMAND");
var FOCUS_COMMAND = createCommand("FOCUS_COMMAND");
var BLUR_COMMAND = createCommand("BLUR_COMMAND");
var KEY_MODIFIER_COMMAND = createCommand("KEY_MODIFIER_COMMAND");
var PASS_THROUGH_COMMAND = Object.freeze({});
var ANDROID_COMPOSITION_LATENCY = 30;
var rootElementEvents = [["keydown", onKeyDown], ["pointerdown", onPointerDown], ["compositionstart", onCompositionStart], ["compositionend", onCompositionEnd], ["input", onInput], ["click", onClick], ["cut", PASS_THROUGH_COMMAND], ["copy", PASS_THROUGH_COMMAND], ["dragstart", PASS_THROUGH_COMMAND], ["dragover", PASS_THROUGH_COMMAND], ["dragend", PASS_THROUGH_COMMAND], ["paste", PASS_THROUGH_COMMAND], ["focus", PASS_THROUGH_COMMAND], ["blur", PASS_THROUGH_COMMAND], ["drop", PASS_THROUGH_COMMAND]];
if (CAN_USE_BEFORE_INPUT) {
  rootElementEvents.push(["beforeinput", (event, editor) => onBeforeInput(event, editor)]);
}
var lastKeyDownTimeStamp = 0;
var lastKeyCode = null;
var lastBeforeInputInsertTextTimeStamp = 0;
var unprocessedBeforeInputData = null;
var rootElementsRegistered = /* @__PURE__ */ new WeakMap();
var isSelectionChangeFromDOMUpdate = false;
var isSelectionChangeFromMouseDown = false;
var isInsertLineBreak = false;
var isFirefoxEndingComposition = false;
var isSafariEndingComposition = false;
var safariEndCompositionEventData = "";
var postDeleteSelectionToRestore = null;
var collapsedSelectionFormat = [0, "", 0, "root", 0];
function $shouldPreventDefaultAndInsertText(selection, domTargetRange, text, timeStamp, isBeforeInput) {
  const anchor = selection.anchor;
  const focus = selection.focus;
  const anchorNode = anchor.getNode();
  const editor = getActiveEditor();
  const domSelection = getDOMSelection(getWindow(editor));
  const domAnchorNode = domSelection !== null ? domSelection.anchorNode : null;
  const anchorKey = anchor.key;
  const backingAnchorElement = editor.getElementByKey(anchorKey);
  const textLength = text.length;
  return anchorKey !== focus.key || // If we're working with a non-text node.
  !$isTextNode(anchorNode) || // If we are replacing a range with a single character or grapheme, and not composing.
  (!isBeforeInput && (!CAN_USE_BEFORE_INPUT || // We check to see if there has been
  // a recent beforeinput event for "textInput". If there has been one in the last
  // 50ms then we proceed as normal. However, if there is not, then this is likely
  // a dangling `input` event caused by execCommand('insertText').
  lastBeforeInputInsertTextTimeStamp < timeStamp + 50) || anchorNode.isDirty() && textLength < 2 || // TODO consider if there are other scenarios when multiple code units
  //      should be addressed here
  doesContainSurrogatePair(text)) && anchor.offset !== focus.offset && !anchorNode.isComposing() || // Any non standard text node.
  $isTokenOrSegmented(anchorNode) || // If the text length is more than a single character and we're either
  // dealing with this in "beforeinput" or where the node has already recently
  // been changed (thus is dirty).
  anchorNode.isDirty() && textLength > 1 || // If the DOM selection element is not the same as the backing node during beforeinput.
  (isBeforeInput || !CAN_USE_BEFORE_INPUT) && backingAnchorElement !== null && !anchorNode.isComposing() && domAnchorNode !== getDOMTextNode(backingAnchorElement) || // If TargetRange is not the same as the DOM selection; browser trying to edit random parts
  // of the editor.
  domSelection !== null && domTargetRange !== null && (!domTargetRange.collapsed || domTargetRange.startContainer !== domSelection.anchorNode || domTargetRange.startOffset !== domSelection.anchorOffset) || // Check if we're changing from bold to italics, or some other format.
  anchorNode.getFormat() !== selection.format || anchorNode.getStyle() !== selection.style || // One last set of heuristics to check against.
  $shouldInsertTextAfterOrBeforeTextNode(selection, anchorNode);
}
function shouldSkipSelectionChange(domNode, offset) {
  return isDOMTextNode(domNode) && domNode.nodeValue !== null && offset !== 0 && offset !== domNode.nodeValue.length;
}
function onSelectionChange(domSelection, editor, isActive) {
  const {
    anchorNode: anchorDOM,
    anchorOffset,
    focusNode: focusDOM,
    focusOffset
  } = domSelection;
  if (isSelectionChangeFromDOMUpdate) {
    isSelectionChangeFromDOMUpdate = false;
    if (shouldSkipSelectionChange(anchorDOM, anchorOffset) && shouldSkipSelectionChange(focusDOM, focusOffset) && !postDeleteSelectionToRestore) {
      return;
    }
  }
  updateEditorSync(editor, () => {
    if (!isActive) {
      $setSelection(null);
      return;
    }
    if (!isSelectionWithinEditor(editor, anchorDOM, focusDOM)) {
      return;
    }
    let selection = $getSelection();
    if (postDeleteSelectionToRestore && $isRangeSelection(selection) && selection.isCollapsed()) {
      const curAnchor = selection.anchor;
      const prevAnchor = postDeleteSelectionToRestore.anchor;
      if (
        // Rightward shift in same node
        curAnchor.key === prevAnchor.key && curAnchor.offset === prevAnchor.offset + 1 || // Or rightward shift into sibling node
        curAnchor.offset === 1 && prevAnchor.getNode().is(curAnchor.getNode().getPreviousSibling())
      ) {
        selection = postDeleteSelectionToRestore.clone();
        $setSelection(selection);
      }
    }
    postDeleteSelectionToRestore = null;
    if ($isRangeSelection(selection)) {
      const anchor = selection.anchor;
      const anchorNode = anchor.getNode();
      if (selection.isCollapsed()) {
        if (domSelection.type === "Range" && domSelection.anchorNode === domSelection.focusNode) {
          selection.dirty = true;
        }
        const windowEvent = getWindow(editor).event;
        const currentTimeStamp = windowEvent ? windowEvent.timeStamp : performance.now();
        const [lastFormat, lastStyle, lastOffset, lastKey, timeStamp] = collapsedSelectionFormat;
        const root = $getRoot();
        const isRootTextContentEmpty = editor.isComposing() === false && root.getTextContent() === "";
        if (currentTimeStamp < timeStamp + 200 && anchor.offset === lastOffset && anchor.key === lastKey) {
          $updateSelectionFormatStyle(selection, lastFormat, lastStyle);
        } else {
          if (anchor.type === "text") {
            if (!$isTextNode(anchorNode)) {
              formatDevErrorMessage(`Point.getNode() must return TextNode when type is text`);
            }
            $updateSelectionFormatStyleFromTextNode(selection, anchorNode);
          } else if (anchor.type === "element" && !isRootTextContentEmpty) {
            if (!$isElementNode(anchorNode)) {
              formatDevErrorMessage(`Point.getNode() must return ElementNode when type is element`);
            }
            const lastNode = anchor.getNode();
            if (
              // This previously applied to all ParagraphNode
              lastNode.isEmpty()
            ) {
              $updateSelectionFormatStyleFromElementNode(selection, lastNode);
            } else {
              $updateSelectionFormatStyle(selection, 0, "");
            }
          }
        }
      } else {
        const anchorKey = anchor.key;
        const focus = selection.focus;
        const focusKey = focus.key;
        const nodes = selection.getNodes();
        const nodesLength = nodes.length;
        const isBackward = selection.isBackward();
        const startOffset = isBackward ? focusOffset : anchorOffset;
        const endOffset = isBackward ? anchorOffset : focusOffset;
        const startKey = isBackward ? focusKey : anchorKey;
        const endKey = isBackward ? anchorKey : focusKey;
        let combinedFormat = IS_ALL_FORMATTING;
        let hasTextNodes = false;
        for (let i2 = 0; i2 < nodesLength; i2++) {
          const node = nodes[i2];
          const textContentSize = node.getTextContentSize();
          if ($isTextNode(node) && textContentSize !== 0 && // Exclude empty text nodes at boundaries resulting from user's selection
          !(i2 === 0 && node.__key === startKey && startOffset === textContentSize || i2 === nodesLength - 1 && node.__key === endKey && endOffset === 0)) {
            hasTextNodes = true;
            combinedFormat &= node.getFormat();
            if (combinedFormat === 0) {
              break;
            }
          }
        }
        selection.format = hasTextNodes ? combinedFormat : 0;
      }
    }
    dispatchCommand(editor, SELECTION_CHANGE_COMMAND, void 0);
  });
}
function $updateSelectionFormatStyle(selection, format, style) {
  if (selection.format !== format || selection.style !== style) {
    selection.format = format;
    selection.style = style;
    selection.dirty = true;
  }
}
function $updateSelectionFormatStyleFromTextNode(selection, node) {
  const format = node.getFormat();
  const style = node.getStyle();
  $updateSelectionFormatStyle(selection, format, style);
}
function $updateSelectionFormatStyleFromElementNode(selection, node) {
  const format = node.getTextFormat();
  const style = node.getTextStyle();
  $updateSelectionFormatStyle(selection, format, style);
}
function onClick(event, editor) {
  updateEditorSync(editor, () => {
    const selection = $getSelection();
    const domSelection = getDOMSelection(getWindow(editor));
    const lastSelection = $getPreviousSelection();
    if (domSelection) {
      if ($isRangeSelection(selection)) {
        const anchor = selection.anchor;
        const anchorNode = anchor.getNode();
        if (anchor.type === "element" && anchor.offset === 0 && selection.isCollapsed() && !$isRootNode(anchorNode) && $getRoot().getChildrenSize() === 1 && anchorNode.getTopLevelElementOrThrow().isEmpty() && lastSelection !== null && selection.is(lastSelection)) {
          domSelection.removeAllRanges();
          selection.dirty = true;
        } else if (event.detail === 3 && !selection.isCollapsed()) {
          const focus = selection.focus;
          const focusNode = focus.getNode();
          if (anchorNode !== focusNode) {
            const parentNode = $findMatchingParent(anchorNode, (node) => $isElementNode(node) && !node.isInline());
            if ($isElementNode(parentNode)) {
              parentNode.select(0);
            }
          }
        }
      } else if (event.pointerType === "touch" || event.pointerType === "pen") {
        const domAnchorNode = domSelection.anchorNode;
        if (isHTMLElement(domAnchorNode) || isDOMTextNode(domAnchorNode)) {
          const newSelection = $internalCreateRangeSelection(lastSelection, domSelection, editor, event);
          $setSelection(newSelection);
        }
      }
    }
    dispatchCommand(editor, CLICK_COMMAND, event);
  });
}
function onPointerDown(event, editor) {
  const target = event.target;
  const pointerType = event.pointerType;
  if (isDOMNode(target) && pointerType !== "touch" && pointerType !== "pen" && event.button === 0) {
    updateEditorSync(editor, () => {
      if (!$isSelectionCapturedInDecorator(target)) {
        isSelectionChangeFromMouseDown = true;
      }
    });
  }
}
function getTargetRange(event) {
  if (!event.getTargetRanges) {
    return null;
  }
  const targetRanges = event.getTargetRanges();
  if (targetRanges.length === 0) {
    return null;
  }
  return targetRanges[0];
}
function $canRemoveText(anchorNode, focusNode) {
  return anchorNode !== focusNode || $isElementNode(anchorNode) || $isElementNode(focusNode) || !$isTokenOrTab(anchorNode) || !$isTokenOrTab(focusNode);
}
function isPossiblyAndroidKeyPress(timeStamp) {
  return lastKeyCode === "MediaLast" && timeStamp < lastKeyDownTimeStamp + ANDROID_COMPOSITION_LATENCY;
}
function onBeforeInput(event, editor) {
  const inputType = event.inputType;
  const targetRange = getTargetRange(event);
  if (inputType === "deleteCompositionText" || // If we're pasting in FF, we shouldn't get this event
  // as the `paste` event should have triggered, unless the
  // user has dom.event.clipboardevents.enabled disabled in
  // about:config. In that case, we need to process the
  // pasted content in the DOM mutation phase.
  IS_FIREFOX && isFirefoxClipboardEvents(editor)) {
    return;
  } else if (inputType === "insertCompositionText") {
    return;
  }
  updateEditorSync(editor, () => {
    const selection = $getSelection();
    if (inputType === "deleteContentBackward") {
      if (selection === null) {
        const prevSelection = $getPreviousSelection();
        if (!$isRangeSelection(prevSelection)) {
          return;
        }
        $setSelection(prevSelection.clone());
      }
      if ($isRangeSelection(selection)) {
        const isSelectionAnchorSameAsFocus = selection.anchor.key === selection.focus.key;
        if (isPossiblyAndroidKeyPress(event.timeStamp) && editor.isComposing() && isSelectionAnchorSameAsFocus) {
          $setCompositionKey(null);
          lastKeyDownTimeStamp = 0;
          setTimeout(() => {
            updateEditorSync(editor, () => {
              $setCompositionKey(null);
            });
          }, ANDROID_COMPOSITION_LATENCY);
          if ($isRangeSelection(selection)) {
            const anchorNode2 = selection.anchor.getNode();
            anchorNode2.markDirty();
            if (!$isTextNode(anchorNode2)) {
              formatDevErrorMessage(`Anchor node must be a TextNode`);
            }
            $updateSelectionFormatStyleFromTextNode(selection, anchorNode2);
          }
        } else {
          $setCompositionKey(null);
          event.preventDefault();
          const selectedNode = selection.anchor.getNode();
          const selectedNodeText = selectedNode.getTextContent();
          const selectedNodeCanInsertTextAfter = selectedNode.canInsertTextAfter();
          const hasSelectedAllTextInNode = selection.anchor.offset === 0 && selection.focus.offset === selectedNodeText.length;
          let shouldLetBrowserHandleDelete = IS_ANDROID_CHROME && isSelectionAnchorSameAsFocus && !hasSelectedAllTextInNode && selectedNodeCanInsertTextAfter;
          if (shouldLetBrowserHandleDelete && selection.isCollapsed()) {
            shouldLetBrowserHandleDelete = !$isDecoratorNode($getAdjacentNode(selection.anchor, true));
          }
          if (!shouldLetBrowserHandleDelete) {
            dispatchCommand(editor, DELETE_CHARACTER_COMMAND, true);
            const selectionAfterDelete = $getSelection();
            if (IS_ANDROID_CHROME && $isRangeSelection(selectionAfterDelete) && selectionAfterDelete.isCollapsed()) {
              postDeleteSelectionToRestore = selectionAfterDelete;
              setTimeout(() => postDeleteSelectionToRestore = null);
            }
          }
        }
        return;
      }
    }
    if (!$isRangeSelection(selection)) {
      return;
    }
    const data = event.data;
    if (unprocessedBeforeInputData !== null) {
      $updateSelectedTextFromDOM(false, editor, unprocessedBeforeInputData);
    }
    if ((!selection.dirty || unprocessedBeforeInputData !== null) && selection.isCollapsed() && !$isRootNode(selection.anchor.getNode()) && targetRange !== null) {
      selection.applyDOMRange(targetRange);
    }
    unprocessedBeforeInputData = null;
    const anchor = selection.anchor;
    const focus = selection.focus;
    const anchorNode = anchor.getNode();
    const focusNode = focus.getNode();
    if (inputType === "insertText" || inputType === "insertTranspose") {
      if (data === "\n") {
        event.preventDefault();
        dispatchCommand(editor, INSERT_LINE_BREAK_COMMAND, false);
      } else if (data === DOUBLE_LINE_BREAK) {
        event.preventDefault();
        dispatchCommand(editor, INSERT_PARAGRAPH_COMMAND, void 0);
      } else if (data == null && event.dataTransfer) {
        const text = event.dataTransfer.getData("text/plain");
        event.preventDefault();
        selection.insertRawText(text);
      } else if (data != null && $shouldPreventDefaultAndInsertText(selection, targetRange, data, event.timeStamp, true)) {
        event.preventDefault();
        dispatchCommand(editor, CONTROLLED_TEXT_INSERTION_COMMAND, data);
      } else {
        unprocessedBeforeInputData = data;
      }
      lastBeforeInputInsertTextTimeStamp = event.timeStamp;
      return;
    }
    event.preventDefault();
    switch (inputType) {
      case "insertFromYank":
      case "insertFromDrop":
      case "insertReplacementText": {
        dispatchCommand(editor, CONTROLLED_TEXT_INSERTION_COMMAND, event);
        break;
      }
      case "insertFromComposition": {
        $setCompositionKey(null);
        dispatchCommand(editor, CONTROLLED_TEXT_INSERTION_COMMAND, event);
        break;
      }
      case "insertLineBreak": {
        $setCompositionKey(null);
        dispatchCommand(editor, INSERT_LINE_BREAK_COMMAND, false);
        break;
      }
      case "insertParagraph": {
        $setCompositionKey(null);
        if (isInsertLineBreak && !IS_IOS) {
          isInsertLineBreak = false;
          dispatchCommand(editor, INSERT_LINE_BREAK_COMMAND, false);
        } else {
          dispatchCommand(editor, INSERT_PARAGRAPH_COMMAND, void 0);
        }
        break;
      }
      case "insertFromPaste":
      case "insertFromPasteAsQuotation": {
        dispatchCommand(editor, PASTE_COMMAND, event);
        break;
      }
      case "deleteByComposition": {
        if ($canRemoveText(anchorNode, focusNode)) {
          dispatchCommand(editor, REMOVE_TEXT_COMMAND, event);
        }
        break;
      }
      case "deleteByDrag":
      case "deleteByCut": {
        dispatchCommand(editor, REMOVE_TEXT_COMMAND, event);
        break;
      }
      case "deleteContent": {
        dispatchCommand(editor, DELETE_CHARACTER_COMMAND, false);
        break;
      }
      case "deleteWordBackward": {
        dispatchCommand(editor, DELETE_WORD_COMMAND, true);
        break;
      }
      case "deleteWordForward": {
        dispatchCommand(editor, DELETE_WORD_COMMAND, false);
        break;
      }
      case "deleteHardLineBackward":
      case "deleteSoftLineBackward": {
        dispatchCommand(editor, DELETE_LINE_COMMAND, true);
        break;
      }
      case "deleteContentForward":
      case "deleteHardLineForward":
      case "deleteSoftLineForward": {
        dispatchCommand(editor, DELETE_LINE_COMMAND, false);
        break;
      }
      case "formatStrikeThrough": {
        dispatchCommand(editor, FORMAT_TEXT_COMMAND, "strikethrough");
        break;
      }
      case "formatBold": {
        dispatchCommand(editor, FORMAT_TEXT_COMMAND, "bold");
        break;
      }
      case "formatItalic": {
        dispatchCommand(editor, FORMAT_TEXT_COMMAND, "italic");
        break;
      }
      case "formatUnderline": {
        dispatchCommand(editor, FORMAT_TEXT_COMMAND, "underline");
        break;
      }
      case "historyUndo": {
        dispatchCommand(editor, UNDO_COMMAND, void 0);
        break;
      }
      case "historyRedo": {
        dispatchCommand(editor, REDO_COMMAND, void 0);
        break;
      }
    }
  });
}
function onInput(event, editor) {
  event.stopPropagation();
  updateEditorSync(editor, () => {
    if (isHTMLElement(event.target) && $isSelectionCapturedInDecorator(event.target)) {
      return;
    }
    const selection = $getSelection();
    const data = event.data;
    const targetRange = getTargetRange(event);
    if (data != null && $isRangeSelection(selection) && $shouldPreventDefaultAndInsertText(selection, targetRange, data, event.timeStamp, false)) {
      if (isFirefoxEndingComposition) {
        $onCompositionEndImpl(editor, data);
        isFirefoxEndingComposition = false;
      }
      const anchor = selection.anchor;
      const anchorNode = anchor.getNode();
      const domSelection = getDOMSelection(getWindow(editor));
      if (domSelection === null) {
        return;
      }
      const isBackward = selection.isBackward();
      const startOffset = isBackward ? selection.anchor.offset : selection.focus.offset;
      const endOffset = isBackward ? selection.focus.offset : selection.anchor.offset;
      if (!CAN_USE_BEFORE_INPUT || selection.isCollapsed() || !$isTextNode(anchorNode) || domSelection.anchorNode === null || anchorNode.getTextContent().slice(0, startOffset) + data + anchorNode.getTextContent().slice(startOffset + endOffset) !== getAnchorTextFromDOM(domSelection.anchorNode)) {
        dispatchCommand(editor, CONTROLLED_TEXT_INSERTION_COMMAND, data);
      }
      const textLength = data.length;
      if (IS_FIREFOX && textLength > 1 && event.inputType === "insertCompositionText" && !editor.isComposing()) {
        selection.anchor.offset -= textLength;
      }
      if (!IS_SAFARI && !IS_IOS && !IS_APPLE_WEBKIT && editor.isComposing()) {
        lastKeyDownTimeStamp = 0;
        $setCompositionKey(null);
      }
    } else {
      const characterData = data !== null ? data : void 0;
      $updateSelectedTextFromDOM(false, editor, characterData);
      if (isFirefoxEndingComposition) {
        $onCompositionEndImpl(editor, data || void 0);
        isFirefoxEndingComposition = false;
      }
    }
    $flushMutations();
  }, {
    event
  });
  unprocessedBeforeInputData = null;
}
function onCompositionStart(event, editor) {
  updateEditorSync(editor, () => {
    const selection = $getSelection();
    if ($isRangeSelection(selection) && !editor.isComposing()) {
      const anchor = selection.anchor;
      const node = selection.anchor.getNode();
      $setCompositionKey(anchor.key);
      if (
        // If it has been 30ms since the last keydown, then we should
        // apply the empty space heuristic. We can't do this for Safari,
        // as the keydown fires after composition start.
        event.timeStamp < lastKeyDownTimeStamp + ANDROID_COMPOSITION_LATENCY || // FF has issues around composing multibyte characters, so we also
        // need to invoke the empty space heuristic below.
        anchor.type === "element" || !selection.isCollapsed() || node.getFormat() !== selection.format || $isTextNode(node) && node.getStyle() !== selection.style
      ) {
        dispatchCommand(editor, CONTROLLED_TEXT_INSERTION_COMMAND, COMPOSITION_START_CHAR);
      }
    }
  });
}
function $onCompositionEndImpl(editor, data) {
  const compositionKey = editor._compositionKey;
  $setCompositionKey(null);
  if (compositionKey !== null && data != null) {
    if (data === "") {
      const node = $getNodeByKey(compositionKey);
      const textNode = getDOMTextNode(editor.getElementByKey(compositionKey));
      if (textNode !== null && textNode.nodeValue !== null && $isTextNode(node)) {
        $updateTextNodeFromDOMContent(node, textNode.nodeValue, null, null, true);
      }
      return;
    }
    if (data[data.length - 1] === "\n") {
      const selection = $getSelection();
      if ($isRangeSelection(selection)) {
        const focus = selection.focus;
        selection.anchor.set(focus.key, focus.offset, focus.type);
        dispatchCommand(editor, KEY_ENTER_COMMAND, null);
        return;
      }
    }
  }
  $updateSelectedTextFromDOM(true, editor, data);
}
function onCompositionEnd(event, editor) {
  if (IS_FIREFOX) {
    isFirefoxEndingComposition = true;
  } else if (!IS_IOS && (IS_SAFARI || IS_APPLE_WEBKIT)) {
    isSafariEndingComposition = true;
    safariEndCompositionEventData = event.data;
  } else {
    updateEditorSync(editor, () => {
      $onCompositionEndImpl(editor, event.data);
    });
  }
}
function onKeyDown(event, editor) {
  lastKeyDownTimeStamp = event.timeStamp;
  lastKeyCode = event.key;
  if (editor.isComposing()) {
    return;
  }
  if (dispatchCommand(editor, KEY_DOWN_COMMAND, event)) {
    return;
  }
  if (event.key == null) {
    return;
  }
  if (isSafariEndingComposition && isBackspace(event)) {
    updateEditorSync(editor, () => {
      $onCompositionEndImpl(editor, safariEndCompositionEventData);
    });
    isSafariEndingComposition = false;
    safariEndCompositionEventData = "";
    return;
  }
  if (isMoveForward(event)) {
    dispatchCommand(editor, KEY_ARROW_RIGHT_COMMAND, event);
  } else if (isMoveToEnd(event)) {
    dispatchCommand(editor, MOVE_TO_END, event);
  } else if (isMoveBackward(event)) {
    dispatchCommand(editor, KEY_ARROW_LEFT_COMMAND, event);
  } else if (isMoveToStart(event)) {
    dispatchCommand(editor, MOVE_TO_START, event);
  } else if (isMoveUp(event)) {
    dispatchCommand(editor, KEY_ARROW_UP_COMMAND, event);
  } else if (isMoveDown(event)) {
    dispatchCommand(editor, KEY_ARROW_DOWN_COMMAND, event);
  } else if (isLineBreak(event)) {
    isInsertLineBreak = true;
    dispatchCommand(editor, KEY_ENTER_COMMAND, event);
  } else if (isSpace(event)) {
    dispatchCommand(editor, KEY_SPACE_COMMAND, event);
  } else if (isOpenLineBreak(event)) {
    event.preventDefault();
    isInsertLineBreak = true;
    dispatchCommand(editor, INSERT_LINE_BREAK_COMMAND, true);
  } else if (isParagraph(event)) {
    isInsertLineBreak = false;
    dispatchCommand(editor, KEY_ENTER_COMMAND, event);
  } else if (isDeleteBackward(event)) {
    if (isBackspace(event)) {
      dispatchCommand(editor, KEY_BACKSPACE_COMMAND, event);
    } else {
      event.preventDefault();
      dispatchCommand(editor, DELETE_CHARACTER_COMMAND, true);
    }
  } else if (isEscape(event)) {
    dispatchCommand(editor, KEY_ESCAPE_COMMAND, event);
  } else if (isDeleteForward(event)) {
    if (isDelete(event)) {
      dispatchCommand(editor, KEY_DELETE_COMMAND, event);
    } else {
      event.preventDefault();
      dispatchCommand(editor, DELETE_CHARACTER_COMMAND, false);
    }
  } else if (isDeleteWordBackward(event)) {
    event.preventDefault();
    dispatchCommand(editor, DELETE_WORD_COMMAND, true);
  } else if (isDeleteWordForward(event)) {
    event.preventDefault();
    dispatchCommand(editor, DELETE_WORD_COMMAND, false);
  } else if (isDeleteLineBackward(event)) {
    event.preventDefault();
    dispatchCommand(editor, DELETE_LINE_COMMAND, true);
  } else if (isDeleteLineForward(event)) {
    event.preventDefault();
    dispatchCommand(editor, DELETE_LINE_COMMAND, false);
  } else if (isBold(event)) {
    event.preventDefault();
    dispatchCommand(editor, FORMAT_TEXT_COMMAND, "bold");
  } else if (isUnderline(event)) {
    event.preventDefault();
    dispatchCommand(editor, FORMAT_TEXT_COMMAND, "underline");
  } else if (isItalic(event)) {
    event.preventDefault();
    dispatchCommand(editor, FORMAT_TEXT_COMMAND, "italic");
  } else if (isTab(event)) {
    dispatchCommand(editor, KEY_TAB_COMMAND, event);
  } else if (isUndo(event)) {
    event.preventDefault();
    dispatchCommand(editor, UNDO_COMMAND, void 0);
  } else if (isRedo(event)) {
    event.preventDefault();
    dispatchCommand(editor, REDO_COMMAND, void 0);
  } else {
    const prevSelection = editor._editorState._selection;
    if (prevSelection !== null && !$isRangeSelection(prevSelection)) {
      if (isCopy(event)) {
        event.preventDefault();
        dispatchCommand(editor, COPY_COMMAND, event);
      } else if (isCut(event)) {
        event.preventDefault();
        dispatchCommand(editor, CUT_COMMAND, event);
      } else if (isSelectAll(event)) {
        event.preventDefault();
        dispatchCommand(editor, SELECT_ALL_COMMAND, event);
      }
    } else if (isSelectAll(event)) {
      event.preventDefault();
      dispatchCommand(editor, SELECT_ALL_COMMAND, event);
    }
  }
  if (isModifier(event)) {
    dispatchCommand(editor, KEY_MODIFIER_COMMAND, event);
  }
}
function getRootElementRemoveHandles(rootElement) {
  let eventHandles = rootElement.__lexicalEventHandles;
  if (eventHandles === void 0) {
    eventHandles = [];
    rootElement.__lexicalEventHandles = eventHandles;
  }
  return eventHandles;
}
var activeNestedEditorsMap = /* @__PURE__ */ new Map();
function onDocumentSelectionChange(event) {
  const domSelection = getDOMSelectionFromTarget(event.target);
  if (domSelection === null) {
    return;
  }
  const nextActiveEditor = getNearestEditorFromDOMNode(domSelection.anchorNode);
  if (nextActiveEditor === null) {
    return;
  }
  if (isSelectionChangeFromMouseDown) {
    isSelectionChangeFromMouseDown = false;
    updateEditorSync(nextActiveEditor, () => {
      const lastSelection = $getPreviousSelection();
      const domAnchorNode = domSelection.anchorNode;
      if (isHTMLElement(domAnchorNode) || isDOMTextNode(domAnchorNode)) {
        const newSelection = $internalCreateRangeSelection(lastSelection, domSelection, nextActiveEditor, event);
        $setSelection(newSelection);
      }
    });
  }
  const editors = getEditorsToPropagate(nextActiveEditor);
  const rootEditor = editors[editors.length - 1];
  const rootEditorKey = rootEditor._key;
  const activeNestedEditor = activeNestedEditorsMap.get(rootEditorKey);
  const prevActiveEditor = activeNestedEditor || rootEditor;
  if (prevActiveEditor !== nextActiveEditor) {
    onSelectionChange(domSelection, prevActiveEditor, false);
  }
  onSelectionChange(domSelection, nextActiveEditor, true);
  if (nextActiveEditor !== rootEditor) {
    activeNestedEditorsMap.set(rootEditorKey, nextActiveEditor);
  } else if (activeNestedEditor) {
    activeNestedEditorsMap.delete(rootEditorKey);
  }
}
function stopLexicalPropagation(event) {
  event._lexicalHandled = true;
}
function hasStoppedLexicalPropagation(event) {
  const stopped = event._lexicalHandled === true;
  return stopped;
}
function addRootElementEvents(rootElement, editor) {
  const doc = rootElement.ownerDocument;
  const documentRootElementsCount = rootElementsRegistered.get(doc);
  if (documentRootElementsCount === void 0 || documentRootElementsCount < 1) {
    doc.addEventListener("selectionchange", onDocumentSelectionChange);
  }
  rootElementsRegistered.set(doc, (documentRootElementsCount || 0) + 1);
  rootElement.__lexicalEditor = editor;
  const removeHandles = getRootElementRemoveHandles(rootElement);
  for (let i2 = 0; i2 < rootElementEvents.length; i2++) {
    const [eventName, onEvent] = rootElementEvents[i2];
    const eventHandler = typeof onEvent === "function" ? (event) => {
      if (hasStoppedLexicalPropagation(event)) {
        return;
      }
      stopLexicalPropagation(event);
      if (editor.isEditable() || eventName === "click") {
        onEvent(event, editor);
      }
    } : (event) => {
      if (hasStoppedLexicalPropagation(event)) {
        return;
      }
      stopLexicalPropagation(event);
      const isEditable = editor.isEditable();
      switch (eventName) {
        case "cut":
          return isEditable && dispatchCommand(editor, CUT_COMMAND, event);
        case "copy":
          return dispatchCommand(editor, COPY_COMMAND, event);
        case "paste":
          return isEditable && dispatchCommand(editor, PASTE_COMMAND, event);
        case "dragstart":
          return isEditable && dispatchCommand(editor, DRAGSTART_COMMAND, event);
        case "dragover":
          return isEditable && dispatchCommand(editor, DRAGOVER_COMMAND, event);
        case "dragend":
          return isEditable && dispatchCommand(editor, DRAGEND_COMMAND, event);
        case "focus":
          return isEditable && dispatchCommand(editor, FOCUS_COMMAND, event);
        case "blur": {
          return isEditable && dispatchCommand(editor, BLUR_COMMAND, event);
        }
        case "drop":
          return isEditable && dispatchCommand(editor, DROP_COMMAND, event);
      }
    };
    rootElement.addEventListener(eventName, eventHandler);
    removeHandles.push(() => {
      rootElement.removeEventListener(eventName, eventHandler);
    });
  }
}
var rootElementNotRegisteredWarning = warnOnlyOnce("Root element not registered");
function removeRootElementEvents(rootElement) {
  const doc = rootElement.ownerDocument;
  const documentRootElementsCount = rootElementsRegistered.get(doc);
  if (documentRootElementsCount === void 0) {
    rootElementNotRegisteredWarning();
    return;
  }
  const newCount = documentRootElementsCount - 1;
  if (!(newCount >= 0)) {
    formatDevErrorMessage(`Root element count less than 0`);
  }
  rootElementsRegistered.set(doc, newCount);
  if (newCount === 0) {
    doc.removeEventListener("selectionchange", onDocumentSelectionChange);
  }
  const editor = getEditorPropertyFromDOMNode(rootElement);
  if (isLexicalEditor(editor)) {
    cleanActiveNestedEditorsMap(editor);
    rootElement.__lexicalEditor = null;
  } else if (editor) {
    {
      formatDevErrorMessage(`Attempted to remove event handlers from a node that does not belong to this build of Lexical`);
    }
  }
  const removeHandles = getRootElementRemoveHandles(rootElement);
  for (let i2 = 0; i2 < removeHandles.length; i2++) {
    removeHandles[i2]();
  }
  rootElement.__lexicalEventHandles = [];
}
function cleanActiveNestedEditorsMap(editor) {
  if (editor._parentEditor !== null) {
    const editors = getEditorsToPropagate(editor);
    const rootEditor = editors[editors.length - 1];
    const rootEditorKey = rootEditor._key;
    if (activeNestedEditorsMap.get(rootEditorKey) === editor) {
      activeNestedEditorsMap.delete(rootEditorKey);
    }
  } else {
    activeNestedEditorsMap.delete(editor._key);
  }
}
function markSelectionChangeFromDOMUpdate() {
  isSelectionChangeFromDOMUpdate = true;
}
function markCollapsedSelectionFormat(format, style, offset, key, timeStamp) {
  collapsedSelectionFormat = [format, style, offset, key, timeStamp];
}
function $removeNode(nodeToRemove, restoreSelection, preserveEmptyParent) {
  errorOnReadOnly();
  const key = nodeToRemove.__key;
  const parent = nodeToRemove.getParent();
  if (parent === null) {
    return;
  }
  const selection = $maybeMoveChildrenSelectionToParent(nodeToRemove);
  let selectionMoved = false;
  if ($isRangeSelection(selection) && restoreSelection) {
    const anchor = selection.anchor;
    const focus = selection.focus;
    if (anchor.key === key) {
      moveSelectionPointToSibling(anchor, nodeToRemove, parent, nodeToRemove.getPreviousSibling(), nodeToRemove.getNextSibling());
      selectionMoved = true;
    }
    if (focus.key === key) {
      moveSelectionPointToSibling(focus, nodeToRemove, parent, nodeToRemove.getPreviousSibling(), nodeToRemove.getNextSibling());
      selectionMoved = true;
    }
  } else if ($isNodeSelection(selection) && restoreSelection && nodeToRemove.isSelected()) {
    nodeToRemove.selectPrevious();
  }
  if ($isRangeSelection(selection) && restoreSelection && !selectionMoved) {
    const index = nodeToRemove.getIndexWithinParent();
    removeFromParent(nodeToRemove);
    $updateElementSelectionOnCreateDeleteNode(selection, parent, index, -1);
  } else {
    removeFromParent(nodeToRemove);
  }
  if (!preserveEmptyParent && !$isRootOrShadowRoot(parent) && !parent.canBeEmpty() && parent.isEmpty()) {
    $removeNode(parent, restoreSelection);
  }
  if (restoreSelection && selection && $isRootNode(parent) && parent.isEmpty()) {
    parent.selectEnd();
  }
}
function buildImportMap(importMap) {
  return importMap;
}
var LexicalNode = class {
  // Allow us to look up the type including static props
  /** @internal */
  /** @internal */
  //@ts-ignore We set the key in the constructor.
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  // Flow doesn't support abstract classes unfortunately, so we can't _force_
  // subclasses of Node to implement statics. All subclasses of Node should have
  // a static getType and clone method though. We define getType and clone here so we can call it
  // on any  Node, and we throw this error by default since the subclass should provide
  // their own implementation.
  /**
   * Returns the string type of this node. Every node must
   * implement this and it MUST BE UNIQUE amongst nodes registered
   * on the editor.
   *
   */
  static getType() {
    const {
      ownNodeType
    } = getStaticNodeConfig(this);
    if (!(ownNodeType !== void 0)) {
      formatDevErrorMessage(`LexicalNode: Node ${this.name} does not implement .getType().`);
    }
    return ownNodeType;
  }
  /**
   * Clones this node, creating a new node with a different key
   * and adding it to the EditorState (but not attaching it anywhere!). All nodes must
   * implement this method.
   *
   */
  static clone(_data) {
    {
      formatDevErrorMessage(`LexicalNode: Node ${this.name} does not implement .clone().`);
    }
  }
  /**
   * Override this to implement the new static node configuration protocol,
   * this method is called directly on the prototype and must not depend
   * on anything initialized in the constructor. Generally it should be
   * a trivial implementation.
   *
   * @example
   * ```ts
   * class MyNode extends TextNode {
   *   $config() {
   *     return this.config('my-node', {extends: TextNode});
   *   }
   * }
   * ```
   */
  $config() {
    return {};
  }
  /**
   * This is a convenience method for $config that
   * aids in type inference. See {@link LexicalNode.$config}
   * for example usage.
   */
  config(type, config) {
    const parentKlass = config.extends || Object.getPrototypeOf(this.constructor);
    Object.assign(config, {
      extends: parentKlass,
      type
    });
    return {
      [type]: config
    };
  }
  /**
   * Perform any state updates on the clone of prevNode that are not already
   * handled by the constructor call in the static clone method. If you have
   * state to update in your clone that is not handled directly by the
   * constructor, it is advisable to override this method but it is required
   * to include a call to `super.afterCloneFrom(prevNode)` in your
   * implementation. This is only intended to be called by
   * {@link $cloneWithProperties} function or via a super call.
   *
   * @example
   * ```ts
   * class ClassesTextNode extends TextNode {
   *   // Not shown: static getType, static importJSON, exportJSON, createDOM, updateDOM
   *   __classes = new Set<string>();
   *   static clone(node: ClassesTextNode): ClassesTextNode {
   *     // The inherited TextNode constructor is used here, so
   *     // classes is not set by this method.
   *     return new ClassesTextNode(node.__text, node.__key);
   *   }
   *   afterCloneFrom(node: this): void {
   *     // This calls TextNode.afterCloneFrom and LexicalNode.afterCloneFrom
   *     // for necessary state updates
   *     super.afterCloneFrom(node);
   *     this.__addClasses(node.__classes);
   *   }
   *   // This method is a private implementation detail, it is not
   *   // suitable for the public API because it does not call getWritable
   *   __addClasses(classNames: Iterable<string>): this {
   *     for (const className of classNames) {
   *       this.__classes.add(className);
   *     }
   *     return this;
   *   }
   *   addClass(...classNames: string[]): this {
   *     return this.getWritable().__addClasses(classNames);
   *   }
   *   removeClass(...classNames: string[]): this {
   *     const node = this.getWritable();
   *     for (const className of classNames) {
   *       this.__classes.delete(className);
   *     }
   *     return this;
   *   }
   *   getClasses(): Set<string> {
   *     return this.getLatest().__classes;
   *   }
   * }
   * ```
   *
   */
  afterCloneFrom(prevNode) {
    if (this.__key === prevNode.__key) {
      this.__parent = prevNode.__parent;
      this.__next = prevNode.__next;
      this.__prev = prevNode.__prev;
      this.__state = prevNode.__state;
    } else if (prevNode.__state) {
      this.__state = prevNode.__state.getWritable(this);
    }
  }
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  constructor(key) {
    this.__type = this.constructor.getType();
    this.__parent = null;
    this.__prev = null;
    this.__next = null;
    Object.defineProperty(this, "__state", {
      configurable: true,
      enumerable: false,
      value: void 0,
      writable: true
    });
    $setNodeKey(this, key);
    {
      if (this.__type !== "root") {
        errorOnReadOnly();
        errorOnTypeKlassMismatch(this.__type, this.constructor);
      }
    }
  }
  // Getters and Traversers
  /**
   * Returns the string type of this node.
   */
  getType() {
    return this.__type;
  }
  isInline() {
    {
      formatDevErrorMessage(`LexicalNode: Node ${this.constructor.name} does not implement .isInline().`);
    }
  }
  /**
   * Returns true if there is a path between this node and the RootNode, false otherwise.
   * This is a way of determining if the node is "attached" EditorState. Unattached nodes
   * won't be reconciled and will ultimately be cleaned up by the Lexical GC.
   */
  isAttached() {
    let nodeKey = this.__key;
    while (nodeKey !== null) {
      if (nodeKey === "root") {
        return true;
      }
      const node = $getNodeByKey(nodeKey);
      if (node === null) {
        break;
      }
      nodeKey = node.__parent;
    }
    return false;
  }
  /**
   * Returns true if this node is contained within the provided Selection., false otherwise.
   * Relies on the algorithms implemented in {@link BaseSelection.getNodes} to determine
   * what's included.
   *
   * @param selection - The selection that we want to determine if the node is in.
   */
  isSelected(selection) {
    const targetSelection = selection || $getSelection();
    if (targetSelection == null) {
      return false;
    }
    const isSelected = targetSelection.getNodes().some((n2) => n2.__key === this.__key);
    if ($isTextNode(this)) {
      return isSelected;
    }
    const isElementRangeSelection = $isRangeSelection(targetSelection) && targetSelection.anchor.type === "element" && targetSelection.focus.type === "element";
    if (isElementRangeSelection) {
      if (targetSelection.isCollapsed()) {
        return false;
      }
      const parentNode = this.getParent();
      if ($isDecoratorNode(this) && this.isInline() && parentNode) {
        const firstPoint = targetSelection.isBackward() ? targetSelection.focus : targetSelection.anchor;
        if (parentNode.is(firstPoint.getNode()) && firstPoint.offset === parentNode.getChildrenSize() && this.is(parentNode.getLastChild())) {
          return false;
        }
      }
    }
    return isSelected;
  }
  /**
   * Returns this nodes key.
   */
  getKey() {
    return this.__key;
  }
  /**
   * Returns the zero-based index of this node within the parent.
   */
  getIndexWithinParent() {
    const parent = this.getParent();
    if (parent === null) {
      return -1;
    }
    let node = parent.getFirstChild();
    let index = 0;
    while (node !== null) {
      if (this.is(node)) {
        return index;
      }
      index++;
      node = node.getNextSibling();
    }
    return -1;
  }
  /**
   * Returns the parent of this node, or null if none is found.
   */
  getParent() {
    const parent = this.getLatest().__parent;
    if (parent === null) {
      return null;
    }
    return $getNodeByKey(parent);
  }
  /**
   * Returns the parent of this node, or throws if none is found.
   */
  getParentOrThrow() {
    const parent = this.getParent();
    if (parent === null) {
      {
        formatDevErrorMessage(`Expected node ${this.__key} to have a parent.`);
      }
    }
    return parent;
  }
  /**
   * Returns the highest (in the EditorState tree)
   * non-root ancestor of this node, or null if none is found. See {@link lexical!$isRootOrShadowRoot}
   * for more information on which Elements comprise "roots".
   */
  getTopLevelElement() {
    let node = this;
    while (node !== null) {
      const parent = node.getParent();
      if ($isRootOrShadowRoot(parent)) {
        if (!($isElementNode(node) || node === this && $isDecoratorNode(node))) {
          formatDevErrorMessage(`Children of root nodes must be elements or decorators`);
        }
        return node;
      }
      node = parent;
    }
    return null;
  }
  /**
   * Returns the highest (in the EditorState tree)
   * non-root ancestor of this node, or throws if none is found. See {@link lexical!$isRootOrShadowRoot}
   * for more information on which Elements comprise "roots".
   */
  getTopLevelElementOrThrow() {
    const parent = this.getTopLevelElement();
    if (parent === null) {
      {
        formatDevErrorMessage(`Expected node ${this.__key} to have a top parent element.`);
      }
    }
    return parent;
  }
  /**
   * Returns a list of the every ancestor of this node,
   * all the way up to the RootNode.
   *
   */
  getParents() {
    const parents = [];
    let node = this.getParent();
    while (node !== null) {
      parents.push(node);
      node = node.getParent();
    }
    return parents;
  }
  /**
   * Returns a list of the keys of every ancestor of this node,
   * all the way up to the RootNode.
   *
   */
  getParentKeys() {
    const parents = [];
    let node = this.getParent();
    while (node !== null) {
      parents.push(node.__key);
      node = node.getParent();
    }
    return parents;
  }
  /**
   * Returns the "previous" siblings - that is, the node that comes
   * before this one in the same parent.
   *
   */
  getPreviousSibling() {
    const self = this.getLatest();
    const prevKey = self.__prev;
    return prevKey === null ? null : $getNodeByKey(prevKey);
  }
  /**
   * Returns the "previous" siblings - that is, the nodes that come between
   * this one and the first child of it's parent, inclusive.
   *
   */
  getPreviousSiblings() {
    const siblings = [];
    const parent = this.getParent();
    if (parent === null) {
      return siblings;
    }
    let node = parent.getFirstChild();
    while (node !== null) {
      if (node.is(this)) {
        break;
      }
      siblings.push(node);
      node = node.getNextSibling();
    }
    return siblings;
  }
  /**
   * Returns the "next" siblings - that is, the node that comes
   * after this one in the same parent
   *
   */
  getNextSibling() {
    const self = this.getLatest();
    const nextKey = self.__next;
    return nextKey === null ? null : $getNodeByKey(nextKey);
  }
  /**
   * Returns all "next" siblings - that is, the nodes that come between this
   * one and the last child of it's parent, inclusive.
   *
   */
  getNextSiblings() {
    const siblings = [];
    let node = this.getNextSibling();
    while (node !== null) {
      siblings.push(node);
      node = node.getNextSibling();
    }
    return siblings;
  }
  /**
   * @deprecated use {@link $getCommonAncestor}
   *
   * Returns the closest common ancestor of this node and the provided one or null
   * if one cannot be found.
   *
   * @param node - the other node to find the common ancestor of.
   */
  getCommonAncestor(node) {
    const a2 = $isElementNode(this) ? this : this.getParent();
    const b2 = $isElementNode(node) ? node : node.getParent();
    const result = a2 && b2 ? $getCommonAncestor(a2, b2) : null;
    return result ? result.commonAncestor : null;
  }
  /**
   * Returns true if the provided node is the exact same one as this node, from Lexical's perspective.
   * Always use this instead of referential equality.
   *
   * @param object - the node to perform the equality comparison on.
   */
  is(object) {
    if (object == null) {
      return false;
    }
    return this.__key === object.__key;
  }
  /**
   * Returns true if this node logically precedes the target node in the
   * editor state, false otherwise (including if there is no common ancestor).
   *
   * Note that this notion of isBefore is based on post-order; a descendant
   * node is always before its ancestors. See also
   * {@link $getCommonAncestor} and {@link $comparePointCaretNext} for
   * more flexible ways to determine the relative positions of nodes.
   *
   * @param targetNode - the node we're testing to see if it's after this one.
   */
  isBefore(targetNode) {
    const compare = $getCommonAncestor(this, targetNode);
    if (compare === null) {
      return false;
    }
    if (compare.type === "descendant") {
      return true;
    }
    if (compare.type === "branch") {
      return $getCommonAncestorResultBranchOrder(compare) === -1;
    }
    if (!(compare.type === "same" || compare.type === "ancestor")) {
      formatDevErrorMessage(`LexicalNode.isBefore: exhaustiveness check`);
    }
    return false;
  }
  /**
   * Returns true if this node is an ancestor of and distinct from the target node, false otherwise.
   *
   * @param targetNode - the would-be child node.
   */
  isParentOf(targetNode) {
    const result = $getCommonAncestor(this, targetNode);
    return result !== null && result.type === "ancestor";
  }
  // TO-DO: this function can be simplified a lot
  /**
   * Returns a list of nodes that are between this node and
   * the target node in the EditorState.
   *
   * @param targetNode - the node that marks the other end of the range of nodes to be returned.
   */
  getNodesBetween(targetNode) {
    const isBefore = this.isBefore(targetNode);
    const nodes = [];
    const visited = /* @__PURE__ */ new Set();
    let node = this;
    while (true) {
      if (node === null) {
        break;
      }
      const key = node.__key;
      if (!visited.has(key)) {
        visited.add(key);
        nodes.push(node);
      }
      if (node === targetNode) {
        break;
      }
      const child = $isElementNode(node) ? isBefore ? node.getFirstChild() : node.getLastChild() : null;
      if (child !== null) {
        node = child;
        continue;
      }
      const nextSibling = isBefore ? node.getNextSibling() : node.getPreviousSibling();
      if (nextSibling !== null) {
        node = nextSibling;
        continue;
      }
      const parent = node.getParentOrThrow();
      if (!visited.has(parent.__key)) {
        nodes.push(parent);
      }
      if (parent === targetNode) {
        break;
      }
      let parentSibling = null;
      let ancestor = parent;
      do {
        if (ancestor === null) {
          {
            formatDevErrorMessage(`getNodesBetween: ancestor is null`);
          }
        }
        parentSibling = isBefore ? ancestor.getNextSibling() : ancestor.getPreviousSibling();
        ancestor = ancestor.getParent();
        if (ancestor !== null) {
          if (parentSibling === null && !visited.has(ancestor.__key)) {
            nodes.push(ancestor);
          }
        } else {
          break;
        }
      } while (parentSibling === null);
      node = parentSibling;
    }
    if (!isBefore) {
      nodes.reverse();
    }
    return nodes;
  }
  /**
   * Returns true if this node has been marked dirty during this update cycle.
   *
   */
  isDirty() {
    const editor = getActiveEditor();
    const dirtyLeaves = editor._dirtyLeaves;
    return dirtyLeaves !== null && dirtyLeaves.has(this.__key);
  }
  /**
   * Returns the latest version of the node from the active EditorState.
   * This is used to avoid getting values from stale node references.
   *
   */
  getLatest() {
    const latest = $getNodeByKey(this.__key);
    if (latest === null) {
      {
        formatDevErrorMessage(`Lexical node does not exist in active editor state. Avoid using the same node references between nested closures from editorState.read/editor.update.`);
      }
    }
    return latest;
  }
  /**
   * Returns a mutable version of the node using {@link $cloneWithProperties}
   * if necessary. Will throw an error if called outside of a Lexical Editor
   * {@link LexicalEditor.update} callback.
   *
   */
  getWritable() {
    errorOnReadOnly();
    const editorState = getActiveEditorState();
    const editor = getActiveEditor();
    const nodeMap = editorState._nodeMap;
    const key = this.__key;
    const latestNode = this.getLatest();
    const cloneNotNeeded = editor._cloneNotNeeded;
    const selection = $getSelection();
    if (selection !== null) {
      selection.setCachedNodes(null);
    }
    if (cloneNotNeeded.has(key)) {
      internalMarkNodeAsDirty(latestNode);
      return latestNode;
    }
    const mutableNode = $cloneWithProperties(latestNode);
    cloneNotNeeded.add(key);
    internalMarkNodeAsDirty(mutableNode);
    nodeMap.set(key, mutableNode);
    return mutableNode;
  }
  /**
   * Returns the text content of the node. Override this for
   * custom nodes that should have a representation in plain text
   * format (for copy + paste, for example)
   *
   */
  getTextContent() {
    return "";
  }
  /**
   * Returns the length of the string produced by calling getTextContent on this node.
   *
   */
  getTextContentSize() {
    return this.getTextContent().length;
  }
  // View
  /**
   * Called during the reconciliation process to determine which nodes
   * to insert into the DOM for this Lexical Node.
   *
   * This method must return exactly one HTMLElement. Nested elements are not supported.
   *
   * Do not attempt to update the Lexical EditorState during this phase of the update lifecycle.
   *
   * @param _config - allows access to things like the EditorTheme (to apply classes) during reconciliation.
   * @param _editor - allows access to the editor for context during reconciliation.
   *
   * */
  createDOM(_config, _editor) {
    {
      formatDevErrorMessage(`createDOM: base method not extended`);
    }
  }
  /**
   * Called when a node changes and should update the DOM
   * in whatever way is necessary to make it align with any changes that might
   * have happened during the update.
   *
   * Returning "true" here will cause lexical to unmount and recreate the DOM node
   * (by calling createDOM). You would need to do this if the element tag changes,
   * for instance.
   *
   * */
  updateDOM(_prevNode, _dom, _config) {
    {
      formatDevErrorMessage(`updateDOM: base method not extended`);
    }
  }
  /**
   * Controls how the this node is serialized to HTML. This is important for
   * copy and paste between Lexical and non-Lexical editors, or Lexical editors with different namespaces,
   * in which case the primary transfer format is HTML. It's also important if you're serializing
   * to HTML for any other reason via {@link @lexical/html!$generateHtmlFromNodes}. You could
   * also use this method to build your own HTML renderer.
   *
   * */
  exportDOM(editor) {
    const element = this.createDOM(editor._config, editor);
    return {
      element
    };
  }
  /**
   * Controls how the this node is serialized to JSON. This is important for
   * copy and paste between Lexical editors sharing the same namespace. It's also important
   * if you're serializing to JSON for persistent storage somewhere.
   * See [Serialization & Deserialization](https://lexical.dev/docs/concepts/serialization#lexical---html).
   *
   * */
  exportJSON() {
    const state = this.__state ? this.__state.toJSON() : void 0;
    return {
      type: this.__type,
      version: 1,
      ...state
    };
  }
  /**
   * Controls how the this node is deserialized from JSON. This is usually boilerplate,
   * but provides an abstraction between the node implementation and serialized interface that can
   * be important if you ever make breaking changes to a node schema (by adding or removing properties).
   * See [Serialization & Deserialization](https://lexical.dev/docs/concepts/serialization#lexical---html).
   *
   * */
  static importJSON(_serializedNode) {
    {
      formatDevErrorMessage(`LexicalNode: Node ${this.name} does not implement .importJSON().`);
    }
  }
  /**
   * Update this LexicalNode instance from serialized JSON. It's recommended
   * to implement as much logic as possible in this method instead of the
   * static importJSON method, so that the functionality can be inherited in subclasses.
   *
   * The LexicalUpdateJSON utility type should be used to ignore any type, version,
   * or children properties in the JSON so that the extended JSON from subclasses
   * are acceptable parameters for the super call.
   *
   * If overridden, this method must call super.
   *
   * @example
   * ```ts
   * class MyTextNode extends TextNode {
   *   // ...
   *   static importJSON(serializedNode: SerializedMyTextNode): MyTextNode {
   *     return $createMyTextNode()
   *       .updateFromJSON(serializedNode);
   *   }
   *   updateFromJSON(
   *     serializedNode: LexicalUpdateJSON<SerializedMyTextNode>,
   *   ): this {
   *     return super.updateFromJSON(serializedNode)
   *       .setMyProperty(serializedNode.myProperty);
   *   }
   * }
   * ```
   **/
  updateFromJSON(serializedNode) {
    return $updateStateFromJSON(this, serializedNode);
  }
  /**
   * @experimental
   *
   * Registers the returned function as a transform on the node during
   * Editor initialization. Most such use cases should be addressed via
   * the {@link LexicalEditor.registerNodeTransform} API.
   *
   * Experimental - use at your own risk.
   */
  static transform() {
    return null;
  }
  // Setters and mutators
  /**
   * Removes this LexicalNode from the EditorState. If the node isn't re-inserted
   * somewhere, the Lexical garbage collector will eventually clean it up.
   *
   * @param preserveEmptyParent - If falsy, the node's parent will be removed if
   * it's empty after the removal operation. This is the default behavior, subject to
   * other node heuristics such as {@link ElementNode#canBeEmpty}
   * */
  remove(preserveEmptyParent) {
    $removeNode(this, true, preserveEmptyParent);
  }
  /**
   * Replaces this LexicalNode with the provided node, optionally transferring the children
   * of the replaced node to the replacing node.
   *
   * @param replaceWith - The node to replace this one with.
   * @param includeChildren - Whether or not to transfer the children of this node to the replacing node.
   * */
  replace(replaceWith, includeChildren) {
    errorOnReadOnly();
    let selection = $getSelection();
    if (selection !== null) {
      selection = selection.clone();
    }
    errorOnInsertTextNodeOnRoot(this, replaceWith);
    const self = this.getLatest();
    const toReplaceKey = this.__key;
    const key = replaceWith.__key;
    const writableReplaceWith = replaceWith.getWritable();
    const writableParent = this.getParentOrThrow().getWritable();
    const size = writableParent.__size;
    removeFromParent(writableReplaceWith);
    const prevSibling = self.getPreviousSibling();
    const nextSibling = self.getNextSibling();
    const prevKey = self.__prev;
    const nextKey = self.__next;
    const parentKey = self.__parent;
    $removeNode(self, false, true);
    if (prevSibling === null) {
      writableParent.__first = key;
    } else {
      const writablePrevSibling = prevSibling.getWritable();
      writablePrevSibling.__next = key;
    }
    writableReplaceWith.__prev = prevKey;
    if (nextSibling === null) {
      writableParent.__last = key;
    } else {
      const writableNextSibling = nextSibling.getWritable();
      writableNextSibling.__prev = key;
    }
    writableReplaceWith.__next = nextKey;
    writableReplaceWith.__parent = parentKey;
    writableParent.__size = size;
    if (includeChildren) {
      if (!($isElementNode(this) && $isElementNode(writableReplaceWith))) {
        formatDevErrorMessage(`includeChildren should only be true for ElementNodes`);
      }
      this.getChildren().forEach((child) => {
        writableReplaceWith.append(child);
      });
    }
    if ($isRangeSelection(selection)) {
      $setSelection(selection);
      const anchor = selection.anchor;
      const focus = selection.focus;
      if (anchor.key === toReplaceKey) {
        $moveSelectionPointToEnd(anchor, writableReplaceWith);
      }
      if (focus.key === toReplaceKey) {
        $moveSelectionPointToEnd(focus, writableReplaceWith);
      }
    }
    if ($getCompositionKey() === toReplaceKey) {
      $setCompositionKey(key);
    }
    return writableReplaceWith;
  }
  /**
   * Inserts a node after this LexicalNode (as the next sibling).
   *
   * @param nodeToInsert - The node to insert after this one.
   * @param restoreSelection - Whether or not to attempt to resolve the
   * selection to the appropriate place after the operation is complete.
   * */
  insertAfter(nodeToInsert, restoreSelection = true) {
    errorOnReadOnly();
    errorOnInsertTextNodeOnRoot(this, nodeToInsert);
    const writableSelf = this.getWritable();
    const writableNodeToInsert = nodeToInsert.getWritable();
    const oldParent = writableNodeToInsert.getParent();
    const selection = $getSelection();
    let elementAnchorSelectionOnNode = false;
    let elementFocusSelectionOnNode = false;
    if (oldParent !== null) {
      const oldIndex = nodeToInsert.getIndexWithinParent();
      removeFromParent(writableNodeToInsert);
      if ($isRangeSelection(selection)) {
        const oldParentKey = oldParent.__key;
        const anchor = selection.anchor;
        const focus = selection.focus;
        elementAnchorSelectionOnNode = anchor.type === "element" && anchor.key === oldParentKey && anchor.offset === oldIndex + 1;
        elementFocusSelectionOnNode = focus.type === "element" && focus.key === oldParentKey && focus.offset === oldIndex + 1;
      }
    }
    const nextSibling = this.getNextSibling();
    const writableParent = this.getParentOrThrow().getWritable();
    const insertKey = writableNodeToInsert.__key;
    const nextKey = writableSelf.__next;
    if (nextSibling === null) {
      writableParent.__last = insertKey;
    } else {
      const writableNextSibling = nextSibling.getWritable();
      writableNextSibling.__prev = insertKey;
    }
    writableParent.__size++;
    writableSelf.__next = insertKey;
    writableNodeToInsert.__next = nextKey;
    writableNodeToInsert.__prev = writableSelf.__key;
    writableNodeToInsert.__parent = writableSelf.__parent;
    if (restoreSelection && $isRangeSelection(selection)) {
      const index = this.getIndexWithinParent();
      $updateElementSelectionOnCreateDeleteNode(selection, writableParent, index + 1);
      const writableParentKey = writableParent.__key;
      if (elementAnchorSelectionOnNode) {
        selection.anchor.set(writableParentKey, index + 2, "element");
      }
      if (elementFocusSelectionOnNode) {
        selection.focus.set(writableParentKey, index + 2, "element");
      }
    }
    return nodeToInsert;
  }
  /**
   * Inserts a node before this LexicalNode (as the previous sibling).
   *
   * @param nodeToInsert - The node to insert before this one.
   * @param restoreSelection - Whether or not to attempt to resolve the
   * selection to the appropriate place after the operation is complete.
   * */
  insertBefore(nodeToInsert, restoreSelection = true) {
    errorOnReadOnly();
    errorOnInsertTextNodeOnRoot(this, nodeToInsert);
    const writableSelf = this.getWritable();
    const writableNodeToInsert = nodeToInsert.getWritable();
    const insertKey = writableNodeToInsert.__key;
    removeFromParent(writableNodeToInsert);
    const prevSibling = this.getPreviousSibling();
    const writableParent = this.getParentOrThrow().getWritable();
    const prevKey = writableSelf.__prev;
    const index = this.getIndexWithinParent();
    if (prevSibling === null) {
      writableParent.__first = insertKey;
    } else {
      const writablePrevSibling = prevSibling.getWritable();
      writablePrevSibling.__next = insertKey;
    }
    writableParent.__size++;
    writableSelf.__prev = insertKey;
    writableNodeToInsert.__prev = prevKey;
    writableNodeToInsert.__next = writableSelf.__key;
    writableNodeToInsert.__parent = writableSelf.__parent;
    const selection = $getSelection();
    if (restoreSelection && $isRangeSelection(selection)) {
      const parent = this.getParentOrThrow();
      $updateElementSelectionOnCreateDeleteNode(selection, parent, index);
    }
    return nodeToInsert;
  }
  /**
   * Whether or not this node has a required parent. Used during copy + paste operations
   * to normalize nodes that would otherwise be orphaned. For example, ListItemNodes without
   * a ListNode parent or TextNodes with a ParagraphNode parent.
   *
   * */
  isParentRequired() {
    return false;
  }
  /**
   * The creation logic for any required parent. Should be implemented if {@link isParentRequired} returns true.
   *
   * */
  createParentElementNode() {
    return $createParagraphNode();
  }
  selectStart() {
    return this.selectPrevious();
  }
  selectEnd() {
    return this.selectNext(0, 0);
  }
  /**
   * Moves selection to the previous sibling of this node, at the specified offsets.
   *
   * @param anchorOffset - The anchor offset for selection.
   * @param focusOffset -  The focus offset for selection
   * */
  selectPrevious(anchorOffset, focusOffset) {
    errorOnReadOnly();
    const prevSibling = this.getPreviousSibling();
    const parent = this.getParentOrThrow();
    if (prevSibling === null) {
      return parent.select(0, 0);
    }
    if ($isElementNode(prevSibling)) {
      return prevSibling.select();
    } else if (!$isTextNode(prevSibling)) {
      const index = prevSibling.getIndexWithinParent() + 1;
      return parent.select(index, index);
    }
    return prevSibling.select(anchorOffset, focusOffset);
  }
  /**
   * Moves selection to the next sibling of this node, at the specified offsets.
   *
   * @param anchorOffset - The anchor offset for selection.
   * @param focusOffset -  The focus offset for selection
   * */
  selectNext(anchorOffset, focusOffset) {
    errorOnReadOnly();
    const nextSibling = this.getNextSibling();
    const parent = this.getParentOrThrow();
    if (nextSibling === null) {
      return parent.select();
    }
    if ($isElementNode(nextSibling)) {
      return nextSibling.select(0, 0);
    } else if (!$isTextNode(nextSibling)) {
      const index = nextSibling.getIndexWithinParent();
      return parent.select(index, index);
    }
    return nextSibling.select(anchorOffset, focusOffset);
  }
  /**
   * Marks a node dirty, triggering transforms and
   * forcing it to be reconciled during the update cycle.
   *
   * */
  markDirty() {
    this.getWritable();
  }
  /**
   * @internal
   *
   * When the reconciler detects that a node was mutated, this method
   * may be called to restore the node to a known good state.
   */
  reconcileObservedMutation(dom, editor) {
    this.markDirty();
  }
};
function errorOnTypeKlassMismatch(type, klass) {
  const registeredNode = getRegisteredNode(getActiveEditor(), type);
  if (registeredNode === void 0) {
    {
      formatDevErrorMessage(`Create node: Attempted to create node ${klass.name} that was not configured to be used on the editor.`);
    }
  }
  const editorKlass = registeredNode.klass;
  if (editorKlass !== klass) {
    {
      formatDevErrorMessage(`Create node: Type ${type} in node ${klass.name} does not match registered node ${editorKlass.name} with the same type`);
    }
  }
}
function insertRangeAfter(node, firstToInsert, lastToInsert) {
  const lastToInsert2 = firstToInsert.getParentOrThrow().getLastChild();
  let current = firstToInsert;
  const nodesToInsert = [firstToInsert];
  while (current !== lastToInsert2) {
    if (!current.getNextSibling()) {
      {
        formatDevErrorMessage(`insertRangeAfter: lastToInsert must be a later sibling of firstToInsert`);
      }
    }
    current = current.getNextSibling();
    nodesToInsert.push(current);
  }
  let currentNode = node;
  for (const nodeToInsert of nodesToInsert) {
    currentNode = currentNode.insertAfter(nodeToInsert);
  }
}
var LineBreakNode = class _LineBreakNode extends LexicalNode {
  static getType() {
    return "linebreak";
  }
  static clone(node) {
    return new _LineBreakNode(node.__key);
  }
  constructor(key) {
    super(key);
  }
  getTextContent() {
    return "\n";
  }
  createDOM() {
    return document.createElement("br");
  }
  updateDOM() {
    return false;
  }
  isInline() {
    return true;
  }
  static importDOM() {
    return {
      br: (node) => {
        if (isOnlyChildInBlockNode(node) || isLastChildInBlockNode(node)) {
          return null;
        }
        return {
          conversion: $convertLineBreakElement,
          priority: 0
        };
      }
    };
  }
  static importJSON(serializedLineBreakNode) {
    return $createLineBreakNode().updateFromJSON(serializedLineBreakNode);
  }
};
function $convertLineBreakElement(node) {
  return {
    node: $createLineBreakNode()
  };
}
function $createLineBreakNode() {
  return $applyNodeReplacement(new LineBreakNode());
}
function $isLineBreakNode(node) {
  return node instanceof LineBreakNode;
}
function isOnlyChildInBlockNode(node) {
  const parentElement = node.parentElement;
  if (parentElement !== null && isBlockDomNode(parentElement)) {
    const firstChild = parentElement.firstChild;
    if (firstChild === node || firstChild.nextSibling === node && isWhitespaceDomTextNode(firstChild)) {
      const lastChild = parentElement.lastChild;
      if (lastChild === node || lastChild.previousSibling === node && isWhitespaceDomTextNode(lastChild)) {
        return true;
      }
    }
  }
  return false;
}
function isLastChildInBlockNode(node) {
  const parentElement = node.parentElement;
  if (parentElement !== null && isBlockDomNode(parentElement)) {
    const firstChild = parentElement.firstChild;
    if (firstChild === node || firstChild.nextSibling === node && isWhitespaceDomTextNode(firstChild)) {
      return false;
    }
    const lastChild = parentElement.lastChild;
    if (lastChild === node || lastChild.previousSibling === node && isWhitespaceDomTextNode(lastChild)) {
      return true;
    }
  }
  return false;
}
function isWhitespaceDomTextNode(node) {
  return isDOMTextNode(node) && /^( |\t|\r?\n)+$/.test(node.textContent || "");
}
function getElementOuterTag(node, format) {
  if (format & IS_CODE) {
    return "code";
  }
  if (format & IS_HIGHLIGHT) {
    return "mark";
  }
  if (format & IS_SUBSCRIPT) {
    return "sub";
  }
  if (format & IS_SUPERSCRIPT) {
    return "sup";
  }
  return null;
}
function getElementInnerTag(node, format) {
  if (format & IS_BOLD) {
    return "strong";
  }
  if (format & IS_ITALIC) {
    return "em";
  }
  return "span";
}
function setTextThemeClassNames(tag, prevFormat, nextFormat, dom, textClassNames) {
  const domClassList = dom.classList;
  let classNames = getCachedClassNameArray(textClassNames, "base");
  if (classNames !== void 0) {
    domClassList.add(...classNames);
  }
  classNames = getCachedClassNameArray(textClassNames, "underlineStrikethrough");
  let hasUnderlineStrikethrough = false;
  const prevUnderlineStrikethrough = prevFormat & IS_UNDERLINE && prevFormat & IS_STRIKETHROUGH;
  const nextUnderlineStrikethrough = nextFormat & IS_UNDERLINE && nextFormat & IS_STRIKETHROUGH;
  if (classNames !== void 0) {
    if (nextUnderlineStrikethrough) {
      hasUnderlineStrikethrough = true;
      if (!prevUnderlineStrikethrough) {
        domClassList.add(...classNames);
      }
    } else if (prevUnderlineStrikethrough) {
      domClassList.remove(...classNames);
    }
  }
  for (const key in TEXT_TYPE_TO_FORMAT) {
    const format = key;
    const flag = TEXT_TYPE_TO_FORMAT[format];
    classNames = getCachedClassNameArray(textClassNames, key);
    if (classNames !== void 0) {
      if (nextFormat & flag) {
        if (hasUnderlineStrikethrough && (key === "underline" || key === "strikethrough")) {
          if (prevFormat & flag) {
            domClassList.remove(...classNames);
          }
          continue;
        }
        if ((prevFormat & flag) === 0 || prevUnderlineStrikethrough && key === "underline" || key === "strikethrough") {
          domClassList.add(...classNames);
        }
      } else if (prevFormat & flag) {
        domClassList.remove(...classNames);
      }
    }
  }
}
function diffComposedText(a2, b2) {
  const aLength = a2.length;
  const bLength = b2.length;
  let left = 0;
  let right = 0;
  while (left < aLength && left < bLength && a2[left] === b2[left]) {
    left++;
  }
  while (right + left < aLength && right + left < bLength && a2[aLength - right - 1] === b2[bLength - right - 1]) {
    right++;
  }
  return [left, aLength - left - right, b2.slice(left, bLength - right)];
}
function setTextContent(nextText, dom, node) {
  const firstChild = dom.firstChild;
  const isComposing = node.isComposing();
  const suffix = isComposing ? COMPOSITION_SUFFIX : "";
  const text = nextText + suffix;
  if (firstChild == null) {
    dom.textContent = text;
  } else {
    const nodeValue = firstChild.nodeValue;
    if (nodeValue !== text) {
      if (isComposing || IS_FIREFOX) {
        const [index, remove, insert] = diffComposedText(nodeValue, text);
        if (remove !== 0) {
          firstChild.deleteData(index, remove);
        }
        firstChild.insertData(index, insert);
      } else {
        firstChild.nodeValue = text;
      }
    }
  }
}
function createTextInnerDOM(innerDOM, node, innerTag, format, text, config) {
  setTextContent(text, innerDOM, node);
  const theme = config.theme;
  const textClassNames = theme.text;
  if (textClassNames !== void 0) {
    setTextThemeClassNames(innerTag, 0, format, innerDOM, textClassNames);
  }
}
function wrapElementWith(element, tag) {
  const el2 = document.createElement(tag);
  el2.appendChild(element);
  return el2;
}
var TextNode = class _TextNode extends LexicalNode {
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  static getType() {
    return "text";
  }
  static clone(node) {
    return new _TextNode(node.__text, node.__key);
  }
  afterCloneFrom(prevNode) {
    super.afterCloneFrom(prevNode);
    this.__text = prevNode.__text;
    this.__format = prevNode.__format;
    this.__style = prevNode.__style;
    this.__mode = prevNode.__mode;
    this.__detail = prevNode.__detail;
  }
  constructor(text = "", key) {
    super(key);
    this.__text = text;
    this.__format = 0;
    this.__style = "";
    this.__mode = 0;
    this.__detail = 0;
  }
  /**
   * Returns a 32-bit integer that represents the TextFormatTypes currently applied to the
   * TextNode. You probably don't want to use this method directly - consider using TextNode.hasFormat instead.
   *
   * @returns a number representing the format of the text node.
   */
  getFormat() {
    const self = this.getLatest();
    return self.__format;
  }
  /**
   * Returns a 32-bit integer that represents the TextDetailTypes currently applied to the
   * TextNode. You probably don't want to use this method directly - consider using TextNode.isDirectionless
   * or TextNode.isUnmergeable instead.
   *
   * @returns a number representing the detail of the text node.
   */
  getDetail() {
    const self = this.getLatest();
    return self.__detail;
  }
  /**
   * Returns the mode (TextModeType) of the TextNode, which may be "normal", "token", or "segmented"
   *
   * @returns TextModeType.
   */
  getMode() {
    const self = this.getLatest();
    return TEXT_TYPE_TO_MODE[self.__mode];
  }
  /**
   * Returns the styles currently applied to the node. This is analogous to CSSText in the DOM.
   *
   * @returns CSSText-like string of styles applied to the underlying DOM node.
   */
  getStyle() {
    const self = this.getLatest();
    return self.__style;
  }
  /**
   * Returns whether or not the node is in "token" mode. TextNodes in token mode can be navigated through character-by-character
   * with a RangeSelection, but are deleted as a single entity (not individually by character).
   *
   * @returns true if the node is in token mode, false otherwise.
   */
  isToken() {
    const self = this.getLatest();
    return self.__mode === IS_TOKEN;
  }
  /**
   *
   * @returns true if Lexical detects that an IME or other 3rd-party script is attempting to
   * mutate the TextNode, false otherwise.
   */
  isComposing() {
    return this.__key === $getCompositionKey();
  }
  /**
   * Returns whether or not the node is in "segmented" mode. TextNodes in segmented mode can be navigated through character-by-character
   * with a RangeSelection, but are deleted in space-delimited "segments".
   *
   * @returns true if the node is in segmented mode, false otherwise.
   */
  isSegmented() {
    const self = this.getLatest();
    return self.__mode === IS_SEGMENTED;
  }
  /**
   * Returns whether or not the node is "directionless". Directionless nodes don't respect changes between RTL and LTR modes.
   *
   * @returns true if the node is directionless, false otherwise.
   */
  isDirectionless() {
    const self = this.getLatest();
    return (self.__detail & IS_DIRECTIONLESS) !== 0;
  }
  /**
   * Returns whether or not the node is unmergeable. In some scenarios, Lexical tries to merge
   * adjacent TextNodes into a single TextNode. If a TextNode is unmergeable, this won't happen.
   *
   * @returns true if the node is unmergeable, false otherwise.
   */
  isUnmergeable() {
    const self = this.getLatest();
    return (self.__detail & IS_UNMERGEABLE) !== 0;
  }
  /**
   * Returns whether or not the node has the provided format applied. Use this with the human-readable TextFormatType
   * string values to get the format of a TextNode.
   *
   * @param type - the TextFormatType to check for.
   *
   * @returns true if the node has the provided format, false otherwise.
   */
  hasFormat(type) {
    const formatFlag = TEXT_TYPE_TO_FORMAT[type];
    return (this.getFormat() & formatFlag) !== 0;
  }
  /**
   * Returns whether or not the node is simple text. Simple text is defined as a TextNode that has the string type "text"
   * (i.e., not a subclass) and has no mode applied to it (i.e., not segmented or token).
   *
   * @returns true if the node is simple text, false otherwise.
   */
  isSimpleText() {
    return this.__type === "text" && this.__mode === 0;
  }
  /**
   * Returns the text content of the node as a string.
   *
   * @returns a string representing the text content of the node.
   */
  getTextContent() {
    const self = this.getLatest();
    return self.__text;
  }
  /**
   * Returns the format flags applied to the node as a 32-bit integer.
   *
   * @returns a number representing the TextFormatTypes applied to the node.
   */
  getFormatFlags(type, alignWithFormat) {
    const self = this.getLatest();
    const format = self.__format;
    return toggleTextFormatType(format, type, alignWithFormat);
  }
  /**
   *
   * @returns true if the text node supports font styling, false otherwise.
   */
  canHaveFormat() {
    return true;
  }
  /**
   * @returns true if the text node is inline, false otherwise.
   */
  isInline() {
    return true;
  }
  // View
  createDOM(config, editor) {
    const format = this.__format;
    const outerTag = getElementOuterTag(this, format);
    const innerTag = getElementInnerTag(this, format);
    const tag = outerTag === null ? innerTag : outerTag;
    const dom = document.createElement(tag);
    let innerDOM = dom;
    if (this.hasFormat("code")) {
      dom.setAttribute("spellcheck", "false");
    }
    if (outerTag !== null) {
      innerDOM = document.createElement(innerTag);
      dom.appendChild(innerDOM);
    }
    const text = this.__text;
    createTextInnerDOM(innerDOM, this, innerTag, format, text, config);
    const style = this.__style;
    if (style !== "") {
      dom.style.cssText = style;
    }
    return dom;
  }
  updateDOM(prevNode, dom, config) {
    const nextText = this.__text;
    const prevFormat = prevNode.__format;
    const nextFormat = this.__format;
    const prevOuterTag = getElementOuterTag(this, prevFormat);
    const nextOuterTag = getElementOuterTag(this, nextFormat);
    const prevInnerTag = getElementInnerTag(this, prevFormat);
    const nextInnerTag = getElementInnerTag(this, nextFormat);
    const prevTag = prevOuterTag === null ? prevInnerTag : prevOuterTag;
    const nextTag = nextOuterTag === null ? nextInnerTag : nextOuterTag;
    if (prevTag !== nextTag) {
      return true;
    }
    if (prevOuterTag === nextOuterTag && prevInnerTag !== nextInnerTag) {
      const prevInnerDOM = dom.firstChild;
      if (prevInnerDOM == null) {
        {
          formatDevErrorMessage(`updateDOM: prevInnerDOM is null or undefined`);
        }
      }
      const nextInnerDOM = document.createElement(nextInnerTag);
      createTextInnerDOM(nextInnerDOM, this, nextInnerTag, nextFormat, nextText, config);
      dom.replaceChild(nextInnerDOM, prevInnerDOM);
      return false;
    }
    let innerDOM = dom;
    if (nextOuterTag !== null) {
      if (prevOuterTag !== null) {
        innerDOM = dom.firstChild;
        if (innerDOM == null) {
          {
            formatDevErrorMessage(`updateDOM: innerDOM is null or undefined`);
          }
        }
      }
    }
    setTextContent(nextText, innerDOM, this);
    const theme = config.theme;
    const textClassNames = theme.text;
    if (textClassNames !== void 0 && prevFormat !== nextFormat) {
      setTextThemeClassNames(nextInnerTag, prevFormat, nextFormat, innerDOM, textClassNames);
    }
    const prevStyle = prevNode.__style;
    const nextStyle = this.__style;
    if (prevStyle !== nextStyle) {
      dom.style.cssText = nextStyle;
    }
    return false;
  }
  static importDOM() {
    return {
      "#text": () => ({
        conversion: $convertTextDOMNode,
        priority: 0
      }),
      b: () => ({
        conversion: convertBringAttentionToElement,
        priority: 0
      }),
      code: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      em: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      i: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      mark: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      s: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      span: () => ({
        conversion: convertSpanElement,
        priority: 0
      }),
      strong: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      sub: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      sup: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      }),
      u: () => ({
        conversion: convertTextFormatElement,
        priority: 0
      })
    };
  }
  static importJSON(serializedNode) {
    return $createTextNode().updateFromJSON(serializedNode);
  }
  updateFromJSON(serializedNode) {
    return super.updateFromJSON(serializedNode).setTextContent(serializedNode.text).setFormat(serializedNode.format).setDetail(serializedNode.detail).setMode(serializedNode.mode).setStyle(serializedNode.style);
  }
  // This improves Lexical's basic text output in copy+paste plus
  // for headless mode where people might use Lexical to generate
  // HTML content and not have the ability to use CSS classes.
  exportDOM(editor) {
    let {
      element
    } = super.exportDOM(editor);
    if (!isHTMLElement(element)) {
      formatDevErrorMessage(`Expected TextNode createDOM to always return a HTMLElement`);
    }
    element.style.whiteSpace = "pre-wrap";
    if (this.hasFormat("lowercase")) {
      element.style.textTransform = "lowercase";
    } else if (this.hasFormat("uppercase")) {
      element.style.textTransform = "uppercase";
    } else if (this.hasFormat("capitalize")) {
      element.style.textTransform = "capitalize";
    }
    if (this.hasFormat("bold")) {
      element = wrapElementWith(element, "b");
    }
    if (this.hasFormat("italic")) {
      element = wrapElementWith(element, "i");
    }
    if (this.hasFormat("strikethrough")) {
      element = wrapElementWith(element, "s");
    }
    if (this.hasFormat("underline")) {
      element = wrapElementWith(element, "u");
    }
    return {
      element
    };
  }
  exportJSON() {
    return {
      detail: this.getDetail(),
      format: this.getFormat(),
      mode: this.getMode(),
      style: this.getStyle(),
      text: this.getTextContent(),
      // As an exception here we invoke super at the end for historical reasons.
      // Namely, to preserve the order of the properties and not to break the tests
      // that use the serialized string representation.
      ...super.exportJSON()
    };
  }
  // Mutators
  selectionTransform(prevSelection, nextSelection) {
    return;
  }
  /**
   * Sets the node format to the provided TextFormatType or 32-bit integer. Note that the TextFormatType
   * version of the argument can only specify one format and doing so will remove all other formats that
   * may be applied to the node. For toggling behavior, consider using {@link TextNode.toggleFormat}
   *
   * @param format - TextFormatType or 32-bit integer representing the node format.
   *
   * @returns this TextNode.
   * // TODO 0.12 This should just be a `string`.
   */
  setFormat(format) {
    const self = this.getWritable();
    self.__format = typeof format === "string" ? TEXT_TYPE_TO_FORMAT[format] : format;
    return self;
  }
  /**
   * Sets the node detail to the provided TextDetailType or 32-bit integer. Note that the TextDetailType
   * version of the argument can only specify one detail value and doing so will remove all other detail values that
   * may be applied to the node. For toggling behavior, consider using {@link TextNode.toggleDirectionless}
   * or {@link TextNode.toggleUnmergeable}
   *
   * @param detail - TextDetailType or 32-bit integer representing the node detail.
   *
   * @returns this TextNode.
   * // TODO 0.12 This should just be a `string`.
   */
  setDetail(detail) {
    const self = this.getWritable();
    self.__detail = typeof detail === "string" ? DETAIL_TYPE_TO_DETAIL[detail] : detail;
    return self;
  }
  /**
   * Sets the node style to the provided CSSText-like string. Set this property as you
   * would an HTMLElement style attribute to apply inline styles to the underlying DOM Element.
   *
   * @param style - CSSText to be applied to the underlying HTMLElement.
   *
   * @returns this TextNode.
   */
  setStyle(style) {
    const self = this.getWritable();
    self.__style = style;
    return self;
  }
  /**
   * Applies the provided format to this TextNode if it's not present. Removes it if it's present.
   * The subscript and superscript formats are mutually exclusive.
   * Prefer using this method to turn specific formats on and off.
   *
   * @param type - TextFormatType to toggle.
   *
   * @returns this TextNode.
   */
  toggleFormat(type) {
    const format = this.getFormat();
    const newFormat = toggleTextFormatType(format, type, null);
    return this.setFormat(newFormat);
  }
  /**
   * Toggles the directionless detail value of the node. Prefer using this method over setDetail.
   *
   * @returns this TextNode.
   */
  toggleDirectionless() {
    const self = this.getWritable();
    self.__detail ^= IS_DIRECTIONLESS;
    return self;
  }
  /**
   * Toggles the unmergeable detail value of the node. Prefer using this method over setDetail.
   *
   * @returns this TextNode.
   */
  toggleUnmergeable() {
    const self = this.getWritable();
    self.__detail ^= IS_UNMERGEABLE;
    return self;
  }
  /**
   * Sets the mode of the node.
   *
   * @returns this TextNode.
   */
  setMode(type) {
    const mode = TEXT_MODE_TO_TYPE[type];
    if (this.__mode === mode) {
      return this;
    }
    const self = this.getWritable();
    self.__mode = mode;
    return self;
  }
  /**
   * Sets the text content of the node.
   *
   * @param text - the string to set as the text value of the node.
   *
   * @returns this TextNode.
   */
  setTextContent(text) {
    if (this.__text === text) {
      return this;
    }
    const self = this.getWritable();
    self.__text = text;
    return self;
  }
  /**
   * Sets the current Lexical selection to be a RangeSelection with anchor and focus on this TextNode at the provided offsets.
   *
   * @param _anchorOffset - the offset at which the Selection anchor will be placed.
   * @param _focusOffset - the offset at which the Selection focus will be placed.
   *
   * @returns the new RangeSelection.
   */
  select(_anchorOffset, _focusOffset) {
    errorOnReadOnly();
    let anchorOffset = _anchorOffset;
    let focusOffset = _focusOffset;
    const selection = $getSelection();
    const text = this.getTextContent();
    const key = this.__key;
    if (typeof text === "string") {
      const lastOffset = text.length;
      if (anchorOffset === void 0) {
        anchorOffset = lastOffset;
      }
      if (focusOffset === void 0) {
        focusOffset = lastOffset;
      }
    } else {
      anchorOffset = 0;
      focusOffset = 0;
    }
    if (!$isRangeSelection(selection)) {
      return $internalMakeRangeSelection(key, anchorOffset, key, focusOffset, "text", "text");
    } else {
      const compositionKey = $getCompositionKey();
      if (compositionKey === selection.anchor.key || compositionKey === selection.focus.key) {
        $setCompositionKey(key);
      }
      selection.setTextNodeRange(this, anchorOffset, this, focusOffset);
    }
    return selection;
  }
  selectStart() {
    return this.select(0, 0);
  }
  selectEnd() {
    const size = this.getTextContentSize();
    return this.select(size, size);
  }
  /**
   * Inserts the provided text into this TextNode at the provided offset, deleting the number of characters
   * specified. Can optionally calculate a new selection after the operation is complete.
   *
   * @param offset - the offset at which the splice operation should begin.
   * @param delCount - the number of characters to delete, starting from the offset.
   * @param newText - the text to insert into the TextNode at the offset.
   * @param moveSelection - optional, whether or not to move selection to the end of the inserted substring.
   *
   * @returns this TextNode.
   */
  spliceText(offset, delCount, newText, moveSelection) {
    const writableSelf = this.getWritable();
    const text = writableSelf.__text;
    const handledTextLength = newText.length;
    let index = offset;
    if (index < 0) {
      index = handledTextLength + index;
      if (index < 0) {
        index = 0;
      }
    }
    const selection = $getSelection();
    if (moveSelection && $isRangeSelection(selection)) {
      const newOffset = offset + handledTextLength;
      selection.setTextNodeRange(writableSelf, newOffset, writableSelf, newOffset);
    }
    const updatedText = text.slice(0, index) + newText + text.slice(index + delCount);
    writableSelf.__text = updatedText;
    return writableSelf;
  }
  /**
   * This method is meant to be overridden by TextNode subclasses to control the behavior of those nodes
   * when a user event would cause text to be inserted before them in the editor. If true, Lexical will attempt
   * to insert text into this node. If false, it will insert the text in a new sibling node.
   *
   * @returns true if text can be inserted before the node, false otherwise.
   */
  canInsertTextBefore() {
    return true;
  }
  /**
   * This method is meant to be overridden by TextNode subclasses to control the behavior of those nodes
   * when a user event would cause text to be inserted after them in the editor. If true, Lexical will attempt
   * to insert text into this node. If false, it will insert the text in a new sibling node.
   *
   * @returns true if text can be inserted after the node, false otherwise.
   */
  canInsertTextAfter() {
    return true;
  }
  /**
   * Splits this TextNode at the provided character offsets, forming new TextNodes from the substrings
   * formed by the split, and inserting those new TextNodes into the editor, replacing the one that was split.
   *
   * @param splitOffsets - rest param of the text content character offsets at which this node should be split.
   *
   * @returns an Array containing the newly-created TextNodes.
   */
  splitText(...splitOffsets) {
    errorOnReadOnly();
    const self = this.getLatest();
    const textContent = self.getTextContent();
    if (textContent === "") {
      return [];
    }
    const key = self.__key;
    const compositionKey = $getCompositionKey();
    const textLength = textContent.length;
    splitOffsets.sort((a2, b2) => a2 - b2);
    splitOffsets.push(textLength);
    const parts = [];
    const splitOffsetsLength = splitOffsets.length;
    for (let start = 0, offsetIndex = 0; start < textLength && offsetIndex <= splitOffsetsLength; offsetIndex++) {
      const end = splitOffsets[offsetIndex];
      if (end > start) {
        parts.push(textContent.slice(start, end));
        start = end;
      }
    }
    const partsLength = parts.length;
    if (partsLength === 1) {
      return [self];
    }
    const firstPart = parts[0];
    const parent = self.getParent();
    let writableNode;
    const format = self.getFormat();
    const style = self.getStyle();
    const detail = self.__detail;
    let hasReplacedSelf = false;
    let startTextPoint = null;
    let endTextPoint = null;
    const selection = $getSelection();
    if ($isRangeSelection(selection)) {
      const [startPoint, endPoint] = selection.isBackward() ? [selection.focus, selection.anchor] : [selection.anchor, selection.focus];
      if (startPoint.type === "text" && startPoint.key === key) {
        startTextPoint = startPoint;
      }
      if (endPoint.type === "text" && endPoint.key === key) {
        endTextPoint = endPoint;
      }
    }
    if (self.isSegmented()) {
      writableNode = $createTextNode(firstPart);
      writableNode.__format = format;
      writableNode.__style = style;
      writableNode.__detail = detail;
      writableNode.__state = $cloneNodeState(self, writableNode);
      hasReplacedSelf = true;
    } else {
      writableNode = self.setTextContent(firstPart);
    }
    const splitNodes = [writableNode];
    for (let i2 = 1; i2 < partsLength; i2++) {
      const part = parts[i2];
      const sibling = $createTextNode(part);
      sibling.__format = format;
      sibling.__style = style;
      sibling.__detail = detail;
      sibling.__state = $cloneNodeState(self, sibling);
      const siblingKey = sibling.__key;
      if (compositionKey === key) {
        $setCompositionKey(siblingKey);
      }
      splitNodes.push(sibling);
    }
    const originalStartOffset = startTextPoint ? startTextPoint.offset : null;
    const originalEndOffset = endTextPoint ? endTextPoint.offset : null;
    let startOffset = 0;
    for (const node of splitNodes) {
      if (!(startTextPoint || endTextPoint)) {
        break;
      }
      const endOffset = startOffset + node.getTextContentSize();
      if (startTextPoint !== null && originalStartOffset !== null && originalStartOffset <= endOffset && originalStartOffset >= startOffset) {
        startTextPoint.set(node.getKey(), originalStartOffset - startOffset, "text");
        if (originalStartOffset < endOffset) {
          startTextPoint = null;
        }
      }
      if (endTextPoint !== null && originalEndOffset !== null && originalEndOffset <= endOffset && originalEndOffset >= startOffset) {
        endTextPoint.set(node.getKey(), originalEndOffset - startOffset, "text");
        break;
      }
      startOffset = endOffset;
    }
    if (parent !== null) {
      internalMarkSiblingsAsDirty(this);
      const writableParent = parent.getWritable();
      const insertionIndex = this.getIndexWithinParent();
      if (hasReplacedSelf) {
        writableParent.splice(insertionIndex, 0, splitNodes);
        this.remove();
      } else {
        writableParent.splice(insertionIndex, 1, splitNodes);
      }
      if ($isRangeSelection(selection)) {
        $updateElementSelectionOnCreateDeleteNode(selection, parent, insertionIndex, partsLength - 1);
      }
    }
    return splitNodes;
  }
  /**
   * Merges the target TextNode into this TextNode, removing the target node.
   *
   * @param target - the TextNode to merge into this one.
   *
   * @returns this TextNode.
   */
  mergeWithSibling(target) {
    const isBefore = target === this.getPreviousSibling();
    if (!isBefore && target !== this.getNextSibling()) {
      {
        formatDevErrorMessage(`mergeWithSibling: sibling must be a previous or next sibling`);
      }
    }
    const key = this.__key;
    const targetKey = target.__key;
    const text = this.__text;
    const textLength = text.length;
    const compositionKey = $getCompositionKey();
    if (compositionKey === targetKey) {
      $setCompositionKey(key);
    }
    const selection = $getSelection();
    if ($isRangeSelection(selection)) {
      const anchor = selection.anchor;
      const focus = selection.focus;
      if (anchor !== null && anchor.key === targetKey) {
        adjustPointOffsetForMergedSibling(anchor, isBefore, key, target, textLength);
      }
      if (focus !== null && focus.key === targetKey) {
        adjustPointOffsetForMergedSibling(focus, isBefore, key, target, textLength);
      }
    }
    const targetText = target.__text;
    const newText = isBefore ? targetText + text : text + targetText;
    this.setTextContent(newText);
    const writableSelf = this.getWritable();
    target.remove();
    return writableSelf;
  }
  /**
   * This method is meant to be overridden by TextNode subclasses to control the behavior of those nodes
   * when used with the registerLexicalTextEntity function. If you're using registerLexicalTextEntity, the
   * node class that you create and replace matched text with should return true from this method.
   *
   * @returns true if the node is to be treated as a "text entity", false otherwise.
   */
  isTextEntity() {
    return false;
  }
};
function convertSpanElement(domNode) {
  const span = domNode;
  const style = span.style;
  return {
    forChild: applyTextFormatFromStyle(style),
    node: null
  };
}
function convertBringAttentionToElement(domNode) {
  const b2 = domNode;
  const hasNormalFontWeight = b2.style.fontWeight === "normal";
  return {
    forChild: applyTextFormatFromStyle(b2.style, hasNormalFontWeight ? void 0 : "bold"),
    node: null
  };
}
var preParentCache = /* @__PURE__ */ new WeakMap();
function isNodePre(node) {
  if (!isHTMLElement(node)) {
    return false;
  } else if (node.nodeName === "PRE") {
    return true;
  }
  const whiteSpace = node.style.whiteSpace;
  return typeof whiteSpace === "string" && whiteSpace.startsWith("pre");
}
function findParentPreDOMNode(node) {
  let cached;
  let parent = node.parentNode;
  const visited = [node];
  while (parent !== null && (cached = preParentCache.get(parent)) === void 0 && !isNodePre(parent)) {
    visited.push(parent);
    parent = parent.parentNode;
  }
  const resultNode = cached === void 0 ? parent : cached;
  for (let i2 = 0; i2 < visited.length; i2++) {
    preParentCache.set(visited[i2], resultNode);
  }
  return resultNode;
}
function $convertTextDOMNode(domNode) {
  const domNode_ = domNode;
  const parentDom = domNode.parentElement;
  if (!(parentDom !== null)) {
    formatDevErrorMessage(`Expected parentElement of Text not to be null`);
  }
  let textContent = domNode_.textContent || "";
  if (findParentPreDOMNode(domNode_) !== null) {
    const parts = textContent.split(/(\r?\n|\t)/);
    const nodes = [];
    const length = parts.length;
    for (let i2 = 0; i2 < length; i2++) {
      const part = parts[i2];
      if (part === "\n" || part === "\r\n") {
        nodes.push($createLineBreakNode());
      } else if (part === "	") {
        nodes.push($createTabNode());
      } else if (part !== "") {
        nodes.push($createTextNode(part));
      }
    }
    return {
      node: nodes
    };
  }
  textContent = textContent.replace(/\r/g, "").replace(/[ \t\n]+/g, " ");
  if (textContent === "") {
    return {
      node: null
    };
  }
  if (textContent[0] === " ") {
    let previousText = domNode_;
    let isStartOfLine = true;
    while (previousText !== null && (previousText = findTextInLine(previousText, false)) !== null) {
      const previousTextContent = previousText.textContent || "";
      if (previousTextContent.length > 0) {
        if (/[ \t\n]$/.test(previousTextContent)) {
          textContent = textContent.slice(1);
        }
        isStartOfLine = false;
        break;
      }
    }
    if (isStartOfLine) {
      textContent = textContent.slice(1);
    }
  }
  if (textContent[textContent.length - 1] === " ") {
    let nextText = domNode_;
    let isEndOfLine = true;
    while (nextText !== null && (nextText = findTextInLine(nextText, true)) !== null) {
      const nextTextContent = (nextText.textContent || "").replace(/^( |\t|\r?\n)+/, "");
      if (nextTextContent.length > 0) {
        isEndOfLine = false;
        break;
      }
    }
    if (isEndOfLine) {
      textContent = textContent.slice(0, textContent.length - 1);
    }
  }
  if (textContent === "") {
    return {
      node: null
    };
  }
  return {
    node: $createTextNode(textContent)
  };
}
function findTextInLine(text, forward) {
  let node = text;
  while (true) {
    let sibling;
    while ((sibling = forward ? node.nextSibling : node.previousSibling) === null) {
      const parentElement = node.parentElement;
      if (parentElement === null) {
        return null;
      }
      node = parentElement;
    }
    node = sibling;
    if (isHTMLElement(node)) {
      const display = node.style.display;
      if (display === "" && !isInlineDomNode(node) || display !== "" && !display.startsWith("inline")) {
        return null;
      }
    }
    let descendant = node;
    while ((descendant = forward ? node.firstChild : node.lastChild) !== null) {
      node = descendant;
    }
    if (isDOMTextNode(node)) {
      return node;
    } else if (node.nodeName === "BR") {
      return null;
    }
  }
}
var nodeNameToTextFormat = {
  code: "code",
  em: "italic",
  i: "italic",
  mark: "highlight",
  s: "strikethrough",
  strong: "bold",
  sub: "subscript",
  sup: "superscript",
  u: "underline"
};
function convertTextFormatElement(domNode) {
  const format = nodeNameToTextFormat[domNode.nodeName.toLowerCase()];
  if (format === void 0) {
    return {
      node: null
    };
  }
  return {
    forChild: applyTextFormatFromStyle(domNode.style, format),
    node: null
  };
}
function $createTextNode(text = "") {
  return $applyNodeReplacement(new TextNode(text));
}
function $isTextNode(node) {
  return node instanceof TextNode;
}
function applyTextFormatFromStyle(style, shouldApply) {
  const fontWeight = style.fontWeight;
  const textDecoration = style.textDecoration.split(" ");
  const hasBoldFontWeight = fontWeight === "700" || fontWeight === "bold";
  const hasLinethroughTextDecoration = textDecoration.includes("line-through");
  const hasItalicFontStyle = style.fontStyle === "italic";
  const hasUnderlineTextDecoration = textDecoration.includes("underline");
  const verticalAlign = style.verticalAlign;
  return (lexicalNode) => {
    if (!$isTextNode(lexicalNode)) {
      return lexicalNode;
    }
    if (hasBoldFontWeight && !lexicalNode.hasFormat("bold")) {
      lexicalNode.toggleFormat("bold");
    }
    if (hasLinethroughTextDecoration && !lexicalNode.hasFormat("strikethrough")) {
      lexicalNode.toggleFormat("strikethrough");
    }
    if (hasItalicFontStyle && !lexicalNode.hasFormat("italic")) {
      lexicalNode.toggleFormat("italic");
    }
    if (hasUnderlineTextDecoration && !lexicalNode.hasFormat("underline")) {
      lexicalNode.toggleFormat("underline");
    }
    if (verticalAlign === "sub" && !lexicalNode.hasFormat("subscript")) {
      lexicalNode.toggleFormat("subscript");
    }
    if (verticalAlign === "super" && !lexicalNode.hasFormat("superscript")) {
      lexicalNode.toggleFormat("superscript");
    }
    if (shouldApply && !lexicalNode.hasFormat(shouldApply)) {
      lexicalNode.toggleFormat(shouldApply);
    }
    return lexicalNode;
  };
}
var TabNode = class _TabNode extends TextNode {
  static getType() {
    return "tab";
  }
  static clone(node) {
    return new _TabNode(node.__key);
  }
  constructor(key) {
    super("	", key);
    this.__detail = IS_UNMERGEABLE;
  }
  static importDOM() {
    return null;
  }
  createDOM(config) {
    const dom = super.createDOM(config);
    const classNames = getCachedClassNameArray(config.theme, "tab");
    if (classNames !== void 0) {
      const domClassList = dom.classList;
      domClassList.add(...classNames);
    }
    return dom;
  }
  static importJSON(serializedTabNode) {
    return $createTabNode().updateFromJSON(serializedTabNode);
  }
  setTextContent(text) {
    if (!(text === "	" || text === "")) {
      formatDevErrorMessage(`TabNode does not support setTextContent`);
    }
    return super.setTextContent("	");
  }
  spliceText(offset, delCount, newText, moveSelection) {
    if (!(newText === "" && delCount === 0 || newText === "	" && delCount === 1)) {
      formatDevErrorMessage(`TabNode does not support spliceText`);
    }
    return this;
  }
  setDetail(detail) {
    if (!(detail === IS_UNMERGEABLE)) {
      formatDevErrorMessage(`TabNode does not support setDetail`);
    }
    return this;
  }
  setMode(type) {
    if (!(type === "normal")) {
      formatDevErrorMessage(`TabNode does not support setMode`);
    }
    return this;
  }
  canInsertTextBefore() {
    return false;
  }
  canInsertTextAfter() {
    return false;
  }
};
function $createTabNode() {
  return $applyNodeReplacement(new TabNode());
}
function $isTabNode(node) {
  return node instanceof TabNode;
}
var Point = class {
  constructor(key, offset, type) {
    {
      Object.defineProperty(this, "_selection", {
        enumerable: false,
        writable: true
      });
    }
    this._selection = null;
    this.key = key;
    this.offset = offset;
    this.type = type;
  }
  is(point) {
    return this.key === point.key && this.offset === point.offset && this.type === point.type;
  }
  isBefore(b2) {
    if (this.key === b2.key) {
      return this.offset < b2.offset;
    }
    const aCaret = $normalizeCaret($caretFromPoint(this, "next"));
    const bCaret = $normalizeCaret($caretFromPoint(b2, "next"));
    return $comparePointCaretNext(aCaret, bCaret) < 0;
  }
  getNode() {
    const key = this.key;
    const node = $getNodeByKey(key);
    if (node === null) {
      {
        formatDevErrorMessage(`Point.getNode: node not found`);
      }
    }
    return node;
  }
  set(key, offset, type, onlyIfChanged) {
    const selection = this._selection;
    const oldKey = this.key;
    if (onlyIfChanged && this.key === key && this.offset === offset && this.type === type) {
      return;
    }
    this.key = key;
    this.offset = offset;
    this.type = type;
    {
      const node = $getNodeByKey(key);
      if (!(type === "text" ? $isTextNode(node) : $isElementNode(node))) {
        formatDevErrorMessage(`PointType.set: node with key ${key} is ${node ? node.__type : "[not found]"} and can not be used for a ${type} point`);
      }
    }
    if (!isCurrentlyReadOnlyMode()) {
      if ($getCompositionKey() === oldKey) {
        $setCompositionKey(key);
      }
      if (selection !== null) {
        selection.setCachedNodes(null);
        selection.dirty = true;
      }
    }
  }
};
function $createPoint(key, offset, type) {
  return new Point(key, offset, type);
}
function selectPointOnNode(point, node) {
  let key = node.__key;
  let offset = point.offset;
  let type = "element";
  if ($isTextNode(node)) {
    type = "text";
    const textContentLength = node.getTextContentSize();
    if (offset > textContentLength) {
      offset = textContentLength;
    }
  } else if (!$isElementNode(node)) {
    const nextSibling = node.getNextSibling();
    if ($isTextNode(nextSibling)) {
      key = nextSibling.__key;
      offset = 0;
      type = "text";
    } else {
      const parentNode = node.getParent();
      if (parentNode) {
        key = parentNode.__key;
        offset = node.getIndexWithinParent() + 1;
      }
    }
  }
  point.set(key, offset, type);
}
function $moveSelectionPointToEnd(point, node) {
  if ($isElementNode(node)) {
    const lastNode = node.getLastDescendant();
    if ($isElementNode(lastNode) || $isTextNode(lastNode)) {
      selectPointOnNode(point, lastNode);
    } else {
      selectPointOnNode(point, node);
    }
  } else {
    selectPointOnNode(point, node);
  }
}
function $transferStartingElementPointToTextPoint(start, end, format, style) {
  const element = start.getNode();
  const placementNode = element.getChildAtIndex(start.offset);
  const textNode = $createTextNode();
  textNode.setFormat(format);
  textNode.setStyle(style);
  if ($isParagraphNode(placementNode)) {
    placementNode.splice(0, 0, [textNode]);
  } else {
    const target = $isRootNode(element) ? $createParagraphNode().append(textNode) : textNode;
    if (placementNode === null) {
      element.append(target);
    } else {
      placementNode.insertBefore(target);
    }
  }
  if (start.is(end)) {
    end.set(textNode.__key, 0, "text");
  }
  start.set(textNode.__key, 0, "text");
}
var NodeSelection = class _NodeSelection {
  constructor(objects) {
    this._cachedNodes = null;
    this._nodes = objects;
    this.dirty = false;
  }
  getCachedNodes() {
    return this._cachedNodes;
  }
  setCachedNodes(nodes) {
    this._cachedNodes = nodes;
  }
  is(selection) {
    if (!$isNodeSelection(selection)) {
      return false;
    }
    const a2 = this._nodes;
    const b2 = selection._nodes;
    return a2.size === b2.size && Array.from(a2).every((key) => b2.has(key));
  }
  isCollapsed() {
    return false;
  }
  isBackward() {
    return false;
  }
  getStartEndPoints() {
    return null;
  }
  add(key) {
    this.dirty = true;
    this._nodes.add(key);
    this._cachedNodes = null;
  }
  delete(key) {
    this.dirty = true;
    this._nodes.delete(key);
    this._cachedNodes = null;
  }
  clear() {
    this.dirty = true;
    this._nodes.clear();
    this._cachedNodes = null;
  }
  has(key) {
    return this._nodes.has(key);
  }
  clone() {
    return new _NodeSelection(new Set(this._nodes));
  }
  extract() {
    return this.getNodes();
  }
  insertRawText(text) {
  }
  insertText() {
  }
  insertNodes(nodes) {
    const selectedNodes = this.getNodes();
    const selectedNodesLength = selectedNodes.length;
    const lastSelectedNode = selectedNodes[selectedNodesLength - 1];
    let selectionAtEnd;
    if ($isTextNode(lastSelectedNode)) {
      selectionAtEnd = lastSelectedNode.select();
    } else {
      const index = lastSelectedNode.getIndexWithinParent() + 1;
      selectionAtEnd = lastSelectedNode.getParentOrThrow().select(index, index);
    }
    selectionAtEnd.insertNodes(nodes);
    for (let i2 = 0; i2 < selectedNodesLength; i2++) {
      selectedNodes[i2].remove();
    }
  }
  getNodes() {
    const cachedNodes = this._cachedNodes;
    if (cachedNodes !== null) {
      return cachedNodes;
    }
    const objects = this._nodes;
    const nodes = [];
    for (const object of objects) {
      const node = $getNodeByKey(object);
      if (node !== null) {
        nodes.push(node);
      }
    }
    if (!isCurrentlyReadOnlyMode()) {
      this._cachedNodes = nodes;
    }
    return nodes;
  }
  getTextContent() {
    const nodes = this.getNodes();
    let textContent = "";
    for (let i2 = 0; i2 < nodes.length; i2++) {
      textContent += nodes[i2].getTextContent();
    }
    return textContent;
  }
  /**
   * Remove all nodes in the NodeSelection. If there were any nodes,
   * replace the selection with a new RangeSelection at the previous
   * location of the first node.
   */
  deleteNodes() {
    const nodes = this.getNodes();
    if (($getSelection() || $getPreviousSelection()) === this && nodes[0]) {
      const firstCaret = $getSiblingCaret(nodes[0], "next");
      $setSelectionFromCaretRange($getCaretRange(firstCaret, firstCaret));
    }
    for (const node of nodes) {
      node.remove();
    }
  }
};
function $isRangeSelection(x2) {
  return x2 instanceof RangeSelection;
}
var RangeSelection = class _RangeSelection {
  constructor(anchor, focus, format, style) {
    this.anchor = anchor;
    this.focus = focus;
    anchor._selection = this;
    focus._selection = this;
    this._cachedNodes = null;
    this.format = format;
    this.style = style;
    this.dirty = false;
  }
  getCachedNodes() {
    return this._cachedNodes;
  }
  setCachedNodes(nodes) {
    this._cachedNodes = nodes;
  }
  /**
   * Used to check if the provided selections is equal to this one by value,
   * including anchor, focus, format, and style properties.
   * @param selection - the Selection to compare this one to.
   * @returns true if the Selections are equal, false otherwise.
   */
  is(selection) {
    if (!$isRangeSelection(selection)) {
      return false;
    }
    return this.anchor.is(selection.anchor) && this.focus.is(selection.focus) && this.format === selection.format && this.style === selection.style;
  }
  /**
   * Returns whether the Selection is "collapsed", meaning the anchor and focus are
   * the same node and have the same offset.
   *
   * @returns true if the Selection is collapsed, false otherwise.
   */
  isCollapsed() {
    return this.anchor.is(this.focus);
  }
  /**
   * Gets all the nodes in the Selection. Uses caching to make it generally suitable
   * for use in hot paths.
   *
   * See also the {@link CaretRange} APIs (starting with
   * {@link $caretRangeFromSelection}), which are likely to provide a better
   * foundation for any operation where partial selection is relevant
   * (e.g. the anchor or focus are inside an ElementNode and TextNode)
   *
   * @returns an Array containing all the nodes in the Selection
   */
  getNodes() {
    const cachedNodes = this._cachedNodes;
    if (cachedNodes !== null) {
      return cachedNodes;
    }
    const range = $getCaretRangeInDirection($caretRangeFromSelection(this), "next");
    const nodes = $getNodesFromCaretRangeCompat(range);
    {
      if (this.isCollapsed() && nodes.length > 1) {
        {
          formatDevErrorMessage(`RangeSelection.getNodes() returned ${String(nodes.length)} > 1 nodes in a collapsed selection`);
        }
      }
    }
    if (!isCurrentlyReadOnlyMode()) {
      this._cachedNodes = nodes;
    }
    return nodes;
  }
  /**
   * Sets this Selection to be of type "text" at the provided anchor and focus values.
   *
   * @param anchorNode - the anchor node to set on the Selection
   * @param anchorOffset - the offset to set on the Selection
   * @param focusNode - the focus node to set on the Selection
   * @param focusOffset - the focus offset to set on the Selection
   */
  setTextNodeRange(anchorNode, anchorOffset, focusNode, focusOffset) {
    this.anchor.set(anchorNode.__key, anchorOffset, "text");
    this.focus.set(focusNode.__key, focusOffset, "text");
  }
  /**
   * Gets the (plain) text content of all the nodes in the selection.
   *
   * @returns a string representing the text content of all the nodes in the Selection
   */
  getTextContent() {
    const nodes = this.getNodes();
    if (nodes.length === 0) {
      return "";
    }
    const firstNode = nodes[0];
    const lastNode = nodes[nodes.length - 1];
    const anchor = this.anchor;
    const focus = this.focus;
    const isBefore = anchor.isBefore(focus);
    const [anchorOffset, focusOffset] = $getCharacterOffsets(this);
    let textContent = "";
    let prevWasElement = true;
    for (let i2 = 0; i2 < nodes.length; i2++) {
      const node = nodes[i2];
      if ($isElementNode(node) && !node.isInline()) {
        if (!prevWasElement) {
          textContent += "\n";
        }
        if (node.isEmpty()) {
          prevWasElement = false;
        } else {
          prevWasElement = true;
        }
      } else {
        prevWasElement = false;
        if ($isTextNode(node)) {
          let text = node.getTextContent();
          if (node === firstNode) {
            if (node === lastNode) {
              if (anchor.type !== "element" || focus.type !== "element" || focus.offset === anchor.offset) {
                text = anchorOffset < focusOffset ? text.slice(anchorOffset, focusOffset) : text.slice(focusOffset, anchorOffset);
              }
            } else {
              text = isBefore ? text.slice(anchorOffset) : text.slice(focusOffset);
            }
          } else if (node === lastNode) {
            text = isBefore ? text.slice(0, focusOffset) : text.slice(0, anchorOffset);
          }
          textContent += text;
        } else if (($isDecoratorNode(node) || $isLineBreakNode(node)) && (node !== lastNode || !this.isCollapsed())) {
          textContent += node.getTextContent();
        }
      }
    }
    return textContent;
  }
  /**
   * Attempts to map a DOM selection range onto this Lexical Selection,
   * setting the anchor, focus, and type accordingly
   *
   * @param range a DOM Selection range conforming to the StaticRange interface.
   */
  applyDOMRange(range) {
    const editor = getActiveEditor();
    const currentEditorState = editor.getEditorState();
    const lastSelection = currentEditorState._selection;
    const resolvedSelectionPoints = $internalResolveSelectionPoints(range.startContainer, range.startOffset, range.endContainer, range.endOffset, editor, lastSelection);
    if (resolvedSelectionPoints === null) {
      return;
    }
    const [anchorPoint, focusPoint] = resolvedSelectionPoints;
    this.anchor.set(anchorPoint.key, anchorPoint.offset, anchorPoint.type, true);
    this.focus.set(focusPoint.key, focusPoint.offset, focusPoint.type, true);
    $normalizeSelection(this);
  }
  /**
   * Creates a new RangeSelection, copying over all the property values from this one.
   *
   * @returns a new RangeSelection with the same property values as this one.
   */
  clone() {
    const anchor = this.anchor;
    const focus = this.focus;
    const selection = new _RangeSelection($createPoint(anchor.key, anchor.offset, anchor.type), $createPoint(focus.key, focus.offset, focus.type), this.format, this.style);
    return selection;
  }
  /**
   * Toggles the provided format on all the TextNodes in the Selection.
   *
   * @param format a string TextFormatType to toggle on the TextNodes in the selection
   */
  toggleFormat(format) {
    this.format = toggleTextFormatType(this.format, format, null);
    this.dirty = true;
  }
  /**
   * Sets the value of the format property on the Selection
   *
   * @param format - the format to set at the value of the format property.
   */
  setFormat(format) {
    this.format = format;
    this.dirty = true;
  }
  /**
   * Sets the value of the style property on the Selection
   *
   * @param style - the style to set at the value of the style property.
   */
  setStyle(style) {
    this.style = style;
    this.dirty = true;
  }
  /**
   * Returns whether the provided TextFormatType is present on the Selection. This will be true if any node in the Selection
   * has the specified format.
   *
   * @param type the TextFormatType to check for.
   * @returns true if the provided format is currently toggled on on the Selection, false otherwise.
   */
  hasFormat(type) {
    const formatFlag = TEXT_TYPE_TO_FORMAT[type];
    return (this.format & formatFlag) !== 0;
  }
  /**
   * Attempts to insert the provided text into the EditorState at the current Selection.
   * converts tabs, newlines, and carriage returns into LexicalNodes.
   *
   * @param text the text to insert into the Selection
   */
  insertRawText(text) {
    const parts = text.split(/(\r?\n|\t)/);
    const nodes = [];
    const length = parts.length;
    for (let i2 = 0; i2 < length; i2++) {
      const part = parts[i2];
      if (part === "\n" || part === "\r\n") {
        nodes.push($createLineBreakNode());
      } else if (part === "	") {
        nodes.push($createTabNode());
      } else {
        nodes.push($createTextNode(part));
      }
    }
    this.insertNodes(nodes);
  }
  /**
   * Insert the provided text into the EditorState at the current Selection.
   *
   * @param text the text to insert into the Selection
   */
  insertText(text) {
    const anchor = this.anchor;
    const focus = this.focus;
    const format = this.format;
    const style = this.style;
    let firstPoint = anchor;
    let endPoint = focus;
    if (!this.isCollapsed() && focus.isBefore(anchor)) {
      firstPoint = focus;
      endPoint = anchor;
    }
    if (firstPoint.type === "element") {
      $transferStartingElementPointToTextPoint(firstPoint, endPoint, format, style);
    }
    if (endPoint.type === "element") {
      $setPointFromCaret(endPoint, $normalizeCaret($caretFromPoint(endPoint, "next")));
    }
    const startOffset = firstPoint.offset;
    let endOffset = endPoint.offset;
    const selectedNodes = this.getNodes();
    const selectedNodesLength = selectedNodes.length;
    let firstNode = selectedNodes[0];
    if (!$isTextNode(firstNode)) {
      {
        formatDevErrorMessage(`insertText: first node is not a text node`);
      }
    }
    const firstNodeText = firstNode.getTextContent();
    const firstNodeTextLength = firstNodeText.length;
    const firstNodeParent = firstNode.getParentOrThrow();
    const lastIndex = selectedNodesLength - 1;
    let lastNode = selectedNodes[lastIndex];
    if (selectedNodesLength === 1 && endPoint.type === "element") {
      endOffset = firstNodeTextLength;
      endPoint.set(firstPoint.key, endOffset, "text");
    }
    if (this.isCollapsed() && startOffset === firstNodeTextLength && ($isTokenOrSegmented(firstNode) || !firstNode.canInsertTextAfter() || !firstNodeParent.canInsertTextAfter() && firstNode.getNextSibling() === null)) {
      let nextSibling = firstNode.getNextSibling();
      if (!$isTextNode(nextSibling) || !nextSibling.canInsertTextBefore() || $isTokenOrSegmented(nextSibling)) {
        nextSibling = $createTextNode();
        nextSibling.setFormat(format);
        nextSibling.setStyle(style);
        if (!firstNodeParent.canInsertTextAfter()) {
          firstNodeParent.insertAfter(nextSibling);
        } else {
          firstNode.insertAfter(nextSibling);
        }
      }
      nextSibling.select(0, 0);
      firstNode = nextSibling;
      if (text !== "") {
        this.insertText(text);
        return;
      }
    } else if (this.isCollapsed() && startOffset === 0 && ($isTokenOrSegmented(firstNode) || !firstNode.canInsertTextBefore() || !firstNodeParent.canInsertTextBefore() && firstNode.getPreviousSibling() === null)) {
      let prevSibling = firstNode.getPreviousSibling();
      if (!$isTextNode(prevSibling) || $isTokenOrSegmented(prevSibling)) {
        prevSibling = $createTextNode();
        prevSibling.setFormat(format);
        if (!firstNodeParent.canInsertTextBefore()) {
          firstNodeParent.insertBefore(prevSibling);
        } else {
          firstNode.insertBefore(prevSibling);
        }
      }
      prevSibling.select();
      firstNode = prevSibling;
      if (text !== "") {
        this.insertText(text);
        return;
      }
    } else if (firstNode.isSegmented() && startOffset !== firstNodeTextLength) {
      const textNode = $createTextNode(firstNode.getTextContent());
      textNode.setFormat(format);
      firstNode.replace(textNode);
      firstNode = textNode;
    } else if (!this.isCollapsed() && text !== "") {
      const lastNodeParent = lastNode.getParent();
      if (!firstNodeParent.canInsertTextBefore() || !firstNodeParent.canInsertTextAfter() || $isElementNode(lastNodeParent) && (!lastNodeParent.canInsertTextBefore() || !lastNodeParent.canInsertTextAfter())) {
        this.insertText("");
        $normalizeSelectionPointsForBoundaries(this.anchor, this.focus, null);
        this.insertText(text);
        return;
      }
    }
    if (selectedNodesLength === 1) {
      if ($isTokenOrTab(firstNode)) {
        const textNode = $createTextNode(text);
        textNode.select();
        firstNode.replace(textNode);
        return;
      }
      const firstNodeFormat = firstNode.getFormat();
      const firstNodeStyle = firstNode.getStyle();
      if (startOffset === endOffset && (firstNodeFormat !== format || firstNodeStyle !== style)) {
        if (firstNode.getTextContent() === "") {
          firstNode.setFormat(format);
          firstNode.setStyle(style);
        } else {
          const textNode = $createTextNode(text);
          textNode.setFormat(format);
          textNode.setStyle(style);
          textNode.select();
          if (startOffset === 0) {
            firstNode.insertBefore(textNode, false);
          } else {
            const [targetNode] = firstNode.splitText(startOffset);
            targetNode.insertAfter(textNode, false);
          }
          if (textNode.isComposing() && this.anchor.type === "text") {
            this.anchor.offset -= text.length;
          }
          return;
        }
      } else if ($isTabNode(firstNode)) {
        const textNode = $createTextNode(text);
        textNode.setFormat(format);
        textNode.setStyle(style);
        textNode.select();
        firstNode.replace(textNode);
        return;
      }
      const delCount = endOffset - startOffset;
      firstNode = firstNode.spliceText(startOffset, delCount, text, true);
      if (firstNode.getTextContent() === "") {
        firstNode.remove();
      } else if (this.anchor.type === "text") {
        if (firstNode.isComposing()) {
          this.anchor.offset -= text.length;
        } else {
          this.format = firstNodeFormat;
          this.style = firstNodeStyle;
        }
      }
    } else {
      const markedNodeKeysForKeep = /* @__PURE__ */ new Set([...firstNode.getParentKeys(), ...lastNode.getParentKeys()]);
      const firstElement = $isElementNode(firstNode) ? firstNode : firstNode.getParentOrThrow();
      let lastElement = $isElementNode(lastNode) ? lastNode : lastNode.getParentOrThrow();
      let lastElementChild = lastNode;
      if (!firstElement.is(lastElement) && lastElement.isInline()) {
        do {
          lastElementChild = lastElement;
          lastElement = lastElement.getParentOrThrow();
        } while (lastElement.isInline());
      }
      if (endPoint.type === "text" && (endOffset !== 0 || lastNode.getTextContent() === "") || endPoint.type === "element" && lastNode.getIndexWithinParent() < endOffset) {
        if ($isTextNode(lastNode) && !$isTokenOrTab(lastNode) && endOffset !== lastNode.getTextContentSize()) {
          if (lastNode.isSegmented()) {
            const textNode = $createTextNode(lastNode.getTextContent());
            lastNode.replace(textNode);
            lastNode = textNode;
          }
          if (!$isRootNode(endPoint.getNode()) && endPoint.type === "text") {
            lastNode = lastNode.spliceText(0, endOffset, "");
          }
          markedNodeKeysForKeep.add(lastNode.__key);
        } else {
          const lastNodeParent = lastNode.getParentOrThrow();
          if (!lastNodeParent.canBeEmpty() && lastNodeParent.getChildrenSize() === 1) {
            lastNodeParent.remove();
          } else {
            lastNode.remove();
          }
        }
      } else {
        markedNodeKeysForKeep.add(lastNode.__key);
      }
      const lastNodeChildren = lastElement.getChildren();
      const selectedNodesSet = new Set(selectedNodes);
      const firstAndLastElementsAreEqual = firstElement.is(lastElement);
      const insertionTarget = firstElement.isInline() && firstNode.getNextSibling() === null ? firstElement : firstNode;
      for (let i2 = lastNodeChildren.length - 1; i2 >= 0; i2--) {
        const lastNodeChild = lastNodeChildren[i2];
        if (lastNodeChild.is(firstNode) || $isElementNode(lastNodeChild) && lastNodeChild.isParentOf(firstNode)) {
          break;
        }
        if (lastNodeChild.isAttached()) {
          if (!selectedNodesSet.has(lastNodeChild) || lastNodeChild.is(lastElementChild)) {
            if (!firstAndLastElementsAreEqual) {
              insertionTarget.insertAfter(lastNodeChild, false);
            }
          } else {
            lastNodeChild.remove();
          }
        }
      }
      if (!firstAndLastElementsAreEqual) {
        let parent = lastElement;
        let lastRemovedParent = null;
        while (parent !== null) {
          const children = parent.getChildren();
          const childrenLength = children.length;
          if (childrenLength === 0 || children[childrenLength - 1].is(lastRemovedParent)) {
            markedNodeKeysForKeep.delete(parent.__key);
            lastRemovedParent = parent;
          }
          parent = parent.getParent();
        }
      }
      if (!$isTokenOrTab(firstNode)) {
        firstNode = firstNode.spliceText(startOffset, firstNodeTextLength - startOffset, text, true);
        if (firstNode.getTextContent() === "") {
          firstNode.remove();
        } else if (firstNode.isComposing() && this.anchor.type === "text") {
          this.anchor.offset -= text.length;
        }
      } else if (startOffset === firstNodeTextLength) {
        firstNode.select();
      } else {
        const textNode = $createTextNode(text);
        textNode.select();
        firstNode.replace(textNode);
      }
      for (let i2 = 1; i2 < selectedNodesLength; i2++) {
        const selectedNode = selectedNodes[i2];
        const key = selectedNode.__key;
        if (!markedNodeKeysForKeep.has(key)) {
          selectedNode.remove();
        }
      }
    }
  }
  /**
   * Removes the text in the Selection, adjusting the EditorState accordingly.
   */
  removeText() {
    const isCurrentSelection = $getSelection() === this;
    const newRange = $removeTextFromCaretRange($caretRangeFromSelection(this));
    $updateRangeSelectionFromCaretRange(this, newRange);
    if (isCurrentSelection && $getSelection() !== this) {
      $setSelection(this);
    }
  }
  // TO-DO: Migrate this method to the new utility function $forEachSelectedTextNode (share similar logic)
  /**
   * Applies the provided format to the TextNodes in the Selection, splitting or
   * merging nodes as necessary.
   *
   * @param formatType the format type to apply to the nodes in the Selection.
   * @param alignWithFormat a 32-bit integer representing formatting flags to align with.
   */
  formatText(formatType, alignWithFormat = null) {
    if (this.isCollapsed()) {
      this.toggleFormat(formatType);
      $setCompositionKey(null);
      return;
    }
    const selectedNodes = this.getNodes();
    const selectedTextNodes = [];
    for (const selectedNode of selectedNodes) {
      if ($isTextNode(selectedNode)) {
        selectedTextNodes.push(selectedNode);
      }
    }
    const applyFormatToElements = (alignWith) => {
      selectedNodes.forEach((node) => {
        if ($isElementNode(node)) {
          const newFormat = node.getFormatFlags(formatType, alignWith);
          node.setTextFormat(newFormat);
        }
      });
    };
    const selectedTextNodesLength = selectedTextNodes.length;
    if (selectedTextNodesLength === 0) {
      this.toggleFormat(formatType);
      $setCompositionKey(null);
      applyFormatToElements(alignWithFormat);
      return;
    }
    const anchor = this.anchor;
    const focus = this.focus;
    const isBackward = this.isBackward();
    const startPoint = isBackward ? focus : anchor;
    const endPoint = isBackward ? anchor : focus;
    let firstIndex = 0;
    let firstNode = selectedTextNodes[0];
    let startOffset = startPoint.type === "element" ? 0 : startPoint.offset;
    if (startPoint.type === "text" && startOffset === firstNode.getTextContentSize()) {
      firstIndex = 1;
      firstNode = selectedTextNodes[1];
      startOffset = 0;
    }
    if (firstNode == null) {
      return;
    }
    const firstNextFormat = firstNode.getFormatFlags(formatType, alignWithFormat);
    applyFormatToElements(firstNextFormat);
    const lastIndex = selectedTextNodesLength - 1;
    let lastNode = selectedTextNodes[lastIndex];
    const endOffset = endPoint.type === "text" ? endPoint.offset : lastNode.getTextContentSize();
    if (firstNode.is(lastNode)) {
      if (startOffset === endOffset) {
        return;
      }
      if ($isTokenOrSegmented(firstNode) || startOffset === 0 && endOffset === firstNode.getTextContentSize()) {
        firstNode.setFormat(firstNextFormat);
      } else {
        const splitNodes = firstNode.splitText(startOffset, endOffset);
        const replacement = startOffset === 0 ? splitNodes[0] : splitNodes[1];
        replacement.setFormat(firstNextFormat);
        if (startPoint.type === "text") {
          startPoint.set(replacement.__key, 0, "text");
        }
        if (endPoint.type === "text") {
          endPoint.set(replacement.__key, endOffset - startOffset, "text");
        }
      }
      this.format = firstNextFormat;
      return;
    }
    if (startOffset !== 0 && !$isTokenOrSegmented(firstNode)) {
      [, firstNode] = firstNode.splitText(startOffset);
      startOffset = 0;
    }
    firstNode.setFormat(firstNextFormat);
    const lastNextFormat = lastNode.getFormatFlags(formatType, firstNextFormat);
    if (endOffset > 0) {
      if (endOffset !== lastNode.getTextContentSize() && !$isTokenOrSegmented(lastNode)) {
        [lastNode] = lastNode.splitText(endOffset);
      }
      lastNode.setFormat(lastNextFormat);
    }
    for (let i2 = firstIndex + 1; i2 < lastIndex; i2++) {
      const textNode = selectedTextNodes[i2];
      const nextFormat = textNode.getFormatFlags(formatType, lastNextFormat);
      textNode.setFormat(nextFormat);
    }
    if (startPoint.type === "text") {
      startPoint.set(firstNode.__key, startOffset, "text");
    }
    if (endPoint.type === "text") {
      endPoint.set(lastNode.__key, endOffset, "text");
    }
    this.format = firstNextFormat | lastNextFormat;
  }
  /**
   * Attempts to "intelligently" insert an arbitrary list of Lexical nodes into the EditorState at the
   * current Selection according to a set of heuristics that determine how surrounding nodes
   * should be changed, replaced, or moved to accommodate the incoming ones.
   *
   * @param nodes - the nodes to insert
   */
  insertNodes(nodes) {
    if (nodes.length === 0) {
      return;
    }
    if (!this.isCollapsed()) {
      this.removeText();
    }
    if (this.anchor.key === "root") {
      this.insertParagraph();
      const selection = $getSelection();
      if (!$isRangeSelection(selection)) {
        formatDevErrorMessage(`Expected RangeSelection after insertParagraph`);
      }
      return selection.insertNodes(nodes);
    }
    const firstPoint = this.isBackward() ? this.focus : this.anchor;
    const firstNode = firstPoint.getNode();
    const firstBlock = $getAncestor(firstNode, INTERNAL_$isBlock);
    const last = nodes[nodes.length - 1];
    if ($isElementNode(firstBlock) && "__language" in firstBlock) {
      if ("__language" in nodes[0]) {
        this.insertText(nodes[0].getTextContent());
      } else {
        const index = $removeTextAndSplitBlock(this);
        firstBlock.splice(index, 0, nodes);
        last.selectEnd();
      }
      return;
    }
    const notInline = (node) => ($isElementNode(node) || $isDecoratorNode(node)) && !node.isInline();
    if (!nodes.some(notInline)) {
      if (!$isElementNode(firstBlock)) {
        formatDevErrorMessage(`Expected node ${firstNode.constructor.name} of type ${firstNode.getType()} to have a block ElementNode ancestor`);
      }
      const index = $removeTextAndSplitBlock(this);
      firstBlock.splice(index, 0, nodes);
      last.selectEnd();
      return;
    }
    const blocksParent = $wrapInlineNodes(nodes);
    const nodeToSelect = blocksParent.getLastDescendant();
    const blocks = blocksParent.getChildren();
    const isMergeable = (node) => $isElementNode(node) && INTERNAL_$isBlock(node) && !node.isEmpty() && $isElementNode(firstBlock) && (!firstBlock.isEmpty() || firstBlock.canMergeWhenEmpty());
    const shouldInsert = !$isElementNode(firstBlock) || !firstBlock.isEmpty();
    const insertedParagraph = shouldInsert ? this.insertParagraph() : null;
    const lastToInsert = blocks[blocks.length - 1];
    let firstToInsert = blocks[0];
    if (isMergeable(firstToInsert)) {
      if (!$isElementNode(firstBlock)) {
        formatDevErrorMessage(`Expected node ${firstNode.constructor.name} of type ${firstNode.getType()} to have a block ElementNode ancestor`);
      }
      firstBlock.append(...firstToInsert.getChildren());
      firstToInsert = blocks[1];
    }
    if (firstToInsert) {
      if (!(firstBlock !== null)) {
        formatDevErrorMessage(`Expected node ${firstNode.constructor.name} of type ${firstNode.getType()} to have a block ancestor`);
      }
      insertRangeAfter(firstBlock, firstToInsert);
    }
    const lastInsertedBlock = $getAncestor(nodeToSelect, INTERNAL_$isBlock);
    if (insertedParagraph && $isElementNode(lastInsertedBlock) && (insertedParagraph.canMergeWhenEmpty() || INTERNAL_$isBlock(lastToInsert))) {
      lastInsertedBlock.append(...insertedParagraph.getChildren());
      insertedParagraph.remove();
    }
    if ($isElementNode(firstBlock) && firstBlock.isEmpty()) {
      firstBlock.remove();
    }
    nodeToSelect.selectEnd();
    const lastChild = $isElementNode(firstBlock) ? firstBlock.getLastChild() : null;
    if ($isLineBreakNode(lastChild) && lastInsertedBlock !== firstBlock) {
      lastChild.remove();
    }
  }
  /**
   * Inserts a new ParagraphNode into the EditorState at the current Selection
   *
   * @returns the newly inserted node.
   */
  insertParagraph() {
    if (this.anchor.key === "root") {
      const paragraph = $createParagraphNode();
      $getRoot().splice(this.anchor.offset, 0, [paragraph]);
      paragraph.select();
      return paragraph;
    }
    const index = $removeTextAndSplitBlock(this);
    const block = $getAncestor(this.anchor.getNode(), INTERNAL_$isBlock);
    if (!$isElementNode(block)) {
      formatDevErrorMessage(`Expected ancestor to be a block ElementNode`);
    }
    const firstToAppend = block.getChildAtIndex(index);
    const nodesToInsert = firstToAppend ? [firstToAppend, ...firstToAppend.getNextSiblings()] : [];
    const newBlock = block.insertNewAfter(this, false);
    if (newBlock) {
      newBlock.append(...nodesToInsert);
      newBlock.selectStart();
      return newBlock;
    }
    return null;
  }
  /**
   * Inserts a logical linebreak, which may be a new LineBreakNode or a new ParagraphNode, into the EditorState at the
   * current Selection.
   */
  insertLineBreak(selectStart) {
    const lineBreak = $createLineBreakNode();
    this.insertNodes([lineBreak]);
    if (selectStart) {
      const parent = lineBreak.getParentOrThrow();
      const index = lineBreak.getIndexWithinParent();
      parent.select(index, index);
    }
  }
  /**
   * Extracts the nodes in the Selection, splitting nodes where necessary
   * to get offset-level precision.
   *
   * @returns The nodes in the Selection
   */
  extract() {
    const selectedNodes = this.getNodes();
    const selectedNodesLength = selectedNodes.length;
    const lastIndex = selectedNodesLength - 1;
    const anchor = this.anchor;
    const focus = this.focus;
    let firstNode = selectedNodes[0];
    let lastNode = selectedNodes[lastIndex];
    const [anchorOffset, focusOffset] = $getCharacterOffsets(this);
    if (selectedNodesLength === 0) {
      return [];
    } else if (selectedNodesLength === 1) {
      if ($isTextNode(firstNode) && !this.isCollapsed()) {
        const startOffset = anchorOffset > focusOffset ? focusOffset : anchorOffset;
        const endOffset = anchorOffset > focusOffset ? anchorOffset : focusOffset;
        const splitNodes = firstNode.splitText(startOffset, endOffset);
        const node = startOffset === 0 ? splitNodes[0] : splitNodes[1];
        return node != null ? [node] : [];
      }
      return [firstNode];
    }
    const isBefore = anchor.isBefore(focus);
    if ($isTextNode(firstNode)) {
      const startOffset = isBefore ? anchorOffset : focusOffset;
      if (startOffset === firstNode.getTextContentSize()) {
        selectedNodes.shift();
      } else if (startOffset !== 0) {
        [, firstNode] = firstNode.splitText(startOffset);
        selectedNodes[0] = firstNode;
      }
    }
    if ($isTextNode(lastNode)) {
      const lastNodeText = lastNode.getTextContent();
      const lastNodeTextLength = lastNodeText.length;
      const endOffset = isBefore ? focusOffset : anchorOffset;
      if (endOffset === 0) {
        selectedNodes.pop();
      } else if (endOffset !== lastNodeTextLength) {
        [lastNode] = lastNode.splitText(endOffset);
        selectedNodes[lastIndex] = lastNode;
      }
    }
    return selectedNodes;
  }
  /**
   * Modifies the Selection according to the parameters and a set of heuristics that account for
   * various node types. Can be used to safely move or extend selection by one logical "unit" without
   * dealing explicitly with all the possible node types.
   *
   * @param alter the type of modification to perform
   * @param isBackward whether or not selection is backwards
   * @param granularity the granularity at which to apply the modification
   */
  modify(alter, isBackward, granularity) {
    if ($modifySelectionAroundDecoratorsAndBlocks(this, alter, isBackward, granularity)) {
      return;
    }
    const collapse = alter === "move";
    const editor = getActiveEditor();
    const domSelection = getDOMSelection(getWindow(editor));
    if (!domSelection) {
      return;
    }
    const blockCursorElement = editor._blockCursorElement;
    const rootElement = editor._rootElement;
    const focusNode = this.focus.getNode();
    if (rootElement !== null && blockCursorElement !== null && $isElementNode(focusNode) && !focusNode.isInline() && !focusNode.canBeEmpty()) {
      removeDOMBlockCursorElement(blockCursorElement, editor, rootElement);
    }
    if (this.dirty) {
      let nextAnchorDOM = getElementByKeyOrThrow(editor, this.anchor.key);
      let nextFocusDOM = getElementByKeyOrThrow(editor, this.focus.key);
      if (this.anchor.type === "text") {
        nextAnchorDOM = getDOMTextNode(nextAnchorDOM);
      }
      if (this.focus.type === "text") {
        nextFocusDOM = getDOMTextNode(nextFocusDOM);
      }
      if (nextAnchorDOM && nextFocusDOM) {
        setDOMSelectionBaseAndExtent(domSelection, nextAnchorDOM, this.anchor.offset, nextFocusDOM, this.focus.offset);
      }
    }
    moveNativeSelection(domSelection, alter, isBackward ? "backward" : "forward", granularity);
    if (domSelection.rangeCount > 0) {
      const range = domSelection.getRangeAt(0);
      const anchorNode = this.anchor.getNode();
      const root = $isRootNode(anchorNode) ? anchorNode : $getNearestRootOrShadowRoot(anchorNode);
      this.applyDOMRange(range);
      this.dirty = true;
      if (!collapse) {
        const nodes = this.getNodes();
        const validNodes = [];
        let shrinkSelection = false;
        for (let i2 = 0; i2 < nodes.length; i2++) {
          const nextNode = nodes[i2];
          if ($hasAncestor(nextNode, root)) {
            validNodes.push(nextNode);
          } else {
            shrinkSelection = true;
          }
        }
        if (shrinkSelection && validNodes.length > 0) {
          if (isBackward) {
            const firstValidNode = validNodes[0];
            if ($isElementNode(firstValidNode)) {
              firstValidNode.selectStart();
            } else {
              firstValidNode.getParentOrThrow().selectStart();
            }
          } else {
            const lastValidNode = validNodes[validNodes.length - 1];
            if ($isElementNode(lastValidNode)) {
              lastValidNode.selectEnd();
            } else {
              lastValidNode.getParentOrThrow().selectEnd();
            }
          }
        }
        if (domSelection.anchorNode !== range.startContainer || domSelection.anchorOffset !== range.startOffset) {
          $swapPoints(this);
        }
      }
    }
    if (granularity === "lineboundary") {
      $modifySelectionAroundDecoratorsAndBlocks(this, alter, isBackward, granularity, "decorators");
    }
  }
  /**
   * Helper for handling forward character and word deletion that prevents element nodes
   * like a table, columns layout being destroyed
   *
   * @param anchor the anchor
   * @param anchorNode the anchor node in the selection
   * @param isBackward whether or not selection is backwards
   */
  forwardDeletion(anchor, anchorNode, isBackward) {
    if (!isBackward && // Delete forward handle case
    (anchor.type === "element" && $isElementNode(anchorNode) && anchor.offset === anchorNode.getChildrenSize() || anchor.type === "text" && anchor.offset === anchorNode.getTextContentSize())) {
      const parent = anchorNode.getParent();
      const nextSibling = anchorNode.getNextSibling() || (parent === null ? null : parent.getNextSibling());
      if ($isElementNode(nextSibling) && nextSibling.isShadowRoot()) {
        return true;
      }
    }
    return false;
  }
  /**
   * Performs one logical character deletion operation on the EditorState based on the current Selection.
   * Handles different node types.
   *
   * @param isBackward whether or not the selection is backwards.
   */
  deleteCharacter(isBackward) {
    const wasCollapsed = this.isCollapsed();
    if (this.isCollapsed()) {
      const anchor = this.anchor;
      let anchorNode = anchor.getNode();
      if (this.forwardDeletion(anchor, anchorNode, isBackward)) {
        return;
      }
      const direction = isBackward ? "previous" : "next";
      const initialCaret = $caretFromPoint(anchor, direction);
      const initialRange = $extendCaretToRange(initialCaret);
      if (initialRange.getTextSlices().every((slice) => slice === null || slice.distance === 0)) {
        let state = {
          type: "initial"
        };
        for (const caret of initialRange.iterNodeCarets("shadowRoot")) {
          if ($isChildCaret(caret)) {
            if (caret.origin.isInline()) ;
            else if (caret.origin.isShadowRoot()) {
              if (state.type === "merge-block") {
                break;
              }
              if ($isElementNode(initialRange.anchor.origin) && initialRange.anchor.origin.isEmpty()) {
                const normCaret = $normalizeCaret(caret);
                $updateRangeSelectionFromCaretRange(this, $getCaretRange(normCaret, normCaret));
                initialRange.anchor.origin.remove();
              }
              return;
            } else if (state.type === "merge-next-block" || state.type === "merge-block") {
              state = {
                block: state.block,
                caret,
                type: "merge-block"
              };
            }
          } else if (state.type === "merge-block") {
            break;
          } else if ($isSiblingCaret(caret)) {
            if ($isElementNode(caret.origin)) {
              if (!caret.origin.isInline()) {
                state = {
                  block: caret.origin,
                  type: "merge-next-block"
                };
              } else if (!caret.origin.isParentOf(initialRange.anchor.origin)) {
                break;
              }
              continue;
            } else if ($isDecoratorNode(caret.origin)) {
              if (caret.origin.isIsolated()) ;
              else if (state.type === "merge-next-block" && (caret.origin.isKeyboardSelectable() || !caret.origin.isInline()) && $isElementNode(initialRange.anchor.origin) && initialRange.anchor.origin.isEmpty()) {
                initialRange.anchor.origin.remove();
                const nodeSelection = $createNodeSelection();
                nodeSelection.add(caret.origin.getKey());
                $setSelection(nodeSelection);
              } else {
                caret.origin.remove();
              }
              return;
            }
            break;
          }
        }
        if (state.type === "merge-block") {
          const {
            caret,
            block
          } = state;
          $updateRangeSelectionFromCaretRange(this, $getCaretRange(!caret.origin.isEmpty() && block.isEmpty() ? $rewindSiblingCaret($getSiblingCaret(block, caret.direction)) : initialRange.anchor, caret));
          return this.removeText();
        }
      }
      const focus = this.focus;
      this.modify("extend", isBackward, "character");
      if (!this.isCollapsed()) {
        const focusNode = focus.type === "text" ? focus.getNode() : null;
        anchorNode = anchor.type === "text" ? anchor.getNode() : null;
        if (focusNode !== null && focusNode.isSegmented()) {
          const offset = focus.offset;
          const textContentSize = focusNode.getTextContentSize();
          if (focusNode.is(anchorNode) || isBackward && offset !== textContentSize || !isBackward && offset !== 0) {
            $removeSegment(focusNode, isBackward, offset);
            return;
          }
        } else if (anchorNode !== null && anchorNode.isSegmented()) {
          const offset = anchor.offset;
          const textContentSize = anchorNode.getTextContentSize();
          if (anchorNode.is(focusNode) || isBackward && offset !== 0 || !isBackward && offset !== textContentSize) {
            $removeSegment(anchorNode, isBackward, offset);
            return;
          }
        }
        $updateCaretSelectionForUnicodeCharacter(this, isBackward);
      } else if (isBackward && anchor.offset === 0) {
        if ($collapseAtStart(this, anchor.getNode())) {
          return;
        }
      }
    }
    this.removeText();
    if (isBackward && !wasCollapsed && this.isCollapsed() && this.anchor.type === "element" && this.anchor.offset === 0) {
      const anchorNode = this.anchor.getNode();
      if (anchorNode.isEmpty() && $isRootNode(anchorNode.getParent()) && anchorNode.getPreviousSibling() === null) {
        $collapseAtStart(this, anchorNode);
      }
    }
  }
  /**
   * Performs one logical line deletion operation on the EditorState based on the current Selection.
   * Handles different node types.
   *
   * @param isBackward whether or not the selection is backwards.
   */
  deleteLine(isBackward) {
    if (this.isCollapsed()) {
      this.modify("extend", isBackward, "lineboundary");
    }
    if (this.isCollapsed()) {
      this.deleteCharacter(isBackward);
    } else {
      this.removeText();
    }
  }
  /**
   * Performs one logical word deletion operation on the EditorState based on the current Selection.
   * Handles different node types.
   *
   * @param isBackward whether or not the selection is backwards.
   */
  deleteWord(isBackward) {
    if (this.isCollapsed()) {
      const anchor = this.anchor;
      const anchorNode = anchor.getNode();
      if (this.forwardDeletion(anchor, anchorNode, isBackward)) {
        return;
      }
      this.modify("extend", isBackward, "word");
    }
    this.removeText();
  }
  /**
   * Returns whether the Selection is "backwards", meaning the focus
   * logically precedes the anchor in the EditorState.
   * @returns true if the Selection is backwards, false otherwise.
   */
  isBackward() {
    return this.focus.isBefore(this.anchor);
  }
  getStartEndPoints() {
    return [this.anchor, this.focus];
  }
};
function $isNodeSelection(x2) {
  return x2 instanceof NodeSelection;
}
function getCharacterOffset(point) {
  const offset = point.offset;
  if (point.type === "text") {
    return offset;
  }
  const parent = point.getNode();
  return offset === parent.getChildrenSize() ? parent.getTextContent().length : 0;
}
function $getCharacterOffsets(selection) {
  const anchorAndFocus = selection.getStartEndPoints();
  if (anchorAndFocus === null) {
    return [0, 0];
  }
  const [anchor, focus] = anchorAndFocus;
  if (anchor.type === "element" && focus.type === "element" && anchor.key === focus.key && anchor.offset === focus.offset) {
    return [0, 0];
  }
  return [getCharacterOffset(anchor), getCharacterOffset(focus)];
}
function $collapseAtStart(selection, startNode) {
  for (let node = startNode; node; node = node.getParent()) {
    if ($isElementNode(node)) {
      if (node.collapseAtStart(selection)) {
        return true;
      }
      if ($isRootOrShadowRoot(node)) {
        break;
      }
    }
    if (node.getPreviousSibling()) {
      break;
    }
  }
  return false;
}
function $swapPoints(selection) {
  const focus = selection.focus;
  const anchor = selection.anchor;
  const anchorKey = anchor.key;
  const anchorOffset = anchor.offset;
  const anchorType = anchor.type;
  anchor.set(focus.key, focus.offset, focus.type, true);
  focus.set(anchorKey, anchorOffset, anchorType, true);
}
function moveNativeSelection(domSelection, alter, direction, granularity) {
  domSelection.modify(alter, direction, granularity);
}
function $updateCaretSelectionForUnicodeCharacter(selection, isBackward) {
  const anchor = selection.anchor;
  const focus = selection.focus;
  const anchorNode = anchor.getNode();
  const focusNode = focus.getNode();
  if (anchorNode === focusNode && anchor.type === "text" && focus.type === "text") {
    const anchorOffset = anchor.offset;
    const focusOffset = focus.offset;
    const isBefore = anchorOffset < focusOffset;
    const startOffset = isBefore ? anchorOffset : focusOffset;
    const endOffset = isBefore ? focusOffset : anchorOffset;
    const characterOffset = endOffset - 1;
    if (startOffset !== characterOffset) {
      const text = anchorNode.getTextContent().slice(startOffset, endOffset);
      if (shouldDeleteExactlyOneCodeUnit(text)) {
        if (isBackward) {
          focus.set(focus.key, characterOffset, focus.type);
        } else {
          anchor.set(anchor.key, characterOffset, anchor.type);
        }
      }
    }
  }
}
function shouldDeleteExactlyOneCodeUnit(text) {
  {
    if (!(text.length > 1)) {
      formatDevErrorMessage(`shouldDeleteExactlyOneCodeUnit: expecting to be called only with sequences of two or more code units`);
    }
  }
  return !(doesContainSurrogatePair(text) || doesContainEmoji(text));
}
var doesContainEmoji = (() => {
  try {
    const re2 = new RegExp("\\p{Emoji}", "u");
    const test = re2.test.bind(re2);
    if (
      // Emoji in the BMP (heart) with variation selector
      test("❤️") && // Emoji in the BMP (#) with variation selector
      test("#️⃣") && // Emoji outside the BMP (thumbs up) that is encoded with a surrogate pair
      test("👍")
    ) {
      return test;
    }
  } catch (e2) {
  }
  return () => false;
})();
function $removeSegment(node, isBackward, offset) {
  const textNode = node;
  const textContent = textNode.getTextContent();
  const split = textContent.split(/(?=\s)/g);
  const splitLength = split.length;
  let segmentOffset = 0;
  let restoreOffset = 0;
  for (let i2 = 0; i2 < splitLength; i2++) {
    const text = split[i2];
    const isLast = i2 === splitLength - 1;
    restoreOffset = segmentOffset;
    segmentOffset += text.length;
    if (isBackward && segmentOffset === offset || segmentOffset > offset || isLast) {
      split.splice(i2, 1);
      if (isLast) {
        restoreOffset = void 0;
      }
      break;
    }
  }
  const nextTextContent = split.join("").trim();
  if (nextTextContent === "") {
    textNode.remove();
  } else {
    textNode.setTextContent(nextTextContent);
    textNode.select(restoreOffset, restoreOffset);
  }
}
function shouldResolveAncestor(resolvedElement, resolvedOffset, lastPoint) {
  const parent = resolvedElement.getParent();
  return lastPoint === null || parent === null || !parent.canBeEmpty() || parent !== lastPoint.getNode();
}
function $internalResolveSelectionPoint(dom, offset, lastPoint, editor) {
  let resolvedOffset = offset;
  let resolvedNode;
  if (isHTMLElement(dom)) {
    let moveSelectionToEnd = false;
    const childNodes = dom.childNodes;
    const childNodesLength = childNodes.length;
    const blockCursorElement = editor._blockCursorElement;
    if (resolvedOffset === childNodesLength) {
      moveSelectionToEnd = true;
      resolvedOffset = childNodesLength - 1;
    }
    let childDOM = childNodes[resolvedOffset];
    let hasBlockCursor = false;
    if (childDOM === blockCursorElement) {
      childDOM = childNodes[resolvedOffset + 1];
      hasBlockCursor = true;
    } else if (blockCursorElement !== null) {
      const blockCursorElementParent = blockCursorElement.parentNode;
      if (dom === blockCursorElementParent) {
        const blockCursorOffset = Array.prototype.indexOf.call(blockCursorElementParent.children, blockCursorElement);
        if (offset > blockCursorOffset) {
          resolvedOffset--;
        }
      }
    }
    resolvedNode = $getNodeFromDOM(childDOM);
    if ($isTextNode(resolvedNode)) {
      resolvedOffset = $getTextNodeOffset(resolvedNode, moveSelectionToEnd ? "next" : "previous");
    } else {
      let resolvedElement = $getNodeFromDOM(dom);
      if (resolvedElement === null) {
        return null;
      }
      if ($isElementNode(resolvedElement)) {
        const elementDOM = editor.getElementByKey(resolvedElement.getKey());
        if (!(elementDOM !== null)) {
          formatDevErrorMessage(`$internalResolveSelectionPoint: node in DOM but not keyToDOMMap`);
        }
        const slot = resolvedElement.getDOMSlot(elementDOM);
        [resolvedElement, resolvedOffset] = slot.resolveChildIndex(resolvedElement, elementDOM, dom, offset);
        if (!$isElementNode(resolvedElement)) {
          formatDevErrorMessage(`$internalResolveSelectionPoint: resolvedElement is not an ElementNode`);
        }
        if (moveSelectionToEnd && resolvedOffset >= resolvedElement.getChildrenSize()) {
          resolvedOffset = Math.max(0, resolvedElement.getChildrenSize() - 1);
        }
        let child = resolvedElement.getChildAtIndex(resolvedOffset);
        if ($isElementNode(child) && shouldResolveAncestor(child, resolvedOffset, lastPoint)) {
          const descendant = moveSelectionToEnd ? child.getLastDescendant() : child.getFirstDescendant();
          if (descendant === null) {
            resolvedElement = child;
          } else {
            child = descendant;
            resolvedElement = $isElementNode(child) ? child : child.getParentOrThrow();
          }
          resolvedOffset = 0;
        }
        if ($isTextNode(child)) {
          resolvedNode = child;
          resolvedElement = null;
          resolvedOffset = $getTextNodeOffset(child, moveSelectionToEnd ? "next" : "previous");
        } else if (child !== resolvedElement && moveSelectionToEnd && !hasBlockCursor) {
          if (!$isElementNode(resolvedElement)) {
            formatDevErrorMessage(`invariant`);
          }
          resolvedOffset = Math.min(resolvedElement.getChildrenSize(), resolvedOffset + 1);
        }
      } else {
        const index = resolvedElement.getIndexWithinParent();
        if (offset === 0 && $isDecoratorNode(resolvedElement) && $getNodeFromDOM(dom) === resolvedElement) {
          resolvedOffset = index;
        } else {
          resolvedOffset = index + 1;
        }
        resolvedElement = resolvedElement.getParentOrThrow();
      }
      if ($isElementNode(resolvedElement)) {
        return $createPoint(resolvedElement.__key, resolvedOffset, "element");
      }
    }
  } else {
    resolvedNode = $getNodeFromDOM(dom);
  }
  if (!$isTextNode(resolvedNode)) {
    return null;
  }
  return $createPoint(resolvedNode.__key, $getTextNodeOffset(resolvedNode, resolvedOffset, "clamp"), "text");
}
function resolveSelectionPointOnBoundary(point, isBackward, isCollapsed) {
  const offset = point.offset;
  const node = point.getNode();
  if (offset === 0) {
    const prevSibling = node.getPreviousSibling();
    const parent = node.getParent();
    if (!isBackward) {
      if ($isElementNode(prevSibling) && !isCollapsed && prevSibling.isInline()) {
        point.set(prevSibling.__key, prevSibling.getChildrenSize(), "element");
      } else if ($isTextNode(prevSibling)) {
        point.set(prevSibling.__key, prevSibling.getTextContent().length, "text");
      }
    } else if ((isCollapsed || !isBackward) && prevSibling === null && $isElementNode(parent) && parent.isInline()) {
      const parentSibling = parent.getPreviousSibling();
      if ($isTextNode(parentSibling)) {
        point.set(parentSibling.__key, parentSibling.getTextContent().length, "text");
      }
    }
  } else if (offset === node.getTextContent().length) {
    const nextSibling = node.getNextSibling();
    const parent = node.getParent();
    if (isBackward && $isElementNode(nextSibling) && nextSibling.isInline()) {
      point.set(nextSibling.__key, 0, "element");
    } else if ((isCollapsed || isBackward) && nextSibling === null && $isElementNode(parent) && parent.isInline() && !parent.canInsertTextAfter()) {
      const parentSibling = parent.getNextSibling();
      if ($isTextNode(parentSibling)) {
        point.set(parentSibling.__key, 0, "text");
      }
    }
  }
}
function $normalizeSelectionPointsForBoundaries(anchor, focus, lastSelection) {
  if (anchor.type === "text" && focus.type === "text") {
    const isBackward = anchor.isBefore(focus);
    const isCollapsed = anchor.is(focus);
    resolveSelectionPointOnBoundary(anchor, isBackward, isCollapsed);
    resolveSelectionPointOnBoundary(focus, !isBackward, isCollapsed);
    if (isCollapsed) {
      focus.set(anchor.key, anchor.offset, anchor.type);
    }
    const editor = getActiveEditor();
    if (editor.isComposing() && editor._compositionKey !== anchor.key && $isRangeSelection(lastSelection)) {
      const lastAnchor = lastSelection.anchor;
      const lastFocus = lastSelection.focus;
      anchor.set(lastAnchor.key, lastAnchor.offset, lastAnchor.type, true);
      focus.set(lastFocus.key, lastFocus.offset, lastFocus.type, true);
    }
  }
}
function $internalResolveSelectionPoints(anchorDOM, anchorOffset, focusDOM, focusOffset, editor, lastSelection) {
  if (anchorDOM === null || focusDOM === null || !isSelectionWithinEditor(editor, anchorDOM, focusDOM)) {
    return null;
  }
  const resolvedAnchorPoint = $internalResolveSelectionPoint(anchorDOM, anchorOffset, $isRangeSelection(lastSelection) ? lastSelection.anchor : null, editor);
  if (resolvedAnchorPoint === null) {
    return null;
  }
  const resolvedFocusPoint = $internalResolveSelectionPoint(focusDOM, focusOffset, $isRangeSelection(lastSelection) ? lastSelection.focus : null, editor);
  if (resolvedFocusPoint === null) {
    return null;
  }
  {
    $validatePoint("anchor", resolvedAnchorPoint);
    $validatePoint("focus", resolvedFocusPoint);
  }
  if (resolvedAnchorPoint.type === "element" && resolvedFocusPoint.type === "element") {
    const anchorNode = $getNodeFromDOM(anchorDOM);
    const focusNode = $getNodeFromDOM(focusDOM);
    if ($isDecoratorNode(anchorNode) && $isDecoratorNode(focusNode)) {
      return null;
    }
  }
  $normalizeSelectionPointsForBoundaries(resolvedAnchorPoint, resolvedFocusPoint, lastSelection);
  return [resolvedAnchorPoint, resolvedFocusPoint];
}
function $isBlockElementNode(node) {
  return $isElementNode(node) && !node.isInline();
}
function $internalMakeRangeSelection(anchorKey, anchorOffset, focusKey, focusOffset, anchorType, focusType) {
  const editorState = getActiveEditorState();
  const selection = new RangeSelection($createPoint(anchorKey, anchorOffset, anchorType), $createPoint(focusKey, focusOffset, focusType), 0, "");
  selection.dirty = true;
  editorState._selection = selection;
  return selection;
}
function $createRangeSelection() {
  const anchor = $createPoint("root", 0, "element");
  const focus = $createPoint("root", 0, "element");
  return new RangeSelection(anchor, focus, 0, "");
}
function $createNodeSelection() {
  return new NodeSelection(/* @__PURE__ */ new Set());
}
function $internalCreateSelection(editor, event) {
  const currentEditorState = editor.getEditorState();
  const lastSelection = currentEditorState._selection;
  const domSelection = getDOMSelection(getWindow(editor));
  if ($isRangeSelection(lastSelection) || lastSelection == null) {
    return $internalCreateRangeSelection(lastSelection, domSelection, editor, event);
  }
  return lastSelection.clone();
}
function $createRangeSelectionFromDom(domSelection, editor) {
  return $internalCreateRangeSelection(null, domSelection, editor, null);
}
function $internalCreateRangeSelection(lastSelection, domSelection, editor, event) {
  const windowObj = editor._window;
  if (windowObj === null) {
    return null;
  }
  const windowEvent = event || windowObj.event;
  const eventType = windowEvent ? windowEvent.type : void 0;
  const isSelectionChange = eventType === "selectionchange";
  const useDOMSelection = !getIsProcessingMutations() && (isSelectionChange || eventType === "beforeinput" || eventType === "compositionstart" || eventType === "compositionend" || eventType === "click" && windowEvent && windowEvent.detail === 3 || eventType === "drop" || eventType === void 0);
  let anchorDOM, focusDOM, anchorOffset, focusOffset;
  if (!$isRangeSelection(lastSelection) || useDOMSelection) {
    if (domSelection === null) {
      return null;
    }
    anchorDOM = domSelection.anchorNode;
    focusDOM = domSelection.focusNode;
    anchorOffset = domSelection.anchorOffset;
    focusOffset = domSelection.focusOffset;
    if (isSelectionChange && $isRangeSelection(lastSelection) && !isSelectionWithinEditor(editor, anchorDOM, focusDOM)) {
      return lastSelection.clone();
    }
  } else {
    return lastSelection.clone();
  }
  const resolvedSelectionPoints = $internalResolveSelectionPoints(anchorDOM, anchorOffset, focusDOM, focusOffset, editor, lastSelection);
  if (resolvedSelectionPoints === null) {
    return null;
  }
  const [resolvedAnchorPoint, resolvedFocusPoint] = resolvedSelectionPoints;
  return new RangeSelection(resolvedAnchorPoint, resolvedFocusPoint, !$isRangeSelection(lastSelection) ? 0 : lastSelection.format, !$isRangeSelection(lastSelection) ? "" : lastSelection.style);
}
function $validatePoint(name, point) {
  const node = $getNodeByKey(point.key);
  if (!(node !== void 0)) {
    formatDevErrorMessage(`$validatePoint: ${name} key ${point.key} not found in current editorState`);
  }
  if (point.type === "text") {
    if (!$isTextNode(node)) {
      formatDevErrorMessage(`$validatePoint: ${name} key ${point.key} is not a TextNode`);
    }
    const size = node.getTextContentSize();
    if (!(point.offset <= size)) {
      formatDevErrorMessage(`$validatePoint: ${name} point.offset > node.getTextContentSize() (${String(point.offset)} > ${String(size)})`);
    }
  } else {
    if (!$isElementNode(node)) {
      formatDevErrorMessage(`$validatePoint: ${name} key ${point.key} is not an ElementNode`);
    }
    const size = node.getChildrenSize();
    if (!(point.offset <= size)) {
      formatDevErrorMessage(`$validatePoint: ${name} point.offset > node.getChildrenSize() (${String(point.offset)} > ${String(size)})`);
    }
  }
}
function $getSelection() {
  const editorState = getActiveEditorState();
  return editorState._selection;
}
function $getPreviousSelection() {
  const editor = getActiveEditor();
  return editor._editorState._selection;
}
function $updateElementSelectionOnCreateDeleteNode(selection, parentNode, nodeOffset, times = 1) {
  const anchor = selection.anchor;
  const focus = selection.focus;
  const anchorNode = anchor.getNode();
  const focusNode = focus.getNode();
  if (!parentNode.is(anchorNode) && !parentNode.is(focusNode)) {
    return;
  }
  const parentKey = parentNode.__key;
  if (selection.isCollapsed()) {
    const selectionOffset = anchor.offset;
    if (nodeOffset <= selectionOffset && times > 0 || nodeOffset < selectionOffset && times < 0) {
      const newSelectionOffset = Math.max(0, selectionOffset + times);
      anchor.set(parentKey, newSelectionOffset, "element");
      focus.set(parentKey, newSelectionOffset, "element");
      $updateSelectionResolveTextNodes(selection);
    }
  } else {
    const isBackward = selection.isBackward();
    const firstPoint = isBackward ? focus : anchor;
    const firstPointNode = firstPoint.getNode();
    const lastPoint = isBackward ? anchor : focus;
    const lastPointNode = lastPoint.getNode();
    if (parentNode.is(firstPointNode)) {
      const firstPointOffset = firstPoint.offset;
      if (nodeOffset <= firstPointOffset && times > 0 || nodeOffset < firstPointOffset && times < 0) {
        firstPoint.set(parentKey, Math.max(0, firstPointOffset + times), "element");
      }
    }
    if (parentNode.is(lastPointNode)) {
      const lastPointOffset = lastPoint.offset;
      if (nodeOffset <= lastPointOffset && times > 0 || nodeOffset < lastPointOffset && times < 0) {
        lastPoint.set(parentKey, Math.max(0, lastPointOffset + times), "element");
      }
    }
  }
  $updateSelectionResolveTextNodes(selection);
}
function $updateSelectionResolveTextNodes(selection) {
  const anchor = selection.anchor;
  const anchorOffset = anchor.offset;
  const focus = selection.focus;
  const focusOffset = focus.offset;
  const anchorNode = anchor.getNode();
  const focusNode = focus.getNode();
  if (selection.isCollapsed()) {
    if (!$isElementNode(anchorNode)) {
      return;
    }
    const childSize = anchorNode.getChildrenSize();
    const anchorOffsetAtEnd = anchorOffset >= childSize;
    const child = anchorOffsetAtEnd ? anchorNode.getChildAtIndex(childSize - 1) : anchorNode.getChildAtIndex(anchorOffset);
    if ($isTextNode(child)) {
      let newOffset = 0;
      if (anchorOffsetAtEnd) {
        newOffset = child.getTextContentSize();
      }
      anchor.set(child.__key, newOffset, "text");
      focus.set(child.__key, newOffset, "text");
    }
    return;
  }
  if ($isElementNode(anchorNode)) {
    const childSize = anchorNode.getChildrenSize();
    const anchorOffsetAtEnd = anchorOffset >= childSize;
    const child = anchorOffsetAtEnd ? anchorNode.getChildAtIndex(childSize - 1) : anchorNode.getChildAtIndex(anchorOffset);
    if ($isTextNode(child)) {
      let newOffset = 0;
      if (anchorOffsetAtEnd) {
        newOffset = child.getTextContentSize();
      }
      anchor.set(child.__key, newOffset, "text");
    }
  }
  if ($isElementNode(focusNode)) {
    const childSize = focusNode.getChildrenSize();
    const focusOffsetAtEnd = focusOffset >= childSize;
    const child = focusOffsetAtEnd ? focusNode.getChildAtIndex(childSize - 1) : focusNode.getChildAtIndex(focusOffset);
    if ($isTextNode(child)) {
      let newOffset = 0;
      if (focusOffsetAtEnd) {
        newOffset = child.getTextContentSize();
      }
      focus.set(child.__key, newOffset, "text");
    }
  }
}
function applySelectionTransforms(nextEditorState, editor) {
  const prevEditorState = editor.getEditorState();
  const prevSelection = prevEditorState._selection;
  const nextSelection = nextEditorState._selection;
  if ($isRangeSelection(nextSelection)) {
    const anchor = nextSelection.anchor;
    const focus = nextSelection.focus;
    let anchorNode;
    if (anchor.type === "text") {
      anchorNode = anchor.getNode();
      anchorNode.selectionTransform(prevSelection, nextSelection);
    }
    if (focus.type === "text") {
      const focusNode = focus.getNode();
      if (anchorNode !== focusNode) {
        focusNode.selectionTransform(prevSelection, nextSelection);
      }
    }
  }
}
function moveSelectionPointToSibling(point, node, parent, prevSibling, nextSibling) {
  let siblingKey = null;
  let offset = 0;
  let type = null;
  if (prevSibling !== null) {
    siblingKey = prevSibling.__key;
    if ($isTextNode(prevSibling)) {
      offset = prevSibling.getTextContentSize();
      type = "text";
    } else if ($isElementNode(prevSibling)) {
      offset = prevSibling.getChildrenSize();
      type = "element";
    }
  } else {
    if (nextSibling !== null) {
      siblingKey = nextSibling.__key;
      if ($isTextNode(nextSibling)) {
        type = "text";
      } else if ($isElementNode(nextSibling)) {
        type = "element";
      }
    }
  }
  if (siblingKey !== null && type !== null) {
    point.set(siblingKey, offset, type);
  } else {
    offset = node.getIndexWithinParent();
    if (offset === -1) {
      offset = parent.getChildrenSize();
    }
    point.set(parent.__key, offset, "element");
  }
}
function adjustPointOffsetForMergedSibling(point, isBefore, key, target, textLength) {
  if (point.type === "text") {
    point.set(key, point.offset + (isBefore ? 0 : textLength), "text");
  } else if (point.offset > target.getIndexWithinParent()) {
    point.set(point.key, point.offset - 1, "element");
  }
}
function setDOMSelectionBaseAndExtent(domSelection, nextAnchorDOM, nextAnchorOffset, nextFocusDOM, nextFocusOffset) {
  try {
    domSelection.setBaseAndExtent(nextAnchorDOM, nextAnchorOffset, nextFocusDOM, nextFocusOffset);
  } catch (error) {
    {
      console.warn(error);
    }
  }
}
function updateDOMSelection(prevSelection, nextSelection, editor, domSelection, tags, rootElement, nodeCount) {
  const anchorDOMNode = domSelection.anchorNode;
  const focusDOMNode = domSelection.focusNode;
  const anchorOffset = domSelection.anchorOffset;
  const focusOffset = domSelection.focusOffset;
  const activeElement = document.activeElement;
  if (tags.has(COLLABORATION_TAG) && activeElement !== rootElement || activeElement !== null && isSelectionCapturedInDecoratorInput(activeElement)) {
    return;
  }
  if (!$isRangeSelection(nextSelection)) {
    if (prevSelection !== null && isSelectionWithinEditor(editor, anchorDOMNode, focusDOMNode)) {
      domSelection.removeAllRanges();
    }
    return;
  }
  const anchor = nextSelection.anchor;
  const focus = nextSelection.focus;
  const anchorKey = anchor.key;
  const focusKey = focus.key;
  const anchorDOM = getElementByKeyOrThrow(editor, anchorKey);
  const focusDOM = getElementByKeyOrThrow(editor, focusKey);
  const nextAnchorOffset = anchor.offset;
  const nextFocusOffset = focus.offset;
  const nextFormat = nextSelection.format;
  const nextStyle = nextSelection.style;
  const isCollapsed = nextSelection.isCollapsed();
  let nextAnchorNode = anchorDOM;
  let nextFocusNode = focusDOM;
  let anchorFormatOrStyleChanged = false;
  if (anchor.type === "text") {
    nextAnchorNode = getDOMTextNode(anchorDOM);
    const anchorNode = anchor.getNode();
    anchorFormatOrStyleChanged = anchorNode.getFormat() !== nextFormat || anchorNode.getStyle() !== nextStyle;
  } else if ($isRangeSelection(prevSelection) && prevSelection.anchor.type === "text") {
    anchorFormatOrStyleChanged = true;
  }
  if (focus.type === "text") {
    nextFocusNode = getDOMTextNode(focusDOM);
  }
  if (nextAnchorNode === null || nextFocusNode === null) {
    return;
  }
  if (isCollapsed && (prevSelection === null || anchorFormatOrStyleChanged || $isRangeSelection(prevSelection) && (prevSelection.format !== nextFormat || prevSelection.style !== nextStyle))) {
    markCollapsedSelectionFormat(nextFormat, nextStyle, nextAnchorOffset, anchorKey, performance.now());
  }
  if (anchorOffset === nextAnchorOffset && focusOffset === nextFocusOffset && anchorDOMNode === nextAnchorNode && focusDOMNode === nextFocusNode && // Badly interpreted range selection when collapsed - #1482
  !(domSelection.type === "Range" && isCollapsed)) {
    if (activeElement === null || !rootElement.contains(activeElement)) {
      rootElement.focus({
        preventScroll: true
      });
    }
    if (anchor.type !== "element") {
      return;
    }
  }
  setDOMSelectionBaseAndExtent(domSelection, nextAnchorNode, nextAnchorOffset, nextFocusNode, nextFocusOffset);
  if (!tags.has(SKIP_SCROLL_INTO_VIEW_TAG) && nextSelection.isCollapsed() && rootElement !== null && rootElement === document.activeElement) {
    const selectionTarget = $isRangeSelection(nextSelection) && nextSelection.anchor.type === "element" ? nextAnchorNode.childNodes[nextAnchorOffset] || null : domSelection.rangeCount > 0 ? domSelection.getRangeAt(0) : null;
    if (selectionTarget !== null) {
      let selectionRect;
      if (selectionTarget instanceof Text) {
        const range = document.createRange();
        range.selectNode(selectionTarget);
        selectionRect = range.getBoundingClientRect();
      } else {
        selectionRect = selectionTarget.getBoundingClientRect();
      }
      scrollIntoViewIfNeeded(editor, selectionRect, rootElement);
    }
  }
  markSelectionChangeFromDOMUpdate();
}
function $insertNodes(nodes) {
  let selection = $getSelection() || $getPreviousSelection();
  if (selection === null) {
    selection = $getRoot().selectEnd();
  }
  selection.insertNodes(nodes);
}
function $getTextContent() {
  const selection = $getSelection();
  if (selection === null) {
    return "";
  }
  return selection.getTextContent();
}
function $removeTextAndSplitBlock(selection) {
  let selection_ = selection;
  if (!selection.isCollapsed()) {
    selection_.removeText();
  }
  const newSelection = $getSelection();
  if ($isRangeSelection(newSelection)) {
    selection_ = newSelection;
  }
  if (!$isRangeSelection(selection_)) {
    formatDevErrorMessage(`Unexpected dirty selection to be null`);
  }
  const anchor = selection_.anchor;
  let node = anchor.getNode();
  let offset = anchor.offset;
  while (!INTERNAL_$isBlock(node)) {
    const prevNode = node;
    [node, offset] = $splitNodeAtPoint(node, offset);
    if (prevNode.is(node)) {
      break;
    }
  }
  return offset;
}
function $splitNodeAtPoint(node, offset) {
  const parent = node.getParent();
  if (!parent) {
    const paragraph = $createParagraphNode();
    $getRoot().append(paragraph);
    paragraph.select();
    return [$getRoot(), 0];
  }
  if ($isTextNode(node)) {
    const split = node.splitText(offset);
    if (split.length === 0) {
      return [parent, node.getIndexWithinParent()];
    }
    const x2 = offset === 0 ? 0 : 1;
    const index = split[0].getIndexWithinParent() + x2;
    return [parent, index];
  }
  if (!$isElementNode(node) || offset === 0) {
    return [parent, node.getIndexWithinParent()];
  }
  const firstToAppend = node.getChildAtIndex(offset);
  if (firstToAppend) {
    const insertPoint = new RangeSelection($createPoint(node.__key, offset, "element"), $createPoint(node.__key, offset, "element"), 0, "");
    const newElement = node.insertNewAfter(insertPoint);
    if (newElement) {
      newElement.append(firstToAppend, ...firstToAppend.getNextSiblings());
    }
  }
  return [parent, node.getIndexWithinParent() + 1];
}
function $wrapInlineNodes(nodes) {
  const virtualRoot = $createParagraphNode();
  let currentBlock = null;
  for (let i2 = 0; i2 < nodes.length; i2++) {
    const node = nodes[i2];
    const isLineBreakNode = $isLineBreakNode(node);
    if (isLineBreakNode || $isDecoratorNode(node) && node.isInline() || $isElementNode(node) && node.isInline() || $isTextNode(node) || node.isParentRequired()) {
      if (currentBlock === null) {
        currentBlock = node.createParentElementNode();
        virtualRoot.append(currentBlock);
        if (isLineBreakNode) {
          continue;
        }
      }
      if (currentBlock !== null) {
        currentBlock.append(node);
      }
    } else {
      virtualRoot.append(node);
      currentBlock = null;
    }
  }
  return virtualRoot;
}
function $getNodesFromCaretRangeCompat(range) {
  const nodes = [];
  const [beforeSlice, afterSlice] = range.getTextSlices();
  if (beforeSlice) {
    nodes.push(beforeSlice.caret.origin);
  }
  const seenAncestors = /* @__PURE__ */ new Set();
  const seenElements = /* @__PURE__ */ new Set();
  for (const caret of range) {
    if ($isChildCaret(caret)) {
      const {
        origin
      } = caret;
      if (nodes.length === 0) {
        seenAncestors.add(origin);
      } else {
        seenElements.add(origin);
        nodes.push(origin);
      }
    } else {
      const {
        origin
      } = caret;
      if (!$isElementNode(origin) || !seenElements.has(origin)) {
        nodes.push(origin);
      }
    }
  }
  if (afterSlice) {
    nodes.push(afterSlice.caret.origin);
  }
  if ($isSiblingCaret(range.focus) && $isElementNode(range.focus.origin) && range.focus.getNodeAtCaret() === null) {
    for (let reverseCaret = $getChildCaret(range.focus.origin, "previous"); $isChildCaret(reverseCaret) && seenAncestors.has(reverseCaret.origin) && !reverseCaret.origin.isEmpty() && reverseCaret.origin.is(nodes[nodes.length - 1]); reverseCaret = $getAdjacentChildCaret(reverseCaret)) {
      seenAncestors.delete(reverseCaret.origin);
      nodes.pop();
    }
  }
  while (nodes.length > 1) {
    const lastIncludedNode = nodes[nodes.length - 1];
    if ($isElementNode(lastIncludedNode)) {
      if (seenElements.has(lastIncludedNode) || lastIncludedNode.isEmpty() || seenAncestors.has(lastIncludedNode)) ;
      else {
        nodes.pop();
        continue;
      }
    }
    break;
  }
  if (nodes.length === 0 && range.isCollapsed()) {
    const normCaret = $normalizeCaret(range.anchor);
    const flippedNormCaret = $normalizeCaret(range.anchor.getFlipped());
    const $getCandidate = (caret) => $isTextPointCaret(caret) ? caret.origin : caret.getNodeAtCaret();
    const node = $getCandidate(normCaret) || $getCandidate(flippedNormCaret) || (range.anchor.getNodeAtCaret() ? normCaret.origin : flippedNormCaret.origin);
    nodes.push(node);
  }
  return nodes;
}
function $modifySelectionAroundDecoratorsAndBlocks(selection, alter, isBackward, granularity, mode = "decorators-and-blocks") {
  if (alter === "move" && granularity === "character" && !selection.isCollapsed()) {
    const [src, dst] = isBackward === selection.isBackward() ? [selection.focus, selection.anchor] : [selection.anchor, selection.focus];
    dst.set(src.key, src.offset, src.type);
    return true;
  }
  const initialFocus = $caretFromPoint(selection.focus, isBackward ? "previous" : "next");
  const isLineBoundary = granularity === "lineboundary";
  const collapse = alter === "move";
  let focus = initialFocus;
  let checkForBlock = mode === "decorators-and-blocks";
  if (!$isExtendableTextPointCaret(focus)) {
    for (const siblingCaret of focus) {
      checkForBlock = false;
      const {
        origin
      } = siblingCaret;
      if ($isDecoratorNode(origin) && !origin.isIsolated()) {
        focus = siblingCaret;
        if (isLineBoundary && origin.isInline()) {
          continue;
        }
      }
      break;
    }
    if (checkForBlock) {
      for (const nextCaret of $extendCaretToRange(initialFocus).iterNodeCarets(alter === "extend" ? "shadowRoot" : "root")) {
        if ($isChildCaret(nextCaret)) {
          if (!nextCaret.origin.isInline()) {
            focus = nextCaret;
          }
        } else if ($isElementNode(nextCaret.origin)) {
          continue;
        } else if ($isDecoratorNode(nextCaret.origin) && !nextCaret.origin.isInline()) {
          focus = nextCaret;
        }
        break;
      }
    }
  }
  if (focus === initialFocus) {
    return false;
  }
  if (collapse && !isLineBoundary && $isDecoratorNode(focus.origin) && focus.origin.isKeyboardSelectable()) {
    const nodeSelection = $createNodeSelection();
    nodeSelection.add(focus.origin.getKey());
    $setSelection(nodeSelection);
    return true;
  }
  focus = $normalizeCaret(focus);
  if (collapse) {
    $setPointFromCaret(selection.anchor, focus);
  }
  $setPointFromCaret(selection.focus, focus);
  return checkForBlock || !isLineBoundary;
}
var activeEditorState = null;
var activeEditor = null;
var isReadOnlyMode = false;
var isAttemptingToRecoverFromReconcilerError = false;
var infiniteTransformCount = 0;
var observerOptions = {
  characterData: true,
  childList: true,
  subtree: true
};
function isCurrentlyReadOnlyMode() {
  return isReadOnlyMode || activeEditorState !== null && activeEditorState._readOnly;
}
function errorOnReadOnly() {
  if (isReadOnlyMode) {
    {
      formatDevErrorMessage(`Cannot use method in read-only mode.`);
    }
  }
}
function errorOnInfiniteTransforms() {
  if (infiniteTransformCount > 99) {
    {
      formatDevErrorMessage(`One or more transforms are endlessly triggering additional transforms. May have encountered infinite recursion caused by transforms that have their preconditions too lose and/or conflict with each other.`);
    }
  }
}
function getActiveEditorState() {
  if (activeEditorState === null) {
    {
      formatDevErrorMessage(`Unable to find an active editor state. State helpers or node methods can only be used synchronously during the callback of editor.update(), editor.read(), or editorState.read().${collectBuildInformation()}`);
    }
  }
  return activeEditorState;
}
function getActiveEditor() {
  if (activeEditor === null) {
    {
      formatDevErrorMessage(`Unable to find an active editor. This method can only be used synchronously during the callback of editor.update() or editor.read().${collectBuildInformation()}`);
    }
  }
  return activeEditor;
}
function collectBuildInformation() {
  let compatibleEditors = 0;
  const incompatibleEditors = /* @__PURE__ */ new Set();
  const thisVersion = LexicalEditor.version;
  if (typeof window !== "undefined") {
    for (const node of document.querySelectorAll("[contenteditable]")) {
      const editor = getEditorPropertyFromDOMNode(node);
      if (isLexicalEditor(editor)) {
        compatibleEditors++;
      } else if (editor) {
        let version = String(editor.constructor.version || "<0.17.1");
        if (version === thisVersion) {
          version += " (separately built, likely a bundler configuration issue)";
        }
        incompatibleEditors.add(version);
      }
    }
  }
  let output = ` Detected on the page: ${compatibleEditors} compatible editor(s) with version ${thisVersion}`;
  if (incompatibleEditors.size) {
    output += ` and incompatible editors with versions ${Array.from(incompatibleEditors).join(", ")}`;
  }
  return output;
}
function internalGetActiveEditor() {
  return activeEditor;
}
function internalGetActiveEditorState() {
  return activeEditorState;
}
function $applyTransforms(editor, node, transformsCache) {
  const type = node.__type;
  const registeredNode = getRegisteredNodeOrThrow(editor, type);
  let transformsArr = transformsCache.get(type);
  if (transformsArr === void 0) {
    transformsArr = Array.from(registeredNode.transforms);
    transformsCache.set(type, transformsArr);
  }
  const transformsArrLength = transformsArr.length;
  for (let i2 = 0; i2 < transformsArrLength; i2++) {
    transformsArr[i2](node);
    if (!node.isAttached()) {
      break;
    }
  }
}
function $isNodeValidForTransform(node, compositionKey) {
  return node !== void 0 && // We don't want to transform nodes being composed
  node.__key !== compositionKey && node.isAttached();
}
function $normalizeAllDirtyTextNodes(editorState, editor) {
  const dirtyLeaves = editor._dirtyLeaves;
  const nodeMap = editorState._nodeMap;
  for (const nodeKey of dirtyLeaves) {
    const node = nodeMap.get(nodeKey);
    if ($isTextNode(node) && node.isAttached() && node.isSimpleText() && !node.isUnmergeable()) {
      $normalizeTextNode(node);
    }
  }
}
function addTags(editor, tags) {
  if (!tags) {
    return;
  }
  const updateTags = editor._updateTags;
  let tags_ = tags;
  if (!Array.isArray(tags)) {
    tags_ = [tags];
  }
  for (const tag of tags_) {
    updateTags.add(tag);
  }
}
function $applyAllTransforms(editorState, editor) {
  const dirtyLeaves = editor._dirtyLeaves;
  const dirtyElements = editor._dirtyElements;
  const nodeMap = editorState._nodeMap;
  const compositionKey = $getCompositionKey();
  const transformsCache = /* @__PURE__ */ new Map();
  let untransformedDirtyLeaves = dirtyLeaves;
  let untransformedDirtyLeavesLength = untransformedDirtyLeaves.size;
  let untransformedDirtyElements = dirtyElements;
  let untransformedDirtyElementsLength = untransformedDirtyElements.size;
  while (untransformedDirtyLeavesLength > 0 || untransformedDirtyElementsLength > 0) {
    if (untransformedDirtyLeavesLength > 0) {
      editor._dirtyLeaves = /* @__PURE__ */ new Set();
      for (const nodeKey of untransformedDirtyLeaves) {
        const node = nodeMap.get(nodeKey);
        if ($isTextNode(node) && node.isAttached() && node.isSimpleText() && !node.isUnmergeable()) {
          $normalizeTextNode(node);
        }
        if (node !== void 0 && $isNodeValidForTransform(node, compositionKey)) {
          $applyTransforms(editor, node, transformsCache);
        }
        dirtyLeaves.add(nodeKey);
      }
      untransformedDirtyLeaves = editor._dirtyLeaves;
      untransformedDirtyLeavesLength = untransformedDirtyLeaves.size;
      if (untransformedDirtyLeavesLength > 0) {
        infiniteTransformCount++;
        continue;
      }
    }
    editor._dirtyLeaves = /* @__PURE__ */ new Set();
    editor._dirtyElements = /* @__PURE__ */ new Map();
    const rootDirty = untransformedDirtyElements.delete("root");
    if (rootDirty) {
      untransformedDirtyElements.set("root", true);
    }
    for (const currentUntransformedDirtyElement of untransformedDirtyElements) {
      const nodeKey = currentUntransformedDirtyElement[0];
      const intentionallyMarkedAsDirty = currentUntransformedDirtyElement[1];
      dirtyElements.set(nodeKey, intentionallyMarkedAsDirty);
      if (!intentionallyMarkedAsDirty) {
        continue;
      }
      const node = nodeMap.get(nodeKey);
      if (node !== void 0 && $isNodeValidForTransform(node, compositionKey)) {
        $applyTransforms(editor, node, transformsCache);
      }
    }
    untransformedDirtyLeaves = editor._dirtyLeaves;
    untransformedDirtyLeavesLength = untransformedDirtyLeaves.size;
    untransformedDirtyElements = editor._dirtyElements;
    untransformedDirtyElementsLength = untransformedDirtyElements.size;
    infiniteTransformCount++;
  }
  editor._dirtyLeaves = dirtyLeaves;
  editor._dirtyElements = dirtyElements;
}
function $parseSerializedNode(serializedNode) {
  const internalSerializedNode = serializedNode;
  return $parseSerializedNodeImpl(internalSerializedNode, getActiveEditor()._nodes);
}
function $parseSerializedNodeImpl(serializedNode, registeredNodes) {
  const type = serializedNode.type;
  const registeredNode = registeredNodes.get(type);
  if (registeredNode === void 0) {
    {
      formatDevErrorMessage(`parseEditorState: type "${type}" + not found`);
    }
  }
  const nodeClass = registeredNode.klass;
  if (serializedNode.type !== nodeClass.getType()) {
    {
      formatDevErrorMessage(`LexicalNode: Node ${nodeClass.name} does not implement .importJSON().`);
    }
  }
  const node = nodeClass.importJSON(serializedNode);
  const children = serializedNode.children;
  if ($isElementNode(node) && Array.isArray(children)) {
    for (let i2 = 0; i2 < children.length; i2++) {
      const serializedJSONChildNode = children[i2];
      const childNode = $parseSerializedNodeImpl(serializedJSONChildNode, registeredNodes);
      node.append(childNode);
    }
  }
  return node;
}
function parseEditorState(serializedEditorState, editor, updateFn) {
  const editorState = createEmptyEditorState();
  const previousActiveEditorState = activeEditorState;
  const previousReadOnlyMode = isReadOnlyMode;
  const previousActiveEditor = activeEditor;
  const previousDirtyElements = editor._dirtyElements;
  const previousDirtyLeaves = editor._dirtyLeaves;
  const previousCloneNotNeeded = editor._cloneNotNeeded;
  const previousDirtyType = editor._dirtyType;
  editor._dirtyElements = /* @__PURE__ */ new Map();
  editor._dirtyLeaves = /* @__PURE__ */ new Set();
  editor._cloneNotNeeded = /* @__PURE__ */ new Set();
  editor._dirtyType = 0;
  activeEditorState = editorState;
  isReadOnlyMode = false;
  activeEditor = editor;
  setPendingNodeToClone(null);
  try {
    const registeredNodes = editor._nodes;
    const serializedNode = serializedEditorState.root;
    $parseSerializedNodeImpl(serializedNode, registeredNodes);
    if (updateFn) {
      updateFn();
    }
    editorState._readOnly = true;
    {
      handleDEVOnlyPendingUpdateGuarantees(editorState);
    }
  } catch (error) {
    if (error instanceof Error) {
      editor._onError(error);
    }
  } finally {
    editor._dirtyElements = previousDirtyElements;
    editor._dirtyLeaves = previousDirtyLeaves;
    editor._cloneNotNeeded = previousCloneNotNeeded;
    editor._dirtyType = previousDirtyType;
    activeEditorState = previousActiveEditorState;
    isReadOnlyMode = previousReadOnlyMode;
    activeEditor = previousActiveEditor;
  }
  return editorState;
}
function readEditorState(editor, editorState, callbackFn) {
  const previousActiveEditorState = activeEditorState;
  const previousReadOnlyMode = isReadOnlyMode;
  const previousActiveEditor = activeEditor;
  activeEditorState = editorState;
  isReadOnlyMode = true;
  activeEditor = editor;
  try {
    return callbackFn();
  } finally {
    activeEditorState = previousActiveEditorState;
    isReadOnlyMode = previousReadOnlyMode;
    activeEditor = previousActiveEditor;
  }
}
function handleDEVOnlyPendingUpdateGuarantees(pendingEditorState) {
  const nodeMap = pendingEditorState._nodeMap;
  nodeMap.set = () => {
    throw new Error("Cannot call set() on a frozen Lexical node map");
  };
  nodeMap.clear = () => {
    throw new Error("Cannot call clear() on a frozen Lexical node map");
  };
  nodeMap.delete = () => {
    throw new Error("Cannot call delete() on a frozen Lexical node map");
  };
}
function $commitPendingUpdates(editor, recoveryEditorState) {
  const pendingEditorState = editor._pendingEditorState;
  const rootElement = editor._rootElement;
  const shouldSkipDOM = editor._headless || rootElement === null;
  if (pendingEditorState === null) {
    return;
  }
  const currentEditorState = editor._editorState;
  const currentSelection = currentEditorState._selection;
  const pendingSelection = pendingEditorState._selection;
  const needsUpdate = editor._dirtyType !== NO_DIRTY_NODES;
  const previousActiveEditorState = activeEditorState;
  const previousReadOnlyMode = isReadOnlyMode;
  const previousActiveEditor = activeEditor;
  const previouslyUpdating = editor._updating;
  const observer = editor._observer;
  let mutatedNodes2 = null;
  editor._pendingEditorState = null;
  editor._editorState = pendingEditorState;
  if (!shouldSkipDOM && needsUpdate && observer !== null) {
    activeEditor = editor;
    activeEditorState = pendingEditorState;
    isReadOnlyMode = false;
    editor._updating = true;
    try {
      const dirtyType = editor._dirtyType;
      const dirtyElements2 = editor._dirtyElements;
      const dirtyLeaves2 = editor._dirtyLeaves;
      observer.disconnect();
      mutatedNodes2 = $reconcileRoot(currentEditorState, pendingEditorState, editor, dirtyType, dirtyElements2, dirtyLeaves2);
    } catch (error) {
      if (error instanceof Error) {
        editor._onError(error);
      }
      if (!isAttemptingToRecoverFromReconcilerError) {
        resetEditor(editor, null, rootElement, pendingEditorState);
        initMutationObserver(editor);
        editor._dirtyType = FULL_RECONCILE;
        isAttemptingToRecoverFromReconcilerError = true;
        $commitPendingUpdates(editor, currentEditorState);
        isAttemptingToRecoverFromReconcilerError = false;
      } else {
        throw error;
      }
      return;
    } finally {
      observer.observe(rootElement, observerOptions);
      editor._updating = previouslyUpdating;
      activeEditorState = previousActiveEditorState;
      isReadOnlyMode = previousReadOnlyMode;
      activeEditor = previousActiveEditor;
    }
  }
  if (!pendingEditorState._readOnly) {
    pendingEditorState._readOnly = true;
    {
      handleDEVOnlyPendingUpdateGuarantees(pendingEditorState);
      if ($isRangeSelection(pendingSelection)) {
        Object.freeze(pendingSelection.anchor);
        Object.freeze(pendingSelection.focus);
      }
      Object.freeze(pendingSelection);
    }
  }
  const dirtyLeaves = editor._dirtyLeaves;
  const dirtyElements = editor._dirtyElements;
  const normalizedNodes = editor._normalizedNodes;
  const tags = editor._updateTags;
  const deferred = editor._deferred;
  if (needsUpdate) {
    editor._dirtyType = NO_DIRTY_NODES;
    editor._cloneNotNeeded.clear();
    editor._dirtyLeaves = /* @__PURE__ */ new Set();
    editor._dirtyElements = /* @__PURE__ */ new Map();
    editor._normalizedNodes = /* @__PURE__ */ new Set();
    editor._updateTags = /* @__PURE__ */ new Set();
  }
  $garbageCollectDetachedDecorators(editor, pendingEditorState);
  const domSelection = shouldSkipDOM ? null : getDOMSelection(getWindow(editor));
  if (editor._editable && // domSelection will be null in headless
  domSelection !== null && (needsUpdate || pendingSelection === null || pendingSelection.dirty) && rootElement !== null && !tags.has(SKIP_DOM_SELECTION_TAG)) {
    activeEditor = editor;
    activeEditorState = pendingEditorState;
    try {
      if (observer !== null) {
        observer.disconnect();
      }
      if (needsUpdate || pendingSelection === null || pendingSelection.dirty) {
        const blockCursorElement = editor._blockCursorElement;
        if (blockCursorElement !== null) {
          removeDOMBlockCursorElement(blockCursorElement, editor, rootElement);
        }
        updateDOMSelection(currentSelection, pendingSelection, editor, domSelection, tags, rootElement);
      }
      updateDOMBlockCursorElement(editor, rootElement, pendingSelection);
    } finally {
      if (observer !== null) {
        observer.observe(rootElement, observerOptions);
      }
      activeEditor = previousActiveEditor;
      activeEditorState = previousActiveEditorState;
    }
  }
  if (mutatedNodes2 !== null) {
    triggerMutationListeners(editor, mutatedNodes2, tags, dirtyLeaves, currentEditorState);
  }
  if (!$isRangeSelection(pendingSelection) && pendingSelection !== null && (currentSelection === null || !currentSelection.is(pendingSelection))) {
    editor.dispatchCommand(SELECTION_CHANGE_COMMAND, void 0);
  }
  const pendingDecorators = editor._pendingDecorators;
  if (pendingDecorators !== null) {
    editor._decorators = pendingDecorators;
    editor._pendingDecorators = null;
    triggerListeners("decorator", editor, true, pendingDecorators);
  }
  triggerTextContentListeners(editor, recoveryEditorState || currentEditorState, pendingEditorState);
  triggerListeners("update", editor, true, {
    dirtyElements,
    dirtyLeaves,
    editorState: pendingEditorState,
    mutatedNodes: mutatedNodes2,
    normalizedNodes,
    prevEditorState: recoveryEditorState || currentEditorState,
    tags
  });
  triggerDeferredUpdateCallbacks(editor, deferred);
  $triggerEnqueuedUpdates(editor);
}
function triggerTextContentListeners(editor, currentEditorState, pendingEditorState) {
  const currentTextContent = getEditorStateTextContent(currentEditorState);
  const latestTextContent = getEditorStateTextContent(pendingEditorState);
  if (currentTextContent !== latestTextContent) {
    triggerListeners("textcontent", editor, true, latestTextContent);
  }
}
function triggerMutationListeners(editor, mutatedNodes2, updateTags, dirtyLeaves, prevEditorState) {
  const listeners = Array.from(editor._listeners.mutation);
  const listenersLength = listeners.length;
  for (let i2 = 0; i2 < listenersLength; i2++) {
    const [listener, klassSet] = listeners[i2];
    for (const klass of klassSet) {
      const mutatedNodesByType = mutatedNodes2.get(klass);
      if (mutatedNodesByType !== void 0) {
        listener(mutatedNodesByType, {
          dirtyLeaves,
          prevEditorState,
          updateTags
        });
      }
    }
  }
}
function triggerListeners(type, editor, isCurrentlyEnqueuingUpdates, ...payload) {
  const previouslyUpdating = editor._updating;
  editor._updating = isCurrentlyEnqueuingUpdates;
  try {
    const listeners = Array.from(editor._listeners[type]);
    for (let i2 = 0; i2 < listeners.length; i2++) {
      listeners[i2].apply(null, payload);
    }
  } finally {
    editor._updating = previouslyUpdating;
  }
}
function triggerCommandListeners(editor, type, payload) {
  const editors = getEditorsToPropagate(editor);
  for (let i2 = 4; i2 >= 0; i2--) {
    for (let e2 = 0; e2 < editors.length; e2++) {
      const currentEditor = editors[e2];
      const commandListeners = currentEditor._commands;
      const listenerInPriorityOrder = commandListeners.get(type);
      if (listenerInPriorityOrder !== void 0) {
        const listenersSet = listenerInPriorityOrder[i2];
        if (listenersSet !== void 0) {
          const listeners = Array.from(listenersSet);
          const listenersLength = listeners.length;
          let returnVal = false;
          updateEditorSync(currentEditor, () => {
            for (let j2 = 0; j2 < listenersLength; j2++) {
              if (listeners[j2](payload, editor)) {
                returnVal = true;
                return;
              }
            }
          });
          if (returnVal) {
            return returnVal;
          }
        }
      }
    }
  }
  return false;
}
function $triggerEnqueuedUpdates(editor) {
  const queuedUpdates = editor._updates;
  if (queuedUpdates.length !== 0) {
    const queuedUpdate = queuedUpdates.shift();
    if (queuedUpdate) {
      const [updateFn, options] = queuedUpdate;
      $beginUpdate(editor, updateFn, options);
    }
  }
}
function triggerDeferredUpdateCallbacks(editor, deferred) {
  editor._deferred = [];
  if (deferred.length !== 0) {
    const previouslyUpdating = editor._updating;
    editor._updating = true;
    try {
      for (let i2 = 0; i2 < deferred.length; i2++) {
        deferred[i2]();
      }
    } finally {
      editor._updating = previouslyUpdating;
    }
  }
}
function $processNestedUpdates(editor, initialSkipTransforms) {
  const queuedUpdates = editor._updates;
  let skipTransforms = initialSkipTransforms || false;
  while (queuedUpdates.length !== 0) {
    const queuedUpdate = queuedUpdates.shift();
    if (queuedUpdate) {
      const [nextUpdateFn, options] = queuedUpdate;
      const pendingEditorState = editor._pendingEditorState;
      let onUpdate;
      if (options !== void 0) {
        onUpdate = options.onUpdate;
        if (options.skipTransforms) {
          skipTransforms = true;
        }
        if (options.discrete) {
          if (!(pendingEditorState !== null)) {
            formatDevErrorMessage(`Unexpected empty pending editor state on discrete nested update`);
          }
          pendingEditorState._flushSync = true;
        }
        if (onUpdate) {
          editor._deferred.push(onUpdate);
        }
        addTags(editor, options.tag);
      }
      if (pendingEditorState == null) {
        $beginUpdate(editor, nextUpdateFn, options);
      } else {
        nextUpdateFn();
      }
    }
  }
  return skipTransforms;
}
function $beginUpdate(editor, updateFn, options) {
  const updateTags = editor._updateTags;
  let onUpdate;
  let skipTransforms = false;
  let discrete = false;
  if (options !== void 0) {
    onUpdate = options.onUpdate;
    addTags(editor, options.tag);
    skipTransforms = options.skipTransforms || false;
    discrete = options.discrete || false;
  }
  if (onUpdate) {
    editor._deferred.push(onUpdate);
  }
  const currentEditorState = editor._editorState;
  let pendingEditorState = editor._pendingEditorState;
  let editorStateWasCloned = false;
  if (pendingEditorState === null || pendingEditorState._readOnly) {
    pendingEditorState = editor._pendingEditorState = cloneEditorState(pendingEditorState || currentEditorState);
    editorStateWasCloned = true;
  }
  pendingEditorState._flushSync = discrete;
  const previousActiveEditorState = activeEditorState;
  const previousReadOnlyMode = isReadOnlyMode;
  const previousActiveEditor = activeEditor;
  const previouslyUpdating = editor._updating;
  activeEditorState = pendingEditorState;
  isReadOnlyMode = false;
  editor._updating = true;
  activeEditor = editor;
  const headless = editor._headless || editor.getRootElement() === null;
  setPendingNodeToClone(null);
  try {
    if (editorStateWasCloned) {
      if (headless) {
        if (currentEditorState._selection !== null) {
          pendingEditorState._selection = currentEditorState._selection.clone();
        }
      } else {
        pendingEditorState._selection = $internalCreateSelection(editor, options && options.event || null);
      }
    }
    const startingCompositionKey = editor._compositionKey;
    updateFn();
    skipTransforms = $processNestedUpdates(editor, skipTransforms);
    applySelectionTransforms(pendingEditorState, editor);
    if (editor._dirtyType !== NO_DIRTY_NODES) {
      if (skipTransforms) {
        $normalizeAllDirtyTextNodes(pendingEditorState, editor);
      } else {
        $applyAllTransforms(pendingEditorState, editor);
      }
      $processNestedUpdates(editor);
      $garbageCollectDetachedNodes(currentEditorState, pendingEditorState, editor._dirtyLeaves, editor._dirtyElements);
    }
    const endingCompositionKey = editor._compositionKey;
    if (startingCompositionKey !== endingCompositionKey) {
      pendingEditorState._flushSync = true;
    }
    const pendingSelection = pendingEditorState._selection;
    if ($isRangeSelection(pendingSelection)) {
      const pendingNodeMap = pendingEditorState._nodeMap;
      const anchorKey = pendingSelection.anchor.key;
      const focusKey = pendingSelection.focus.key;
      if (pendingNodeMap.get(anchorKey) === void 0 || pendingNodeMap.get(focusKey) === void 0) {
        {
          formatDevErrorMessage(`updateEditor: selection has been lost because the previously selected nodes have been removed and selection wasn't moved to another node. Ensure selection changes after removing/replacing a selected node.`);
        }
      }
    } else if ($isNodeSelection(pendingSelection)) {
      if (pendingSelection._nodes.size === 0) {
        pendingEditorState._selection = null;
      }
    }
  } catch (error) {
    if (error instanceof Error) {
      editor._onError(error);
    }
    editor._pendingEditorState = currentEditorState;
    editor._dirtyType = FULL_RECONCILE;
    editor._cloneNotNeeded.clear();
    editor._dirtyLeaves = /* @__PURE__ */ new Set();
    editor._dirtyElements.clear();
    $commitPendingUpdates(editor);
    return;
  } finally {
    activeEditorState = previousActiveEditorState;
    isReadOnlyMode = previousReadOnlyMode;
    activeEditor = previousActiveEditor;
    editor._updating = previouslyUpdating;
    infiniteTransformCount = 0;
  }
  const shouldUpdate = editor._dirtyType !== NO_DIRTY_NODES || editor._deferred.length > 0 || editorStateHasDirtySelection(pendingEditorState, editor);
  if (shouldUpdate) {
    if (pendingEditorState._flushSync) {
      pendingEditorState._flushSync = false;
      $commitPendingUpdates(editor);
    } else if (editorStateWasCloned) {
      scheduleMicroTask(() => {
        $commitPendingUpdates(editor);
      });
    }
  } else {
    pendingEditorState._flushSync = false;
    if (editorStateWasCloned) {
      updateTags.clear();
      editor._deferred = [];
      editor._pendingEditorState = null;
    }
  }
}
function updateEditorSync(editor, updateFn, options) {
  if (activeEditor === editor && options === void 0) {
    updateFn();
  } else {
    $beginUpdate(editor, updateFn, options);
  }
}
function updateEditor(editor, updateFn, options) {
  if (editor._updating) {
    editor._updates.push([updateFn, options]);
  } else {
    $beginUpdate(editor, updateFn, options);
  }
}
var ElementDOMSlot = class _ElementDOMSlot {
  constructor(element, before, after) {
    this.element = element;
    this.before = before || null;
    this.after = after || null;
  }
  /**
   * Return a new ElementDOMSlot where all managed children will be inserted before this node
   */
  withBefore(before) {
    return new _ElementDOMSlot(this.element, before, this.after);
  }
  /**
   * Return a new ElementDOMSlot where all managed children will be inserted after this node
   */
  withAfter(after) {
    return new _ElementDOMSlot(this.element, this.before, after);
  }
  /**
   * Return a new ElementDOMSlot with an updated root element
   */
  withElement(element) {
    if (this.element === element) {
      return this;
    }
    return new _ElementDOMSlot(element, this.before, this.after);
  }
  /**
   * Insert the given child before this.before and any reconciler managed line break node,
   * or append it if this.before is not defined
   */
  insertChild(dom) {
    const before = this.before || this.getManagedLineBreak();
    if (!(before === null || before.parentElement === this.element)) {
      formatDevErrorMessage(`ElementDOMSlot.insertChild: before is not in element`);
    }
    this.element.insertBefore(dom, before);
    return this;
  }
  /**
   * Remove the managed child from this container, will throw if it was not already there
   */
  removeChild(dom) {
    if (!(dom.parentElement === this.element)) {
      formatDevErrorMessage(`ElementDOMSlot.removeChild: dom is not in element`);
    }
    this.element.removeChild(dom);
    return this;
  }
  /**
   * Replace managed child prevDom with dom. Will throw if prevDom is not a child
   *
   * @param dom The new node to replace prevDom
   * @param prevDom the node that will be replaced
   */
  replaceChild(dom, prevDom) {
    if (!(prevDom.parentElement === this.element)) {
      formatDevErrorMessage(`ElementDOMSlot.replaceChild: prevDom is not in element`);
    }
    this.element.replaceChild(dom, prevDom);
    return this;
  }
  /**
   * Returns the first managed child of this node,
   * which will either be this.after.nextSibling or this.element.firstChild,
   * and will never be this.before if it is defined.
   */
  getFirstChild() {
    const firstChild = this.after ? this.after.nextSibling : this.element.firstChild;
    return firstChild === this.before || firstChild === this.getManagedLineBreak() ? null : firstChild;
  }
  /**
   * @internal
   */
  getManagedLineBreak() {
    const element = this.element;
    return element.__lexicalLineBreak || null;
  }
  /** @internal */
  setManagedLineBreak(lineBreakType) {
    if (lineBreakType === null) {
      this.removeManagedLineBreak();
    } else {
      const webkitHack = lineBreakType === "decorator" && (IS_IOS || IS_SAFARI);
      this.insertManagedLineBreak(webkitHack);
    }
  }
  /** @internal */
  removeManagedLineBreak() {
    const br2 = this.getManagedLineBreak();
    if (br2) {
      const element = this.element;
      const sibling = br2.nodeName === "IMG" ? br2.nextSibling : null;
      if (sibling) {
        element.removeChild(sibling);
      }
      element.removeChild(br2);
      element.__lexicalLineBreak = void 0;
    }
  }
  /** @internal */
  insertManagedLineBreak(webkitHack) {
    const prevBreak = this.getManagedLineBreak();
    if (prevBreak) {
      if (webkitHack === (prevBreak.nodeName === "IMG")) {
        return;
      }
      this.removeManagedLineBreak();
    }
    const element = this.element;
    const before = this.before;
    const br2 = document.createElement("br");
    element.insertBefore(br2, before);
    if (webkitHack) {
      const img = document.createElement("img");
      img.setAttribute("data-lexical-linebreak", "true");
      img.style.cssText = "display: inline !important; border: 0px !important; margin: 0px !important;";
      img.alt = "";
      element.insertBefore(img, br2);
      element.__lexicalLineBreak = img;
    } else {
      element.__lexicalLineBreak = br2;
    }
  }
  /**
   * @internal
   *
   * Returns the offset of the first child
   */
  getFirstChildOffset() {
    let i2 = 0;
    for (let node = this.after; node !== null; node = node.previousSibling) {
      i2++;
    }
    return i2;
  }
  /**
   * @internal
   */
  resolveChildIndex(element, elementDOM, initialDOM, initialOffset) {
    if (initialDOM === this.element) {
      const firstChildOffset = this.getFirstChildOffset();
      return [element, Math.min(firstChildOffset + element.getChildrenSize(), Math.max(firstChildOffset, initialOffset))];
    }
    const initialPath = indexPath(elementDOM, initialDOM);
    initialPath.push(initialOffset);
    const elementPath = indexPath(elementDOM, this.element);
    let offset = element.getIndexWithinParent();
    for (let i2 = 0; i2 < elementPath.length; i2++) {
      const target = initialPath[i2];
      const source = elementPath[i2];
      if (target === void 0 || target < source) {
        break;
      } else if (target > source) {
        offset += 1;
        break;
      }
    }
    return [element.getParentOrThrow(), offset];
  }
};
function indexPath(root, child) {
  const path = [];
  let node = child;
  for (; node !== root && node !== null; node = node.parentNode) {
    let i2 = 0;
    for (let sibling = node.previousSibling; sibling !== null; sibling = sibling.previousSibling) {
      i2++;
    }
    path.push(i2);
  }
  if (!(node === root)) {
    formatDevErrorMessage(`indexPath: root is not a parent of child`);
  }
  return path.reverse();
}
var ElementNode = class extends LexicalNode {
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  constructor(key) {
    super(key);
    this.__first = null;
    this.__last = null;
    this.__size = 0;
    this.__format = 0;
    this.__style = "";
    this.__indent = 0;
    this.__dir = null;
    this.__textFormat = 0;
    this.__textStyle = "";
  }
  afterCloneFrom(prevNode) {
    super.afterCloneFrom(prevNode);
    if (this.__key === prevNode.__key) {
      this.__first = prevNode.__first;
      this.__last = prevNode.__last;
      this.__size = prevNode.__size;
    }
    this.__indent = prevNode.__indent;
    this.__format = prevNode.__format;
    this.__style = prevNode.__style;
    this.__dir = prevNode.__dir;
    this.__textFormat = prevNode.__textFormat;
    this.__textStyle = prevNode.__textStyle;
  }
  getFormat() {
    const self = this.getLatest();
    return self.__format;
  }
  getFormatType() {
    const format = this.getFormat();
    return ELEMENT_FORMAT_TO_TYPE[format] || "";
  }
  getStyle() {
    const self = this.getLatest();
    return self.__style;
  }
  getIndent() {
    const self = this.getLatest();
    return self.__indent;
  }
  getChildren() {
    const children = [];
    let child = this.getFirstChild();
    while (child !== null) {
      children.push(child);
      child = child.getNextSibling();
    }
    return children;
  }
  getChildrenKeys() {
    const children = [];
    let child = this.getFirstChild();
    while (child !== null) {
      children.push(child.__key);
      child = child.getNextSibling();
    }
    return children;
  }
  getChildrenSize() {
    const self = this.getLatest();
    return self.__size;
  }
  isEmpty() {
    return this.getChildrenSize() === 0;
  }
  isDirty() {
    const editor = getActiveEditor();
    const dirtyElements = editor._dirtyElements;
    return dirtyElements !== null && dirtyElements.has(this.__key);
  }
  isLastChild() {
    const self = this.getLatest();
    const parentLastChild = this.getParentOrThrow().getLastChild();
    return parentLastChild !== null && parentLastChild.is(self);
  }
  getAllTextNodes() {
    const textNodes = [];
    let child = this.getFirstChild();
    while (child !== null) {
      if ($isTextNode(child)) {
        textNodes.push(child);
      }
      if ($isElementNode(child)) {
        const subChildrenNodes = child.getAllTextNodes();
        textNodes.push(...subChildrenNodes);
      }
      child = child.getNextSibling();
    }
    return textNodes;
  }
  getFirstDescendant() {
    let node = this.getFirstChild();
    while ($isElementNode(node)) {
      const child = node.getFirstChild();
      if (child === null) {
        break;
      }
      node = child;
    }
    return node;
  }
  getLastDescendant() {
    let node = this.getLastChild();
    while ($isElementNode(node)) {
      const child = node.getLastChild();
      if (child === null) {
        break;
      }
      node = child;
    }
    return node;
  }
  getDescendantByIndex(index) {
    const children = this.getChildren();
    const childrenLength = children.length;
    if (index >= childrenLength) {
      const resolvedNode2 = children[childrenLength - 1];
      return $isElementNode(resolvedNode2) && resolvedNode2.getLastDescendant() || resolvedNode2 || null;
    }
    const resolvedNode = children[index];
    return $isElementNode(resolvedNode) && resolvedNode.getFirstDescendant() || resolvedNode || null;
  }
  getFirstChild() {
    const self = this.getLatest();
    const firstKey = self.__first;
    return firstKey === null ? null : $getNodeByKey(firstKey);
  }
  getFirstChildOrThrow() {
    const firstChild = this.getFirstChild();
    if (firstChild === null) {
      {
        formatDevErrorMessage(`Expected node ${this.__key} to have a first child.`);
      }
    }
    return firstChild;
  }
  getLastChild() {
    const self = this.getLatest();
    const lastKey = self.__last;
    return lastKey === null ? null : $getNodeByKey(lastKey);
  }
  getLastChildOrThrow() {
    const lastChild = this.getLastChild();
    if (lastChild === null) {
      {
        formatDevErrorMessage(`Expected node ${this.__key} to have a last child.`);
      }
    }
    return lastChild;
  }
  getChildAtIndex(index) {
    const size = this.getChildrenSize();
    let node;
    let i2;
    if (index < size / 2) {
      node = this.getFirstChild();
      i2 = 0;
      while (node !== null && i2 <= index) {
        if (i2 === index) {
          return node;
        }
        node = node.getNextSibling();
        i2++;
      }
      return null;
    }
    node = this.getLastChild();
    i2 = size - 1;
    while (node !== null && i2 >= index) {
      if (i2 === index) {
        return node;
      }
      node = node.getPreviousSibling();
      i2--;
    }
    return null;
  }
  getTextContent() {
    let textContent = "";
    const children = this.getChildren();
    const childrenLength = children.length;
    for (let i2 = 0; i2 < childrenLength; i2++) {
      const child = children[i2];
      textContent += child.getTextContent();
      if ($isElementNode(child) && i2 !== childrenLength - 1 && !child.isInline()) {
        textContent += DOUBLE_LINE_BREAK;
      }
    }
    return textContent;
  }
  getTextContentSize() {
    let textContentSize = 0;
    const children = this.getChildren();
    const childrenLength = children.length;
    for (let i2 = 0; i2 < childrenLength; i2++) {
      const child = children[i2];
      textContentSize += child.getTextContentSize();
      if ($isElementNode(child) && i2 !== childrenLength - 1 && !child.isInline()) {
        textContentSize += DOUBLE_LINE_BREAK.length;
      }
    }
    return textContentSize;
  }
  getDirection() {
    const self = this.getLatest();
    return self.__dir;
  }
  getTextFormat() {
    const self = this.getLatest();
    return self.__textFormat;
  }
  hasFormat(type) {
    if (type !== "") {
      const formatFlag = ELEMENT_TYPE_TO_FORMAT[type];
      return (this.getFormat() & formatFlag) !== 0;
    }
    return false;
  }
  hasTextFormat(type) {
    const formatFlag = TEXT_TYPE_TO_FORMAT[type];
    return (this.getTextFormat() & formatFlag) !== 0;
  }
  /**
   * Returns the format flags applied to the node as a 32-bit integer.
   *
   * @returns a number representing the TextFormatTypes applied to the node.
   */
  getFormatFlags(type, alignWithFormat) {
    const self = this.getLatest();
    const format = self.__textFormat;
    return toggleTextFormatType(format, type, alignWithFormat);
  }
  getTextStyle() {
    const self = this.getLatest();
    return self.__textStyle;
  }
  // Mutators
  select(_anchorOffset, _focusOffset) {
    errorOnReadOnly();
    const selection = $getSelection();
    let anchorOffset = _anchorOffset;
    let focusOffset = _focusOffset;
    const childrenCount = this.getChildrenSize();
    if (!this.canBeEmpty()) {
      if (_anchorOffset === 0 && _focusOffset === 0) {
        const firstChild = this.getFirstChild();
        if ($isTextNode(firstChild) || $isElementNode(firstChild)) {
          return firstChild.select(0, 0);
        }
      } else if ((_anchorOffset === void 0 || _anchorOffset === childrenCount) && (_focusOffset === void 0 || _focusOffset === childrenCount)) {
        const lastChild = this.getLastChild();
        if ($isTextNode(lastChild) || $isElementNode(lastChild)) {
          return lastChild.select();
        }
      }
    }
    if (anchorOffset === void 0) {
      anchorOffset = childrenCount;
    }
    if (focusOffset === void 0) {
      focusOffset = childrenCount;
    }
    const key = this.__key;
    if (!$isRangeSelection(selection)) {
      return $internalMakeRangeSelection(key, anchorOffset, key, focusOffset, "element", "element");
    } else {
      selection.anchor.set(key, anchorOffset, "element");
      selection.focus.set(key, focusOffset, "element");
      selection.dirty = true;
    }
    return selection;
  }
  selectStart() {
    const firstNode = this.getFirstDescendant();
    return firstNode ? firstNode.selectStart() : this.select();
  }
  selectEnd() {
    const lastNode = this.getLastDescendant();
    return lastNode ? lastNode.selectEnd() : this.select();
  }
  clear() {
    const writableSelf = this.getWritable();
    const children = this.getChildren();
    children.forEach((child) => child.remove());
    return writableSelf;
  }
  append(...nodesToAppend) {
    return this.splice(this.getChildrenSize(), 0, nodesToAppend);
  }
  setDirection(direction) {
    const self = this.getWritable();
    self.__dir = direction;
    return self;
  }
  setFormat(type) {
    const self = this.getWritable();
    self.__format = type !== "" ? ELEMENT_TYPE_TO_FORMAT[type] : 0;
    return this;
  }
  setStyle(style) {
    const self = this.getWritable();
    self.__style = style || "";
    return this;
  }
  setTextFormat(type) {
    const self = this.getWritable();
    self.__textFormat = type;
    return self;
  }
  setTextStyle(style) {
    const self = this.getWritable();
    self.__textStyle = style;
    return self;
  }
  setIndent(indentLevel) {
    const self = this.getWritable();
    self.__indent = indentLevel;
    return this;
  }
  splice(start, deleteCount, nodesToInsert) {
    const nodesToInsertLength = nodesToInsert.length;
    const oldSize = this.getChildrenSize();
    const writableSelf = this.getWritable();
    if (!(start + deleteCount <= oldSize)) {
      formatDevErrorMessage(`ElementNode.splice: start + deleteCount > oldSize (${String(start)} + ${String(deleteCount)} > ${String(oldSize)})`);
    }
    const writableSelfKey = writableSelf.__key;
    const nodesToInsertKeys = [];
    const nodesToRemoveKeys = [];
    const nodeAfterRange = this.getChildAtIndex(start + deleteCount);
    let nodeBeforeRange = null;
    let newSize = oldSize - deleteCount + nodesToInsertLength;
    if (start !== 0) {
      if (start === oldSize) {
        nodeBeforeRange = this.getLastChild();
      } else {
        const node = this.getChildAtIndex(start);
        if (node !== null) {
          nodeBeforeRange = node.getPreviousSibling();
        }
      }
    }
    if (deleteCount > 0) {
      let nodeToDelete = nodeBeforeRange === null ? this.getFirstChild() : nodeBeforeRange.getNextSibling();
      for (let i2 = 0; i2 < deleteCount; i2++) {
        if (nodeToDelete === null) {
          {
            formatDevErrorMessage(`splice: sibling not found`);
          }
        }
        const nextSibling = nodeToDelete.getNextSibling();
        const nodeKeyToDelete = nodeToDelete.__key;
        const writableNodeToDelete = nodeToDelete.getWritable();
        removeFromParent(writableNodeToDelete);
        nodesToRemoveKeys.push(nodeKeyToDelete);
        nodeToDelete = nextSibling;
      }
    }
    let prevNode = nodeBeforeRange;
    for (let i2 = 0; i2 < nodesToInsertLength; i2++) {
      const nodeToInsert = nodesToInsert[i2];
      if (prevNode !== null && nodeToInsert.is(prevNode)) {
        nodeBeforeRange = prevNode = prevNode.getPreviousSibling();
      }
      const writableNodeToInsert = nodeToInsert.getWritable();
      if (writableNodeToInsert.__parent === writableSelfKey) {
        newSize--;
      }
      removeFromParent(writableNodeToInsert);
      const nodeKeyToInsert = nodeToInsert.__key;
      if (prevNode === null) {
        writableSelf.__first = nodeKeyToInsert;
        writableNodeToInsert.__prev = null;
      } else {
        const writablePrevNode = prevNode.getWritable();
        writablePrevNode.__next = nodeKeyToInsert;
        writableNodeToInsert.__prev = writablePrevNode.__key;
      }
      if (nodeToInsert.__key === writableSelfKey) {
        {
          formatDevErrorMessage(`append: attempting to append self`);
        }
      }
      writableNodeToInsert.__parent = writableSelfKey;
      nodesToInsertKeys.push(nodeKeyToInsert);
      prevNode = nodeToInsert;
    }
    if (start + deleteCount === oldSize) {
      if (prevNode !== null) {
        const writablePrevNode = prevNode.getWritable();
        writablePrevNode.__next = null;
        writableSelf.__last = prevNode.__key;
      }
    } else if (nodeAfterRange !== null) {
      const writableNodeAfterRange = nodeAfterRange.getWritable();
      if (prevNode !== null) {
        const writablePrevNode = prevNode.getWritable();
        writableNodeAfterRange.__prev = prevNode.__key;
        writablePrevNode.__next = nodeAfterRange.__key;
      } else {
        writableNodeAfterRange.__prev = null;
      }
    }
    writableSelf.__size = newSize;
    if (nodesToRemoveKeys.length) {
      const selection = $getSelection();
      if ($isRangeSelection(selection)) {
        const nodesToRemoveKeySet = new Set(nodesToRemoveKeys);
        const nodesToInsertKeySet = new Set(nodesToInsertKeys);
        const {
          anchor,
          focus
        } = selection;
        if (isPointRemoved(anchor, nodesToRemoveKeySet, nodesToInsertKeySet)) {
          moveSelectionPointToSibling(anchor, anchor.getNode(), this, nodeBeforeRange, nodeAfterRange);
        }
        if (isPointRemoved(focus, nodesToRemoveKeySet, nodesToInsertKeySet)) {
          moveSelectionPointToSibling(focus, focus.getNode(), this, nodeBeforeRange, nodeAfterRange);
        }
        if (newSize === 0 && !this.canBeEmpty() && !$isRootOrShadowRoot(this)) {
          this.remove();
        }
      }
    }
    return writableSelf;
  }
  /**
   * @internal
   *
   * An experimental API that an ElementNode can override to control where its
   * children are inserted into the DOM, this is useful to add a wrapping node
   * or accessory nodes before or after the children. The root of the node returned
   * by createDOM must still be exactly one HTMLElement.
   */
  getDOMSlot(element) {
    return new ElementDOMSlot(element);
  }
  exportDOM(editor) {
    const {
      element
    } = super.exportDOM(editor);
    if (isHTMLElement(element)) {
      const indent = this.getIndent();
      if (indent > 0) {
        element.style.paddingInlineStart = `${indent * 40}px`;
      }
      const direction = this.getDirection();
      if (direction) {
        element.dir = direction;
      }
    }
    return {
      element
    };
  }
  // JSON serialization
  exportJSON() {
    const json = {
      children: [],
      direction: this.getDirection(),
      format: this.getFormatType(),
      indent: this.getIndent(),
      // As an exception here we invoke super at the end for historical reasons.
      // Namely, to preserve the order of the properties and not to break the tests
      // that use the serialized string representation.
      ...super.exportJSON()
    };
    const textFormat = this.getTextFormat();
    const textStyle = this.getTextStyle();
    if (textFormat !== 0) {
      json.textFormat = textFormat;
    }
    if (textStyle !== "") {
      json.textStyle = textStyle;
    }
    return json;
  }
  updateFromJSON(serializedNode) {
    return super.updateFromJSON(serializedNode).setFormat(serializedNode.format).setIndent(serializedNode.indent).setDirection(serializedNode.direction).setTextFormat(serializedNode.textFormat || 0).setTextStyle(serializedNode.textStyle || "");
  }
  // These are intended to be extends for specific element heuristics.
  insertNewAfter(selection, restoreSelection) {
    return null;
  }
  canIndent() {
    return true;
  }
  /*
   * This method controls the behavior of the node during backwards
   * deletion (i.e., backspace) when selection is at the beginning of
   * the node (offset 0). You may use this to have the node replace
   * itself, change its state, or do nothing. When you do make such
   * a change, you should return true.
   *
   * When true is returned, the collapse phase will stop.
   * When false is returned, and isInline() is true, and getPreviousSibling() is null,
   * then this function will be called on its parent.
   */
  collapseAtStart(selection) {
    return false;
  }
  excludeFromCopy(destination) {
    return false;
  }
  /** @deprecated @internal */
  canReplaceWith(replacement) {
    return true;
  }
  /** @deprecated @internal */
  canInsertAfter(node) {
    return true;
  }
  canBeEmpty() {
    return true;
  }
  canInsertTextBefore() {
    return true;
  }
  canInsertTextAfter() {
    return true;
  }
  isInline() {
    return false;
  }
  // A shadow root is a Node that behaves like RootNode. The shadow root (and RootNode) mark the
  // end of the hierarchy, most implementations should treat it as there's nothing (upwards)
  // beyond this point. For example, node.getTopLevelElement(), when performed inside a TableCellNode
  // will return the immediate first child underneath TableCellNode instead of RootNode.
  isShadowRoot() {
    return false;
  }
  /** @deprecated @internal */
  canMergeWith(node) {
    return false;
  }
  extractWithChild(child, selection, destination) {
    return false;
  }
  /**
   * Determines whether this node, when empty, can merge with a first block
   * of nodes being inserted.
   *
   * This method is specifically called in {@link RangeSelection.insertNodes}
   * to determine merging behavior during nodes insertion.
   *
   * @example
   * // In a ListItemNode or QuoteNode implementation:
   * canMergeWhenEmpty(): true {
   *  return true;
   * }
   */
  canMergeWhenEmpty() {
    return false;
  }
  /** @internal */
  reconcileObservedMutation(dom, editor) {
    const slot = this.getDOMSlot(dom);
    let currentDOM = slot.getFirstChild();
    for (let currentNode = this.getFirstChild(); currentNode; currentNode = currentNode.getNextSibling()) {
      const correctDOM = editor.getElementByKey(currentNode.getKey());
      if (correctDOM === null) {
        continue;
      }
      if (currentDOM == null) {
        slot.insertChild(correctDOM);
        currentDOM = correctDOM;
      } else if (currentDOM !== correctDOM) {
        slot.replaceChild(correctDOM, currentDOM);
      }
      currentDOM = currentDOM.nextSibling;
    }
  }
};
function $isElementNode(node) {
  return node instanceof ElementNode;
}
function isPointRemoved(point, nodesToRemoveKeySet, nodesToInsertKeySet) {
  let node = point.getNode();
  while (node) {
    const nodeKey = node.__key;
    if (nodesToRemoveKeySet.has(nodeKey) && !nodesToInsertKeySet.has(nodeKey)) {
      return true;
    }
    node = node.getParent();
  }
  return false;
}
var DecoratorNode = class extends LexicalNode {
  /**
   * The returned value is added to the LexicalEditor._decorators
   */
  decorate(editor, config) {
    {
      formatDevErrorMessage(`decorate: base method not extended`);
    }
  }
  isIsolated() {
    return false;
  }
  isInline() {
    return true;
  }
  isKeyboardSelectable() {
    return true;
  }
};
function $isDecoratorNode(node) {
  return node instanceof DecoratorNode;
}
var RootNode = class _RootNode extends ElementNode {
  /** @internal */
  static getType() {
    return "root";
  }
  static clone() {
    return new _RootNode();
  }
  constructor() {
    super("root");
    this.__cachedText = null;
  }
  getTopLevelElementOrThrow() {
    {
      formatDevErrorMessage(`getTopLevelElementOrThrow: root nodes are not top level elements`);
    }
  }
  getTextContent() {
    const cachedText = this.__cachedText;
    if (isCurrentlyReadOnlyMode() || getActiveEditor()._dirtyType === NO_DIRTY_NODES) {
      if (cachedText !== null) {
        return cachedText;
      }
    }
    return super.getTextContent();
  }
  remove() {
    {
      formatDevErrorMessage(`remove: cannot be called on root nodes`);
    }
  }
  replace(node) {
    {
      formatDevErrorMessage(`replace: cannot be called on root nodes`);
    }
  }
  insertBefore(nodeToInsert) {
    {
      formatDevErrorMessage(`insertBefore: cannot be called on root nodes`);
    }
  }
  insertAfter(nodeToInsert) {
    {
      formatDevErrorMessage(`insertAfter: cannot be called on root nodes`);
    }
  }
  // View
  updateDOM(prevNode, dom) {
    return false;
  }
  // Mutate
  splice(start, deleteCount, nodesToInsert) {
    for (const node of nodesToInsert) {
      if (!($isElementNode(node) || $isDecoratorNode(node))) {
        formatDevErrorMessage(`rootNode.splice: Only element or decorator nodes can be inserted to the root node`);
      }
    }
    return super.splice(start, deleteCount, nodesToInsert);
  }
  static importJSON(serializedNode) {
    return $getRoot().updateFromJSON(serializedNode);
  }
  collapseAtStart() {
    return true;
  }
};
function $createRootNode() {
  return new RootNode();
}
function $isRootNode(node) {
  return node instanceof RootNode;
}
function editorStateHasDirtySelection(editorState, editor) {
  const currentSelection = editor.getEditorState()._selection;
  const pendingSelection = editorState._selection;
  if (pendingSelection !== null) {
    if (pendingSelection.dirty || !pendingSelection.is(currentSelection)) {
      return true;
    }
  } else if (currentSelection !== null) {
    return true;
  }
  return false;
}
function cloneEditorState(current) {
  return new EditorState(new Map(current._nodeMap));
}
function createEmptyEditorState() {
  return new EditorState(/* @__PURE__ */ new Map([["root", $createRootNode()]]));
}
function exportNodeToJSON(node) {
  const serializedNode = node.exportJSON();
  const nodeClass = node.constructor;
  if (serializedNode.type !== nodeClass.getType()) {
    {
      formatDevErrorMessage(`LexicalNode: Node ${nodeClass.name} does not match the serialized type. Check if .exportJSON() is implemented and it is returning the correct type.`);
    }
  }
  if ($isElementNode(node)) {
    const serializedChildren = serializedNode.children;
    if (!Array.isArray(serializedChildren)) {
      {
        formatDevErrorMessage(`LexicalNode: Node ${nodeClass.name} is an element but .exportJSON() does not have a children array.`);
      }
    }
    const children = node.getChildren();
    for (let i2 = 0; i2 < children.length; i2++) {
      const child = children[i2];
      const serializedChildNode = exportNodeToJSON(child);
      serializedChildren.push(serializedChildNode);
    }
  }
  return serializedNode;
}
var EditorState = class _EditorState {
  constructor(nodeMap, selection) {
    this._nodeMap = nodeMap;
    this._selection = selection || null;
    this._flushSync = false;
    this._readOnly = false;
  }
  isEmpty() {
    return this._nodeMap.size === 1 && this._selection === null;
  }
  read(callbackFn, options) {
    return readEditorState(options && options.editor || null, this, callbackFn);
  }
  clone(selection) {
    const editorState = new _EditorState(this._nodeMap, selection === void 0 ? this._selection : selection);
    editorState._readOnly = true;
    return editorState;
  }
  toJSON() {
    return readEditorState(null, this, () => ({
      root: exportNodeToJSON($getRoot())
    }));
  }
};
var HISTORIC_TAG = "historic";
var HISTORY_PUSH_TAG = "history-push";
var HISTORY_MERGE_TAG = "history-merge";
var PASTE_TAG = "paste";
var COLLABORATION_TAG = "collaboration";
var SKIP_COLLAB_TAG = "skip-collab";
var SKIP_SCROLL_INTO_VIEW_TAG = "skip-scroll-into-view";
var SKIP_DOM_SELECTION_TAG = "skip-dom-selection";
var FOCUS_TAG = "focus";
var ArtificialNode__DO_NOT_USE = class extends ElementNode {
  static getType() {
    return "artificial";
  }
  createDOM(config) {
    const dom = document.createElement("div");
    return dom;
  }
};
var ParagraphNode = class _ParagraphNode extends ElementNode {
  static getType() {
    return "paragraph";
  }
  static clone(node) {
    return new _ParagraphNode(node.__key);
  }
  // View
  createDOM(config) {
    const dom = document.createElement("p");
    const classNames = getCachedClassNameArray(config.theme, "paragraph");
    if (classNames !== void 0) {
      const domClassList = dom.classList;
      domClassList.add(...classNames);
    }
    return dom;
  }
  updateDOM(prevNode, dom, config) {
    return false;
  }
  static importDOM() {
    return {
      p: (node) => ({
        conversion: $convertParagraphElement,
        priority: 0
      })
    };
  }
  exportDOM(editor) {
    const {
      element
    } = super.exportDOM(editor);
    if (isHTMLElement(element)) {
      if (this.isEmpty()) {
        element.append(document.createElement("br"));
      }
      const formatType = this.getFormatType();
      if (formatType) {
        element.style.textAlign = formatType;
      }
    }
    return {
      element
    };
  }
  static importJSON(serializedNode) {
    return $createParagraphNode().updateFromJSON(serializedNode);
  }
  exportJSON() {
    return {
      ...super.exportJSON(),
      // These are included explicitly for backwards compatibility
      textFormat: this.getTextFormat(),
      textStyle: this.getTextStyle()
    };
  }
  // Mutation
  insertNewAfter(rangeSelection, restoreSelection) {
    const newElement = $createParagraphNode();
    newElement.setTextFormat(rangeSelection.format);
    newElement.setTextStyle(rangeSelection.style);
    const direction = this.getDirection();
    newElement.setDirection(direction);
    newElement.setFormat(this.getFormatType());
    newElement.setStyle(this.getStyle());
    this.insertAfter(newElement, restoreSelection);
    return newElement;
  }
  collapseAtStart() {
    const children = this.getChildren();
    if (children.length === 0 || $isTextNode(children[0]) && children[0].getTextContent().trim() === "") {
      const nextSibling = this.getNextSibling();
      if (nextSibling !== null) {
        this.selectNext();
        this.remove();
        return true;
      }
      const prevSibling = this.getPreviousSibling();
      if (prevSibling !== null) {
        this.selectPrevious();
        this.remove();
        return true;
      }
    }
    return false;
  }
};
function $convertParagraphElement(element) {
  const node = $createParagraphNode();
  if (element.style) {
    node.setFormat(element.style.textAlign);
    setNodeIndentFromDOM(element, node);
  }
  return {
    node
  };
}
function $createParagraphNode() {
  return $applyNodeReplacement(new ParagraphNode());
}
function $isParagraphNode(node) {
  return node instanceof ParagraphNode;
}
var DEFAULT_SKIP_INITIALIZATION = false;
var COMMAND_PRIORITY_EDITOR = 0;
var COMMAND_PRIORITY_LOW = 1;
var COMMAND_PRIORITY_NORMAL = 2;
var COMMAND_PRIORITY_HIGH = 3;
var COMMAND_PRIORITY_CRITICAL = 4;
function resetEditor(editor, prevRootElement, nextRootElement, pendingEditorState) {
  const keyNodeMap = editor._keyToDOMMap;
  keyNodeMap.clear();
  editor._editorState = createEmptyEditorState();
  editor._pendingEditorState = pendingEditorState;
  editor._compositionKey = null;
  editor._dirtyType = NO_DIRTY_NODES;
  editor._cloneNotNeeded.clear();
  editor._dirtyLeaves = /* @__PURE__ */ new Set();
  editor._dirtyElements.clear();
  editor._normalizedNodes = /* @__PURE__ */ new Set();
  editor._updateTags = /* @__PURE__ */ new Set();
  editor._updates = [];
  editor._blockCursorElement = null;
  const observer = editor._observer;
  if (observer !== null) {
    observer.disconnect();
    editor._observer = null;
  }
  if (prevRootElement !== null) {
    prevRootElement.textContent = "";
  }
  if (nextRootElement !== null) {
    nextRootElement.textContent = "";
    keyNodeMap.set("root", nextRootElement);
  }
}
function initializeConversionCache(nodes, additionalConversions) {
  const conversionCache = /* @__PURE__ */ new Map();
  const handledConversions = /* @__PURE__ */ new Set();
  const addConversionsToCache = (map) => {
    Object.keys(map).forEach((key) => {
      let currentCache = conversionCache.get(key);
      if (currentCache === void 0) {
        currentCache = [];
        conversionCache.set(key, currentCache);
      }
      currentCache.push(map[key]);
    });
  };
  nodes.forEach((node) => {
    const importDOM = node.klass.importDOM;
    if (importDOM == null || handledConversions.has(importDOM)) {
      return;
    }
    handledConversions.add(importDOM);
    const map = importDOM.call(node.klass);
    if (map !== null) {
      addConversionsToCache(map);
    }
  });
  if (additionalConversions) {
    addConversionsToCache(additionalConversions);
  }
  return conversionCache;
}
function createEditor(editorConfig) {
  const config = editorConfig || {};
  const activeEditor2 = internalGetActiveEditor();
  const theme = config.theme || {};
  const parentEditor = editorConfig === void 0 ? activeEditor2 : config.parentEditor || null;
  const disableEvents = config.disableEvents || false;
  const editorState = createEmptyEditorState();
  const namespace = config.namespace || (parentEditor !== null ? parentEditor._config.namespace : createUID());
  const initialEditorState = config.editorState;
  const nodes = [RootNode, TextNode, LineBreakNode, TabNode, ParagraphNode, ArtificialNode__DO_NOT_USE, ...config.nodes || []];
  const {
    onError,
    html
  } = config;
  const isEditable = config.editable !== void 0 ? config.editable : true;
  let registeredNodes;
  if (editorConfig === void 0 && activeEditor2 !== null) {
    registeredNodes = activeEditor2._nodes;
  } else {
    registeredNodes = /* @__PURE__ */ new Map();
    for (let i2 = 0; i2 < nodes.length; i2++) {
      let klass = nodes[i2];
      let replace = null;
      let replaceWithKlass = null;
      if (typeof klass !== "function") {
        const options = klass;
        klass = options.replace;
        replace = options.with;
        replaceWithKlass = options.withKlass || null;
      }
      const {
        ownNodeConfig
      } = getStaticNodeConfig(klass);
      {
        const name = klass.name;
        const nodeType = hasOwnStaticMethod(klass, "getType") && klass.getType();
        if (replaceWithKlass) {
          if (!(replaceWithKlass.prototype instanceof klass)) {
            formatDevErrorMessage(`${replaceWithKlass.name} doesn't extend the ${name}`);
          }
        } else if (replace) {
          console.warn(`Override for ${name} specifies 'replace' without 'withKlass'. 'withKlass' will be required in a future version.`);
        }
        if (name !== "RootNode" && nodeType !== "root" && nodeType !== "artificial" && // This is mostly for the unit test suite which
        // uses LexicalNode in an otherwise incorrect way
        // by mocking its static getType
        klass !== LexicalNode) {
          const proto = klass.prototype;
          ["getType", "clone"].forEach((method) => {
            if (!hasOwnStaticMethod(klass, method)) {
              console.warn(`${name} must implement static "${method}" method`);
            }
          });
          if (!hasOwnStaticMethod(klass, "importDOM") && hasOwnExportDOM(klass)) {
            console.warn(`${name} should implement "importDOM" if using a custom "exportDOM" method to ensure HTML serialization (important for copy & paste) works as expected`);
          }
          if ($isDecoratorNode(proto)) {
            if (proto.decorate === DecoratorNode.prototype.decorate) {
              console.warn(`${proto.constructor.name} must implement "decorate" method`);
            }
          }
          if (!hasOwnStaticMethod(klass, "importJSON")) {
            console.warn(`${name} should implement "importJSON" method to ensure JSON and default HTML serialization works as expected`);
          }
        }
      }
      const type = klass.getType();
      const transform = klass.transform();
      const transforms = /* @__PURE__ */ new Set();
      if (ownNodeConfig && ownNodeConfig.$transform) {
        transforms.add(ownNodeConfig.$transform);
      }
      if (transform !== null) {
        transforms.add(transform);
      }
      registeredNodes.set(type, {
        exportDOM: html && html.export ? html.export.get(klass) : void 0,
        klass,
        replace,
        replaceWithKlass,
        sharedNodeState: createSharedNodeState(nodes[i2]),
        transforms
      });
    }
  }
  const editor = new LexicalEditor(editorState, parentEditor, registeredNodes, {
    disableEvents,
    namespace,
    theme
  }, onError ? onError : console.error, initializeConversionCache(registeredNodes, html ? html.import : void 0), isEditable, editorConfig);
  if (initialEditorState !== void 0) {
    editor._pendingEditorState = initialEditorState;
    editor._dirtyType = FULL_RECONCILE;
  }
  return editor;
}
var LexicalEditor = class {
  /** The version with build identifiers for this editor (since 0.17.1) */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  /** @internal */
  constructor(editorState, parentEditor, nodes, config, onError, htmlConversions, editable, createEditorArgs) {
    this._createEditorArgs = createEditorArgs;
    this._parentEditor = parentEditor;
    this._rootElement = null;
    this._editorState = editorState;
    this._pendingEditorState = null;
    this._compositionKey = null;
    this._deferred = [];
    this._keyToDOMMap = /* @__PURE__ */ new Map();
    this._updates = [];
    this._updating = false;
    this._listeners = {
      decorator: /* @__PURE__ */ new Set(),
      editable: /* @__PURE__ */ new Set(),
      mutation: /* @__PURE__ */ new Map(),
      root: /* @__PURE__ */ new Set(),
      textcontent: /* @__PURE__ */ new Set(),
      update: /* @__PURE__ */ new Set()
    };
    this._commands = /* @__PURE__ */ new Map();
    this._config = config;
    this._nodes = nodes;
    this._decorators = {};
    this._pendingDecorators = null;
    this._dirtyType = NO_DIRTY_NODES;
    this._cloneNotNeeded = /* @__PURE__ */ new Set();
    this._dirtyLeaves = /* @__PURE__ */ new Set();
    this._dirtyElements = /* @__PURE__ */ new Map();
    this._normalizedNodes = /* @__PURE__ */ new Set();
    this._updateTags = /* @__PURE__ */ new Set();
    this._observer = null;
    this._key = createUID();
    this._onError = onError;
    this._htmlConversions = htmlConversions;
    this._editable = editable;
    this._headless = parentEditor !== null && parentEditor._headless;
    this._window = null;
    this._blockCursorElement = null;
  }
  /**
   *
   * @returns true if the editor is currently in "composition" mode due to receiving input
   * through an IME, or 3P extension, for example. Returns false otherwise.
   */
  isComposing() {
    return this._compositionKey != null;
  }
  /**
   * Registers a listener for Editor update event. Will trigger the provided callback
   * each time the editor goes through an update (via {@link LexicalEditor.update}) until the
   * teardown function is called.
   *
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerUpdateListener(listener) {
    const listenerSetOrMap = this._listeners.update;
    listenerSetOrMap.add(listener);
    return () => {
      listenerSetOrMap.delete(listener);
    };
  }
  /**
   * Registers a listener for for when the editor changes between editable and non-editable states.
   * Will trigger the provided callback each time the editor transitions between these states until the
   * teardown function is called.
   *
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerEditableListener(listener) {
    const listenerSetOrMap = this._listeners.editable;
    listenerSetOrMap.add(listener);
    return () => {
      listenerSetOrMap.delete(listener);
    };
  }
  /**
   * Registers a listener for when the editor's decorator object changes. The decorator object contains
   * all DecoratorNode keys -> their decorated value. This is primarily used with external UI frameworks.
   *
   * Will trigger the provided callback each time the editor transitions between these states until the
   * teardown function is called.
   *
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerDecoratorListener(listener) {
    const listenerSetOrMap = this._listeners.decorator;
    listenerSetOrMap.add(listener);
    return () => {
      listenerSetOrMap.delete(listener);
    };
  }
  /**
   * Registers a listener for when Lexical commits an update to the DOM and the text content of
   * the editor changes from the previous state of the editor. If the text content is the
   * same between updates, no notifications to the listeners will happen.
   *
   * Will trigger the provided callback each time the editor transitions between these states until the
   * teardown function is called.
   *
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerTextContentListener(listener) {
    const listenerSetOrMap = this._listeners.textcontent;
    listenerSetOrMap.add(listener);
    return () => {
      listenerSetOrMap.delete(listener);
    };
  }
  /**
   * Registers a listener for when the editor's root DOM element (the content editable
   * Lexical attaches to) changes. This is primarily used to attach event listeners to the root
   *  element. The root listener function is executed directly upon registration and then on
   * any subsequent update.
   *
   * Will trigger the provided callback each time the editor transitions between these states until the
   * teardown function is called.
   *
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerRootListener(listener) {
    const listenerSetOrMap = this._listeners.root;
    listener(this._rootElement, null);
    listenerSetOrMap.add(listener);
    return () => {
      listener(null, this._rootElement);
      listenerSetOrMap.delete(listener);
    };
  }
  /**
   * Registers a listener that will trigger anytime the provided command
   * is dispatched with {@link LexicalEditor.dispatch}, subject to priority.
   * Listeners that run at a higher priority can "intercept" commands and
   * prevent them from propagating to other handlers by returning true.
   *
   * Listeners are always invoked in an {@link LexicalEditor.update} and can
   * call dollar functions.
   *
   * Listeners registered at the same priority level will run
   * deterministically in the order of registration.
   *
   * @param command - the command that will trigger the callback.
   * @param listener - the function that will execute when the command is dispatched.
   * @param priority - the relative priority of the listener. 0 | 1 | 2 | 3 | 4
   *   (or {@link COMMAND_PRIORITY_EDITOR} |
   *     {@link COMMAND_PRIORITY_LOW} |
   *     {@link COMMAND_PRIORITY_NORMAL} |
   *     {@link COMMAND_PRIORITY_HIGH} |
   *     {@link COMMAND_PRIORITY_CRITICAL})
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerCommand(command, listener, priority) {
    if (priority === void 0) {
      {
        formatDevErrorMessage(`Listener for type "command" requires a "priority".`);
      }
    }
    const commandsMap = this._commands;
    if (!commandsMap.has(command)) {
      commandsMap.set(command, [/* @__PURE__ */ new Set(), /* @__PURE__ */ new Set(), /* @__PURE__ */ new Set(), /* @__PURE__ */ new Set(), /* @__PURE__ */ new Set()]);
    }
    const listenersInPriorityOrder = commandsMap.get(command);
    if (listenersInPriorityOrder === void 0) {
      {
        formatDevErrorMessage(`registerCommand: Command ${String(command)} not found in command map`);
      }
    }
    const listeners = listenersInPriorityOrder[priority];
    listeners.add(listener);
    return () => {
      listeners.delete(listener);
      if (listenersInPriorityOrder.every((listenersSet) => listenersSet.size === 0)) {
        commandsMap.delete(command);
      }
    };
  }
  /**
   * Registers a listener that will run when a Lexical node of the provided class is
   * mutated. The listener will receive a list of nodes along with the type of mutation
   * that was performed on each: created, destroyed, or updated.
   *
   * One common use case for this is to attach DOM event listeners to the underlying DOM nodes as Lexical nodes are created.
   * {@link LexicalEditor.getElementByKey} can be used for this.
   *
   * If any existing nodes are in the DOM, and skipInitialization is not true, the listener
   * will be called immediately with an updateTag of 'registerMutationListener' where all
   * nodes have the 'created' NodeMutation. This can be controlled with the skipInitialization option
   * (whose default was previously true for backwards compatibility with &lt;=0.16.1 but has been changed to false as of 0.21.0).
   *
   * @param klass - The class of the node that you want to listen to mutations on.
   * @param listener - The logic you want to run when the node is mutated.
   * @param options - see {@link MutationListenerOptions}
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerMutationListener(klass, listener, options) {
    const klassToMutate = this.resolveRegisteredNodeAfterReplacements(this.getRegisteredNode(klass)).klass;
    const mutations = this._listeners.mutation;
    let klassSet = mutations.get(listener);
    if (klassSet === void 0) {
      klassSet = /* @__PURE__ */ new Set();
      mutations.set(listener, klassSet);
    }
    klassSet.add(klassToMutate);
    const skipInitialization = options && options.skipInitialization;
    if (!(skipInitialization === void 0 ? DEFAULT_SKIP_INITIALIZATION : skipInitialization)) {
      this.initializeMutationListener(listener, klassToMutate);
    }
    return () => {
      klassSet.delete(klassToMutate);
      if (klassSet.size === 0) {
        mutations.delete(listener);
      }
    };
  }
  /** @internal */
  getRegisteredNode(klass) {
    const registeredNode = this._nodes.get(klass.getType());
    if (registeredNode === void 0) {
      {
        formatDevErrorMessage(`Node ${klass.name} has not been registered. Ensure node has been passed to createEditor.`);
      }
    }
    return registeredNode;
  }
  /** @internal */
  resolveRegisteredNodeAfterReplacements(registeredNode) {
    while (registeredNode.replaceWithKlass) {
      registeredNode = this.getRegisteredNode(registeredNode.replaceWithKlass);
    }
    return registeredNode;
  }
  /** @internal */
  initializeMutationListener(listener, klass) {
    const prevEditorState = this._editorState;
    const nodeMap = getCachedTypeToNodeMap(prevEditorState).get(klass.getType());
    if (!nodeMap) {
      return;
    }
    const nodeMutationMap = /* @__PURE__ */ new Map();
    for (const k of nodeMap.keys()) {
      nodeMutationMap.set(k, "created");
    }
    if (nodeMutationMap.size > 0) {
      listener(nodeMutationMap, {
        dirtyLeaves: /* @__PURE__ */ new Set(),
        prevEditorState,
        updateTags: /* @__PURE__ */ new Set(["registerMutationListener"])
      });
    }
  }
  /** @internal */
  registerNodeTransformToKlass(klass, listener) {
    const registeredNode = this.getRegisteredNode(klass);
    registeredNode.transforms.add(listener);
    return registeredNode;
  }
  /**
   * Registers a listener that will run when a Lexical node of the provided class is
   * marked dirty during an update. The listener will continue to run as long as the node
   * is marked dirty. There are no guarantees around the order of transform execution!
   *
   * Watch out for infinite loops. See [Node Transforms](https://lexical.dev/docs/concepts/transforms)
   * @param klass - The class of the node that you want to run transforms on.
   * @param listener - The logic you want to run when the node is updated.
   * @returns a teardown function that can be used to cleanup the listener.
   */
  registerNodeTransform(klass, listener) {
    const registeredNode = this.registerNodeTransformToKlass(klass, listener);
    const registeredNodes = [registeredNode];
    const replaceWithKlass = registeredNode.replaceWithKlass;
    if (replaceWithKlass != null) {
      const registeredReplaceWithNode = this.registerNodeTransformToKlass(replaceWithKlass, listener);
      registeredNodes.push(registeredReplaceWithNode);
    }
    markNodesWithTypesAsDirty(this, registeredNodes.map((node) => node.klass.getType()));
    return () => {
      registeredNodes.forEach((node) => node.transforms.delete(listener));
    };
  }
  /**
   * Used to assert that a certain node is registered, usually by plugins to ensure nodes that they
   * depend on have been registered.
   * @returns True if the editor has registered the provided node type, false otherwise.
   */
  hasNode(node) {
    return this._nodes.has(node.getType());
  }
  /**
   * Used to assert that certain nodes are registered, usually by plugins to ensure nodes that they
   * depend on have been registered.
   * @returns True if the editor has registered all of the provided node types, false otherwise.
   */
  hasNodes(nodes) {
    return nodes.every(this.hasNode.bind(this));
  }
  /**
   * Dispatches a command of the specified type with the specified payload.
   * This triggers all command listeners (set by {@link LexicalEditor.registerCommand})
   * for this type, passing them the provided payload. The command listeners
   * will be triggered in an implicit {@link LexicalEditor.update}, unless
   * this was invoked from inside an update in which case that update context
   * will be re-used (as if this was a dollar function itself).
   * @param type - the type of command listeners to trigger.
   * @param payload - the data to pass as an argument to the command listeners.
   */
  dispatchCommand(type, payload) {
    return dispatchCommand(this, type, payload);
  }
  /**
   * Gets a map of all decorators in the editor.
   * @returns A mapping of call decorator keys to their decorated content
   */
  getDecorators() {
    return this._decorators;
  }
  /**
   *
   * @returns the current root element of the editor. If you want to register
   * an event listener, do it via {@link LexicalEditor.registerRootListener}, since
   * this reference may not be stable.
   */
  getRootElement() {
    return this._rootElement;
  }
  /**
   * Gets the key of the editor
   * @returns The editor key
   */
  getKey() {
    return this._key;
  }
  /**
   * Imperatively set the root contenteditable element that Lexical listens
   * for events on.
   */
  setRootElement(nextRootElement) {
    const prevRootElement = this._rootElement;
    if (nextRootElement !== prevRootElement) {
      const classNames = getCachedClassNameArray(this._config.theme, "root");
      const pendingEditorState = this._pendingEditorState || this._editorState;
      this._rootElement = nextRootElement;
      resetEditor(this, prevRootElement, nextRootElement, pendingEditorState);
      if (prevRootElement !== null) {
        if (!this._config.disableEvents) {
          removeRootElementEvents(prevRootElement);
        }
        if (classNames != null) {
          prevRootElement.classList.remove(...classNames);
        }
      }
      if (nextRootElement !== null) {
        const windowObj = getDefaultView(nextRootElement);
        const style = nextRootElement.style;
        style.userSelect = "text";
        style.whiteSpace = "pre-wrap";
        style.wordBreak = "break-word";
        nextRootElement.setAttribute("data-lexical-editor", "true");
        this._window = windowObj;
        this._dirtyType = FULL_RECONCILE;
        initMutationObserver(this);
        this._updateTags.add(HISTORY_MERGE_TAG);
        $commitPendingUpdates(this);
        if (!this._config.disableEvents) {
          addRootElementEvents(nextRootElement, this);
        }
        if (classNames != null) {
          nextRootElement.classList.add(...classNames);
        }
        {
          const nextRootElementParent = nextRootElement.parentElement;
          if (nextRootElementParent != null && ["flex", "inline-flex"].includes(getComputedStyle(nextRootElementParent).display)) {
            console.warn(`When using "display: flex" or "display: inline-flex" on an element containing content editable, Chrome may have unwanted focusing behavior when clicking outside of it. Consider wrapping the content editable within a non-flex element.`);
          }
        }
      } else {
        this._window = null;
        this._updateTags.add(HISTORY_MERGE_TAG);
        $commitPendingUpdates(this);
      }
      triggerListeners("root", this, false, nextRootElement, prevRootElement);
    }
  }
  /**
   * Gets the underlying HTMLElement associated with the LexicalNode for the given key.
   * @returns the HTMLElement rendered by the LexicalNode associated with the key.
   * @param key - the key of the LexicalNode.
   */
  getElementByKey(key) {
    return this._keyToDOMMap.get(key) || null;
  }
  /**
   * Gets the active editor state.
   * @returns The editor state
   */
  getEditorState() {
    return this._editorState;
  }
  /**
   * Imperatively set the EditorState. Triggers reconciliation like an update.
   * @param editorState - the state to set the editor
   * @param options - options for the update.
   */
  setEditorState(editorState, options) {
    if (editorState.isEmpty()) {
      {
        formatDevErrorMessage(`setEditorState: the editor state is empty. Ensure the editor state's root node never becomes empty.`);
      }
    }
    let writableEditorState = editorState;
    if (writableEditorState._readOnly) {
      writableEditorState = cloneEditorState(editorState);
      writableEditorState._selection = editorState._selection ? editorState._selection.clone() : null;
    }
    flushRootMutations(this);
    const pendingEditorState = this._pendingEditorState;
    const tags = this._updateTags;
    const tag = options !== void 0 ? options.tag : null;
    if (pendingEditorState !== null && !pendingEditorState.isEmpty()) {
      if (tag != null) {
        tags.add(tag);
      }
      $commitPendingUpdates(this);
    }
    this._pendingEditorState = writableEditorState;
    this._dirtyType = FULL_RECONCILE;
    this._dirtyElements.set("root", false);
    this._compositionKey = null;
    if (tag != null) {
      tags.add(tag);
    }
    if (!this._updating) {
      $commitPendingUpdates(this);
    }
  }
  /**
   * Parses a SerializedEditorState (usually produced by {@link EditorState.toJSON}) and returns
   * and EditorState object that can be, for example, passed to {@link LexicalEditor.setEditorState}. Typically,
   * deserialization from JSON stored in a database uses this method.
   * @param maybeStringifiedEditorState
   * @param updateFn
   * @returns
   */
  parseEditorState(maybeStringifiedEditorState, updateFn) {
    const serializedEditorState = typeof maybeStringifiedEditorState === "string" ? JSON.parse(maybeStringifiedEditorState) : maybeStringifiedEditorState;
    return parseEditorState(serializedEditorState, this, updateFn);
  }
  /**
   * Executes a read of the editor's state, with the
   * editor context available (useful for exporting and read-only DOM
   * operations). Much like update, but prevents any mutation of the
   * editor's state. Any pending updates will be flushed immediately before
   * the read.
   * @param callbackFn - A function that has access to read-only editor state.
   */
  read(callbackFn) {
    $commitPendingUpdates(this);
    return this.getEditorState().read(callbackFn, {
      editor: this
    });
  }
  /**
   * Executes an update to the editor state. The updateFn callback is the ONLY place
   * where Lexical editor state can be safely mutated.
   * @param updateFn - A function that has access to writable editor state.
   * @param options - A bag of options to control the behavior of the update.
   */
  update(updateFn, options) {
    updateEditor(this, updateFn, options);
  }
  /**
   * Focuses the editor by marking the existing selection as dirty, or by
   * creating a new selection at `defaultSelection` if one does not already
   * exist. If you want to force a specific selection, you should call
   * `root.selectStart()` or `root.selectEnd()` in an update.
   *
   * @param callbackFn - A function to run after the editor is focused.
   * @param options - A bag of options
   */
  focus(callbackFn, options = {}) {
    const rootElement = this._rootElement;
    if (rootElement !== null) {
      rootElement.setAttribute("autocapitalize", "off");
      updateEditorSync(this, () => {
        const selection = $getSelection();
        const root = $getRoot();
        if (selection !== null) {
          if (!selection.dirty) {
            $setSelection(selection.clone());
          }
        } else if (root.getChildrenSize() !== 0) {
          if (options.defaultSelection === "rootStart") {
            root.selectStart();
          } else {
            root.selectEnd();
          }
        }
        $addUpdateTag(FOCUS_TAG);
        $onUpdate(() => {
          rootElement.removeAttribute("autocapitalize");
          if (callbackFn) {
            callbackFn();
          }
        });
      });
      if (this._pendingEditorState === null) {
        rootElement.removeAttribute("autocapitalize");
      }
    }
  }
  /**
   * Removes focus from the editor.
   */
  blur() {
    const rootElement = this._rootElement;
    if (rootElement !== null) {
      rootElement.blur();
    }
    const domSelection = getDOMSelection(this._window);
    if (domSelection !== null) {
      domSelection.removeAllRanges();
    }
  }
  /**
   * Returns true if the editor is editable, false otherwise.
   * @returns True if the editor is editable, false otherwise.
   */
  isEditable() {
    return this._editable;
  }
  /**
   * Sets the editable property of the editor. When false, the
   * editor will not listen for user events on the underling contenteditable.
   * @param editable - the value to set the editable mode to.
   */
  setEditable(editable) {
    if (this._editable !== editable) {
      this._editable = editable;
      triggerListeners("editable", this, true, editable);
    }
  }
  /**
   * Returns a JSON-serializable javascript object NOT a JSON string.
   * You still must call JSON.stringify (or something else) to turn the
   * state into a string you can transfer over the wire and store in a database.
   *
   * See {@link LexicalNode.exportJSON}
   *
   * @returns A JSON-serializable javascript object
   */
  toJSON() {
    return {
      editorState: this._editorState.toJSON()
    };
  }
};
LexicalEditor.version = "0.35.0+dev.esm";
var pendingNodeToClone = null;
function setPendingNodeToClone(pendingNode) {
  pendingNodeToClone = pendingNode;
}
function getPendingNodeToClone() {
  const node = pendingNodeToClone;
  pendingNodeToClone = null;
  return node;
}
var keyCounter = 1;
function resetRandomKey() {
  keyCounter = 1;
}
function generateRandomKey() {
  return "" + keyCounter++;
}
function getRegisteredNodeOrThrow(editor, nodeType) {
  const registeredNode = getRegisteredNode(editor, nodeType);
  if (registeredNode === void 0) {
    {
      formatDevErrorMessage(`registeredNode: Type ${nodeType} not found`);
    }
  }
  return registeredNode;
}
function getRegisteredNode(editor, nodeType) {
  return editor._nodes.get(nodeType);
}
var scheduleMicroTask = typeof queueMicrotask === "function" ? queueMicrotask : (fn2) => {
  Promise.resolve().then(fn2);
};
function $isSelectionCapturedInDecorator(node) {
  return $isDecoratorNode($getNearestNodeFromDOMNode(node));
}
function isSelectionCapturedInDecoratorInput(anchorDOM) {
  const activeElement = document.activeElement;
  if (!isHTMLElement(activeElement)) {
    return false;
  }
  const nodeName = activeElement.nodeName;
  return $isDecoratorNode($getNearestNodeFromDOMNode(anchorDOM)) && (nodeName === "INPUT" || nodeName === "TEXTAREA" || activeElement.contentEditable === "true" && getEditorPropertyFromDOMNode(activeElement) == null);
}
function isSelectionWithinEditor(editor, anchorDOM, focusDOM) {
  const rootElement = editor.getRootElement();
  try {
    return rootElement !== null && rootElement.contains(anchorDOM) && rootElement.contains(focusDOM) && // Ignore if selection is within nested editor
    anchorDOM !== null && !isSelectionCapturedInDecoratorInput(anchorDOM) && getNearestEditorFromDOMNode(anchorDOM) === editor;
  } catch (error) {
    return false;
  }
}
function isLexicalEditor(editor) {
  return editor instanceof LexicalEditor;
}
function getNearestEditorFromDOMNode(node) {
  let currentNode = node;
  while (currentNode != null) {
    const editor = getEditorPropertyFromDOMNode(currentNode);
    if (isLexicalEditor(editor)) {
      return editor;
    }
    currentNode = getParentElement(currentNode);
  }
  return null;
}
function getEditorPropertyFromDOMNode(node) {
  return node ? node.__lexicalEditor : null;
}
function $isTokenOrTab(node) {
  return $isTabNode(node) || node.isToken();
}
function $isTokenOrSegmented(node) {
  return $isTokenOrTab(node) || node.isSegmented();
}
function isDOMTextNode(node) {
  return isDOMNode(node) && node.nodeType === DOM_TEXT_TYPE;
}
function isDOMDocumentNode(node) {
  return isDOMNode(node) && node.nodeType === DOM_DOCUMENT_TYPE;
}
function getDOMTextNode(element) {
  let node = element;
  while (node != null) {
    if (isDOMTextNode(node)) {
      return node;
    }
    node = node.firstChild;
  }
  return null;
}
function toggleTextFormatType(format, type, alignWithFormat) {
  const activeFormat = TEXT_TYPE_TO_FORMAT[type];
  if (alignWithFormat !== null && (format & activeFormat) === (alignWithFormat & activeFormat)) {
    return format;
  }
  let newFormat = format ^ activeFormat;
  if (type === "subscript") {
    newFormat &= ~TEXT_TYPE_TO_FORMAT.superscript;
  } else if (type === "superscript") {
    newFormat &= ~TEXT_TYPE_TO_FORMAT.subscript;
  } else if (type === "lowercase") {
    newFormat &= ~TEXT_TYPE_TO_FORMAT.uppercase;
    newFormat &= ~TEXT_TYPE_TO_FORMAT.capitalize;
  } else if (type === "uppercase") {
    newFormat &= ~TEXT_TYPE_TO_FORMAT.lowercase;
    newFormat &= ~TEXT_TYPE_TO_FORMAT.capitalize;
  } else if (type === "capitalize") {
    newFormat &= ~TEXT_TYPE_TO_FORMAT.lowercase;
    newFormat &= ~TEXT_TYPE_TO_FORMAT.uppercase;
  }
  return newFormat;
}
function $isLeafNode(node) {
  return $isTextNode(node) || $isLineBreakNode(node) || $isDecoratorNode(node);
}
function $setNodeKey(node, existingKey) {
  const pendingNode = getPendingNodeToClone();
  existingKey = existingKey || pendingNode && pendingNode.__key;
  if (existingKey != null) {
    {
      errorOnNodeKeyConstructorMismatch(node, existingKey, pendingNode);
    }
    node.__key = existingKey;
    return;
  }
  errorOnReadOnly();
  errorOnInfiniteTransforms();
  const editor = getActiveEditor();
  const editorState = getActiveEditorState();
  const key = generateRandomKey();
  editorState._nodeMap.set(key, node);
  if ($isElementNode(node)) {
    editor._dirtyElements.set(key, true);
  } else {
    editor._dirtyLeaves.add(key);
  }
  editor._cloneNotNeeded.add(key);
  editor._dirtyType = HAS_DIRTY_NODES;
  node.__key = key;
}
function errorOnNodeKeyConstructorMismatch(node, existingKey, pendingNode) {
  const editorState = internalGetActiveEditorState();
  if (!editorState) {
    return;
  }
  const existingNode = editorState._nodeMap.get(existingKey);
  if (pendingNode) {
    if (!(existingKey === pendingNode.__key)) {
      formatDevErrorMessage(`Lexical node with constructor ${node.constructor.name} (type ${node.getType()}) has an incorrect clone implementation, got ${String(existingKey)} for nodeKey when expecting ${pendingNode.__key}`);
    }
  }
  if (existingNode && existingNode.constructor !== node.constructor) {
    if (node.constructor.name !== existingNode.constructor.name) {
      {
        formatDevErrorMessage(`Lexical node with constructor ${node.constructor.name} attempted to re-use key from node in active editor state with constructor ${existingNode.constructor.name}. Keys must not be re-used when the type is changed.`);
      }
    } else {
      {
        formatDevErrorMessage(`Lexical node with constructor ${node.constructor.name} attempted to re-use key from node in active editor state with different constructor with the same name (possibly due to invalid Hot Module Replacement). Keys must not be re-used when the type is changed.`);
      }
    }
  }
}
function internalMarkParentElementsAsDirty(parentKey, nodeMap, dirtyElements) {
  let nextParentKey = parentKey;
  while (nextParentKey !== null) {
    if (dirtyElements.has(nextParentKey)) {
      return;
    }
    const node = nodeMap.get(nextParentKey);
    if (node === void 0) {
      break;
    }
    dirtyElements.set(nextParentKey, false);
    nextParentKey = node.__parent;
  }
}
function removeFromParent(node) {
  const oldParent = node.getParent();
  if (oldParent !== null) {
    const writableNode = node.getWritable();
    const writableParent = oldParent.getWritable();
    const prevSibling = node.getPreviousSibling();
    const nextSibling = node.getNextSibling();
    const nextSiblingKey = nextSibling !== null ? nextSibling.__key : null;
    const prevSiblingKey = prevSibling !== null ? prevSibling.__key : null;
    const writablePrevSibling = prevSibling !== null ? prevSibling.getWritable() : null;
    const writableNextSibling = nextSibling !== null ? nextSibling.getWritable() : null;
    if (prevSibling === null) {
      writableParent.__first = nextSiblingKey;
    }
    if (nextSibling === null) {
      writableParent.__last = prevSiblingKey;
    }
    if (writablePrevSibling !== null) {
      writablePrevSibling.__next = nextSiblingKey;
    }
    if (writableNextSibling !== null) {
      writableNextSibling.__prev = prevSiblingKey;
    }
    writableNode.__prev = null;
    writableNode.__next = null;
    writableNode.__parent = null;
    writableParent.__size--;
  }
}
function internalMarkNodeAsDirty(node) {
  errorOnInfiniteTransforms();
  const latest = node.getLatest();
  const parent = latest.__parent;
  const editorState = getActiveEditorState();
  const editor = getActiveEditor();
  const nodeMap = editorState._nodeMap;
  const dirtyElements = editor._dirtyElements;
  if (parent !== null) {
    internalMarkParentElementsAsDirty(parent, nodeMap, dirtyElements);
  }
  const key = latest.__key;
  editor._dirtyType = HAS_DIRTY_NODES;
  if ($isElementNode(node)) {
    dirtyElements.set(key, true);
  } else {
    editor._dirtyLeaves.add(key);
  }
}
function internalMarkSiblingsAsDirty(node) {
  const previousNode = node.getPreviousSibling();
  const nextNode = node.getNextSibling();
  if (previousNode !== null) {
    internalMarkNodeAsDirty(previousNode);
  }
  if (nextNode !== null) {
    internalMarkNodeAsDirty(nextNode);
  }
}
function $setCompositionKey(compositionKey) {
  errorOnReadOnly();
  const editor = getActiveEditor();
  const previousCompositionKey = editor._compositionKey;
  if (compositionKey !== previousCompositionKey) {
    editor._compositionKey = compositionKey;
    if (previousCompositionKey !== null) {
      const node = $getNodeByKey(previousCompositionKey);
      if (node !== null) {
        node.getWritable();
      }
    }
    if (compositionKey !== null) {
      const node = $getNodeByKey(compositionKey);
      if (node !== null) {
        node.getWritable();
      }
    }
  }
}
function $getCompositionKey() {
  if (isCurrentlyReadOnlyMode()) {
    return null;
  }
  const editor = getActiveEditor();
  return editor._compositionKey;
}
function $getNodeByKey(key, _editorState) {
  const editorState = _editorState || getActiveEditorState();
  const node = editorState._nodeMap.get(key);
  if (node === void 0) {
    return null;
  }
  return node;
}
function $getNodeFromDOMNode(dom, editorState) {
  const editor = getActiveEditor();
  const key = getNodeKeyFromDOMNode(dom, editor);
  if (key !== void 0) {
    return $getNodeByKey(key, editorState);
  }
  return null;
}
function setNodeKeyOnDOMNode(dom, editor, key) {
  const prop = `__lexicalKey_${editor._key}`;
  dom[prop] = key;
}
function getNodeKeyFromDOMNode(dom, editor) {
  const prop = `__lexicalKey_${editor._key}`;
  return dom[prop];
}
function $getNearestNodeFromDOMNode(startingDOM, editorState) {
  let dom = startingDOM;
  while (dom != null) {
    const node = $getNodeFromDOMNode(dom, editorState);
    if (node !== null) {
      return node;
    }
    dom = getParentElement(dom);
  }
  return null;
}
function cloneDecorators(editor) {
  const currentDecorators = editor._decorators;
  const pendingDecorators = Object.assign({}, currentDecorators);
  editor._pendingDecorators = pendingDecorators;
  return pendingDecorators;
}
function getEditorStateTextContent(editorState) {
  return editorState.read(() => $getRoot().getTextContent());
}
function markNodesWithTypesAsDirty(editor, types) {
  const cachedMap = getCachedTypeToNodeMap(editor.getEditorState());
  const dirtyNodeMaps = [];
  for (const type of types) {
    const nodeMap = cachedMap.get(type);
    if (nodeMap) {
      dirtyNodeMaps.push(nodeMap);
    }
  }
  if (dirtyNodeMaps.length === 0) {
    return;
  }
  editor.update(() => {
    for (const nodeMap of dirtyNodeMaps) {
      for (const nodeKey of nodeMap.keys()) {
        const latest = $getNodeByKey(nodeKey);
        if (latest) {
          latest.markDirty();
        }
      }
    }
  }, editor._pendingEditorState === null ? {
    tag: HISTORY_MERGE_TAG
  } : void 0);
}
function $getRoot() {
  return internalGetRoot(getActiveEditorState());
}
function internalGetRoot(editorState) {
  return editorState._nodeMap.get("root");
}
function $setSelection(selection) {
  errorOnReadOnly();
  const editorState = getActiveEditorState();
  if (selection !== null) {
    {
      if (Object.isFrozen(selection)) {
        {
          formatDevErrorMessage(`$setSelection called on frozen selection object. Ensure selection is cloned before passing in.`);
        }
      }
    }
    selection.dirty = true;
    selection.setCachedNodes(null);
  }
  editorState._selection = selection;
}
function $flushMutations() {
  errorOnReadOnly();
  const editor = getActiveEditor();
  flushRootMutations(editor);
}
function $getNodeFromDOM(dom) {
  const editor = getActiveEditor();
  const nodeKey = getNodeKeyFromDOMTree(dom, editor);
  if (nodeKey === null) {
    const rootElement = editor.getRootElement();
    if (dom === rootElement) {
      return $getNodeByKey("root");
    }
    return null;
  }
  return $getNodeByKey(nodeKey);
}
function getNodeKeyFromDOMTree(dom, editor) {
  let node = dom;
  while (node != null) {
    const key = getNodeKeyFromDOMNode(node, editor);
    if (key !== void 0) {
      return key;
    }
    node = getParentElement(node);
  }
  return null;
}
function doesContainSurrogatePair(str) {
  return /[\uD800-\uDBFF][\uDC00-\uDFFF]/g.test(str);
}
function getEditorsToPropagate(editor) {
  const editorsToPropagate = [];
  let currentEditor = editor;
  while (currentEditor !== null) {
    editorsToPropagate.push(currentEditor);
    currentEditor = currentEditor._parentEditor;
  }
  return editorsToPropagate;
}
function createUID() {
  return Math.random().toString(36).replace(/[^a-z]+/g, "").substring(0, 5);
}
function getAnchorTextFromDOM(anchorNode) {
  return isDOMTextNode(anchorNode) ? anchorNode.nodeValue : null;
}
function $updateSelectedTextFromDOM(isCompositionEnd, editor, data) {
  const domSelection = getDOMSelection(getWindow(editor));
  if (domSelection === null) {
    return;
  }
  const anchorNode = domSelection.anchorNode;
  let {
    anchorOffset,
    focusOffset
  } = domSelection;
  if (anchorNode !== null) {
    let textContent = getAnchorTextFromDOM(anchorNode);
    const node = $getNearestNodeFromDOMNode(anchorNode);
    if (textContent !== null && $isTextNode(node)) {
      if (textContent === COMPOSITION_SUFFIX && data) {
        const offset = data.length;
        textContent = data;
        anchorOffset = offset;
        focusOffset = offset;
      }
      if (textContent !== null) {
        $updateTextNodeFromDOMContent(node, textContent, anchorOffset, focusOffset, isCompositionEnd);
      }
    }
  }
}
function $updateTextNodeFromDOMContent(textNode, textContent, anchorOffset, focusOffset, compositionEnd) {
  let node = textNode;
  if (node.isAttached() && (compositionEnd || !node.isDirty())) {
    const isComposing = node.isComposing();
    let normalizedTextContent = textContent;
    if ((isComposing || compositionEnd) && textContent[textContent.length - 1] === COMPOSITION_SUFFIX) {
      normalizedTextContent = textContent.slice(0, -1);
    }
    const prevTextContent = node.getTextContent();
    if (compositionEnd || normalizedTextContent !== prevTextContent) {
      if (normalizedTextContent === "") {
        $setCompositionKey(null);
        if (!IS_SAFARI && !IS_IOS && !IS_APPLE_WEBKIT) {
          const editor = getActiveEditor();
          setTimeout(() => {
            editor.update(() => {
              if (node.isAttached()) {
                node.remove();
              }
            });
          }, 20);
        } else {
          node.remove();
        }
        return;
      }
      const parent = node.getParent();
      const prevSelection = $getPreviousSelection();
      const prevTextContentSize = node.getTextContentSize();
      const compositionKey = $getCompositionKey();
      const nodeKey = node.getKey();
      if (node.isToken() || compositionKey !== null && nodeKey === compositionKey && !isComposing || // Check if character was added at the start or boundaries when not insertable, and we need
      // to clear this input from occurring as that action wasn't permitted.
      $isRangeSelection(prevSelection) && (parent !== null && !parent.canInsertTextBefore() && prevSelection.anchor.offset === 0 || prevSelection.anchor.key === textNode.__key && prevSelection.anchor.offset === 0 && !node.canInsertTextBefore() && !isComposing || prevSelection.focus.key === textNode.__key && prevSelection.focus.offset === prevTextContentSize && !node.canInsertTextAfter() && !isComposing)) {
        node.markDirty();
        return;
      }
      const selection = $getSelection();
      if (!$isRangeSelection(selection) || anchorOffset === null || focusOffset === null) {
        $setTextContentWithSelection(node, normalizedTextContent, selection);
        return;
      }
      selection.setTextNodeRange(node, anchorOffset, node, focusOffset);
      if (node.isSegmented()) {
        const originalTextContent = node.getTextContent();
        const replacement = $createTextNode(originalTextContent);
        node.replace(replacement);
        node = replacement;
      }
      $setTextContentWithSelection(node, normalizedTextContent, selection);
    }
  }
}
function $setTextContentWithSelection(node, textContent, selection) {
  node.setTextContent(textContent);
  if ($isRangeSelection(selection)) {
    const key = node.getKey();
    for (const k of ["anchor", "focus"]) {
      const pt2 = selection[k];
      if (pt2.type === "text" && pt2.key === key) {
        pt2.offset = $getTextNodeOffset(node, pt2.offset, "clamp");
      }
    }
  }
}
function $previousSiblingDoesNotAcceptText(node) {
  const previousSibling = node.getPreviousSibling();
  return ($isTextNode(previousSibling) || $isElementNode(previousSibling) && previousSibling.isInline()) && !previousSibling.canInsertTextAfter();
}
function $shouldInsertTextAfterOrBeforeTextNode(selection, node) {
  if (node.isSegmented()) {
    return true;
  }
  if (!selection.isCollapsed()) {
    return false;
  }
  const offset = selection.anchor.offset;
  const parent = node.getParentOrThrow();
  const isToken = $isTokenOrTab(node);
  if (offset === 0) {
    return !node.canInsertTextBefore() || !parent.canInsertTextBefore() && !node.isComposing() || isToken || $previousSiblingDoesNotAcceptText(node);
  } else if (offset === node.getTextContentSize()) {
    return !node.canInsertTextAfter() || !parent.canInsertTextAfter() && !node.isComposing() || isToken;
  } else {
    return false;
  }
}
function matchModifier(event, mask, prop) {
  const expected = mask[prop] || false;
  return expected === "any" || expected === event[prop];
}
function isModifierMatch(event, mask) {
  return matchModifier(event, mask, "altKey") && matchModifier(event, mask, "ctrlKey") && matchModifier(event, mask, "shiftKey") && matchModifier(event, mask, "metaKey");
}
function isExactShortcutMatch(event, expectedKey, mask) {
  return isModifierMatch(event, mask) && event.key.toLowerCase() === expectedKey.toLowerCase();
}
var CONTROL_OR_META = {
  ctrlKey: !IS_APPLE,
  metaKey: IS_APPLE
};
var CONTROL_OR_ALT = {
  altKey: IS_APPLE,
  ctrlKey: !IS_APPLE
};
function isTab(event) {
  return isExactShortcutMatch(event, "Tab", {
    shiftKey: "any"
  });
}
function isBold(event) {
  return isExactShortcutMatch(event, "b", CONTROL_OR_META);
}
function isItalic(event) {
  return isExactShortcutMatch(event, "i", CONTROL_OR_META);
}
function isUnderline(event) {
  return isExactShortcutMatch(event, "u", CONTROL_OR_META);
}
function isParagraph(event) {
  return isExactShortcutMatch(event, "Enter", {
    altKey: "any",
    ctrlKey: "any",
    metaKey: "any"
  });
}
function isLineBreak(event) {
  return isExactShortcutMatch(event, "Enter", {
    altKey: "any",
    ctrlKey: "any",
    metaKey: "any",
    shiftKey: true
  });
}
function isOpenLineBreak(event) {
  return IS_APPLE && isExactShortcutMatch(event, "o", {
    ctrlKey: true
  });
}
function isDeleteWordBackward(event) {
  return isExactShortcutMatch(event, "Backspace", CONTROL_OR_ALT);
}
function isDeleteWordForward(event) {
  return isExactShortcutMatch(event, "Delete", CONTROL_OR_ALT);
}
function isDeleteLineBackward(event) {
  return IS_APPLE && isExactShortcutMatch(event, "Backspace", {
    metaKey: true
  });
}
function isDeleteLineForward(event) {
  return IS_APPLE && (isExactShortcutMatch(event, "Delete", {
    metaKey: true
  }) || isExactShortcutMatch(event, "k", {
    ctrlKey: true
  }));
}
function isDeleteBackward(event) {
  return isExactShortcutMatch(event, "Backspace", {
    shiftKey: "any"
  }) || IS_APPLE && isExactShortcutMatch(event, "h", {
    ctrlKey: true
  });
}
function isDeleteForward(event) {
  return isExactShortcutMatch(event, "Delete", {}) || IS_APPLE && isExactShortcutMatch(event, "d", {
    ctrlKey: true
  });
}
function isUndo(event) {
  return isExactShortcutMatch(event, "z", CONTROL_OR_META);
}
function isRedo(event) {
  if (IS_APPLE) {
    return isExactShortcutMatch(event, "z", {
      metaKey: true,
      shiftKey: true
    });
  }
  return isExactShortcutMatch(event, "y", {
    ctrlKey: true
  }) || isExactShortcutMatch(event, "z", {
    ctrlKey: true,
    shiftKey: true
  });
}
function isCopy(event) {
  return isExactShortcutMatch(event, "c", CONTROL_OR_META);
}
function isCut(event) {
  return isExactShortcutMatch(event, "x", CONTROL_OR_META);
}
function isMoveBackward(event) {
  return isExactShortcutMatch(event, "ArrowLeft", {
    shiftKey: "any"
  });
}
function isMoveToStart(event) {
  return isExactShortcutMatch(event, "ArrowLeft", CONTROL_OR_META);
}
function isMoveForward(event) {
  return isExactShortcutMatch(event, "ArrowRight", {
    shiftKey: "any"
  });
}
function isMoveToEnd(event) {
  return isExactShortcutMatch(event, "ArrowRight", CONTROL_OR_META);
}
function isMoveUp(event) {
  return isExactShortcutMatch(event, "ArrowUp", {
    altKey: "any",
    shiftKey: "any"
  });
}
function isMoveDown(event) {
  return isExactShortcutMatch(event, "ArrowDown", {
    altKey: "any",
    shiftKey: "any"
  });
}
function isModifier(event) {
  return event.ctrlKey || event.shiftKey || event.altKey || event.metaKey;
}
function isSpace(event) {
  return event.key === " ";
}
function isBackspace(event) {
  return event.key === "Backspace";
}
function isEscape(event) {
  return event.key === "Escape";
}
function isDelete(event) {
  return event.key === "Delete";
}
function isSelectAll(event) {
  return isExactShortcutMatch(event, "a", CONTROL_OR_META);
}
function $selectAll(selection) {
  const root = $getRoot();
  if ($isRangeSelection(selection)) {
    const anchor = selection.anchor;
    const focus = selection.focus;
    const anchorNode = anchor.getNode();
    const topParent = anchorNode.getTopLevelElementOrThrow();
    const rootNode = topParent.getParentOrThrow();
    anchor.set(rootNode.getKey(), 0, "element");
    focus.set(rootNode.getKey(), rootNode.getChildrenSize(), "element");
    $normalizeSelection(selection);
    return selection;
  } else {
    const newSelection = root.select(0, root.getChildrenSize());
    $setSelection($normalizeSelection(newSelection));
    return newSelection;
  }
}
function getCachedClassNameArray(classNamesTheme, classNameThemeType) {
  if (classNamesTheme.__lexicalClassNameCache === void 0) {
    classNamesTheme.__lexicalClassNameCache = {};
  }
  const classNamesCache = classNamesTheme.__lexicalClassNameCache;
  const cachedClassNames = classNamesCache[classNameThemeType];
  if (cachedClassNames !== void 0) {
    return cachedClassNames;
  }
  const classNames = classNamesTheme[classNameThemeType];
  if (typeof classNames === "string") {
    const classNamesArr = normalizeClassNames(classNames);
    classNamesCache[classNameThemeType] = classNamesArr;
    return classNamesArr;
  }
  return classNames;
}
function setMutatedNode(mutatedNodes2, registeredNodes, mutationListeners, node, mutation) {
  if (mutationListeners.size === 0) {
    return;
  }
  const nodeType = node.__type;
  const nodeKey = node.__key;
  const registeredNode = registeredNodes.get(nodeType);
  if (registeredNode === void 0) {
    {
      formatDevErrorMessage(`Type ${nodeType} not in registeredNodes`);
    }
  }
  const klass = registeredNode.klass;
  let mutatedNodesByType = mutatedNodes2.get(klass);
  if (mutatedNodesByType === void 0) {
    mutatedNodesByType = /* @__PURE__ */ new Map();
    mutatedNodes2.set(klass, mutatedNodesByType);
  }
  const prevMutation = mutatedNodesByType.get(nodeKey);
  const isMove = prevMutation === "destroyed" && mutation === "created";
  if (prevMutation === void 0 || isMove) {
    mutatedNodesByType.set(nodeKey, isMove ? "updated" : mutation);
  }
}
function $nodesOfType(klass) {
  const klassType = klass.getType();
  const editorState = getActiveEditorState();
  if (editorState._readOnly) {
    const nodes2 = getCachedTypeToNodeMap(editorState).get(klassType);
    return nodes2 ? Array.from(nodes2.values()) : [];
  }
  const nodes = editorState._nodeMap;
  const nodesOfType = [];
  for (const [, node] of nodes) {
    if (node instanceof klass && node.__type === klassType && node.isAttached()) {
      nodesOfType.push(node);
    }
  }
  return nodesOfType;
}
function resolveElement(element, isBackward, focusOffset) {
  const parent = element.getParent();
  let offset = focusOffset;
  let block = element;
  if (parent !== null) {
    if (isBackward && focusOffset === 0) {
      offset = block.getIndexWithinParent();
      block = parent;
    } else if (!isBackward && focusOffset === block.getChildrenSize()) {
      offset = block.getIndexWithinParent() + 1;
      block = parent;
    }
  }
  return block.getChildAtIndex(isBackward ? offset - 1 : offset);
}
function $getAdjacentNode(focus, isBackward) {
  const focusOffset = focus.offset;
  if (focus.type === "element") {
    const block = focus.getNode();
    return resolveElement(block, isBackward, focusOffset);
  } else {
    const focusNode = focus.getNode();
    if (isBackward && focusOffset === 0 || !isBackward && focusOffset === focusNode.getTextContentSize()) {
      const possibleNode = isBackward ? focusNode.getPreviousSibling() : focusNode.getNextSibling();
      if (possibleNode === null) {
        return resolveElement(focusNode.getParentOrThrow(), isBackward, focusNode.getIndexWithinParent() + (isBackward ? 0 : 1));
      }
      return possibleNode;
    }
  }
  return null;
}
function isFirefoxClipboardEvents(editor) {
  const event = getWindow(editor).event;
  const inputType = event && event.inputType;
  return inputType === "insertFromPaste" || inputType === "insertFromPasteAsQuotation";
}
function dispatchCommand(editor, command, payload) {
  return triggerCommandListeners(editor, command, payload);
}
function $textContentRequiresDoubleLinebreakAtEnd(node) {
  return !$isRootNode(node) && !node.isLastChild() && !node.isInline();
}
function getElementByKeyOrThrow(editor, key) {
  const element = editor._keyToDOMMap.get(key);
  if (element === void 0) {
    {
      formatDevErrorMessage(`Reconciliation: could not find DOM element for node key ${key}`);
    }
  }
  return element;
}
function getParentElement(node) {
  const parentElement = node.assignedSlot || node.parentElement;
  return isDocumentFragment(parentElement) ? parentElement.host : parentElement;
}
function getDOMOwnerDocument(target) {
  return isDOMDocumentNode(target) ? target : isHTMLElement(target) ? target.ownerDocument : null;
}
function scrollIntoViewIfNeeded(editor, selectionRect, rootElement) {
  const doc = getDOMOwnerDocument(rootElement);
  const defaultView = getDefaultView(doc);
  if (doc === null || defaultView === null) {
    return;
  }
  let {
    top: currentTop,
    bottom: currentBottom
  } = selectionRect;
  let targetTop = 0;
  let targetBottom = 0;
  let element = rootElement;
  while (element !== null) {
    const isBodyElement = element === doc.body;
    if (isBodyElement) {
      targetTop = 0;
      targetBottom = getWindow(editor).innerHeight;
    } else {
      const targetRect = element.getBoundingClientRect();
      targetTop = targetRect.top;
      targetBottom = targetRect.bottom;
    }
    let diff = 0;
    if (currentTop < targetTop) {
      diff = -(targetTop - currentTop);
    } else if (currentBottom > targetBottom) {
      diff = currentBottom - targetBottom;
    }
    if (diff !== 0) {
      if (isBodyElement) {
        defaultView.scrollBy(0, diff);
      } else {
        const scrollTop = element.scrollTop;
        element.scrollTop += diff;
        const yOffset = element.scrollTop - scrollTop;
        currentTop -= yOffset;
        currentBottom -= yOffset;
      }
    }
    if (isBodyElement) {
      break;
    }
    element = getParentElement(element);
  }
}
function $hasUpdateTag(tag) {
  const editor = getActiveEditor();
  return editor._updateTags.has(tag);
}
function $addUpdateTag(tag) {
  errorOnReadOnly();
  const editor = getActiveEditor();
  editor._updateTags.add(tag);
}
function $onUpdate(updateFn) {
  errorOnReadOnly();
  const editor = getActiveEditor();
  editor._deferred.push(updateFn);
}
function $maybeMoveChildrenSelectionToParent(parentNode) {
  const selection = $getSelection();
  if (!$isRangeSelection(selection) || !$isElementNode(parentNode)) {
    return selection;
  }
  const {
    anchor,
    focus
  } = selection;
  const anchorNode = anchor.getNode();
  const focusNode = focus.getNode();
  if ($hasAncestor(anchorNode, parentNode)) {
    anchor.set(parentNode.__key, 0, "element");
  }
  if ($hasAncestor(focusNode, parentNode)) {
    focus.set(parentNode.__key, 0, "element");
  }
  return selection;
}
function $hasAncestor(child, targetNode) {
  let parent = child.getParent();
  while (parent !== null) {
    if (parent.is(targetNode)) {
      return true;
    }
    parent = parent.getParent();
  }
  return false;
}
function getDefaultView(domElem) {
  const ownerDoc = getDOMOwnerDocument(domElem);
  return ownerDoc ? ownerDoc.defaultView : null;
}
function getWindow(editor) {
  const windowObj = editor._window;
  if (windowObj === null) {
    {
      formatDevErrorMessage(`window object not found`);
    }
  }
  return windowObj;
}
function $isInlineElementOrDecoratorNode(node) {
  return $isElementNode(node) && node.isInline() || $isDecoratorNode(node) && node.isInline();
}
function $getNearestRootOrShadowRoot(node) {
  let parent = node.getParentOrThrow();
  while (parent !== null) {
    if ($isRootOrShadowRoot(parent)) {
      return parent;
    }
    parent = parent.getParentOrThrow();
  }
  return parent;
}
function $isRootOrShadowRoot(node) {
  return $isRootNode(node) || $isElementNode(node) && node.isShadowRoot();
}
function $copyNode(node) {
  const copy = node.constructor.clone(node);
  $setNodeKey(copy, null);
  copy.afterCloneFrom(node);
  return copy;
}
function $applyNodeReplacement(node) {
  const editor = getActiveEditor();
  const nodeType = node.getType();
  const registeredNode = getRegisteredNode(editor, nodeType);
  if (!(registeredNode !== void 0)) {
    formatDevErrorMessage(`$applyNodeReplacement node ${node.constructor.name} with type ${nodeType} must be registered to the editor. You can do this by passing the node class via the "nodes" array in the editor config.`);
  }
  const {
    replace,
    replaceWithKlass
  } = registeredNode;
  if (replace !== null) {
    const replacementNode = replace(node);
    const replacementNodeKlass = replacementNode.constructor;
    if (replaceWithKlass !== null) {
      if (!(replacementNode instanceof replaceWithKlass)) {
        formatDevErrorMessage(`$applyNodeReplacement failed. Expected replacement node to be an instance of ${replaceWithKlass.name} with type ${replaceWithKlass.getType()} but returned ${replacementNodeKlass.name} with type ${replacementNodeKlass.getType()} from original node ${node.constructor.name} with type ${nodeType}`);
      }
    } else {
      if (!(replacementNode instanceof node.constructor && replacementNodeKlass !== node.constructor)) {
        formatDevErrorMessage(`$applyNodeReplacement failed. Ensure replacement node ${replacementNodeKlass.name} with type ${replacementNodeKlass.getType()} is a subclass of the original node ${node.constructor.name} with type ${nodeType}.`);
      }
    }
    if (!(replacementNode.__key !== node.__key)) {
      formatDevErrorMessage(`$applyNodeReplacement failed. Ensure that the key argument is *not* used in your replace function (from node ${node.constructor.name} with type ${nodeType} to node ${replacementNodeKlass.name} with type ${replacementNodeKlass.getType()}), Node keys must never be re-used except by the static clone method.`);
    }
    return replacementNode;
  }
  return node;
}
function errorOnInsertTextNodeOnRoot(node, insertNode) {
  const parentNode = node.getParent();
  if ($isRootNode(parentNode) && !$isElementNode(insertNode) && !$isDecoratorNode(insertNode)) {
    {
      formatDevErrorMessage(`Only element or decorator nodes can be inserted in to the root node`);
    }
  }
}
function $getNodeByKeyOrThrow(key) {
  const node = $getNodeByKey(key);
  if (node === null) {
    {
      formatDevErrorMessage(`Expected node with key ${key} to exist but it's not in the nodeMap.`);
    }
  }
  return node;
}
function createBlockCursorElement(editorConfig) {
  const theme = editorConfig.theme;
  const element = document.createElement("div");
  element.contentEditable = "false";
  element.setAttribute("data-lexical-cursor", "true");
  let blockCursorTheme = theme.blockCursor;
  if (blockCursorTheme !== void 0) {
    if (typeof blockCursorTheme === "string") {
      const classNamesArr = normalizeClassNames(blockCursorTheme);
      blockCursorTheme = theme.blockCursor = classNamesArr;
    }
    if (blockCursorTheme !== void 0) {
      element.classList.add(...blockCursorTheme);
    }
  }
  return element;
}
function needsBlockCursor(node) {
  return ($isDecoratorNode(node) || $isElementNode(node) && !node.canBeEmpty()) && !node.isInline();
}
function removeDOMBlockCursorElement(blockCursorElement, editor, rootElement) {
  rootElement.style.removeProperty("caret-color");
  editor._blockCursorElement = null;
  const parentElement = blockCursorElement.parentElement;
  if (parentElement !== null) {
    parentElement.removeChild(blockCursorElement);
  }
}
function updateDOMBlockCursorElement(editor, rootElement, nextSelection) {
  let blockCursorElement = editor._blockCursorElement;
  if ($isRangeSelection(nextSelection) && nextSelection.isCollapsed() && nextSelection.anchor.type === "element" && rootElement.contains(document.activeElement)) {
    const anchor = nextSelection.anchor;
    const elementNode = anchor.getNode();
    const offset = anchor.offset;
    const elementNodeSize = elementNode.getChildrenSize();
    let isBlockCursor = false;
    let insertBeforeElement = null;
    if (offset === elementNodeSize) {
      const child = elementNode.getChildAtIndex(offset - 1);
      if (needsBlockCursor(child)) {
        isBlockCursor = true;
      }
    } else {
      const child = elementNode.getChildAtIndex(offset);
      if (child !== null && needsBlockCursor(child)) {
        const sibling = child.getPreviousSibling();
        if (sibling === null || needsBlockCursor(sibling)) {
          isBlockCursor = true;
          insertBeforeElement = editor.getElementByKey(child.__key);
        }
      }
    }
    if (isBlockCursor) {
      const elementDOM = editor.getElementByKey(elementNode.__key);
      if (blockCursorElement === null) {
        editor._blockCursorElement = blockCursorElement = createBlockCursorElement(editor._config);
      }
      rootElement.style.caretColor = "transparent";
      if (insertBeforeElement === null) {
        elementDOM.appendChild(blockCursorElement);
      } else {
        elementDOM.insertBefore(blockCursorElement, insertBeforeElement);
      }
      return;
    }
  }
  if (blockCursorElement !== null) {
    removeDOMBlockCursorElement(blockCursorElement, editor, rootElement);
  }
}
function getDOMSelection(targetWindow) {
  return !CAN_USE_DOM ? null : (targetWindow || window).getSelection();
}
function getDOMSelectionFromTarget(eventTarget) {
  const defaultView = getDefaultView(eventTarget);
  return defaultView ? defaultView.getSelection() : null;
}
function $splitNode(node, offset) {
  let startNode = node.getChildAtIndex(offset);
  if (startNode == null) {
    startNode = node;
  }
  if (!!$isRootOrShadowRoot(node)) {
    formatDevErrorMessage(`Can not call $splitNode() on root element`);
  }
  const recurse = (currentNode) => {
    const parent = currentNode.getParentOrThrow();
    const isParentRoot = $isRootOrShadowRoot(parent);
    const nodeToMove = currentNode === startNode && !isParentRoot ? currentNode : $copyNode(currentNode);
    if (isParentRoot) {
      if (!($isElementNode(currentNode) && $isElementNode(nodeToMove))) {
        formatDevErrorMessage(`Children of a root must be ElementNode`);
      }
      currentNode.insertAfter(nodeToMove);
      return [currentNode, nodeToMove, nodeToMove];
    } else {
      const [leftTree2, rightTree2, newParent] = recurse(parent);
      const nextSiblings = currentNode.getNextSiblings();
      newParent.append(nodeToMove, ...nextSiblings);
      return [leftTree2, rightTree2, nodeToMove];
    }
  };
  const [leftTree, rightTree] = recurse(startNode);
  return [leftTree, rightTree];
}
function $findMatchingParent(startingNode, findFn) {
  let curr = startingNode;
  while (curr !== $getRoot() && curr != null) {
    if (findFn(curr)) {
      return curr;
    }
    curr = curr.getParent();
  }
  return null;
}
function isHTMLAnchorElement(x2) {
  return isHTMLElement(x2) && x2.tagName === "A";
}
function isHTMLElement(x2) {
  return isDOMNode(x2) && x2.nodeType === DOM_ELEMENT_TYPE;
}
function isDOMNode(x2) {
  return typeof x2 === "object" && x2 !== null && "nodeType" in x2 && typeof x2.nodeType === "number";
}
function isDocumentFragment(x2) {
  return isDOMNode(x2) && x2.nodeType === DOM_DOCUMENT_FRAGMENT_TYPE;
}
function isInlineDomNode(node) {
  const inlineNodes = new RegExp(/^(a|abbr|acronym|b|cite|code|del|em|i|ins|kbd|label|mark|output|q|ruby|s|samp|span|strong|sub|sup|time|u|tt|var|#text)$/, "i");
  return node.nodeName.match(inlineNodes) !== null;
}
function isBlockDomNode(node) {
  const blockNodes = new RegExp(/^(address|article|aside|blockquote|canvas|dd|div|dl|dt|fieldset|figcaption|figure|footer|form|h1|h2|h3|h4|h5|h6|header|hr|li|main|nav|noscript|ol|p|pre|section|table|td|tfoot|ul|video)$/, "i");
  return node.nodeName.match(blockNodes) !== null;
}
function INTERNAL_$isBlock(node) {
  if ($isDecoratorNode(node) && !node.isInline()) {
    return true;
  }
  if (!$isElementNode(node) || $isRootOrShadowRoot(node)) {
    return false;
  }
  const firstChild = node.getFirstChild();
  const isLeafElement = firstChild === null || $isLineBreakNode(firstChild) || $isTextNode(firstChild) || firstChild.isInline();
  return !node.isInline() && node.canBeEmpty() !== false && isLeafElement;
}
function $getAncestor(node, predicate) {
  let parent = node;
  while (parent !== null && parent.getParent() !== null && !predicate(parent)) {
    parent = parent.getParentOrThrow();
  }
  return predicate(parent) ? parent : null;
}
function $getEditor() {
  return getActiveEditor();
}
var cachedNodeMaps = /* @__PURE__ */ new WeakMap();
var EMPTY_TYPE_TO_NODE_MAP = /* @__PURE__ */ new Map();
function getCachedTypeToNodeMap(editorState) {
  if (!editorState._readOnly && editorState.isEmpty()) {
    return EMPTY_TYPE_TO_NODE_MAP;
  }
  if (!editorState._readOnly) {
    formatDevErrorMessage(`getCachedTypeToNodeMap called with a writable EditorState`);
  }
  let typeToNodeMap = cachedNodeMaps.get(editorState);
  if (!typeToNodeMap) {
    typeToNodeMap = computeTypeToNodeMap(editorState);
    cachedNodeMaps.set(editorState, typeToNodeMap);
  }
  return typeToNodeMap;
}
function computeTypeToNodeMap(editorState) {
  const typeToNodeMap = /* @__PURE__ */ new Map();
  for (const [nodeKey, node] of editorState._nodeMap) {
    const nodeType = node.__type;
    let nodeMap = typeToNodeMap.get(nodeType);
    if (!nodeMap) {
      nodeMap = /* @__PURE__ */ new Map();
      typeToNodeMap.set(nodeType, nodeMap);
    }
    nodeMap.set(nodeKey, node);
  }
  return typeToNodeMap;
}
function $cloneWithProperties(latestNode) {
  const constructor = latestNode.constructor;
  const mutableNode = constructor.clone(latestNode);
  mutableNode.afterCloneFrom(latestNode);
  {
    if (!(mutableNode.__key === latestNode.__key)) {
      formatDevErrorMessage(`$cloneWithProperties: ${constructor.name}.clone(node) (with type '${constructor.getType()}') did not return a node with the same key, make sure to specify node.__key as the last argument to the constructor`);
    }
    if (!(mutableNode.__parent === latestNode.__parent && mutableNode.__next === latestNode.__next && mutableNode.__prev === latestNode.__prev)) {
      formatDevErrorMessage(`$cloneWithProperties: ${constructor.name}.clone(node) (with type '${constructor.getType()}') overrode afterCloneFrom but did not call super.afterCloneFrom(prevNode)`);
    }
  }
  return mutableNode;
}
function setNodeIndentFromDOM(elementDom, elementNode) {
  const indentSize = parseInt(elementDom.style.paddingInlineStart, 10) || 0;
  const indent = Math.round(indentSize / 40);
  elementNode.setIndent(indent);
}
function setDOMUnmanaged(elementDom) {
  const el2 = elementDom;
  el2.__lexicalUnmanaged = true;
}
function isDOMUnmanaged(elementDom) {
  const el2 = elementDom;
  return el2.__lexicalUnmanaged === true;
}
function hasOwn(o2, k) {
  return Object.prototype.hasOwnProperty.call(o2, k);
}
function hasOwnStaticMethod(klass, k) {
  return hasOwn(klass, k) && klass[k] !== LexicalNode[k];
}
function hasOwnExportDOM(klass) {
  return hasOwn(klass.prototype, "exportDOM");
}
function isAbstractNodeClass(klass) {
  return klass === DecoratorNode || klass === ElementNode || klass === LexicalNode;
}
function getStaticNodeConfig(klass) {
  const nodeConfigRecord = PROTOTYPE_CONFIG_METHOD in klass.prototype ? klass.prototype[PROTOTYPE_CONFIG_METHOD]() : void 0;
  const isAbstract = isAbstractNodeClass(klass);
  const nodeType = !isAbstract && hasOwnStaticMethod(klass, "getType") ? klass.getType() : void 0;
  let ownNodeConfig;
  let ownNodeType = nodeType;
  if (nodeConfigRecord) {
    if (nodeType) {
      ownNodeConfig = nodeConfigRecord[nodeType];
    } else {
      for (const [k, v2] of Object.entries(nodeConfigRecord)) {
        ownNodeType = k;
        ownNodeConfig = v2;
      }
    }
  }
  if (!isAbstract && ownNodeType) {
    if (!hasOwnStaticMethod(klass, "getType")) {
      klass.getType = () => ownNodeType;
    }
    if (!hasOwnStaticMethod(klass, "clone")) {
      if (TextNode.length === 0) {
        if (!(klass.length === 0)) {
          formatDevErrorMessage(`${klass.name} (type ${ownNodeType}) must implement a static clone method since its constructor has ${String(klass.length)} required arguments (expecting 0). Use an explicit default in the first argument of your constructor(prop: T=X, nodeKey?: NodeKey).`);
        }
      }
      klass.clone = (prevNode) => {
        setPendingNodeToClone(prevNode);
        return new klass();
      };
    }
    if (!hasOwnStaticMethod(klass, "importJSON")) {
      if (TextNode.length === 0) {
        if (!(klass.length === 0)) {
          formatDevErrorMessage(`${klass.name} (type ${ownNodeType}) must implement a static importJSON method since its constructor has ${String(klass.length)} required arguments (expecting 0). Use an explicit default in the first argument of your constructor(prop: T=X, nodeKey?: NodeKey).`);
        }
      }
      klass.importJSON = ownNodeConfig && ownNodeConfig.$importJSON || ((serializedNode) => new klass().updateFromJSON(serializedNode));
    }
    if (!hasOwnStaticMethod(klass, "importDOM") && ownNodeConfig) {
      const {
        importDOM
      } = ownNodeConfig;
      if (importDOM) {
        klass.importDOM = () => importDOM;
      }
    }
  }
  return {
    ownNodeConfig,
    ownNodeType
  };
}
function $create(klass) {
  const editor = $getEditor();
  errorOnReadOnly();
  const registeredNode = editor.resolveRegisteredNodeAfterReplacements(editor.getRegisteredNode(klass));
  return new registeredNode.klass();
}
var FLIP_DIRECTION = {
  next: "previous",
  previous: "next"
};
var AbstractCaret = class {
  constructor(origin) {
    this.origin = origin;
  }
  [Symbol.iterator]() {
    return makeStepwiseIterator({
      hasNext: $isSiblingCaret,
      initial: this.getAdjacentCaret(),
      map: (caret) => caret,
      step: (caret) => caret.getAdjacentCaret()
    });
  }
  getAdjacentCaret() {
    return $getSiblingCaret(this.getNodeAtCaret(), this.direction);
  }
  getSiblingCaret() {
    return $getSiblingCaret(this.origin, this.direction);
  }
  remove() {
    const node = this.getNodeAtCaret();
    if (node) {
      node.remove();
    }
    return this;
  }
  replaceOrInsert(node, includeChildren) {
    const target = this.getNodeAtCaret();
    if (node.is(this.origin) || node.is(target)) ;
    else if (target === null) {
      this.insert(node);
    } else {
      target.replace(node, includeChildren);
    }
    return this;
  }
  splice(deleteCount, nodes, nodesDirection = "next") {
    const nodeIter = nodesDirection === this.direction ? nodes : Array.from(nodes).reverse();
    let caret = this;
    const parent = this.getParentAtCaret();
    const nodesToRemove = /* @__PURE__ */ new Map();
    for (let removeCaret = caret.getAdjacentCaret(); removeCaret !== null && nodesToRemove.size < deleteCount; removeCaret = removeCaret.getAdjacentCaret()) {
      const writableNode = removeCaret.origin.getWritable();
      nodesToRemove.set(writableNode.getKey(), writableNode);
    }
    for (const node of nodeIter) {
      if (nodesToRemove.size > 0) {
        const target = caret.getNodeAtCaret();
        if (target) {
          nodesToRemove.delete(target.getKey());
          nodesToRemove.delete(node.getKey());
          if (target.is(node) || caret.origin.is(node)) ;
          else {
            const nodeParent = node.getParent();
            if (nodeParent && nodeParent.is(parent)) {
              node.remove();
            }
            target.replace(node);
          }
        } else {
          if (!(target !== null)) {
            formatDevErrorMessage(`NodeCaret.splice: Underflow of expected nodesToRemove during splice (keys: ${Array.from(nodesToRemove).join(" ")})`);
          }
        }
      } else {
        caret.insert(node);
      }
      caret = $getSiblingCaret(node, this.direction);
    }
    for (const node of nodesToRemove.values()) {
      node.remove();
    }
    return this;
  }
};
var AbstractChildCaret = class _AbstractChildCaret extends AbstractCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "type", "child");
  }
  getLatest() {
    const origin = this.origin.getLatest();
    return origin === this.origin ? this : $getChildCaret(origin, this.direction);
  }
  /**
   * Get the SiblingCaret from this origin in the same direction.
   *
   * @param mode 'root' to return null at the root, 'shadowRoot' to return null at the root or any shadow root
   * @returns A SiblingCaret with this origin, or null if origin is a root according to mode.
   */
  getParentCaret(mode = "root") {
    return $getSiblingCaret($filterByMode(this.getParentAtCaret(), mode), this.direction);
  }
  getFlipped() {
    const dir = flipDirection(this.direction);
    return $getSiblingCaret(this.getNodeAtCaret(), dir) || $getChildCaret(this.origin, dir);
  }
  getParentAtCaret() {
    return this.origin;
  }
  getChildCaret() {
    return this;
  }
  isSameNodeCaret(other) {
    return other instanceof _AbstractChildCaret && this.direction === other.direction && this.origin.is(other.origin);
  }
  isSamePointCaret(other) {
    return this.isSameNodeCaret(other);
  }
};
var ChildCaretFirst = class extends AbstractChildCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "next");
  }
  getNodeAtCaret() {
    return this.origin.getFirstChild();
  }
  insert(node) {
    this.origin.splice(0, 0, [node]);
    return this;
  }
};
var ChildCaretLast = class extends AbstractChildCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "previous");
  }
  getNodeAtCaret() {
    return this.origin.getLastChild();
  }
  insert(node) {
    this.origin.splice(this.origin.getChildrenSize(), 0, [node]);
    return this;
  }
};
var MODE_PREDICATE = {
  root: $isRootNode,
  shadowRoot: $isRootOrShadowRoot
};
function flipDirection(direction) {
  return FLIP_DIRECTION[direction];
}
function $filterByMode(node, mode = "root") {
  return MODE_PREDICATE[mode](node) ? null : node;
}
var AbstractSiblingCaret = class _AbstractSiblingCaret extends AbstractCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "type", "sibling");
  }
  getLatest() {
    const origin = this.origin.getLatest();
    return origin === this.origin ? this : $getSiblingCaret(origin, this.direction);
  }
  getSiblingCaret() {
    return this;
  }
  getParentAtCaret() {
    return this.origin.getParent();
  }
  getChildCaret() {
    return $isElementNode(this.origin) ? $getChildCaret(this.origin, this.direction) : null;
  }
  getParentCaret(mode = "root") {
    return $getSiblingCaret($filterByMode(this.getParentAtCaret(), mode), this.direction);
  }
  getFlipped() {
    const dir = flipDirection(this.direction);
    return $getSiblingCaret(this.getNodeAtCaret(), dir) || $getChildCaret(this.origin.getParentOrThrow(), dir);
  }
  isSamePointCaret(other) {
    return other instanceof _AbstractSiblingCaret && this.direction === other.direction && this.origin.is(other.origin);
  }
  isSameNodeCaret(other) {
    return (other instanceof _AbstractSiblingCaret || other instanceof AbstractTextPointCaret) && this.direction === other.direction && this.origin.is(other.origin);
  }
};
var AbstractTextPointCaret = class _AbstractTextPointCaret extends AbstractCaret {
  constructor(origin, offset) {
    super(origin);
    __publicField(this, "type", "text");
    this.offset = offset;
  }
  getLatest() {
    const origin = this.origin.getLatest();
    return origin === this.origin ? this : $getTextPointCaret(origin, this.direction, this.offset);
  }
  getParentAtCaret() {
    return this.origin.getParent();
  }
  getChildCaret() {
    return null;
  }
  getParentCaret(mode = "root") {
    return $getSiblingCaret($filterByMode(this.getParentAtCaret(), mode), this.direction);
  }
  getFlipped() {
    return $getTextPointCaret(this.origin, flipDirection(this.direction), this.offset);
  }
  isSamePointCaret(other) {
    return other instanceof _AbstractTextPointCaret && this.direction === other.direction && this.origin.is(other.origin) && this.offset === other.offset;
  }
  isSameNodeCaret(other) {
    return (other instanceof AbstractSiblingCaret || other instanceof _AbstractTextPointCaret) && this.direction === other.direction && this.origin.is(other.origin);
  }
  getSiblingCaret() {
    return $getSiblingCaret(this.origin, this.direction);
  }
};
function $isTextPointCaret(caret) {
  return caret instanceof AbstractTextPointCaret;
}
function $isNodeCaret(caret) {
  return caret instanceof AbstractCaret;
}
function $isSiblingCaret(caret) {
  return caret instanceof AbstractSiblingCaret;
}
function $isChildCaret(caret) {
  return caret instanceof AbstractChildCaret;
}
var SiblingCaretNext = class extends AbstractSiblingCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "next");
  }
  getNodeAtCaret() {
    return this.origin.getNextSibling();
  }
  insert(node) {
    this.origin.insertAfter(node);
    return this;
  }
};
var SiblingCaretPrevious = class extends AbstractSiblingCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "previous");
  }
  getNodeAtCaret() {
    return this.origin.getPreviousSibling();
  }
  insert(node) {
    this.origin.insertBefore(node);
    return this;
  }
};
var TextPointCaretNext = class extends AbstractTextPointCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "next");
  }
  getNodeAtCaret() {
    return this.origin.getNextSibling();
  }
  insert(node) {
    this.origin.insertAfter(node);
    return this;
  }
};
var TextPointCaretPrevious = class extends AbstractTextPointCaret {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "previous");
  }
  getNodeAtCaret() {
    return this.origin.getPreviousSibling();
  }
  insert(node) {
    this.origin.insertBefore(node);
    return this;
  }
};
var TEXT_CTOR = {
  next: TextPointCaretNext,
  previous: TextPointCaretPrevious
};
var SIBLING_CTOR = {
  next: SiblingCaretNext,
  previous: SiblingCaretPrevious
};
var CHILD_CTOR = {
  next: ChildCaretFirst,
  previous: ChildCaretLast
};
function $getSiblingCaret(origin, direction) {
  return origin ? new SIBLING_CTOR[direction](origin) : null;
}
function $getTextPointCaret(origin, direction, offset) {
  return origin ? new TEXT_CTOR[direction](origin, $getTextNodeOffset(origin, offset)) : null;
}
function $getTextNodeOffset(origin, offset, mode = "error") {
  const size = origin.getTextContentSize();
  let numericOffset = offset === "next" ? size : offset === "previous" ? 0 : offset;
  if (numericOffset < 0 || numericOffset > size) {
    if (!(mode === "clamp")) {
      formatDevErrorMessage(`$getTextNodeOffset: invalid offset ${String(offset)} for size ${String(size)} at key ${origin.getKey()}`);
    }
    numericOffset = numericOffset < 0 ? 0 : size;
  }
  return numericOffset;
}
function $getTextPointCaretSlice(caret, distance) {
  return new TextPointCaretSliceImpl(caret, distance);
}
function $getChildCaret(origin, direction) {
  return $isElementNode(origin) ? new CHILD_CTOR[direction](origin) : null;
}
function $getChildCaretOrSelf(caret) {
  return caret && caret.getChildCaret() || caret;
}
function $getAdjacentChildCaret(caret) {
  return caret && $getChildCaretOrSelf(caret.getAdjacentCaret());
}
var CaretRangeImpl = class _CaretRangeImpl {
  constructor(anchor, focus, direction) {
    __publicField(this, "type", "node-caret-range");
    this.anchor = anchor;
    this.focus = focus;
    this.direction = direction;
  }
  getLatest() {
    const anchor = this.anchor.getLatest();
    const focus = this.focus.getLatest();
    return anchor === this.anchor && focus === this.focus ? this : new _CaretRangeImpl(anchor, focus, this.direction);
  }
  isCollapsed() {
    return this.anchor.isSamePointCaret(this.focus);
  }
  getTextSlices() {
    const getSlice = (k) => {
      const caret = this[k].getLatest();
      return $isTextPointCaret(caret) ? $getSliceFromTextPointCaret(caret, k) : null;
    };
    const anchorSlice = getSlice("anchor");
    const focusSlice = getSlice("focus");
    if (anchorSlice && focusSlice) {
      const {
        caret: anchorCaret
      } = anchorSlice;
      const {
        caret: focusCaret
      } = focusSlice;
      if (anchorCaret.isSameNodeCaret(focusCaret)) {
        return [$getTextPointCaretSlice(anchorCaret, focusCaret.offset - anchorCaret.offset), null];
      }
    }
    return [anchorSlice, focusSlice];
  }
  iterNodeCarets(rootMode = "root") {
    const anchor = $isTextPointCaret(this.anchor) ? this.anchor.getSiblingCaret() : this.anchor.getLatest();
    const focus = this.focus.getLatest();
    const isTextFocus = $isTextPointCaret(focus);
    const step = (state) => state.isSameNodeCaret(focus) ? null : $getAdjacentChildCaret(state) || state.getParentCaret(rootMode);
    return makeStepwiseIterator({
      hasNext: (state) => state !== null && !(isTextFocus && focus.isSameNodeCaret(state)),
      initial: anchor.isSameNodeCaret(focus) ? null : step(anchor),
      map: (state) => state,
      step
    });
  }
  [Symbol.iterator]() {
    return this.iterNodeCarets("root");
  }
};
var TextPointCaretSliceImpl = class {
  constructor(caret, distance) {
    __publicField(this, "type", "slice");
    this.caret = caret;
    this.distance = distance;
  }
  getSliceIndices() {
    const {
      distance,
      caret: {
        offset
      }
    } = this;
    const offsetB = offset + distance;
    return offsetB < offset ? [offsetB, offset] : [offset, offsetB];
  }
  getTextContent() {
    const [startIndex, endIndex] = this.getSliceIndices();
    return this.caret.origin.getTextContent().slice(startIndex, endIndex);
  }
  getTextContentSize() {
    return Math.abs(this.distance);
  }
  removeTextSlice() {
    const {
      caret: {
        origin,
        direction
      }
    } = this;
    const [indexStart, indexEnd] = this.getSliceIndices();
    const text = origin.getTextContent();
    return $getTextPointCaret(origin.setTextContent(text.slice(0, indexStart) + text.slice(indexEnd)), direction, indexStart);
  }
};
function $getSliceFromTextPointCaret(caret, anchorOrFocus) {
  const {
    direction,
    origin
  } = caret;
  const offsetB = $getTextNodeOffset(origin, anchorOrFocus === "focus" ? flipDirection(direction) : direction);
  return $getTextPointCaretSlice(caret, offsetB - caret.offset);
}
function $isTextPointCaretSlice(caretOrSlice) {
  return caretOrSlice instanceof TextPointCaretSliceImpl;
}
function $extendCaretToRange(anchor) {
  return $getCaretRange(anchor, $getSiblingCaret($getRoot(), anchor.direction));
}
function $getCollapsedCaretRange(anchor) {
  return $getCaretRange(anchor, anchor);
}
function $getCaretRange(anchor, focus) {
  if (!(anchor.direction === focus.direction)) {
    formatDevErrorMessage(`$getCaretRange: anchor and focus must be in the same direction`);
  }
  return new CaretRangeImpl(anchor, focus, anchor.direction);
}
function makeStepwiseIterator(config) {
  const {
    initial,
    hasNext,
    step,
    map
  } = config;
  let state = initial;
  return {
    [Symbol.iterator]() {
      return this;
    },
    next() {
      if (!hasNext(state)) {
        return {
          done: true,
          value: void 0
        };
      }
      const rval = {
        done: false,
        value: map(state)
      };
      state = step(state);
      return rval;
    }
  };
}
function compareNumber(a2, b2) {
  return Math.sign(a2 - b2);
}
function $comparePointCaretNext(a2, b2) {
  const compare = $getCommonAncestor(a2.origin, b2.origin);
  if (!(compare !== null)) {
    formatDevErrorMessage(`$comparePointCaretNext: a (key ${a2.origin.getKey()}) and b (key ${b2.origin.getKey()}) do not have a common ancestor`);
  }
  switch (compare.type) {
    case "same": {
      const aIsText = a2.type === "text";
      const bIsText = b2.type === "text";
      return aIsText && bIsText ? compareNumber(a2.offset, b2.offset) : a2.type === b2.type ? 0 : aIsText ? -1 : bIsText ? 1 : a2.type === "child" ? -1 : 1;
    }
    case "ancestor": {
      return a2.type === "child" ? -1 : 1;
    }
    case "descendant": {
      return b2.type === "child" ? 1 : -1;
    }
    case "branch": {
      return $getCommonAncestorResultBranchOrder(compare);
    }
  }
}
function $getCommonAncestorResultBranchOrder(compare) {
  const {
    a: a2,
    b: b2
  } = compare;
  const aKey = a2.__key;
  const bKey = b2.__key;
  let na = a2;
  let nb = b2;
  for (; na && nb; na = na.getNextSibling(), nb = nb.getNextSibling()) {
    if (na.__key === bKey) {
      return -1;
    } else if (nb.__key === aKey) {
      return 1;
    }
  }
  return na === null ? 1 : -1;
}
function $isSameNode(reference, other) {
  return other.is(reference);
}
function $initialElementTuple(node) {
  return $isElementNode(node) ? [node.getLatest(), null] : [node.getParent(), node.getLatest()];
}
function $getCommonAncestor(a2, b2) {
  if (a2.is(b2)) {
    return {
      commonAncestor: a2,
      type: "same"
    };
  }
  const aMap = /* @__PURE__ */ new Map();
  for (let [parent, child] = $initialElementTuple(a2); parent; child = parent, parent = parent.getParent()) {
    aMap.set(parent, child);
  }
  for (let [parent, child] = $initialElementTuple(b2); parent; child = parent, parent = parent.getParent()) {
    const aChild = aMap.get(parent);
    if (aChild === void 0) ;
    else if (aChild === null) {
      if (!$isSameNode(a2, parent)) {
        formatDevErrorMessage(`$originComparison: ancestor logic error`);
      }
      return {
        commonAncestor: parent,
        type: "ancestor"
      };
    } else if (child === null) {
      if (!$isSameNode(b2, parent)) {
        formatDevErrorMessage(`$originComparison: descendant logic error`);
      }
      return {
        commonAncestor: parent,
        type: "descendant"
      };
    } else {
      if (!(($isElementNode(aChild) || $isSameNode(a2, aChild)) && ($isElementNode(child) || $isSameNode(b2, child)) && parent.is(aChild.getParent()) && parent.is(child.getParent()))) {
        formatDevErrorMessage(`$originComparison: branch logic error`);
      }
      return {
        a: aChild,
        b: child,
        commonAncestor: parent,
        type: "branch"
      };
    }
  }
  return null;
}
function $caretFromPoint(point, direction) {
  const {
    type,
    key,
    offset
  } = point;
  const node = $getNodeByKeyOrThrow(point.key);
  if (type === "text") {
    if (!$isTextNode(node)) {
      formatDevErrorMessage(`$caretFromPoint: Node with type ${node.getType()} and key ${key} that does not inherit from TextNode encountered for text point`);
    }
    return $getTextPointCaret(node, direction, offset);
  }
  if (!$isElementNode(node)) {
    formatDevErrorMessage(`$caretFromPoint: Node with type ${node.getType()} and key ${key} that does not inherit from ElementNode encountered for element point`);
  }
  return $getChildCaretAtIndex(node, point.offset, direction);
}
function $setPointFromCaret(point, caret) {
  const {
    origin,
    direction
  } = caret;
  const isNext = direction === "next";
  if ($isTextPointCaret(caret)) {
    point.set(origin.getKey(), caret.offset, "text");
  } else if ($isSiblingCaret(caret)) {
    if ($isTextNode(origin)) {
      point.set(origin.getKey(), $getTextNodeOffset(origin, direction), "text");
    } else {
      point.set(origin.getParentOrThrow().getKey(), origin.getIndexWithinParent() + (isNext ? 1 : 0), "element");
    }
  } else {
    if (!($isChildCaret(caret) && $isElementNode(origin))) {
      formatDevErrorMessage(`$setPointFromCaret: exhaustiveness check`);
    }
    point.set(origin.getKey(), isNext ? 0 : origin.getChildrenSize(), "element");
  }
}
function $setSelectionFromCaretRange(caretRange) {
  const currentSelection = $getSelection();
  const selection = $isRangeSelection(currentSelection) ? currentSelection : $createRangeSelection();
  $updateRangeSelectionFromCaretRange(selection, caretRange);
  $setSelection(selection);
  return selection;
}
function $updateRangeSelectionFromCaretRange(selection, caretRange) {
  $setPointFromCaret(selection.anchor, caretRange.anchor);
  $setPointFromCaret(selection.focus, caretRange.focus);
}
function $caretRangeFromSelection(selection) {
  const {
    anchor,
    focus
  } = selection;
  const anchorCaret = $caretFromPoint(anchor, "next");
  const focusCaret = $caretFromPoint(focus, "next");
  const direction = $comparePointCaretNext(anchorCaret, focusCaret) <= 0 ? "next" : "previous";
  return $getCaretRange($getCaretInDirection(anchorCaret, direction), $getCaretInDirection(focusCaret, direction));
}
function $rewindSiblingCaret(caret) {
  const {
    direction,
    origin
  } = caret;
  const rewindOrigin = $getSiblingCaret(origin, flipDirection(direction)).getNodeAtCaret();
  return rewindOrigin ? $getSiblingCaret(rewindOrigin, direction) : $getChildCaret(origin.getParentOrThrow(), direction);
}
function $getAnchorCandidates(anchor, rootMode = "root") {
  const carets = [anchor];
  for (let parent = $isChildCaret(anchor) ? anchor.getParentCaret(rootMode) : anchor.getSiblingCaret(); parent !== null; parent = parent.getParentCaret(rootMode)) {
    carets.push($rewindSiblingCaret(parent));
  }
  return carets;
}
function $isCaretAttached(caret) {
  return !!caret && caret.origin.isAttached();
}
function $removeTextFromCaretRange(initialRange, sliceMode = "removeEmptySlices") {
  if (initialRange.isCollapsed()) {
    return initialRange;
  }
  const rootMode = "root";
  const nextDirection = "next";
  let sliceState = sliceMode;
  const range = $getCaretRangeInDirection(initialRange, nextDirection);
  const anchorCandidates = $getAnchorCandidates(range.anchor, rootMode);
  const focusCandidates = $getAnchorCandidates(range.focus.getFlipped(), rootMode);
  const seenStart = /* @__PURE__ */ new Set();
  const removedNodes = [];
  for (const caret of range.iterNodeCarets(rootMode)) {
    if ($isChildCaret(caret)) {
      seenStart.add(caret.origin.getKey());
    } else if ($isSiblingCaret(caret)) {
      const {
        origin
      } = caret;
      if (!$isElementNode(origin) || seenStart.has(origin.getKey())) {
        removedNodes.push(origin);
      }
    }
  }
  for (const node of removedNodes) {
    node.remove();
  }
  for (const slice of range.getTextSlices()) {
    if (!slice) {
      continue;
    }
    const {
      origin
    } = slice.caret;
    const contentSize = origin.getTextContentSize();
    const caretBefore = $rewindSiblingCaret($getSiblingCaret(origin, nextDirection));
    const mode = origin.getMode();
    if (Math.abs(slice.distance) === contentSize && sliceState === "removeEmptySlices" || mode === "token" && slice.distance !== 0) {
      caretBefore.remove();
    } else if (slice.distance !== 0) {
      sliceState = "removeEmptySlices";
      let nextCaret = slice.removeTextSlice();
      const sliceOrigin = slice.caret.origin;
      if (mode === "segmented") {
        const src = nextCaret.origin;
        const plainTextNode = $createTextNode(src.getTextContent()).setStyle(src.getStyle()).setFormat(src.getFormat());
        caretBefore.replaceOrInsert(plainTextNode);
        nextCaret = $getTextPointCaret(plainTextNode, nextDirection, nextCaret.offset);
      }
      if (sliceOrigin.is(anchorCandidates[0].origin)) {
        anchorCandidates[0] = nextCaret;
      }
      if (sliceOrigin.is(focusCandidates[0].origin)) {
        focusCandidates[0] = nextCaret.getFlipped();
      }
    }
  }
  let anchorCandidate;
  let focusCandidate;
  for (const candidate of anchorCandidates) {
    if ($isCaretAttached(candidate)) {
      anchorCandidate = $normalizeCaret(candidate);
      break;
    }
  }
  for (const candidate of focusCandidates) {
    if ($isCaretAttached(candidate)) {
      focusCandidate = $normalizeCaret(candidate);
      break;
    }
  }
  const mergeTargets = $getBlockMergeTargets(anchorCandidate, focusCandidate, seenStart);
  if (mergeTargets) {
    const [anchorBlock, focusBlock] = mergeTargets;
    $getChildCaret(anchorBlock, "previous").splice(0, focusBlock.getChildren());
    focusBlock.remove();
  }
  const bestCandidate = [anchorCandidate, focusCandidate, ...anchorCandidates, ...focusCandidates].find($isCaretAttached);
  if (bestCandidate) {
    const anchor = $getCaretInDirection($normalizeCaret(bestCandidate), initialRange.direction);
    return $getCollapsedCaretRange(anchor);
  }
  {
    formatDevErrorMessage(`$removeTextFromCaretRange: selection was lost, could not find a new anchor given candidates with keys: ${JSON.stringify(anchorCandidates.map((n2) => n2.origin.__key))}`);
  }
}
function $getBlockMergeTargets(anchor, focus, seenStart) {
  if (!anchor || !focus) {
    return null;
  }
  const anchorParent = anchor.getParentAtCaret();
  const focusParent = focus.getParentAtCaret();
  if (!anchorParent || !focusParent) {
    return null;
  }
  const anchorElements = anchorParent.getParents().reverse();
  anchorElements.push(anchorParent);
  const focusElements = focusParent.getParents().reverse();
  focusElements.push(focusParent);
  const maxLen = Math.min(anchorElements.length, focusElements.length);
  let commonAncestorCount;
  for (commonAncestorCount = 0; commonAncestorCount < maxLen && anchorElements[commonAncestorCount] === focusElements[commonAncestorCount]; commonAncestorCount++) {
  }
  const $getBlock = (arr, predicate) => {
    let block;
    for (let i2 = commonAncestorCount; i2 < arr.length; i2++) {
      const ancestor = arr[i2];
      if ($isRootOrShadowRoot(ancestor)) {
        return;
      } else if (!block && predicate(ancestor)) {
        block = ancestor;
      }
    }
    return block;
  };
  const anchorBlock = $getBlock(anchorElements, INTERNAL_$isBlock);
  const focusBlock = anchorBlock && $getBlock(focusElements, (node) => seenStart.has(node.getKey()) && INTERNAL_$isBlock(node));
  return anchorBlock && focusBlock ? [anchorBlock, focusBlock] : null;
}
function $getDeepestChildOrSelf(initialCaret) {
  let caret = initialCaret;
  while ($isChildCaret(caret)) {
    const adjacent = $getAdjacentChildCaret(caret);
    if (!$isChildCaret(adjacent)) {
      break;
    }
    caret = adjacent;
  }
  return caret;
}
function $normalizeCaret(initialCaret) {
  const caret = $getDeepestChildOrSelf(initialCaret.getLatest());
  const {
    direction
  } = caret;
  if ($isTextNode(caret.origin)) {
    return $isTextPointCaret(caret) ? caret : $getTextPointCaret(caret.origin, direction, direction);
  }
  const adj = caret.getAdjacentCaret();
  return $isSiblingCaret(adj) && $isTextNode(adj.origin) ? $getTextPointCaret(adj.origin, direction, flipDirection(direction)) : caret;
}
function $isExtendableTextPointCaret(caret) {
  return $isTextPointCaret(caret) && caret.offset !== $getTextNodeOffset(caret.origin, caret.direction);
}
function $getCaretInDirection(caret, direction) {
  return caret.direction === direction ? caret : caret.getFlipped();
}
function $getCaretRangeInDirection(range, direction) {
  if (range.direction === direction) {
    return range;
  }
  return $getCaretRange(
    // focus and anchor get flipped here
    $getCaretInDirection(range.focus, direction),
    $getCaretInDirection(range.anchor, direction)
  );
}
function $getChildCaretAtIndex(parent, index, direction) {
  let caret = $getChildCaret(parent, "next");
  for (let i2 = 0; i2 < index; i2++) {
    const nextCaret = caret.getAdjacentCaret();
    if (nextCaret === null) {
      break;
    }
    caret = nextCaret;
  }
  return $getCaretInDirection(caret, direction);
}
function $getAdjacentSiblingOrParentSiblingCaret(startCaret, rootMode = "root") {
  let depthDiff = 0;
  let caret = startCaret;
  let nextCaret = $getAdjacentChildCaret(caret);
  while (nextCaret === null) {
    depthDiff--;
    nextCaret = caret.getParentCaret(rootMode);
    if (!nextCaret) {
      return null;
    }
    caret = nextCaret;
    nextCaret = $getAdjacentChildCaret(caret);
  }
  return nextCaret && [nextCaret, depthDiff];
}
function $getAdjacentNodes(initialCaret) {
  const siblings = [];
  for (let caret = initialCaret.getAdjacentCaret(); caret; caret = caret.getAdjacentCaret()) {
    siblings.push(caret.origin);
  }
  return siblings;
}
function $splitTextPointCaret(textPointCaret) {
  const {
    origin,
    offset,
    direction
  } = textPointCaret;
  if (offset === $getTextNodeOffset(origin, direction)) {
    return textPointCaret.getSiblingCaret();
  } else if (offset === $getTextNodeOffset(origin, flipDirection(direction))) {
    return $rewindSiblingCaret(textPointCaret.getSiblingCaret());
  }
  const [textNode] = origin.splitText(offset);
  if (!$isTextNode(textNode)) {
    formatDevErrorMessage(`$splitTextPointCaret: splitText must return at least one TextNode`);
  }
  return $getCaretInDirection($getSiblingCaret(textNode, "next"), direction);
}
function $alwaysSplit(_node, _edge) {
  return true;
}
function $splitAtPointCaretNext(pointCaret, {
  $copyElementNode = $copyNode,
  $splitTextPointCaretNext = $splitTextPointCaret,
  rootMode = "shadowRoot",
  $shouldSplit = $alwaysSplit
} = {}) {
  if ($isTextPointCaret(pointCaret)) {
    return $splitTextPointCaretNext(pointCaret);
  }
  const parentCaret = pointCaret.getParentCaret(rootMode);
  if (parentCaret) {
    const {
      origin
    } = parentCaret;
    if ($isChildCaret(pointCaret) && !(origin.canBeEmpty() && $shouldSplit(origin, "first"))) {
      return $rewindSiblingCaret(parentCaret);
    }
    const siblings = $getAdjacentNodes(pointCaret);
    if (siblings.length > 0 || origin.canBeEmpty() && $shouldSplit(origin, "last")) {
      parentCaret.insert($copyElementNode(origin).splice(0, 0, siblings));
    }
  }
  return parentCaret;
}

// ../node_modules/.pnpm/lexical@0.35.0/node_modules/lexical/Lexical.prod.mjs
function t(t2, ...e2) {
  const n2 = new URL("https://lexical.dev/docs/error"), r2 = new URLSearchParams();
  r2.append("code", t2);
  for (const t3 of e2) r2.append("v", t3);
  throw n2.search = r2.toString(), Error(`Minified Lexical error #${t2}; visit ${n2.toString()} for the full message or use the non-minified dev environment for full errors and additional helpful warnings.`);
}
var e = "undefined" != typeof window && void 0 !== window.document && void 0 !== window.document.createElement;
var n = e && "documentMode" in document ? document.documentMode : null;
var r = e && /Mac|iPod|iPhone|iPad/.test(navigator.platform);
var i = e && /^(?!.*Seamonkey)(?=.*Firefox).*/i.test(navigator.userAgent);
var s = !(!e || !("InputEvent" in window) || n) && "getTargetRanges" in new window.InputEvent("input");
var o = e && /Version\/[\d.]+.*Safari/.test(navigator.userAgent);
var l = e && /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
var c = e && /Android/.test(navigator.userAgent);
var a = e && /^(?=.*Chrome).*/i.test(navigator.userAgent);
var u = e && c && a;
var f = e && /AppleWebKit\/[\d.]+/.test(navigator.userAgent) && !a;
function d(...t2) {
  const e2 = [];
  for (const n2 of t2) if (n2 && "string" == typeof n2) for (const [t3] of n2.matchAll(/\S+/g)) e2.push(t3);
  return e2;
}
var h = 1;
var g = 3;
var _ = 9;
var p = 11;
var y = 0;
var m = 1;
var x = 2;
var C = 0;
var S = 1;
var v = 2;
var N = 4;
var b = 8;
var M = 128;
var A = 1792 | (112 | (3 | N | b) | M);
var P = 1;
var D = 2;
var F = 3;
var L = 4;
var I = 5;
var K = 6;
var z = o || l || f ? " " : "​";
var B = "\n\n";
var R = i ? " " : z;
var W = { bold: 1, capitalize: 1024, code: 16, highlight: M, italic: 2, lowercase: 256, strikethrough: N, subscript: 32, superscript: 64, underline: b, uppercase: 512 };
var J = { directionless: 1, unmergeable: 2 };
var $ = { center: D, end: K, justify: L, left: P, right: F, start: I };
var j = { [D]: "center", [K]: "end", [L]: "justify", [P]: "left", [F]: "right", [I]: "start" };
var U = { normal: 0, segmented: 2, token: 1 };
var V = { [C]: "normal", [v]: "segmented", [S]: "token" };
var H = "$config";
function q(t2, e2, n2, r2, i2, s2) {
  let o2 = t2.getFirstChild();
  for (; null !== o2; ) {
    const t3 = o2.__key;
    o2.__parent === e2 && (hi(o2) && q(o2, t3, n2, r2, i2, s2), n2.has(t3) || s2.delete(t3), i2.push(t3)), o2 = o2.getNextSibling();
  }
}
var G = 100;
var X = false;
var Q = 0;
function Z(t2) {
  Q = t2.timeStamp;
}
function tt(t2, e2, n2) {
  const r2 = "BR" === t2.nodeName, i2 = e2.__lexicalLineBreak;
  return i2 && (t2 === i2 || r2 && t2.previousSibling === i2) || r2 && void 0 !== ys(t2, n2);
}
function et(t2, e2, n2) {
  const r2 = ho(no(n2));
  let i2 = null, s2 = null;
  null !== r2 && r2.anchorNode === t2 && (i2 = r2.anchorOffset, s2 = r2.focusOffset);
  const o2 = t2.nodeValue;
  null !== o2 && Ms(e2, o2, i2, s2, false);
}
function nt(t2, e2, n2) {
  if (ar(t2)) {
    const e3 = t2.anchor.getNode();
    if (e3.is(n2) && t2.format !== e3.getFormat()) return false;
  }
  return ss(e2) && n2.isAttached();
}
function rt(t2, e2, n2, r2) {
  for (let i2 = t2; i2 && !Ao(i2); i2 = qs(i2)) {
    const t3 = ys(i2, e2);
    if (void 0 !== t3) {
      const e3 = _s(t3, n2);
      if (e3) return pi(e3) || !yo(i2) ? void 0 : [i2, e3];
    } else if (i2 === r2) return [r2, vs(n2)];
  }
}
function it(t2, e2, n2) {
  X = true;
  const r2 = performance.now() - Q > G;
  try {
    ai(t2, (() => {
      const s2 = wr() || (function(t3) {
        return t3.getEditorState().read((() => {
          const t4 = wr();
          return null !== t4 ? t4.clone() : null;
        }));
      })(t2), o2 = /* @__PURE__ */ new Map(), l2 = t2.getRootElement(), c2 = t2._editorState, a2 = t2._blockCursorElement;
      let u2 = false, f2 = "";
      for (let n3 = 0; n3 < e2.length; n3++) {
        const d3 = e2[n3], h2 = d3.type, g2 = d3.target, _2 = rt(g2, t2, c2, l2);
        if (!_2) continue;
        const [p2, y2] = _2;
        if ("characterData" === h2) r2 && Zn(y2) && ss(g2) && nt(s2, g2, y2) && et(g2, y2, t2);
        else if ("childList" === h2) {
          u2 = true;
          const e3 = d3.addedNodes;
          for (let n5 = 0; n5 < e3.length; n5++) {
            const r4 = e3[n5], s3 = ps(r4), o3 = r4.parentNode;
            if (null != o3 && r4 !== a2 && null === s3 && !tt(r4, o3, t2)) {
              if (i) {
                const t3 = (yo(r4) ? r4.innerText : null) || r4.nodeValue;
                t3 && (f2 += t3);
              }
              o3.removeChild(r4);
            }
          }
          const n4 = d3.removedNodes, r3 = n4.length;
          if (r3 > 0) {
            let e4 = 0;
            for (let i2 = 0; i2 < r3; i2++) {
              const r4 = n4[i2];
              (tt(r4, g2, t2) || a2 === r4) && (g2.appendChild(r4), e4++);
            }
            r3 !== e4 && o2.set(p2, y2);
          }
        }
      }
      if (o2.size > 0) for (const [e3, n3] of o2) n3.reconcileObservedMutation(e3, t2);
      const d2 = n2.takeRecords();
      if (d2.length > 0) {
        for (let e3 = 0; e3 < d2.length; e3++) {
          const n3 = d2[e3], r3 = n3.addedNodes, i2 = n3.target;
          for (let e4 = 0; e4 < r3.length; e4++) {
            const n4 = r3[e4], s3 = n4.parentNode;
            null == s3 || "BR" !== n4.nodeName || tt(n4, i2, t2) || s3.removeChild(n4);
          }
        }
        n2.takeRecords();
      }
      null !== s2 && (u2 && ks(s2), i && Us(t2) && s2.insertRawText(f2));
    }));
  } finally {
    X = false;
  }
}
function st(t2) {
  const e2 = t2._observer;
  if (null !== e2) {
    it(t2, e2.takeRecords(), e2);
  }
}
function ot(t2) {
  !(function(t3) {
    0 === Q && no(t3).addEventListener("textInput", Z, true);
  })(t2), t2._observer = new MutationObserver(((e2, n2) => {
    it(t2, e2, n2);
  }));
}
var ht = class _ht {
  constructor(t2, e2, n2 = void 0, r2 = /* @__PURE__ */ new Map(), i2 = void 0) {
    this.node = t2, this.sharedNodeState = e2, this.unknownState = n2, this.knownState = r2;
    const { sharedConfigMap: s2 } = this.sharedNodeState, o2 = void 0 !== i2 ? i2 : (function(t3, e3, n3) {
      let r3 = n3.size;
      if (e3) for (const i3 in e3) {
        const e4 = t3.get(i3);
        e4 && n3.has(e4) || r3++;
      }
      return r3;
    })(s2, n2, r2);
    this.size = o2;
  }
  getValue(t2) {
    const e2 = this.knownState.get(t2);
    if (void 0 !== e2) return e2;
    this.sharedNodeState.sharedConfigMap.set(t2.key, t2);
    let n2 = t2.defaultValue;
    if (this.unknownState && t2.key in this.unknownState) {
      const e3 = this.unknownState[t2.key];
      void 0 !== e3 && (n2 = t2.parse(e3)), this.updateFromKnown(t2, n2);
    }
    return n2;
  }
  getInternalState() {
    return [this.unknownState, this.knownState];
  }
  toJSON() {
    const t2 = { ...this.unknownState }, e2 = {};
    for (const [e3, n2] of this.knownState) e3.isEqual(n2, e3.defaultValue) ? delete t2[e3.key] : t2[e3.key] = e3.unparse(n2);
    for (const n2 of this.sharedNodeState.flatKeys) n2 in t2 && (e2[n2] = t2[n2], delete t2[n2]);
    return pt(t2) && (e2.$ = t2), e2;
  }
  getWritable(t2) {
    if (this.node === t2) return this;
    const { sharedNodeState: e2, unknownState: n2 } = this, r2 = new Map(this.knownState);
    return new _ht(t2, e2, (function(t3, e3, n3) {
      let r3;
      if (n3) for (const [i2, s2] of Object.entries(n3)) {
        const n4 = t3.get(i2);
        n4 ? e3.has(n4) || e3.set(n4, n4.parse(s2)) : (r3 = r3 || {}, r3[i2] = s2);
      }
      return r3;
    })(e2.sharedConfigMap, r2, n2), r2, this.size);
  }
  updateFromKnown(t2, e2) {
    const n2 = t2.key;
    this.sharedNodeState.sharedConfigMap.set(n2, t2);
    const { knownState: r2, unknownState: i2 } = this;
    r2.has(t2) || i2 && n2 in i2 || (i2 && (delete i2[n2], this.unknownState = pt(i2)), this.size++), r2.set(t2, e2);
  }
  updateFromUnknown(t2, e2) {
    const n2 = this.sharedNodeState.sharedConfigMap.get(t2);
    n2 ? this.updateFromKnown(n2, n2.parse(e2)) : (this.unknownState = this.unknownState || {}, t2 in this.unknownState || this.size++, this.unknownState[t2] = e2);
  }
  updateFromJSON(t2) {
    const { knownState: e2 } = this;
    for (const t3 of e2.keys()) e2.set(t3, t3.defaultValue);
    if (this.size = e2.size, this.unknownState = void 0, t2) for (const [e3, n2] of Object.entries(t2)) this.updateFromUnknown(e3, n2);
  }
};
function gt(t2) {
  const e2 = t2.getWritable(), n2 = e2.__state ? e2.__state.getWritable(e2) : new ht(e2, _t(e2));
  return e2.__state = n2, n2;
}
function _t(t2) {
  return t2.__state ? t2.__state.sharedNodeState : Hi(To(), t2.getType()).sharedNodeState;
}
function pt(t2) {
  if (t2) for (const e2 in t2) return t2;
}
function mt(t2, e2, n2) {
  for (const [r2, i2] of e2.knownState) {
    if (t2.has(r2.key)) continue;
    t2.add(r2.key);
    const e3 = n2 ? n2.getValue(r2) : r2.defaultValue;
    if (e3 !== i2 && !r2.isEqual(e3, i2)) return true;
  }
  return false;
}
function xt(t2, e2, n2) {
  const { unknownState: r2 } = e2, i2 = n2 ? n2.unknownState : void 0;
  if (r2) for (const [e3, n3] of Object.entries(r2)) {
    if (t2.has(e3)) continue;
    t2.add(e3);
    if (n3 !== (i2 ? i2[e3] : void 0)) return true;
  }
  return false;
}
function Ct(t2, e2) {
  const n2 = t2.__state;
  return n2 && n2.node === t2 ? n2.getWritable(e2) : n2;
}
function St(t2, e2) {
  const n2 = t2.__mode, r2 = t2.__format, i2 = t2.__style, s2 = e2.__mode, o2 = e2.__format, l2 = e2.__style, c2 = t2.__state, a2 = e2.__state;
  return (null === n2 || n2 === s2) && (null === r2 || r2 === o2) && (null === i2 || i2 === l2) && (null === t2.__state || c2 === a2 || (function(t3, e3) {
    if (t3 === e3) return true;
    if (t3 && e3 && t3.size !== e3.size) return false;
    const n3 = /* @__PURE__ */ new Set();
    return !(t3 && mt(n3, t3, e3) || e3 && mt(n3, e3, t3) || t3 && xt(n3, t3, e3) || e3 && xt(n3, e3, t3));
  })(c2, a2));
}
function vt(t2, e2) {
  const n2 = t2.mergeWithSibling(e2), r2 = Gr()._normalizedNodes;
  return r2.add(t2.__key), r2.add(e2.__key), n2;
}
function kt(t2) {
  let e2, n2, r2 = t2;
  if ("" !== r2.__text || !r2.isSimpleText() || r2.isUnmergeable()) {
    for (; null !== (e2 = r2.getPreviousSibling()) && Zn(e2) && e2.isSimpleText() && !e2.isUnmergeable(); ) {
      if ("" !== e2.__text) {
        if (St(e2, r2)) {
          r2 = vt(e2, r2);
          break;
        }
        break;
      }
      e2.remove();
    }
    for (; null !== (n2 = r2.getNextSibling()) && Zn(n2) && n2.isSimpleText() && !n2.isUnmergeable(); ) {
      if ("" !== n2.__text) {
        if (St(r2, n2)) {
          r2 = vt(r2, n2);
          break;
        }
        break;
      }
      n2.remove();
    }
  } else r2.remove();
}
function Tt(t2) {
  return Nt(t2.anchor), Nt(t2.focus), t2;
}
function Nt(t2) {
  for (; "element" === t2.type; ) {
    const e2 = t2.getNode(), n2 = t2.offset;
    let r2, i2;
    if (n2 === e2.getChildrenSize() ? (r2 = e2.getChildAtIndex(n2 - 1), i2 = true) : (r2 = e2.getChildAtIndex(n2), i2 = false), Zn(r2)) {
      t2.set(r2.__key, i2 ? r2.getTextContentSize() : 0, "text", true);
      break;
    }
    if (!hi(r2)) break;
    t2.set(r2.__key, i2 ? r2.getChildrenSize() : 0, "element", true);
  }
}
var bt;
var wt;
var Et;
var Ot;
var Mt;
var At;
var Pt;
var Dt;
var Ft;
var Lt;
var It = "";
var Kt = null;
var zt = "";
var Bt = "";
var Rt = false;
var Wt = false;
function Jt(t2, e2) {
  const n2 = Pt.get(t2);
  if (null !== e2) {
    const n3 = se(t2);
    n3.parentNode === e2 && e2.removeChild(n3);
  }
  if (Dt.has(t2) || wt._keyToDOMMap.delete(t2), hi(n2)) {
    const t3 = te(n2, Pt);
    $t(t3, 0, t3.length - 1, null);
  }
  void 0 !== n2 && Ws(Lt, Et, Ot, n2, "destroyed");
}
function $t(t2, e2, n2, r2) {
  let i2 = e2;
  for (; i2 <= n2; ++i2) {
    const e3 = t2[i2];
    void 0 !== e3 && Jt(e3, r2);
  }
}
function jt(t2, e2) {
  t2.setProperty("text-align", e2);
}
var Ut = "40px";
function Vt(t2, e2) {
  const n2 = bt.theme.indent;
  if ("string" == typeof n2) {
    const r3 = t2.classList.contains(n2);
    e2 > 0 && !r3 ? t2.classList.add(n2) : e2 < 1 && r3 && t2.classList.remove(n2);
  }
  const r2 = getComputedStyle(t2).getPropertyValue("--lexical-indent-base-value") || Ut;
  t2.style.setProperty("padding-inline-start", 0 === e2 ? "" : `calc(${e2} * ${r2})`);
}
function Yt(t2, e2) {
  const n2 = t2.style;
  0 === e2 ? jt(n2, "") : e2 === P ? jt(n2, "left") : e2 === D ? jt(n2, "center") : e2 === F ? jt(n2, "right") : e2 === L ? jt(n2, "justify") : e2 === I ? jt(n2, "start") : e2 === K && jt(n2, "end");
}
function Ht(t2, e2) {
  const n2 = (function(t3) {
    const e3 = t3.__dir;
    if (null !== e3) return e3;
    if (mi(t3)) return null;
    const n3 = t3.getParentOrThrow();
    return mi(n3) && null === n3.__dir ? "auto" : null;
  })(e2);
  null !== n2 ? t2.dir = n2 : t2.removeAttribute("dir");
}
function qt(e2, n2) {
  const r2 = Dt.get(e2);
  void 0 === r2 && t(60);
  const i2 = r2.createDOM(bt, wt);
  if ((function(t2, e3, n3) {
    const r3 = n3._keyToDOMMap;
    (function(t3, e4, n4) {
      const r4 = `__lexicalKey_${e4._key}`;
      t3[r4] = n4;
    })(e3, n3, t2), r3.set(t2, e3);
  })(e2, i2, wt), Zn(r2) ? i2.setAttribute("data-lexical-text", "true") : pi(r2) && i2.setAttribute("data-lexical-decorator", "true"), hi(r2)) {
    const t2 = r2.__indent, e3 = r2.__size;
    if (Ht(i2, r2), 0 !== t2 && Vt(i2, t2), 0 !== e3) {
      const t3 = e3 - 1;
      Gt(te(r2, Dt), r2, 0, t3, r2.getDOMSlot(i2));
    }
    const n3 = r2.__format;
    0 !== n3 && Yt(i2, n3), r2.isInline() || Qt(null, r2, i2), Ys(r2) && (It += B, Bt += B);
  } else {
    const t2 = r2.getTextContent();
    if (pi(r2)) {
      const t3 = r2.decorate(wt, bt);
      null !== t3 && ne(e2, t3), i2.contentEditable = "false";
    }
    It += t2, Bt += t2;
  }
  return null !== n2 && n2.insertChild(i2), Ws(Lt, Et, Ot, r2, "created"), i2;
}
function Gt(t2, e2, n2, r2, i2) {
  const s2 = It;
  It = "";
  let o2 = n2;
  for (; o2 <= r2; ++o2) {
    qt(t2[o2], i2);
    const e3 = Dt.get(t2[o2]);
    null !== e3 && Zn(e3) && (null === Kt && (Kt = e3.getFormat()), "" === zt && (zt = e3.getStyle()));
  }
  Ys(e2) && (It += B);
  i2.element.__lexicalTextContent = It, It = s2 + It;
}
function Xt(t2, e2) {
  if (t2) {
    const n2 = t2.__last;
    if (n2) {
      const t3 = e2.get(n2);
      if (t3) return Ln(t3) ? "line-break" : pi(t3) && t3.isInline() ? "decorator" : null;
    }
    return "empty";
  }
  return null;
}
function Qt(t2, e2, n2) {
  const r2 = Xt(t2, Pt), i2 = Xt(e2, Dt);
  r2 !== i2 && e2.getDOMSlot(n2).setManagedLineBreak(i2);
}
function Zt(e2, n2, r2) {
  var i2;
  Kt = null, zt = "", (function(e3, n3, r3) {
    const i3 = It, s2 = e3.__size, o2 = n3.__size;
    It = "";
    const l2 = r3.element;
    if (1 === s2 && 1 === o2) {
      const t2 = e3.__first, r4 = n3.__first;
      if (t2 === r4) ee(t2, l2);
      else {
        const e4 = se(t2), n4 = qt(r4, null);
        try {
          l2.replaceChild(n4, e4);
        } catch (i5) {
          if ("object" == typeof i5 && null != i5) {
            const s3 = `${i5.toString()} Parent: ${l2.tagName}, new child: {tag: ${n4.tagName} key: ${r4}}, old child: {tag: ${e4.tagName}, key: ${t2}}.`;
            throw new Error(s3);
          }
          throw i5;
        }
        Jt(t2, null);
      }
      const i4 = Dt.get(r4);
      Zn(i4) && (null === Kt && (Kt = i4.getFormat()), "" === zt && (zt = i4.getStyle()));
    } else {
      const i4 = te(e3, Pt), c2 = te(n3, Dt);
      if (i4.length !== s2 && t(227), c2.length !== o2 && t(228), 0 === s2) 0 !== o2 && Gt(c2, n3, 0, o2 - 1, r3);
      else if (0 === o2) {
        if (0 !== s2) {
          const t2 = null == r3.after && null == r3.before && null == r3.element.__lexicalLineBreak;
          $t(i4, 0, s2 - 1, t2 ? null : l2), t2 && (l2.textContent = "");
        }
      } else !(function(t2, e4, n4, r4, i5, s3) {
        const o3 = r4 - 1, l3 = i5 - 1;
        let c3, a2, u2 = s3.getFirstChild(), f2 = 0, d2 = 0;
        for (; f2 <= o3 && d2 <= l3; ) {
          const t3 = e4[f2], r5 = n4[d2];
          if (t3 === r5) u2 = re(ee(r5, s3.element)), f2++, d2++;
          else {
            void 0 === c3 && (c3 = new Set(e4)), void 0 === a2 && (a2 = new Set(n4));
            const i7 = a2.has(t3), o4 = c3.has(r5);
            if (i7) if (o4) {
              const t4 = Hs(wt, r5);
              t4 === u2 ? u2 = re(ee(r5, s3.element)) : (s3.withBefore(u2).insertChild(t4), ee(r5, s3.element)), f2++, d2++;
            } else qt(r5, s3.withBefore(u2)), d2++;
            else u2 = re(se(t3)), Jt(t3, s3.element), f2++;
          }
          const i6 = Dt.get(r5);
          null !== i6 && Zn(i6) && (null === Kt && (Kt = i6.getFormat()), "" === zt && (zt = i6.getStyle()));
        }
        const h2 = f2 > o3, g2 = d2 > l3;
        if (h2 && !g2) {
          const e5 = n4[l3 + 1], r5 = void 0 === e5 ? null : wt.getElementByKey(e5);
          Gt(n4, t2, d2, l3, s3.withBefore(r5));
        } else g2 && !h2 && $t(e4, f2, o3, s3.element);
      })(n3, i4, c2, s2, o2, r3);
    }
    Ys(n3) && (It += B);
    l2.__lexicalTextContent = It, It = i3 + It;
  })(e2, n2, n2.getDOMSlot(r2)), i2 = n2, null == Kt || Kt === i2.__textFormat || Wt || i2.setTextFormat(Kt), (function(t2) {
    "" === zt || zt === t2.__textStyle || Wt || t2.setTextStyle(zt);
  })(n2);
}
function te(e2, n2) {
  const r2 = [];
  let i2 = e2.__first;
  for (; null !== i2; ) {
    const e3 = n2.get(i2);
    void 0 === e3 && t(101), r2.push(i2), i2 = e3.__next;
  }
  return r2;
}
function ee(e2, n2) {
  const r2 = Pt.get(e2);
  let i2 = Dt.get(e2);
  void 0 !== r2 && void 0 !== i2 || t(61);
  const s2 = Rt || At.has(e2) || Mt.has(e2), o2 = Hs(wt, e2);
  if (r2 === i2 && !s2) {
    if (hi(r2)) {
      const t2 = o2.__lexicalTextContent;
      void 0 !== t2 && (It += t2, Bt += t2);
    } else {
      const t2 = r2.getTextContent();
      Bt += t2, It += t2;
    }
    return o2;
  }
  if (r2 !== i2 && s2 && Ws(Lt, Et, Ot, i2, "updated"), i2.updateDOM(r2, o2, bt)) {
    const r3 = qt(e2, null);
    return null === n2 && t(62), n2.replaceChild(r3, o2), Jt(e2, null), r3;
  }
  if (hi(r2) && hi(i2)) {
    const t2 = i2.__indent;
    (Rt || t2 !== r2.__indent) && Vt(o2, t2);
    const e3 = i2.__format;
    if ((Rt || e3 !== r2.__format) && Yt(o2, e3), s2 && (Zt(r2, i2, o2), mi(i2) || i2.isInline() || Qt(r2, i2, o2)), Ys(i2) && (It += B, Bt += B), (Rt || i2.__dir !== r2.__dir) && (Ht(o2, i2), mi(i2) && !Rt)) {
      for (const t3 of i2.getChildren()) if (hi(t3)) {
        Ht(Hs(wt, t3.getKey()), t3);
      }
    }
  } else {
    const t2 = i2.getTextContent();
    if (pi(i2)) {
      const t3 = i2.decorate(wt, bt);
      null !== t3 && ne(e2, t3);
    }
    It += t2, Bt += t2;
  }
  if (!Wt && mi(i2) && i2.__cachedText !== Bt) {
    const t2 = i2.getWritable();
    t2.__cachedText = Bt, i2 = t2;
  }
  return o2;
}
function ne(t2, e2) {
  let n2 = wt._pendingDecorators;
  const r2 = wt._decorators;
  if (null === n2) {
    if (r2[t2] === e2) return;
    n2 = xs(wt);
  }
  n2[t2] = e2;
}
function re(t2) {
  let e2 = t2.nextSibling;
  return null !== e2 && e2 === wt._blockCursorElement && (e2 = e2.nextSibling), e2;
}
function ie(t2, e2, n2, r2, i2, s2) {
  It = "", Bt = "", Rt = r2 === x, wt = n2, bt = n2._config, Et = n2._nodes, Ot = wt._listeners.mutation, Mt = i2, At = s2, Pt = t2._nodeMap, Dt = e2._nodeMap, Wt = e2._readOnly, Ft = new Map(n2._keyToDOMMap);
  const o2 = /* @__PURE__ */ new Map();
  return Lt = o2, ee("root", null), wt = void 0, Et = void 0, Mt = void 0, At = void 0, Pt = void 0, Dt = void 0, bt = void 0, Ft = void 0, Lt = void 0, o2;
}
function se(e2) {
  const n2 = Ft.get(e2);
  return void 0 === n2 && t(75, e2), n2;
}
function oe(t2) {
  return { type: t2 };
}
var le = oe("SELECTION_CHANGE_COMMAND");
var ce = oe("SELECTION_INSERT_CLIPBOARD_NODES_COMMAND");
var ae = oe("CLICK_COMMAND");
var ue = oe("DELETE_CHARACTER_COMMAND");
var fe = oe("INSERT_LINE_BREAK_COMMAND");
var de = oe("INSERT_PARAGRAPH_COMMAND");
var he = oe("CONTROLLED_TEXT_INSERTION_COMMAND");
var ge = oe("PASTE_COMMAND");
var _e = oe("REMOVE_TEXT_COMMAND");
var pe = oe("DELETE_WORD_COMMAND");
var ye = oe("DELETE_LINE_COMMAND");
var me = oe("FORMAT_TEXT_COMMAND");
var xe = oe("UNDO_COMMAND");
var Ce = oe("REDO_COMMAND");
var Se = oe("KEYDOWN_COMMAND");
var ve = oe("KEY_ARROW_RIGHT_COMMAND");
var ke = oe("MOVE_TO_END");
var Te = oe("KEY_ARROW_LEFT_COMMAND");
var Ne = oe("MOVE_TO_START");
var be = oe("KEY_ARROW_UP_COMMAND");
var we = oe("KEY_ARROW_DOWN_COMMAND");
var Ee = oe("KEY_ENTER_COMMAND");
var Oe = oe("KEY_SPACE_COMMAND");
var Me = oe("KEY_BACKSPACE_COMMAND");
var Ae = oe("KEY_ESCAPE_COMMAND");
var Pe = oe("KEY_DELETE_COMMAND");
var De = oe("KEY_TAB_COMMAND");
var Fe = oe("INSERT_TAB_COMMAND");
var Le = oe("INDENT_CONTENT_COMMAND");
var Ie = oe("OUTDENT_CONTENT_COMMAND");
var Ke = oe("DROP_COMMAND");
var ze = oe("FORMAT_ELEMENT_COMMAND");
var Be = oe("DRAGSTART_COMMAND");
var Re = oe("DRAGOVER_COMMAND");
var We = oe("DRAGEND_COMMAND");
var Je = oe("COPY_COMMAND");
var $e = oe("CUT_COMMAND");
var je = oe("SELECT_ALL_COMMAND");
var Ue = oe("CLEAR_EDITOR_COMMAND");
var Ve = oe("CLEAR_HISTORY_COMMAND");
var Ye = oe("CAN_REDO_COMMAND");
var He = oe("CAN_UNDO_COMMAND");
var qe = oe("FOCUS_COMMAND");
var Ge = oe("BLUR_COMMAND");
var Xe = oe("KEY_MODIFIER_COMMAND");
var Qe = Object.freeze({});
var Ze = 30;
var tn = [["keydown", function(t2, e2) {
  if (en = t2.timeStamp, nn = t2.key, e2.isComposing()) return;
  if (Vs(e2, Se, t2)) return;
  if (null == t2.key) return;
  if (fn && Ks(t2)) return ai(e2, (() => {
    Sn(e2, dn);
  })), fn = false, void (dn = "");
  if ((function(t3) {
    return Fs(t3, "ArrowRight", { shiftKey: "any" });
  })(t2)) Vs(e2, ve, t2);
  else if ((function(t3) {
    return Fs(t3, "ArrowRight", Ls);
  })(t2)) Vs(e2, ke, t2);
  else if ((function(t3) {
    return Fs(t3, "ArrowLeft", { shiftKey: "any" });
  })(t2)) Vs(e2, Te, t2);
  else if ((function(t3) {
    return Fs(t3, "ArrowLeft", Ls);
  })(t2)) Vs(e2, Ne, t2);
  else if ((function(t3) {
    return Fs(t3, "ArrowUp", { altKey: "any", shiftKey: "any" });
  })(t2)) Vs(e2, be, t2);
  else if ((function(t3) {
    return Fs(t3, "ArrowDown", { altKey: "any", shiftKey: "any" });
  })(t2)) Vs(e2, we, t2);
  else if ((function(t3) {
    return Fs(t3, "Enter", { altKey: "any", ctrlKey: "any", metaKey: "any", shiftKey: true });
  })(t2)) an = true, Vs(e2, Ee, t2);
  else if ((function(t3) {
    return " " === t3.key;
  })(t2)) Vs(e2, Oe, t2);
  else if ((function(t3) {
    return r && Fs(t3, "o", { ctrlKey: true });
  })(t2)) t2.preventDefault(), an = true, Vs(e2, fe, true);
  else if ((function(t3) {
    return Fs(t3, "Enter", { altKey: "any", ctrlKey: "any", metaKey: "any" });
  })(t2)) an = false, Vs(e2, Ee, t2);
  else if ((function(t3) {
    return Fs(t3, "Backspace", { shiftKey: "any" }) || r && Fs(t3, "h", { ctrlKey: true });
  })(t2)) Ks(t2) ? Vs(e2, Me, t2) : (t2.preventDefault(), Vs(e2, ue, true));
  else if ((function(t3) {
    return "Escape" === t3.key;
  })(t2)) Vs(e2, Ae, t2);
  else if ((function(t3) {
    return Fs(t3, "Delete", {}) || r && Fs(t3, "d", { ctrlKey: true });
  })(t2)) !(function(t3) {
    return "Delete" === t3.key;
  })(t2) ? (t2.preventDefault(), Vs(e2, ue, false)) : Vs(e2, Pe, t2);
  else if ((function(t3) {
    return Fs(t3, "Backspace", Is);
  })(t2)) t2.preventDefault(), Vs(e2, pe, true);
  else if ((function(t3) {
    return Fs(t3, "Delete", Is);
  })(t2)) t2.preventDefault(), Vs(e2, pe, false);
  else if ((function(t3) {
    return r && Fs(t3, "Backspace", { metaKey: true });
  })(t2)) t2.preventDefault(), Vs(e2, ye, true);
  else if ((function(t3) {
    return r && (Fs(t3, "Delete", { metaKey: true }) || Fs(t3, "k", { ctrlKey: true }));
  })(t2)) t2.preventDefault(), Vs(e2, ye, false);
  else if ((function(t3) {
    return Fs(t3, "b", Ls);
  })(t2)) t2.preventDefault(), Vs(e2, me, "bold");
  else if ((function(t3) {
    return Fs(t3, "u", Ls);
  })(t2)) t2.preventDefault(), Vs(e2, me, "underline");
  else if ((function(t3) {
    return Fs(t3, "i", Ls);
  })(t2)) t2.preventDefault(), Vs(e2, me, "italic");
  else if ((function(t3) {
    return Fs(t3, "Tab", { shiftKey: "any" });
  })(t2)) Vs(e2, De, t2);
  else if ((function(t3) {
    return Fs(t3, "z", Ls);
  })(t2)) t2.preventDefault(), Vs(e2, xe, void 0);
  else if ((function(t3) {
    if (r) return Fs(t3, "z", { metaKey: true, shiftKey: true });
    return Fs(t3, "y", { ctrlKey: true }) || Fs(t3, "z", { ctrlKey: true, shiftKey: true });
  })(t2)) t2.preventDefault(), Vs(e2, Ce, void 0);
  else {
    const n2 = e2._editorState._selection;
    null === n2 || ar(n2) ? zs(t2) && (t2.preventDefault(), Vs(e2, je, t2)) : !(function(t3) {
      return Fs(t3, "c", Ls);
    })(t2) ? !(function(t3) {
      return Fs(t3, "x", Ls);
    })(t2) ? zs(t2) && (t2.preventDefault(), Vs(e2, je, t2)) : (t2.preventDefault(), Vs(e2, $e, t2)) : (t2.preventDefault(), Vs(e2, Je, t2));
  }
  (function(t3) {
    return t3.ctrlKey || t3.shiftKey || t3.altKey || t3.metaKey;
  })(t2) && Vs(e2, Xe, t2);
}], ["pointerdown", function(t2, e2) {
  const n2 = t2.target, r2 = t2.pointerType;
  mo(n2) && "touch" !== r2 && "pen" !== r2 && 0 === t2.button && ai(e2, (() => {
    Xi(n2) || (cn = true);
  }));
}], ["compositionstart", function(t2, e2) {
  ai(e2, (() => {
    const n2 = wr();
    if (ar(n2) && !e2.isComposing()) {
      const r2 = n2.anchor, i2 = n2.anchor.getNode();
      hs(r2.key), (t2.timeStamp < en + Ze || "element" === r2.type || !n2.isCollapsed() || i2.getFormat() !== n2.format || Zn(i2) && i2.getStyle() !== n2.style) && Vs(e2, he, R);
    }
  }));
}], ["compositionend", function(t2, e2) {
  i ? un = true : l || !o && !f ? ai(e2, (() => {
    Sn(e2, t2.data);
  })) : (fn = true, dn = t2.data);
}], ["input", function(t2, e2) {
  t2.stopPropagation(), ai(e2, (() => {
    if (yo(t2.target) && Xi(t2.target)) return;
    const n2 = wr(), r2 = t2.data, c2 = Cn(t2);
    if (null != r2 && ar(n2) && _n(n2, c2, r2, t2.timeStamp, false)) {
      un && (Sn(e2, r2), un = false);
      const c3 = n2.anchor.getNode(), a2 = ho(no(e2));
      if (null === a2) return;
      const u2 = n2.isBackward(), d2 = u2 ? n2.anchor.offset : n2.focus.offset, h2 = u2 ? n2.focus.offset : n2.anchor.offset;
      s && !n2.isCollapsed() && Zn(c3) && null !== a2.anchorNode && c3.getTextContent().slice(0, d2) + r2 + c3.getTextContent().slice(d2 + h2) === Es(a2.anchorNode) || Vs(e2, he, r2);
      const g2 = r2.length;
      i && g2 > 1 && "insertCompositionText" === t2.inputType && !e2.isComposing() && (n2.anchor.offset -= g2), o || l || f || !e2.isComposing() || (en = 0, hs(null));
    } else {
      Os(false, e2, null !== r2 ? r2 : void 0), un && (Sn(e2, r2 || void 0), un = false);
    }
    !(function() {
      Yr();
      const t3 = Gr();
      st(t3);
    })();
  }), { event: t2 }), sn = null;
}], ["click", function(t2, e2) {
  ai(e2, (() => {
    const n2 = wr(), r2 = ho(no(e2)), i2 = Er();
    if (r2) {
      if (ar(n2)) {
        const e3 = n2.anchor, s2 = e3.getNode();
        if ("element" === e3.type && 0 === e3.offset && n2.isCollapsed() && !mi(s2) && 1 === Ss().getChildrenSize() && s2.getTopLevelElementOrThrow().isEmpty() && null !== i2 && n2.is(i2)) r2.removeAllRanges(), n2.dirty = true;
        else if (3 === t2.detail && !n2.isCollapsed()) {
          if (s2 !== n2.focus.getNode()) {
            const t3 = (function(t4, e4) {
              let n3 = t4;
              for (; n3 !== Ss() && null != n3; ) {
                if (e4(n3)) return n3;
                n3 = n3.getParent();
              }
              return null;
            })(s2, ((t4) => hi(t4) && !t4.isInline()));
            hi(t3) && t3.select(0);
          }
        }
      } else if ("touch" === t2.pointerType || "pen" === t2.pointerType) {
        const n3 = r2.anchorNode;
        if (yo(n3) || ss(n3)) {
          ks(br(i2, r2, e2, t2));
        }
      }
    }
    Vs(e2, ae, t2);
  }));
}], ["cut", Qe], ["copy", Qe], ["dragstart", Qe], ["dragover", Qe], ["dragend", Qe], ["paste", Qe], ["focus", Qe], ["blur", Qe], ["drop", Qe]];
s && tn.push(["beforeinput", (e2, n2) => (function(e3, n3) {
  const r2 = e3.inputType, s2 = Cn(e3);
  if ("deleteCompositionText" === r2 || i && Us(n3)) return;
  if ("insertCompositionText" === r2) return;
  ai(n3, (() => {
    const i2 = wr();
    if ("deleteContentBackward" === r2) {
      if (null === i2) {
        const t2 = Er();
        if (!ar(t2)) return;
        ks(t2.clone());
      }
      if (ar(i2)) {
        const r3 = i2.anchor.key === i2.focus.key;
        if (o2 = e3.timeStamp, "MediaLast" === nn && o2 < en + Ze && n3.isComposing() && r3) {
          if (hs(null), en = 0, setTimeout((() => {
            ai(n3, (() => {
              hs(null);
            }));
          }), Ze), ar(i2)) {
            const e4 = i2.anchor.getNode();
            e4.markDirty(), Zn(e4) || t(142), xn(i2, e4);
          }
        } else {
          hs(null), e3.preventDefault();
          const t2 = i2.anchor.getNode(), s3 = t2.getTextContent(), o3 = t2.canInsertTextAfter(), l2 = 0 === i2.anchor.offset && i2.focus.offset === s3.length;
          let c3 = u && r3 && !l2 && o3;
          if (c3 && i2.isCollapsed() && (c3 = !pi(js(i2.anchor, true))), !c3) {
            Vs(n3, ue, true);
            const t3 = wr();
            u && ar(t3) && t3.isCollapsed() && (hn = t3, setTimeout((() => hn = null)));
          }
        }
        return;
      }
    }
    var o2;
    if (!ar(i2)) return;
    const c2 = e3.data;
    null !== sn && Os(false, n3, sn), i2.dirty && null === sn || !i2.isCollapsed() || mi(i2.anchor.getNode()) || null === s2 || i2.applyDOMRange(s2), sn = null;
    const a2 = i2.anchor, f2 = i2.focus, d2 = a2.getNode(), h2 = f2.getNode();
    if ("insertText" !== r2 && "insertTranspose" !== r2) switch (e3.preventDefault(), r2) {
      case "insertFromYank":
      case "insertFromDrop":
      case "insertReplacementText":
        Vs(n3, he, e3);
        break;
      case "insertFromComposition":
        hs(null), Vs(n3, he, e3);
        break;
      case "insertLineBreak":
        hs(null), Vs(n3, fe, false);
        break;
      case "insertParagraph":
        hs(null), an && !l ? (an = false, Vs(n3, fe, false)) : Vs(n3, de, void 0);
        break;
      case "insertFromPaste":
      case "insertFromPasteAsQuotation":
        Vs(n3, ge, e3);
        break;
      case "deleteByComposition":
        (function(t2, e4) {
          return t2 !== e4 || hi(t2) || hi(e4) || !rs(t2) || !rs(e4);
        })(d2, h2) && Vs(n3, _e, e3);
        break;
      case "deleteByDrag":
      case "deleteByCut":
        Vs(n3, _e, e3);
        break;
      case "deleteContent":
        Vs(n3, ue, false);
        break;
      case "deleteWordBackward":
        Vs(n3, pe, true);
        break;
      case "deleteWordForward":
        Vs(n3, pe, false);
        break;
      case "deleteHardLineBackward":
      case "deleteSoftLineBackward":
        Vs(n3, ye, true);
        break;
      case "deleteContentForward":
      case "deleteHardLineForward":
      case "deleteSoftLineForward":
        Vs(n3, ye, false);
        break;
      case "formatStrikeThrough":
        Vs(n3, me, "strikethrough");
        break;
      case "formatBold":
        Vs(n3, me, "bold");
        break;
      case "formatItalic":
        Vs(n3, me, "italic");
        break;
      case "formatUnderline":
        Vs(n3, me, "underline");
        break;
      case "historyUndo":
        Vs(n3, xe, void 0);
        break;
      case "historyRedo":
        Vs(n3, Ce, void 0);
    }
    else {
      if ("\n" === c2) e3.preventDefault(), Vs(n3, fe, false);
      else if (c2 === B) e3.preventDefault(), Vs(n3, de, void 0);
      else if (null == c2 && e3.dataTransfer) {
        const t2 = e3.dataTransfer.getData("text/plain");
        e3.preventDefault(), i2.insertRawText(t2);
      } else null != c2 && _n(i2, s2, c2, e3.timeStamp, true) ? (e3.preventDefault(), Vs(n3, he, c2)) : sn = c2;
      rn = e3.timeStamp;
    }
  }));
})(e2, n2)]);
var en = 0;
var nn = null;
var rn = 0;
var sn = null;
var on = /* @__PURE__ */ new WeakMap();
var ln = false;
var cn = false;
var an = false;
var un = false;
var fn = false;
var dn = "";
var hn = null;
var gn = [0, "", 0, "root", 0];
function _n(t2, e2, n2, r2, i2) {
  const o2 = t2.anchor, l2 = t2.focus, c2 = o2.getNode(), a2 = Gr(), u2 = ho(no(a2)), f2 = null !== u2 ? u2.anchorNode : null, d2 = o2.key, h2 = a2.getElementByKey(d2), g2 = n2.length;
  return d2 !== l2.key || !Zn(c2) || (!i2 && (!s || rn < r2 + 50) || c2.isDirty() && g2 < 2 || Ns(n2)) && o2.offset !== l2.offset && !c2.isComposing() || is(c2) || c2.isDirty() && g2 > 1 || (i2 || !s) && null !== h2 && !c2.isComposing() && f2 !== ls(h2) || null !== u2 && null !== e2 && (!e2.collapsed || e2.startContainer !== u2.anchorNode || e2.startOffset !== u2.anchorOffset) || c2.getFormat() !== t2.format || c2.getStyle() !== t2.style || (function(t3, e3) {
    if (e3.isSegmented()) return true;
    if (!t3.isCollapsed()) return false;
    const n3 = t3.anchor.offset, r3 = e3.getParentOrThrow(), i3 = rs(e3);
    return 0 === n3 ? !e3.canInsertTextBefore() || !r3.canInsertTextBefore() && !e3.isComposing() || i3 || (function(t4) {
      const e4 = t4.getPreviousSibling();
      return (Zn(e4) || hi(e4) && e4.isInline()) && !e4.canInsertTextAfter();
    })(e3) : n3 === e3.getTextContentSize() && (!e3.canInsertTextAfter() || !r3.canInsertTextAfter() && !e3.isComposing() || i3);
  })(t2, c2);
}
function pn(t2, e2) {
  return ss(t2) && null !== t2.nodeValue && 0 !== e2 && e2 !== t2.nodeValue.length;
}
function yn(e2, n2, r2) {
  const { anchorNode: i2, anchorOffset: s2, focusNode: o2, focusOffset: l2 } = e2;
  ln && (ln = false, pn(i2, s2) && pn(o2, l2) && !hn) || ai(n2, (() => {
    if (!r2) return void ks(null);
    if (!Zi(n2, i2, o2)) return;
    let c2 = wr();
    if (hn && ar(c2) && c2.isCollapsed()) {
      const t2 = c2.anchor, e3 = hn.anchor;
      (t2.key === e3.key && t2.offset === e3.offset + 1 || 1 === t2.offset && e3.getNode().is(t2.getNode().getPreviousSibling())) && (c2 = hn.clone(), ks(c2));
    }
    if (hn = null, ar(c2)) {
      const r3 = c2.anchor, i3 = r3.getNode();
      if (c2.isCollapsed()) {
        "Range" === e2.type && e2.anchorNode === e2.focusNode && (c2.dirty = true);
        const s3 = no(n2).event, o3 = s3 ? s3.timeStamp : performance.now(), [l3, a2, u2, f2, d2] = gn, h2 = Ss(), g2 = false === n2.isComposing() && "" === h2.getTextContent();
        if (o3 < d2 + 200 && r3.offset === u2 && r3.key === f2) mn(c2, l3, a2);
        else if ("text" === r3.type) Zn(i3) || t(141), xn(c2, i3);
        else if ("element" === r3.type && !g2) {
          hi(i3) || t(259);
          const e3 = r3.getNode();
          e3.isEmpty() ? (function(t2, e4) {
            const n3 = e4.getTextFormat(), r4 = e4.getTextStyle();
            mn(t2, n3, r4);
          })(c2, e3) : mn(c2, 0, "");
        }
      } else {
        const t2 = r3.key, e3 = c2.focus.key, n3 = c2.getNodes(), i4 = n3.length, o3 = c2.isBackward(), a2 = o3 ? l2 : s2, u2 = o3 ? s2 : l2, f2 = o3 ? e3 : t2, d2 = o3 ? t2 : e3;
        let h2 = A, g2 = false;
        for (let t3 = 0; t3 < i4; t3++) {
          const e4 = n3[t3], r4 = e4.getTextContentSize();
          if (Zn(e4) && 0 !== r4 && !(0 === t3 && e4.__key === f2 && a2 === r4 || t3 === i4 - 1 && e4.__key === d2 && 0 === u2) && (g2 = true, h2 &= e4.getFormat(), 0 === h2)) break;
        }
        c2.format = g2 ? h2 : 0;
      }
    }
    Vs(n2, le, void 0);
  }));
}
function mn(t2, e2, n2) {
  t2.format === e2 && t2.style === n2 || (t2.format = e2, t2.style = n2, t2.dirty = true);
}
function xn(t2, e2) {
  mn(t2, e2.getFormat(), e2.getStyle());
}
function Cn(t2) {
  if (!t2.getTargetRanges) return null;
  const e2 = t2.getTargetRanges();
  return 0 === e2.length ? null : e2[0];
}
function Sn(t2, e2) {
  const n2 = t2._compositionKey;
  if (hs(null), null !== n2 && null != e2) {
    if ("" === e2) {
      const e3 = _s(n2), r2 = ls(t2.getElementByKey(n2));
      return void (null !== r2 && null !== r2.nodeValue && Zn(e3) && Ms(e3, r2.nodeValue, null, null, true));
    }
    if ("\n" === e2[e2.length - 1]) {
      const e3 = wr();
      if (ar(e3)) {
        const n3 = e3.focus;
        return e3.anchor.set(n3.key, n3.offset, n3.type), void Vs(t2, Ee, null);
      }
    }
  }
  Os(true, t2, e2);
}
function vn(t2) {
  let e2 = t2.__lexicalEventHandles;
  return void 0 === e2 && (e2 = [], t2.__lexicalEventHandles = e2), e2;
}
var kn = /* @__PURE__ */ new Map();
function Tn(t2) {
  const e2 = go(t2.target);
  if (null === e2) return;
  const n2 = es(e2.anchorNode);
  if (null === n2) return;
  cn && (cn = false, ai(n2, (() => {
    const r3 = Er(), i3 = e2.anchorNode;
    if (yo(i3) || ss(i3)) {
      ks(br(r3, e2, n2, t2));
    }
  })));
  const r2 = bs(n2), i2 = r2[r2.length - 1], s2 = i2._key, o2 = kn.get(s2), l2 = o2 || i2;
  l2 !== n2 && yn(e2, l2, false), yn(e2, n2, true), n2 !== i2 ? kn.set(s2, n2) : o2 && kn.delete(s2);
}
function Nn(t2) {
  t2._lexicalHandled = true;
}
function bn(t2) {
  return true === t2._lexicalHandled;
}
var wn = () => {
};
function En(e2) {
  const n2 = e2.ownerDocument, r2 = on.get(n2);
  if (void 0 === r2) return void wn();
  const i2 = r2 - 1;
  i2 >= 0 || t(164), on.set(n2, i2), 0 === i2 && n2.removeEventListener("selectionchange", Tn);
  const s2 = ns(e2);
  ts(s2) ? (!(function(t2) {
    if (null !== t2._parentEditor) {
      const e3 = bs(t2), n3 = e3[e3.length - 1]._key;
      kn.get(n3) === t2 && kn.delete(n3);
    } else kn.delete(t2._key);
  })(s2), e2.__lexicalEditor = null) : s2 && t(198);
  const o2 = vn(e2);
  for (let t2 = 0; t2 < o2.length; t2++) o2[t2]();
  e2.__lexicalEventHandles = [];
}
function On(t2, e2, n2) {
  Yr();
  const r2 = t2.__key, i2 = t2.getParent();
  if (null === i2) return;
  const s2 = (function(t3) {
    const e3 = wr();
    if (!ar(e3) || !hi(t3)) return e3;
    const { anchor: n3, focus: r3 } = e3, i3 = n3.getNode(), s3 = r3.getNode();
    to(i3, t3) && n3.set(t3.__key, 0, "element");
    to(s3, t3) && r3.set(t3.__key, 0, "element");
    return e3;
  })(t2);
  let o2 = false;
  if (ar(s2) && e2) {
    const e3 = s2.anchor, n3 = s2.focus;
    e3.key === r2 && (Ar(e3, t2, i2, t2.getPreviousSibling(), t2.getNextSibling()), o2 = true), n3.key === r2 && (Ar(n3, t2, i2, t2.getPreviousSibling(), t2.getNextSibling()), o2 = true);
  } else fr(s2) && e2 && t2.isSelected() && t2.selectPrevious();
  if (ar(s2) && e2 && !o2) {
    const e3 = t2.getIndexWithinParent();
    fs(t2), Or(s2, i2, e3, -1);
  } else fs(t2);
  n2 || so(i2) || i2.canBeEmpty() || !i2.isEmpty() || On(i2, e2), e2 && s2 && mi(i2) && i2.isEmpty() && i2.selectEnd();
}
var An = class {
  static getType() {
    const { ownNodeType: e2 } = Do(this);
    return void 0 === e2 && t(64, this.name), e2;
  }
  static clone(e2) {
    t(65, this.name);
  }
  $config() {
    return {};
  }
  config(t2, e2) {
    const n2 = e2.extends || Object.getPrototypeOf(this.constructor);
    return Object.assign(e2, { extends: n2, type: t2 }), { [t2]: e2 };
  }
  afterCloneFrom(t2) {
    this.__key === t2.__key ? (this.__parent = t2.__parent, this.__next = t2.__next, this.__prev = t2.__prev, this.__state = t2.__state) : t2.__state && (this.__state = t2.__state.getWritable(this));
  }
  constructor(t2) {
    this.__type = this.constructor.getType(), this.__parent = null, this.__prev = null, this.__next = null, Object.defineProperty(this, "__state", { configurable: true, enumerable: false, value: void 0, writable: true }), us(this, t2);
  }
  getType() {
    return this.__type;
  }
  isInline() {
    t(137, this.constructor.name);
  }
  isAttached() {
    let t2 = this.__key;
    for (; null !== t2; ) {
      if ("root" === t2) return true;
      const e2 = _s(t2);
      if (null === e2) break;
      t2 = e2.__parent;
    }
    return false;
  }
  isSelected(t2) {
    const e2 = t2 || wr();
    if (null == e2) return false;
    const n2 = e2.getNodes().some(((t3) => t3.__key === this.__key));
    if (Zn(this)) return n2;
    if (ar(e2) && "element" === e2.anchor.type && "element" === e2.focus.type) {
      if (e2.isCollapsed()) return false;
      const t3 = this.getParent();
      if (pi(this) && this.isInline() && t3) {
        const n3 = e2.isBackward() ? e2.focus : e2.anchor;
        if (t3.is(n3.getNode()) && n3.offset === t3.getChildrenSize() && this.is(t3.getLastChild())) return false;
      }
    }
    return n2;
  }
  getKey() {
    return this.__key;
  }
  getIndexWithinParent() {
    const t2 = this.getParent();
    if (null === t2) return -1;
    let e2 = t2.getFirstChild(), n2 = 0;
    for (; null !== e2; ) {
      if (this.is(e2)) return n2;
      n2++, e2 = e2.getNextSibling();
    }
    return -1;
  }
  getParent() {
    const t2 = this.getLatest().__parent;
    return null === t2 ? null : _s(t2);
  }
  getParentOrThrow() {
    const e2 = this.getParent();
    return null === e2 && t(66, this.__key), e2;
  }
  getTopLevelElement() {
    let e2 = this;
    for (; null !== e2; ) {
      const n2 = e2.getParent();
      if (so(n2)) return hi(e2) || e2 === this && pi(e2) || t(194), e2;
      e2 = n2;
    }
    return null;
  }
  getTopLevelElementOrThrow() {
    const e2 = this.getTopLevelElement();
    return null === e2 && t(67, this.__key), e2;
  }
  getParents() {
    const t2 = [];
    let e2 = this.getParent();
    for (; null !== e2; ) t2.push(e2), e2 = e2.getParent();
    return t2;
  }
  getParentKeys() {
    const t2 = [];
    let e2 = this.getParent();
    for (; null !== e2; ) t2.push(e2.__key), e2 = e2.getParent();
    return t2;
  }
  getPreviousSibling() {
    const t2 = this.getLatest().__prev;
    return null === t2 ? null : _s(t2);
  }
  getPreviousSiblings() {
    const t2 = [], e2 = this.getParent();
    if (null === e2) return t2;
    let n2 = e2.getFirstChild();
    for (; null !== n2 && !n2.is(this); ) t2.push(n2), n2 = n2.getNextSibling();
    return t2;
  }
  getNextSibling() {
    const t2 = this.getLatest().__next;
    return null === t2 ? null : _s(t2);
  }
  getNextSiblings() {
    const t2 = [];
    let e2 = this.getNextSibling();
    for (; null !== e2; ) t2.push(e2), e2 = e2.getNextSibling();
    return t2;
  }
  getCommonAncestor(t2) {
    const e2 = hi(this) ? this : this.getParent(), n2 = hi(t2) ? t2 : t2.getParent(), r2 = e2 && n2 ? gl(e2, n2) : null;
    return r2 ? r2.commonAncestor : null;
  }
  is(t2) {
    return null != t2 && this.__key === t2.__key;
  }
  isBefore(e2) {
    const n2 = gl(this, e2);
    return null !== n2 && ("descendant" === n2.type || ("branch" === n2.type ? -1 === fl(n2) : ("same" !== n2.type && "ancestor" !== n2.type && t(279), false)));
  }
  isParentOf(t2) {
    const e2 = gl(this, t2);
    return null !== e2 && "ancestor" === e2.type;
  }
  getNodesBetween(e2) {
    const n2 = this.isBefore(e2), r2 = [], i2 = /* @__PURE__ */ new Set();
    let s2 = this;
    for (; null !== s2; ) {
      const o2 = s2.__key;
      if (i2.has(o2) || (i2.add(o2), r2.push(s2)), s2 === e2) break;
      const l2 = hi(s2) ? n2 ? s2.getFirstChild() : s2.getLastChild() : null;
      if (null !== l2) {
        s2 = l2;
        continue;
      }
      const c2 = n2 ? s2.getNextSibling() : s2.getPreviousSibling();
      if (null !== c2) {
        s2 = c2;
        continue;
      }
      const a2 = s2.getParentOrThrow();
      if (i2.has(a2.__key) || r2.push(a2), a2 === e2) break;
      let u2 = null, f2 = a2;
      do {
        if (null === f2 && t(68), u2 = n2 ? f2.getNextSibling() : f2.getPreviousSibling(), f2 = f2.getParent(), null === f2) break;
        null !== u2 || i2.has(f2.__key) || r2.push(f2);
      } while (null === u2);
      s2 = u2;
    }
    return n2 || r2.reverse(), r2;
  }
  isDirty() {
    const t2 = Gr()._dirtyLeaves;
    return null !== t2 && t2.has(this.__key);
  }
  getLatest() {
    const e2 = _s(this.__key);
    return null === e2 && t(113), e2;
  }
  getWritable() {
    Yr();
    const t2 = qr(), e2 = Gr(), n2 = t2._nodeMap, r2 = this.__key, i2 = this.getLatest(), s2 = e2._cloneNotNeeded, o2 = wr();
    if (null !== o2 && o2.setCachedNodes(null), s2.has(r2)) return ds(i2), i2;
    const l2 = Eo(i2);
    return s2.add(r2), ds(l2), n2.set(r2, l2), l2;
  }
  getTextContent() {
    return "";
  }
  getTextContentSize() {
    return this.getTextContent().length;
  }
  createDOM(e2, n2) {
    t(70);
  }
  updateDOM(e2, n2, r2) {
    t(71);
  }
  exportDOM(t2) {
    return { element: this.createDOM(t2._config, t2) };
  }
  exportJSON() {
    const t2 = this.__state ? this.__state.toJSON() : void 0;
    return { type: this.__type, version: 1, ...t2 };
  }
  static importJSON(e2) {
    t(18, this.name);
  }
  updateFromJSON(t2) {
    return (function(t3, e2) {
      const n2 = t3.getWritable(), r2 = e2.$;
      let i2 = r2;
      for (const t4 of _t(n2).flatKeys) t4 in e2 && (void 0 !== i2 && i2 !== r2 || (i2 = { ...r2 }), i2[t4] = e2[t4]);
      return (n2.__state || i2) && gt(t3).updateFromJSON(i2), n2;
    })(this, t2);
  }
  static transform() {
    return null;
  }
  remove(t2) {
    On(this, true, t2);
  }
  replace(e2, n2) {
    Yr();
    let r2 = wr();
    null !== r2 && (r2 = r2.clone()), co(this, e2);
    const i2 = this.getLatest(), s2 = this.__key, o2 = e2.__key, l2 = e2.getWritable(), c2 = this.getParentOrThrow().getWritable(), a2 = c2.__size;
    fs(l2);
    const u2 = i2.getPreviousSibling(), f2 = i2.getNextSibling(), d2 = i2.__prev, h2 = i2.__next, g2 = i2.__parent;
    if (On(i2, false, true), null === u2) c2.__first = o2;
    else {
      u2.getWritable().__next = o2;
    }
    if (l2.__prev = d2, null === f2) c2.__last = o2;
    else {
      f2.getWritable().__prev = o2;
    }
    if (l2.__next = h2, l2.__parent = g2, c2.__size = a2, n2 && (hi(this) && hi(l2) || t(139), this.getChildren().forEach(((t2) => {
      l2.append(t2);
    }))), ar(r2)) {
      ks(r2);
      const t2 = r2.anchor, e3 = r2.focus;
      t2.key === s2 && lr(t2, l2), e3.key === s2 && lr(e3, l2);
    }
    return gs() === s2 && hs(o2), l2;
  }
  insertAfter(t2, e2 = true) {
    Yr(), co(this, t2);
    const n2 = this.getWritable(), r2 = t2.getWritable(), i2 = r2.getParent(), s2 = wr();
    let o2 = false, l2 = false;
    if (null !== i2) {
      const e3 = t2.getIndexWithinParent();
      if (fs(r2), ar(s2)) {
        const t3 = i2.__key, n3 = s2.anchor, r3 = s2.focus;
        o2 = "element" === n3.type && n3.key === t3 && n3.offset === e3 + 1, l2 = "element" === r3.type && r3.key === t3 && r3.offset === e3 + 1;
      }
    }
    const c2 = this.getNextSibling(), a2 = this.getParentOrThrow().getWritable(), u2 = r2.__key, f2 = n2.__next;
    if (null === c2) a2.__last = u2;
    else {
      c2.getWritable().__prev = u2;
    }
    if (a2.__size++, n2.__next = u2, r2.__next = f2, r2.__prev = n2.__key, r2.__parent = n2.__parent, e2 && ar(s2)) {
      const t3 = this.getIndexWithinParent();
      Or(s2, a2, t3 + 1);
      const e3 = a2.__key;
      o2 && s2.anchor.set(e3, t3 + 2, "element"), l2 && s2.focus.set(e3, t3 + 2, "element");
    }
    return t2;
  }
  insertBefore(t2, e2 = true) {
    Yr(), co(this, t2);
    const n2 = this.getWritable(), r2 = t2.getWritable(), i2 = r2.__key;
    fs(r2);
    const s2 = this.getPreviousSibling(), o2 = this.getParentOrThrow().getWritable(), l2 = n2.__prev, c2 = this.getIndexWithinParent();
    if (null === s2) o2.__first = i2;
    else {
      s2.getWritable().__next = i2;
    }
    o2.__size++, n2.__prev = i2, r2.__prev = l2, r2.__next = n2.__key, r2.__parent = n2.__parent;
    const a2 = wr();
    if (e2 && ar(a2)) {
      Or(a2, this.getParentOrThrow(), c2);
    }
    return t2;
  }
  isParentRequired() {
    return false;
  }
  createParentElementNode() {
    return Fi();
  }
  selectStart() {
    return this.selectPrevious();
  }
  selectEnd() {
    return this.selectNext(0, 0);
  }
  selectPrevious(t2, e2) {
    Yr();
    const n2 = this.getPreviousSibling(), r2 = this.getParentOrThrow();
    if (null === n2) return r2.select(0, 0);
    if (hi(n2)) return n2.select();
    if (!Zn(n2)) {
      const t3 = n2.getIndexWithinParent() + 1;
      return r2.select(t3, t3);
    }
    return n2.select(t2, e2);
  }
  selectNext(t2, e2) {
    Yr();
    const n2 = this.getNextSibling(), r2 = this.getParentOrThrow();
    if (null === n2) return r2.select();
    if (hi(n2)) return n2.select(0, 0);
    if (!Zn(n2)) {
      const t3 = n2.getIndexWithinParent();
      return r2.select(t3, t3);
    }
    return n2.select(t2, e2);
  }
  markDirty() {
    this.getWritable();
  }
  reconcileObservedMutation(t2, e2) {
    this.markDirty();
  }
};
var Pn = class _Pn extends An {
  static getType() {
    return "linebreak";
  }
  static clone(t2) {
    return new _Pn(t2.__key);
  }
  constructor(t2) {
    super(t2);
  }
  getTextContent() {
    return "\n";
  }
  createDOM() {
    return document.createElement("br");
  }
  updateDOM() {
    return false;
  }
  isInline() {
    return true;
  }
  static importDOM() {
    return { br: (t2) => (function(t3) {
      const e2 = t3.parentElement;
      if (null !== e2 && So(e2)) {
        const n2 = e2.firstChild;
        if (n2 === t3 || n2.nextSibling === t3 && In(n2)) {
          const n3 = e2.lastChild;
          if (n3 === t3 || n3.previousSibling === t3 && In(n3)) return true;
        }
      }
      return false;
    })(t2) || (function(t3) {
      const e2 = t3.parentElement;
      if (null !== e2 && So(e2)) {
        const n2 = e2.firstChild;
        if (n2 === t3 || n2.nextSibling === t3 && In(n2)) return false;
        const r2 = e2.lastChild;
        if (r2 === t3 || r2.previousSibling === t3 && In(r2)) return true;
      }
      return false;
    })(t2) ? null : { conversion: Dn, priority: 0 } };
  }
  static importJSON(t2) {
    return Fn().updateFromJSON(t2);
  }
};
function Dn(t2) {
  return { node: Fn() };
}
function Fn() {
  return lo(new Pn());
}
function Ln(t2) {
  return t2 instanceof Pn;
}
function In(t2) {
  return ss(t2) && /^( |\t|\r?\n)+$/.test(t2.textContent || "");
}
function Kn(t2, e2) {
  return 16 & e2 ? "code" : e2 & M ? "mark" : 32 & e2 ? "sub" : 64 & e2 ? "sup" : null;
}
function zn(t2, e2) {
  return 1 & e2 ? "strong" : 2 & e2 ? "em" : "span";
}
function Bn(t2, e2, n2, r2, i2) {
  const s2 = r2.classList;
  let o2 = Rs(i2, "base");
  void 0 !== o2 && s2.add(...o2), o2 = Rs(i2, "underlineStrikethrough");
  let l2 = false;
  const c2 = e2 & b && e2 & N;
  void 0 !== o2 && (n2 & b && n2 & N ? (l2 = true, c2 || s2.add(...o2)) : c2 && s2.remove(...o2));
  for (const t3 in W) {
    const r3 = W[t3];
    if (o2 = Rs(i2, t3), void 0 !== o2) if (n2 & r3) {
      if (l2 && ("underline" === t3 || "strikethrough" === t3)) {
        e2 & r3 && s2.remove(...o2);
        continue;
      }
      e2 & r3 && (!c2 || "underline" !== t3) && "strikethrough" !== t3 || s2.add(...o2);
    } else e2 & r3 && s2.remove(...o2);
  }
}
function Rn(t2, e2, n2) {
  const r2 = e2.firstChild, s2 = n2.isComposing(), o2 = t2 + (s2 ? z : "");
  if (null == r2) e2.textContent = o2;
  else {
    const t3 = r2.nodeValue;
    if (t3 !== o2) if (s2 || i) {
      const [e3, n3, i2] = (function(t4, e4) {
        const n4 = t4.length, r3 = e4.length;
        let i3 = 0, s3 = 0;
        for (; i3 < n4 && i3 < r3 && t4[i3] === e4[i3]; ) i3++;
        for (; s3 + i3 < n4 && s3 + i3 < r3 && t4[n4 - s3 - 1] === e4[r3 - s3 - 1]; ) s3++;
        return [i3, n4 - i3 - s3, e4.slice(i3, r3 - s3)];
      })(t3, o2);
      0 !== n3 && r2.deleteData(e3, n3), r2.insertData(e3, i2);
    } else r2.nodeValue = o2;
  }
}
function Wn(t2, e2, n2, r2, i2, s2) {
  Rn(i2, t2, e2);
  const o2 = s2.theme.text;
  void 0 !== o2 && Bn(0, 0, r2, t2, o2);
}
function Jn(t2, e2) {
  const n2 = document.createElement(e2);
  return n2.appendChild(t2), n2;
}
var $n = class _$n extends An {
  static getType() {
    return "text";
  }
  static clone(t2) {
    return new _$n(t2.__text, t2.__key);
  }
  afterCloneFrom(t2) {
    super.afterCloneFrom(t2), this.__text = t2.__text, this.__format = t2.__format, this.__style = t2.__style, this.__mode = t2.__mode, this.__detail = t2.__detail;
  }
  constructor(t2 = "", e2) {
    super(e2), this.__text = t2, this.__format = 0, this.__style = "", this.__mode = 0, this.__detail = 0;
  }
  getFormat() {
    return this.getLatest().__format;
  }
  getDetail() {
    return this.getLatest().__detail;
  }
  getMode() {
    const t2 = this.getLatest();
    return V[t2.__mode];
  }
  getStyle() {
    return this.getLatest().__style;
  }
  isToken() {
    return 1 === this.getLatest().__mode;
  }
  isComposing() {
    return this.__key === gs();
  }
  isSegmented() {
    return 2 === this.getLatest().__mode;
  }
  isDirectionless() {
    return !!(1 & this.getLatest().__detail);
  }
  isUnmergeable() {
    return !!(2 & this.getLatest().__detail);
  }
  hasFormat(t2) {
    const e2 = W[t2];
    return !!(this.getFormat() & e2);
  }
  isSimpleText() {
    return "text" === this.__type && 0 === this.__mode;
  }
  getTextContent() {
    return this.getLatest().__text;
  }
  getFormatFlags(t2, e2) {
    return cs(this.getLatest().__format, t2, e2);
  }
  canHaveFormat() {
    return true;
  }
  isInline() {
    return true;
  }
  createDOM(t2, e2) {
    const n2 = this.__format, r2 = Kn(0, n2), i2 = zn(0, n2), s2 = null === r2 ? i2 : r2, o2 = document.createElement(s2);
    let l2 = o2;
    this.hasFormat("code") && o2.setAttribute("spellcheck", "false"), null !== r2 && (l2 = document.createElement(i2), o2.appendChild(l2));
    Wn(l2, this, 0, n2, this.__text, t2);
    const c2 = this.__style;
    return "" !== c2 && (o2.style.cssText = c2), o2;
  }
  updateDOM(e2, n2, r2) {
    const i2 = this.__text, s2 = e2.__format, o2 = this.__format, l2 = Kn(0, s2), c2 = Kn(0, o2), a2 = zn(0, s2), u2 = zn(0, o2);
    if ((null === l2 ? a2 : l2) !== (null === c2 ? u2 : c2)) return true;
    if (l2 === c2 && a2 !== u2) {
      const e3 = n2.firstChild;
      null == e3 && t(48);
      const s3 = document.createElement(u2);
      return Wn(s3, this, 0, o2, i2, r2), n2.replaceChild(s3, e3), false;
    }
    let f2 = n2;
    null !== c2 && null !== l2 && (f2 = n2.firstChild, null == f2 && t(49)), Rn(i2, f2, this);
    const d2 = r2.theme.text;
    void 0 !== d2 && s2 !== o2 && Bn(0, s2, o2, f2, d2);
    const h2 = e2.__style, g2 = this.__style;
    return h2 !== g2 && (n2.style.cssText = g2), false;
  }
  static importDOM() {
    return { "#text": () => ({ conversion: Hn, priority: 0 }), b: () => ({ conversion: Un, priority: 0 }), code: () => ({ conversion: Xn, priority: 0 }), em: () => ({ conversion: Xn, priority: 0 }), i: () => ({ conversion: Xn, priority: 0 }), mark: () => ({ conversion: Xn, priority: 0 }), s: () => ({ conversion: Xn, priority: 0 }), span: () => ({ conversion: jn, priority: 0 }), strong: () => ({ conversion: Xn, priority: 0 }), sub: () => ({ conversion: Xn, priority: 0 }), sup: () => ({ conversion: Xn, priority: 0 }), u: () => ({ conversion: Xn, priority: 0 }) };
  }
  static importJSON(t2) {
    return Qn().updateFromJSON(t2);
  }
  updateFromJSON(t2) {
    return super.updateFromJSON(t2).setTextContent(t2.text).setFormat(t2.format).setDetail(t2.detail).setMode(t2.mode).setStyle(t2.style);
  }
  exportDOM(e2) {
    let { element: n2 } = super.exportDOM(e2);
    return yo(n2) || t(132), n2.style.whiteSpace = "pre-wrap", this.hasFormat("lowercase") ? n2.style.textTransform = "lowercase" : this.hasFormat("uppercase") ? n2.style.textTransform = "uppercase" : this.hasFormat("capitalize") && (n2.style.textTransform = "capitalize"), this.hasFormat("bold") && (n2 = Jn(n2, "b")), this.hasFormat("italic") && (n2 = Jn(n2, "i")), this.hasFormat("strikethrough") && (n2 = Jn(n2, "s")), this.hasFormat("underline") && (n2 = Jn(n2, "u")), { element: n2 };
  }
  exportJSON() {
    return { detail: this.getDetail(), format: this.getFormat(), mode: this.getMode(), style: this.getStyle(), text: this.getTextContent(), ...super.exportJSON() };
  }
  selectionTransform(t2, e2) {
  }
  setFormat(t2) {
    const e2 = this.getWritable();
    return e2.__format = "string" == typeof t2 ? W[t2] : t2, e2;
  }
  setDetail(t2) {
    const e2 = this.getWritable();
    return e2.__detail = "string" == typeof t2 ? J[t2] : t2, e2;
  }
  setStyle(t2) {
    const e2 = this.getWritable();
    return e2.__style = t2, e2;
  }
  toggleFormat(t2) {
    const e2 = cs(this.getFormat(), t2, null);
    return this.setFormat(e2);
  }
  toggleDirectionless() {
    const t2 = this.getWritable();
    return t2.__detail ^= 1, t2;
  }
  toggleUnmergeable() {
    const t2 = this.getWritable();
    return t2.__detail ^= 2, t2;
  }
  setMode(t2) {
    const e2 = U[t2];
    if (this.__mode === e2) return this;
    const n2 = this.getWritable();
    return n2.__mode = e2, n2;
  }
  setTextContent(t2) {
    if (this.__text === t2) return this;
    const e2 = this.getWritable();
    return e2.__text = t2, e2;
  }
  select(t2, e2) {
    Yr();
    let n2 = t2, r2 = e2;
    const i2 = wr(), s2 = this.getTextContent(), o2 = this.__key;
    if ("string" == typeof s2) {
      const t3 = s2.length;
      void 0 === n2 && (n2 = t3), void 0 === r2 && (r2 = t3);
    } else n2 = 0, r2 = 0;
    if (!ar(i2)) return vr(o2, n2, o2, r2, "text", "text");
    {
      const t3 = gs();
      t3 !== i2.anchor.key && t3 !== i2.focus.key || hs(o2), i2.setTextNodeRange(this, n2, this, r2);
    }
    return i2;
  }
  selectStart() {
    return this.select(0, 0);
  }
  selectEnd() {
    const t2 = this.getTextContentSize();
    return this.select(t2, t2);
  }
  spliceText(t2, e2, n2, r2) {
    const i2 = this.getWritable(), s2 = i2.__text, o2 = n2.length;
    let l2 = t2;
    l2 < 0 && (l2 = o2 + l2, l2 < 0 && (l2 = 0));
    const c2 = wr();
    if (r2 && ar(c2)) {
      const e3 = t2 + o2;
      c2.setTextNodeRange(i2, e3, i2, e3);
    }
    const a2 = s2.slice(0, l2) + n2 + s2.slice(l2 + e2);
    return i2.__text = a2, i2;
  }
  canInsertTextBefore() {
    return true;
  }
  canInsertTextAfter() {
    return true;
  }
  splitText(...t2) {
    Yr();
    const e2 = this.getLatest(), n2 = e2.getTextContent();
    if ("" === n2) return [];
    const r2 = e2.__key, i2 = gs(), s2 = n2.length;
    t2.sort(((t3, e3) => t3 - e3)), t2.push(s2);
    const o2 = [], l2 = t2.length;
    for (let e3 = 0, r3 = 0; e3 < s2 && r3 <= l2; r3++) {
      const i3 = t2[r3];
      i3 > e3 && (o2.push(n2.slice(e3, i3)), e3 = i3);
    }
    const c2 = o2.length;
    if (1 === c2) return [e2];
    const a2 = o2[0], u2 = e2.getParent();
    let f2;
    const d2 = e2.getFormat(), h2 = e2.getStyle(), g2 = e2.__detail;
    let _2 = false, p2 = null, y2 = null;
    const m2 = wr();
    if (ar(m2)) {
      const [t3, e3] = m2.isBackward() ? [m2.focus, m2.anchor] : [m2.anchor, m2.focus];
      "text" === t3.type && t3.key === r2 && (p2 = t3), "text" === e3.type && e3.key === r2 && (y2 = e3);
    }
    e2.isSegmented() ? (f2 = Qn(a2), f2.__format = d2, f2.__style = h2, f2.__detail = g2, f2.__state = Ct(e2, f2), _2 = true) : f2 = e2.setTextContent(a2);
    const x2 = [f2];
    for (let t3 = 1; t3 < c2; t3++) {
      const n3 = Qn(o2[t3]);
      n3.__format = d2, n3.__style = h2, n3.__detail = g2, n3.__state = Ct(e2, n3);
      const s3 = n3.__key;
      i2 === r2 && hs(s3), x2.push(n3);
    }
    const C2 = p2 ? p2.offset : null, S2 = y2 ? y2.offset : null;
    let v2 = 0;
    for (const t3 of x2) {
      if (!p2 && !y2) break;
      const e3 = v2 + t3.getTextContentSize();
      if (null !== p2 && null !== C2 && C2 <= e3 && C2 >= v2 && (p2.set(t3.getKey(), C2 - v2, "text"), C2 < e3 && (p2 = null)), null !== y2 && null !== S2 && S2 <= e3 && S2 >= v2) {
        y2.set(t3.getKey(), S2 - v2, "text");
        break;
      }
      v2 = e3;
    }
    if (null !== u2) {
      !(function(t4) {
        const e4 = t4.getPreviousSibling(), n3 = t4.getNextSibling();
        null !== e4 && ds(e4);
        null !== n3 && ds(n3);
      })(this);
      const t3 = u2.getWritable(), e3 = this.getIndexWithinParent();
      _2 ? (t3.splice(e3, 0, x2), this.remove()) : t3.splice(e3, 1, x2), ar(m2) && Or(m2, u2, e3, c2 - 1);
    }
    return x2;
  }
  mergeWithSibling(e2) {
    const n2 = e2 === this.getPreviousSibling();
    n2 || e2 === this.getNextSibling() || t(50);
    const r2 = this.__key, i2 = e2.__key, s2 = this.__text, o2 = s2.length;
    gs() === i2 && hs(r2);
    const l2 = wr();
    if (ar(l2)) {
      const t2 = l2.anchor, s3 = l2.focus;
      null !== t2 && t2.key === i2 && Pr(t2, n2, r2, e2, o2), null !== s3 && s3.key === i2 && Pr(s3, n2, r2, e2, o2);
    }
    const c2 = e2.__text, a2 = n2 ? c2 + s2 : s2 + c2;
    this.setTextContent(a2);
    const u2 = this.getWritable();
    return e2.remove(), u2;
  }
  isTextEntity() {
    return false;
  }
};
function jn(t2) {
  return { forChild: tr(t2.style), node: null };
}
function Un(t2) {
  const e2 = t2, n2 = "normal" === e2.style.fontWeight;
  return { forChild: tr(e2.style, n2 ? void 0 : "bold"), node: null };
}
var Vn = /* @__PURE__ */ new WeakMap();
function Yn(t2) {
  if (!yo(t2)) return false;
  if ("PRE" === t2.nodeName) return true;
  const e2 = t2.style.whiteSpace;
  return "string" == typeof e2 && e2.startsWith("pre");
}
function Hn(e2) {
  const n2 = e2;
  null === e2.parentElement && t(129);
  let r2 = n2.textContent || "";
  if (null !== (function(t2) {
    let e3, n3 = t2.parentNode;
    const r3 = [t2];
    for (; null !== n3 && void 0 === (e3 = Vn.get(n3)) && !Yn(n3); ) r3.push(n3), n3 = n3.parentNode;
    const i2 = void 0 === e3 ? n3 : e3;
    for (let t3 = 0; t3 < r3.length; t3++) Vn.set(r3[t3], i2);
    return i2;
  })(n2)) {
    const t2 = r2.split(/(\r?\n|\t)/), e3 = [], n3 = t2.length;
    for (let r3 = 0; r3 < n3; r3++) {
      const n4 = t2[r3];
      "\n" === n4 || "\r\n" === n4 ? e3.push(Fn()) : "	" === n4 ? e3.push(nr()) : "" !== n4 && e3.push(Qn(n4));
    }
    return { node: e3 };
  }
  if (r2 = r2.replace(/\r/g, "").replace(/[ \t\n]+/g, " "), "" === r2) return { node: null };
  if (" " === r2[0]) {
    let t2 = n2, e3 = true;
    for (; null !== t2 && null !== (t2 = qn(t2, false)); ) {
      const n3 = t2.textContent || "";
      if (n3.length > 0) {
        /[ \t\n]$/.test(n3) && (r2 = r2.slice(1)), e3 = false;
        break;
      }
    }
    e3 && (r2 = r2.slice(1));
  }
  if (" " === r2[r2.length - 1]) {
    let t2 = n2, e3 = true;
    for (; null !== t2 && null !== (t2 = qn(t2, true)); ) {
      if ((t2.textContent || "").replace(/^( |\t|\r?\n)+/, "").length > 0) {
        e3 = false;
        break;
      }
    }
    e3 && (r2 = r2.slice(0, r2.length - 1));
  }
  return "" === r2 ? { node: null } : { node: Qn(r2) };
}
function qn(t2, e2) {
  let n2 = t2;
  for (; ; ) {
    let t3;
    for (; null === (t3 = e2 ? n2.nextSibling : n2.previousSibling); ) {
      const t4 = n2.parentElement;
      if (null === t4) return null;
      n2 = t4;
    }
    if (n2 = t3, yo(n2)) {
      const t4 = n2.style.display;
      if ("" === t4 && !Co(n2) || "" !== t4 && !t4.startsWith("inline")) return null;
    }
    let r2 = n2;
    for (; null !== (r2 = e2 ? n2.firstChild : n2.lastChild); ) n2 = r2;
    if (ss(n2)) return n2;
    if ("BR" === n2.nodeName) return null;
  }
}
var Gn = { code: "code", em: "italic", i: "italic", mark: "highlight", s: "strikethrough", strong: "bold", sub: "subscript", sup: "superscript", u: "underline" };
function Xn(t2) {
  const e2 = Gn[t2.nodeName.toLowerCase()];
  return void 0 === e2 ? { node: null } : { forChild: tr(t2.style, e2), node: null };
}
function Qn(t2 = "") {
  return lo(new $n(t2));
}
function Zn(t2) {
  return t2 instanceof $n;
}
function tr(t2, e2) {
  const n2 = t2.fontWeight, r2 = t2.textDecoration.split(" "), i2 = "700" === n2 || "bold" === n2, s2 = r2.includes("line-through"), o2 = "italic" === t2.fontStyle, l2 = r2.includes("underline"), c2 = t2.verticalAlign;
  return (t3) => Zn(t3) ? (i2 && !t3.hasFormat("bold") && t3.toggleFormat("bold"), s2 && !t3.hasFormat("strikethrough") && t3.toggleFormat("strikethrough"), o2 && !t3.hasFormat("italic") && t3.toggleFormat("italic"), l2 && !t3.hasFormat("underline") && t3.toggleFormat("underline"), "sub" !== c2 || t3.hasFormat("subscript") || t3.toggleFormat("subscript"), "super" !== c2 || t3.hasFormat("superscript") || t3.toggleFormat("superscript"), e2 && !t3.hasFormat(e2) && t3.toggleFormat(e2), t3) : t3;
}
var er = class _er extends $n {
  static getType() {
    return "tab";
  }
  static clone(t2) {
    return new _er(t2.__key);
  }
  constructor(t2) {
    super("	", t2), this.__detail = 2;
  }
  static importDOM() {
    return null;
  }
  createDOM(t2) {
    const e2 = super.createDOM(t2), n2 = Rs(t2.theme, "tab");
    if (void 0 !== n2) {
      e2.classList.add(...n2);
    }
    return e2;
  }
  static importJSON(t2) {
    return nr().updateFromJSON(t2);
  }
  setTextContent(e2) {
    return "	" !== e2 && "" !== e2 && t(126), super.setTextContent("	");
  }
  spliceText(e2, n2, r2, i2) {
    return "" === r2 && 0 === n2 || "	" === r2 && 1 === n2 || t(286), this;
  }
  setDetail(e2) {
    return 2 !== e2 && t(127), this;
  }
  setMode(e2) {
    return "normal" !== e2 && t(128), this;
  }
  canInsertTextBefore() {
    return false;
  }
  canInsertTextAfter() {
    return false;
  }
};
function nr() {
  return lo(new er());
}
function rr(t2) {
  return t2 instanceof er;
}
var ir = class {
  constructor(t2, e2, n2) {
    this._selection = null, this.key = t2, this.offset = e2, this.type = n2;
  }
  is(t2) {
    return this.key === t2.key && this.offset === t2.offset && this.type === t2.type;
  }
  isBefore(t2) {
    if (this.key === t2.key) return this.offset < t2.offset;
    return ul(Tl(_l(this, "next")), Tl(_l(t2, "next"))) < 0;
  }
  getNode() {
    const e2 = _s(this.key);
    return null === e2 && t(20), e2;
  }
  set(t2, e2, n2, r2) {
    const i2 = this._selection, s2 = this.key;
    r2 && this.key === t2 && this.offset === e2 && this.type === n2 || (this.key = t2, this.offset = e2, this.type = n2, Vr() || (gs() === s2 && hs(t2), null !== i2 && (i2.setCachedNodes(null), i2.dirty = true)));
  }
};
function sr(t2, e2, n2) {
  return new ir(t2, e2, n2);
}
function or(t2, e2) {
  let n2 = e2.__key, r2 = t2.offset, i2 = "element";
  if (Zn(e2)) {
    i2 = "text";
    const t3 = e2.getTextContentSize();
    r2 > t3 && (r2 = t3);
  } else if (!hi(e2)) {
    const t3 = e2.getNextSibling();
    if (Zn(t3)) n2 = t3.__key, r2 = 0, i2 = "text";
    else {
      const t4 = e2.getParent();
      t4 && (n2 = t4.__key, r2 = e2.getIndexWithinParent() + 1);
    }
  }
  t2.set(n2, r2, i2);
}
function lr(t2, e2) {
  if (hi(e2)) {
    const n2 = e2.getLastDescendant();
    hi(n2) || Zn(n2) ? or(t2, n2) : or(t2, e2);
  } else or(t2, e2);
}
var cr = class _cr {
  constructor(t2) {
    this._cachedNodes = null, this._nodes = t2, this.dirty = false;
  }
  getCachedNodes() {
    return this._cachedNodes;
  }
  setCachedNodes(t2) {
    this._cachedNodes = t2;
  }
  is(t2) {
    if (!fr(t2)) return false;
    const e2 = this._nodes, n2 = t2._nodes;
    return e2.size === n2.size && Array.from(e2).every(((t3) => n2.has(t3)));
  }
  isCollapsed() {
    return false;
  }
  isBackward() {
    return false;
  }
  getStartEndPoints() {
    return null;
  }
  add(t2) {
    this.dirty = true, this._nodes.add(t2), this._cachedNodes = null;
  }
  delete(t2) {
    this.dirty = true, this._nodes.delete(t2), this._cachedNodes = null;
  }
  clear() {
    this.dirty = true, this._nodes.clear(), this._cachedNodes = null;
  }
  has(t2) {
    return this._nodes.has(t2);
  }
  clone() {
    return new _cr(new Set(this._nodes));
  }
  extract() {
    return this.getNodes();
  }
  insertRawText(t2) {
  }
  insertText() {
  }
  insertNodes(t2) {
    const e2 = this.getNodes(), n2 = e2.length, r2 = e2[n2 - 1];
    let i2;
    if (Zn(r2)) i2 = r2.select();
    else {
      const t3 = r2.getIndexWithinParent() + 1;
      i2 = r2.getParentOrThrow().select(t3, t3);
    }
    i2.insertNodes(t2);
    for (let t3 = 0; t3 < n2; t3++) e2[t3].remove();
  }
  getNodes() {
    const t2 = this._cachedNodes;
    if (null !== t2) return t2;
    const e2 = this._nodes, n2 = [];
    for (const t3 of e2) {
      const e3 = _s(t3);
      null !== e3 && n2.push(e3);
    }
    return Vr() || (this._cachedNodes = n2), n2;
  }
  getTextContent() {
    const t2 = this.getNodes();
    let e2 = "";
    for (let n2 = 0; n2 < t2.length; n2++) e2 += t2[n2].getTextContent();
    return e2;
  }
  deleteNodes() {
    const t2 = this.getNodes();
    if ((wr() || Er()) === this && t2[0]) {
      const e2 = Go(t2[0], "next");
      yl(cl(e2, e2));
    }
    for (const e2 of t2) e2.remove();
  }
};
function ar(t2) {
  return t2 instanceof ur;
}
var ur = class _ur {
  constructor(t2, e2, n2, r2) {
    this.anchor = t2, this.focus = e2, t2._selection = this, e2._selection = this, this._cachedNodes = null, this.format = n2, this.style = r2, this.dirty = false;
  }
  getCachedNodes() {
    return this._cachedNodes;
  }
  setCachedNodes(t2) {
    this._cachedNodes = t2;
  }
  is(t2) {
    return !!ar(t2) && (this.anchor.is(t2.anchor) && this.focus.is(t2.focus) && this.format === t2.format && this.style === t2.style);
  }
  isCollapsed() {
    return this.anchor.is(this.focus);
  }
  getNodes() {
    const t2 = this._cachedNodes;
    if (null !== t2) return t2;
    const e2 = (function(t3) {
      const e3 = [], [n2, r2] = t3.getTextSlices();
      n2 && e3.push(n2.caret.origin);
      const i2 = /* @__PURE__ */ new Set(), s2 = /* @__PURE__ */ new Set();
      for (const n3 of t3) if (Vo(n3)) {
        const { origin: t4 } = n3;
        0 === e3.length ? i2.add(t4) : (s2.add(t4), e3.push(t4));
      } else {
        const { origin: t4 } = n3;
        hi(t4) && s2.has(t4) || e3.push(t4);
      }
      r2 && e3.push(r2.caret.origin);
      if (Uo(t3.focus) && hi(t3.focus.origin) && null === t3.focus.getNodeAtCaret()) for (let n3 = tl(t3.focus.origin, "previous"); Vo(n3) && i2.has(n3.origin) && !n3.origin.isEmpty() && n3.origin.is(e3[e3.length - 1]); n3 = nl(n3)) i2.delete(n3.origin), e3.pop();
      for (; e3.length > 1; ) {
        const t4 = e3[e3.length - 1];
        if (!hi(t4) || s2.has(t4) || t4.isEmpty() || i2.has(t4)) break;
        e3.pop();
      }
      if (0 === e3.length && t3.isCollapsed()) {
        const n3 = Tl(t3.anchor), r3 = Tl(t3.anchor.getFlipped()), i3 = (t4) => $o(t4) ? t4.origin : t4.getNodeAtCaret(), s3 = i3(n3) || i3(r3) || (t3.anchor.getNodeAtCaret() ? n3.origin : r3.origin);
        e3.push(s3);
      }
      return e3;
    })(wl(xl(this), "next"));
    return Vr() || (this._cachedNodes = e2), e2;
  }
  setTextNodeRange(t2, e2, n2, r2) {
    this.anchor.set(t2.__key, e2, "text"), this.focus.set(n2.__key, r2, "text");
  }
  getTextContent() {
    const t2 = this.getNodes();
    if (0 === t2.length) return "";
    const e2 = t2[0], n2 = t2[t2.length - 1], r2 = this.anchor, i2 = this.focus, s2 = r2.isBefore(i2), [o2, l2] = hr(this);
    let c2 = "", a2 = true;
    for (let u2 = 0; u2 < t2.length; u2++) {
      const f2 = t2[u2];
      if (hi(f2) && !f2.isInline()) a2 || (c2 += "\n"), a2 = !f2.isEmpty();
      else if (a2 = false, Zn(f2)) {
        let t3 = f2.getTextContent();
        f2 === e2 ? f2 === n2 ? "element" === r2.type && "element" === i2.type && i2.offset !== r2.offset || (t3 = o2 < l2 ? t3.slice(o2, l2) : t3.slice(l2, o2)) : t3 = s2 ? t3.slice(o2) : t3.slice(l2) : f2 === n2 && (t3 = s2 ? t3.slice(0, l2) : t3.slice(0, o2)), c2 += t3;
      } else !pi(f2) && !Ln(f2) || f2 === n2 && this.isCollapsed() || (c2 += f2.getTextContent());
    }
    return c2;
  }
  applyDOMRange(t2) {
    const e2 = Gr(), n2 = e2.getEditorState()._selection, r2 = Cr(t2.startContainer, t2.startOffset, t2.endContainer, t2.endOffset, e2, n2);
    if (null === r2) return;
    const [i2, s2] = r2;
    this.anchor.set(i2.key, i2.offset, i2.type, true), this.focus.set(s2.key, s2.offset, s2.type, true), Tt(this);
  }
  clone() {
    const t2 = this.anchor, e2 = this.focus;
    return new _ur(sr(t2.key, t2.offset, t2.type), sr(e2.key, e2.offset, e2.type), this.format, this.style);
  }
  toggleFormat(t2) {
    this.format = cs(this.format, t2, null), this.dirty = true;
  }
  setFormat(t2) {
    this.format = t2, this.dirty = true;
  }
  setStyle(t2) {
    this.style = t2, this.dirty = true;
  }
  hasFormat(t2) {
    const e2 = W[t2];
    return !!(this.format & e2);
  }
  insertRawText(t2) {
    const e2 = t2.split(/(\r?\n|\t)/), n2 = [], r2 = e2.length;
    for (let t3 = 0; t3 < r2; t3++) {
      const r3 = e2[t3];
      "\n" === r3 || "\r\n" === r3 ? n2.push(Fn()) : "	" === r3 ? n2.push(nr()) : n2.push(Qn(r3));
    }
    this.insertNodes(n2);
  }
  insertText(e2) {
    const n2 = this.anchor, r2 = this.focus, i2 = this.format, s2 = this.style;
    let o2 = n2, l2 = r2;
    !this.isCollapsed() && r2.isBefore(n2) && (o2 = r2, l2 = n2), "element" === o2.type && (function(t2, e3, n3, r3) {
      const i3 = t2.getNode(), s3 = i3.getChildAtIndex(t2.offset), o3 = Qn();
      if (o3.setFormat(n3), o3.setStyle(r3), Li(s3)) s3.splice(0, 0, [o3]);
      else {
        const t3 = mi(i3) ? Fi().append(o3) : o3;
        null === s3 ? i3.append(t3) : s3.insertBefore(t3);
      }
      t2.is(e3) && e3.set(o3.__key, 0, "text"), t2.set(o3.__key, 0, "text");
    })(o2, l2, i2, s2), "element" === l2.type && pl(l2, Tl(_l(l2, "next")));
    const c2 = o2.offset;
    let a2 = l2.offset;
    const u2 = this.getNodes(), f2 = u2.length;
    let d2 = u2[0];
    Zn(d2) || t(26);
    const h2 = d2.getTextContent().length, g2 = d2.getParentOrThrow();
    let _2 = u2[f2 - 1];
    if (1 === f2 && "element" === l2.type && (a2 = h2, l2.set(o2.key, a2, "text")), this.isCollapsed() && c2 === h2 && (is(d2) || !d2.canInsertTextAfter() || !g2.canInsertTextAfter() && null === d2.getNextSibling())) {
      let t2 = d2.getNextSibling();
      if (Zn(t2) && t2.canInsertTextBefore() && !is(t2) || (t2 = Qn(), t2.setFormat(i2), t2.setStyle(s2), g2.canInsertTextAfter() ? d2.insertAfter(t2) : g2.insertAfter(t2)), t2.select(0, 0), d2 = t2, "" !== e2) return void this.insertText(e2);
    } else if (this.isCollapsed() && 0 === c2 && (is(d2) || !d2.canInsertTextBefore() || !g2.canInsertTextBefore() && null === d2.getPreviousSibling())) {
      let t2 = d2.getPreviousSibling();
      if (Zn(t2) && !is(t2) || (t2 = Qn(), t2.setFormat(i2), g2.canInsertTextBefore() ? d2.insertBefore(t2) : g2.insertBefore(t2)), t2.select(), d2 = t2, "" !== e2) return void this.insertText(e2);
    } else if (d2.isSegmented() && c2 !== h2) {
      const t2 = Qn(d2.getTextContent());
      t2.setFormat(i2), d2.replace(t2), d2 = t2;
    } else if (!this.isCollapsed() && "" !== e2) {
      const t2 = _2.getParent();
      if (!g2.canInsertTextBefore() || !g2.canInsertTextAfter() || hi(t2) && (!t2.canInsertTextBefore() || !t2.canInsertTextAfter())) return this.insertText(""), xr(this.anchor, this.focus, null), void this.insertText(e2);
    }
    if (1 === f2) {
      if (rs(d2)) {
        const t3 = Qn(e2);
        return t3.select(), void d2.replace(t3);
      }
      const t2 = d2.getFormat(), n3 = d2.getStyle();
      if (c2 !== a2 || t2 === i2 && n3 === s2) {
        if (rr(d2)) {
          const t3 = Qn(e2);
          return t3.setFormat(i2), t3.setStyle(s2), t3.select(), void d2.replace(t3);
        }
      } else {
        if ("" !== d2.getTextContent()) {
          const t3 = Qn(e2);
          if (t3.setFormat(i2), t3.setStyle(s2), t3.select(), 0 === c2) d2.insertBefore(t3, false);
          else {
            const [e3] = d2.splitText(c2);
            e3.insertAfter(t3, false);
          }
          return void (t3.isComposing() && "text" === this.anchor.type && (this.anchor.offset -= e2.length));
        }
        d2.setFormat(i2), d2.setStyle(s2);
      }
      const r3 = a2 - c2;
      d2 = d2.spliceText(c2, r3, e2, true), "" === d2.getTextContent() ? d2.remove() : "text" === this.anchor.type && (d2.isComposing() ? this.anchor.offset -= e2.length : (this.format = t2, this.style = n3));
    } else {
      const t2 = /* @__PURE__ */ new Set([...d2.getParentKeys(), ..._2.getParentKeys()]), n3 = hi(d2) ? d2 : d2.getParentOrThrow();
      let r3 = hi(_2) ? _2 : _2.getParentOrThrow(), i3 = _2;
      if (!n3.is(r3) && r3.isInline()) do {
        i3 = r3, r3 = r3.getParentOrThrow();
      } while (r3.isInline());
      if ("text" === l2.type && (0 !== a2 || "" === _2.getTextContent()) || "element" === l2.type && _2.getIndexWithinParent() < a2) if (Zn(_2) && !rs(_2) && a2 !== _2.getTextContentSize()) {
        if (_2.isSegmented()) {
          const t3 = Qn(_2.getTextContent());
          _2.replace(t3), _2 = t3;
        }
        mi(l2.getNode()) || "text" !== l2.type || (_2 = _2.spliceText(0, a2, "")), t2.add(_2.__key);
      } else {
        const t3 = _2.getParentOrThrow();
        t3.canBeEmpty() || 1 !== t3.getChildrenSize() ? _2.remove() : t3.remove();
      }
      else t2.add(_2.__key);
      const s3 = r3.getChildren(), o3 = new Set(u2), g3 = n3.is(r3), p2 = n3.isInline() && null === d2.getNextSibling() ? n3 : d2;
      for (let t3 = s3.length - 1; t3 >= 0; t3--) {
        const e3 = s3[t3];
        if (e3.is(d2) || hi(e3) && e3.isParentOf(d2)) break;
        e3.isAttached() && (!o3.has(e3) || e3.is(i3) ? g3 || p2.insertAfter(e3, false) : e3.remove());
      }
      if (!g3) {
        let e3 = r3, n4 = null;
        for (; null !== e3; ) {
          const r4 = e3.getChildren(), i4 = r4.length;
          (0 === i4 || r4[i4 - 1].is(n4)) && (t2.delete(e3.__key), n4 = e3), e3 = e3.getParent();
        }
      }
      if (rs(d2)) if (c2 === h2) d2.select();
      else {
        const t3 = Qn(e2);
        t3.select(), d2.replace(t3);
      }
      else d2 = d2.spliceText(c2, h2 - c2, e2, true), "" === d2.getTextContent() ? d2.remove() : d2.isComposing() && "text" === this.anchor.type && (this.anchor.offset -= e2.length);
      for (let e3 = 1; e3 < f2; e3++) {
        const n4 = u2[e3], r4 = n4.__key;
        t2.has(r4) || n4.remove();
      }
    }
  }
  removeText() {
    const t2 = wr() === this;
    ml(this, kl(xl(this))), t2 && wr() !== this && ks(this);
  }
  formatText(t2, e2 = null) {
    if (this.isCollapsed()) return this.toggleFormat(t2), void hs(null);
    const n2 = this.getNodes(), r2 = [];
    for (const t3 of n2) Zn(t3) && r2.push(t3);
    const i2 = (e3) => {
      n2.forEach(((n3) => {
        if (hi(n3)) {
          const r3 = n3.getFormatFlags(t2, e3);
          n3.setTextFormat(r3);
        }
      }));
    }, s2 = r2.length;
    if (0 === s2) return this.toggleFormat(t2), hs(null), void i2(e2);
    const o2 = this.anchor, l2 = this.focus, c2 = this.isBackward(), a2 = c2 ? l2 : o2, u2 = c2 ? o2 : l2;
    let f2 = 0, d2 = r2[0], h2 = "element" === a2.type ? 0 : a2.offset;
    if ("text" === a2.type && h2 === d2.getTextContentSize() && (f2 = 1, d2 = r2[1], h2 = 0), null == d2) return;
    const g2 = d2.getFormatFlags(t2, e2);
    i2(g2);
    const _2 = s2 - 1;
    let p2 = r2[_2];
    const y2 = "text" === u2.type ? u2.offset : p2.getTextContentSize();
    if (d2.is(p2)) {
      if (h2 === y2) return;
      if (is(d2) || 0 === h2 && y2 === d2.getTextContentSize()) d2.setFormat(g2);
      else {
        const t3 = d2.splitText(h2, y2), e3 = 0 === h2 ? t3[0] : t3[1];
        e3.setFormat(g2), "text" === a2.type && a2.set(e3.__key, 0, "text"), "text" === u2.type && u2.set(e3.__key, y2 - h2, "text");
      }
      return void (this.format = g2);
    }
    0 === h2 || is(d2) || ([, d2] = d2.splitText(h2), h2 = 0), d2.setFormat(g2);
    const m2 = p2.getFormatFlags(t2, g2);
    y2 > 0 && (y2 === p2.getTextContentSize() || is(p2) || ([p2] = p2.splitText(y2)), p2.setFormat(m2));
    for (let e3 = f2 + 1; e3 < _2; e3++) {
      const n3 = r2[e3], i3 = n3.getFormatFlags(t2, m2);
      n3.setFormat(i3);
    }
    "text" === a2.type && a2.set(d2.__key, h2, "text"), "text" === u2.type && u2.set(p2.__key, y2, "text"), this.format = g2 | m2;
  }
  insertNodes(e2) {
    if (0 === e2.length) return;
    if (this.isCollapsed() || this.removeText(), "root" === this.anchor.key) {
      this.insertParagraph();
      const n3 = wr();
      return ar(n3) || t(134), n3.insertNodes(e2);
    }
    const n2 = (this.isBackward() ? this.focus : this.anchor).getNode(), r2 = ko(n2, vo), i2 = e2[e2.length - 1];
    if (hi(r2) && "__language" in r2) {
      if ("__language" in e2[0]) this.insertText(e2[0].getTextContent());
      else {
        const t2 = Kr(this);
        r2.splice(t2, 0, e2), i2.selectEnd();
      }
      return;
    }
    if (!e2.some(((t2) => (hi(t2) || pi(t2)) && !t2.isInline()))) {
      hi(r2) || t(211, n2.constructor.name, n2.getType());
      const s3 = Kr(this);
      return r2.splice(s3, 0, e2), void i2.selectEnd();
    }
    const s2 = (function(t2) {
      const e3 = Fi();
      let n3 = null;
      for (let r3 = 0; r3 < t2.length; r3++) {
        const i3 = t2[r3], s3 = Ln(i3);
        if (s3 || pi(i3) && i3.isInline() || hi(i3) && i3.isInline() || Zn(i3) || i3.isParentRequired()) {
          if (null === n3 && (n3 = i3.createParentElementNode(), e3.append(n3), s3)) continue;
          null !== n3 && n3.append(i3);
        } else e3.append(i3), n3 = null;
      }
      return e3;
    })(e2), o2 = s2.getLastDescendant(), l2 = s2.getChildren(), c2 = !hi(r2) || !r2.isEmpty() ? this.insertParagraph() : null, a2 = l2[l2.length - 1];
    let u2 = l2[0];
    var f2;
    hi(f2 = u2) && vo(f2) && !f2.isEmpty() && hi(r2) && (!r2.isEmpty() || r2.canMergeWhenEmpty()) && (hi(r2) || t(211, n2.constructor.name, n2.getType()), r2.append(...u2.getChildren()), u2 = l2[1]), u2 && (null === r2 && t(212, n2.constructor.name, n2.getType()), (function(e3, n3, r3) {
      const i3 = n3.getParentOrThrow().getLastChild();
      let s3 = n3;
      const o3 = [n3];
      for (; s3 !== i3; ) s3.getNextSibling() || t(140), s3 = s3.getNextSibling(), o3.push(s3);
      let l3 = e3;
      for (const t2 of o3) l3 = l3.insertAfter(t2);
    })(r2, u2));
    const d2 = ko(o2, vo);
    c2 && hi(d2) && (c2.canMergeWhenEmpty() || vo(a2)) && (d2.append(...c2.getChildren()), c2.remove()), hi(r2) && r2.isEmpty() && r2.remove(), o2.selectEnd();
    const h2 = hi(r2) ? r2.getLastChild() : null;
    Ln(h2) && d2 !== r2 && h2.remove();
  }
  insertParagraph() {
    if ("root" === this.anchor.key) {
      const t2 = Fi();
      return Ss().splice(this.anchor.offset, 0, [t2]), t2.select(), t2;
    }
    const e2 = Kr(this), n2 = ko(this.anchor.getNode(), vo);
    hi(n2) || t(213);
    const r2 = n2.getChildAtIndex(e2), i2 = r2 ? [r2, ...r2.getNextSiblings()] : [], s2 = n2.insertNewAfter(this, false);
    return s2 ? (s2.append(...i2), s2.selectStart(), s2) : null;
  }
  insertLineBreak(t2) {
    const e2 = Fn();
    if (this.insertNodes([e2]), t2) {
      const t3 = e2.getParentOrThrow(), n2 = e2.getIndexWithinParent();
      t3.select(n2, n2);
    }
  }
  extract() {
    const t2 = this.getNodes(), e2 = t2.length, n2 = e2 - 1, r2 = this.anchor, i2 = this.focus;
    let s2 = t2[0], o2 = t2[n2];
    const [l2, c2] = hr(this);
    if (0 === e2) return [];
    if (1 === e2) {
      if (Zn(s2) && !this.isCollapsed()) {
        const t3 = l2 > c2 ? c2 : l2, e3 = l2 > c2 ? l2 : c2, n3 = s2.splitText(t3, e3), r3 = 0 === t3 ? n3[0] : n3[1];
        return null != r3 ? [r3] : [];
      }
      return [s2];
    }
    const a2 = r2.isBefore(i2);
    if (Zn(s2)) {
      const e3 = a2 ? l2 : c2;
      e3 === s2.getTextContentSize() ? t2.shift() : 0 !== e3 && ([, s2] = s2.splitText(e3), t2[0] = s2);
    }
    if (Zn(o2)) {
      const e3 = o2.getTextContent().length, r3 = a2 ? c2 : l2;
      0 === r3 ? t2.pop() : r3 !== e3 && ([o2] = o2.splitText(r3), t2[n2] = o2);
    }
    return t2;
  }
  modify(t2, e2, n2) {
    if (Br(this, t2, e2, n2)) return;
    const r2 = "move" === t2, i2 = Gr(), s2 = ho(no(i2));
    if (!s2) return;
    const o2 = i2._blockCursorElement, l2 = i2._rootElement, c2 = this.focus.getNode();
    if (null === l2 || null === o2 || !hi(c2) || c2.isInline() || c2.canBeEmpty() || fo(o2, i2, l2), this.dirty) {
      let t3 = Hs(i2, this.anchor.key), e3 = Hs(i2, this.focus.key);
      "text" === this.anchor.type && (t3 = ls(t3)), "text" === this.focus.type && (e3 = ls(e3)), t3 && e3 && Dr(s2, t3, this.anchor.offset, e3, this.focus.offset);
    }
    if ((function(t3, e3, n3, r3) {
      t3.modify(e3, n3, r3);
    })(s2, t2, e2 ? "backward" : "forward", n2), s2.rangeCount > 0) {
      const t3 = s2.getRangeAt(0), n3 = this.anchor.getNode(), i3 = mi(n3) ? n3 : io(n3);
      if (this.applyDOMRange(t3), this.dirty = true, !r2) {
        const n4 = this.getNodes(), r3 = [];
        let o3 = false;
        for (let t4 = 0; t4 < n4.length; t4++) {
          const e3 = n4[t4];
          to(e3, i3) ? r3.push(e3) : o3 = true;
        }
        if (o3 && r3.length > 0) if (e2) {
          const t4 = r3[0];
          hi(t4) ? t4.selectStart() : t4.getParentOrThrow().selectStart();
        } else {
          const t4 = r3[r3.length - 1];
          hi(t4) ? t4.selectEnd() : t4.getParentOrThrow().selectEnd();
        }
        s2.anchorNode === t3.startContainer && s2.anchorOffset === t3.startOffset || (function(t4) {
          const e3 = t4.focus, n5 = t4.anchor, r4 = n5.key, i4 = n5.offset, s3 = n5.type;
          n5.set(e3.key, e3.offset, e3.type, true), e3.set(r4, i4, s3, true);
        })(this);
      }
    }
    "lineboundary" === n2 && Br(this, t2, e2, n2, "decorators");
  }
  forwardDeletion(t2, e2, n2) {
    if (!n2 && ("element" === t2.type && hi(e2) && t2.offset === e2.getChildrenSize() || "text" === t2.type && t2.offset === e2.getTextContentSize())) {
      const t3 = e2.getParent(), n3 = e2.getNextSibling() || (null === t3 ? null : t3.getNextSibling());
      if (hi(n3) && n3.isShadowRoot()) return true;
    }
    return false;
  }
  deleteCharacter(t2) {
    const e2 = this.isCollapsed();
    if (this.isCollapsed()) {
      const e3 = this.anchor;
      let n2 = e3.getNode();
      if (this.forwardDeletion(e3, n2, t2)) return;
      const r2 = ol(_l(e3, t2 ? "previous" : "next"));
      if (r2.getTextSlices().every(((t3) => null === t3 || 0 === t3.distance))) {
        let t3 = { type: "initial" };
        for (const e4 of r2.iterNodeCarets("shadowRoot")) if (Vo(e4)) if (e4.origin.isInline()) ;
        else {
          if (e4.origin.isShadowRoot()) {
            if ("merge-block" === t3.type) break;
            if (hi(r2.anchor.origin) && r2.anchor.origin.isEmpty()) {
              const t4 = Tl(e4);
              ml(this, cl(t4, t4)), r2.anchor.origin.remove();
            }
            return;
          }
          "merge-next-block" !== t3.type && "merge-block" !== t3.type || (t3 = { block: t3.block, caret: e4, type: "merge-block" });
        }
        else {
          if ("merge-block" === t3.type) break;
          if (Uo(e4)) {
            if (hi(e4.origin)) {
              if (e4.origin.isInline()) {
                if (!e4.origin.isParentOf(r2.anchor.origin)) break;
              } else t3 = { block: e4.origin, type: "merge-next-block" };
              continue;
            }
            if (pi(e4.origin)) {
              if (e4.origin.isIsolated()) ;
              else if ("merge-next-block" === t3.type && (e4.origin.isKeyboardSelectable() || !e4.origin.isInline()) && hi(r2.anchor.origin) && r2.anchor.origin.isEmpty()) {
                r2.anchor.origin.remove();
                const t4 = Tr();
                t4.add(e4.origin.getKey()), ks(t4);
              } else e4.origin.remove();
              return;
            }
            break;
          }
        }
        if ("merge-block" === t3.type) {
          const { caret: e4, block: n3 } = t3;
          return ml(this, cl(!e4.origin.isEmpty() && n3.isEmpty() ? Cl(Go(n3, e4.direction)) : r2.anchor, e4)), this.removeText();
        }
      }
      const i2 = this.focus;
      if (this.modify("extend", t2, "character"), this.isCollapsed()) {
        if (t2 && 0 === e3.offset && gr(this, e3.getNode())) return;
      } else {
        const r3 = "text" === i2.type ? i2.getNode() : null;
        if (n2 = "text" === e3.type ? e3.getNode() : null, null !== r3 && r3.isSegmented()) {
          const e4 = i2.offset, s2 = r3.getTextContentSize();
          if (r3.is(n2) || t2 && e4 !== s2 || !t2 && 0 !== e4) return void pr(r3, t2, e4);
        } else if (null !== n2 && n2.isSegmented()) {
          const i3 = e3.offset, s2 = n2.getTextContentSize();
          if (n2.is(r3) || t2 && 0 !== i3 || !t2 && i3 !== s2) return void pr(n2, t2, i3);
        }
        !(function(t3, e4) {
          const n3 = t3.anchor, r4 = t3.focus, i3 = n3.getNode(), s2 = r4.getNode();
          if (i3 === s2 && "text" === n3.type && "text" === r4.type) {
            const t4 = n3.offset, s3 = r4.offset, o2 = t4 < s3, l2 = o2 ? t4 : s3, c2 = o2 ? s3 : t4, a2 = c2 - 1;
            if (l2 !== a2) {
              (function(t5) {
                return !(Ns(t5) || _r(t5));
              })(i3.getTextContent().slice(l2, c2)) && (e4 ? r4.set(r4.key, a2, r4.type) : n3.set(n3.key, a2, n3.type));
            }
          }
        })(this, t2);
      }
    }
    if (this.removeText(), t2 && !e2 && this.isCollapsed() && "element" === this.anchor.type && 0 === this.anchor.offset) {
      const t3 = this.anchor.getNode();
      t3.isEmpty() && mi(t3.getParent()) && null === t3.getPreviousSibling() && gr(this, t3);
    }
  }
  deleteLine(t2) {
    this.isCollapsed() && this.modify("extend", t2, "lineboundary"), this.isCollapsed() ? this.deleteCharacter(t2) : this.removeText();
  }
  deleteWord(t2) {
    if (this.isCollapsed()) {
      const e2 = this.anchor, n2 = e2.getNode();
      if (this.forwardDeletion(e2, n2, t2)) return;
      this.modify("extend", t2, "word");
    }
    this.removeText();
  }
  isBackward() {
    return this.focus.isBefore(this.anchor);
  }
  getStartEndPoints() {
    return [this.anchor, this.focus];
  }
};
function fr(t2) {
  return t2 instanceof cr;
}
function dr(t2) {
  const e2 = t2.offset;
  if ("text" === t2.type) return e2;
  const n2 = t2.getNode();
  return e2 === n2.getChildrenSize() ? n2.getTextContent().length : 0;
}
function hr(t2) {
  const e2 = t2.getStartEndPoints();
  if (null === e2) return [0, 0];
  const [n2, r2] = e2;
  return "element" === n2.type && "element" === r2.type && n2.key === r2.key && n2.offset === r2.offset ? [0, 0] : [dr(n2), dr(r2)];
}
function gr(t2, e2) {
  for (let n2 = e2; n2; n2 = n2.getParent()) {
    if (hi(n2)) {
      if (n2.collapseAtStart(t2)) return true;
      if (so(n2)) break;
    }
    if (n2.getPreviousSibling()) break;
  }
  return false;
}
var _r = (() => {
  try {
    const t2 = new RegExp("\\p{Emoji}", "u"), e2 = t2.test.bind(t2);
    if (e2("❤️") && e2("#️⃣") && e2("👍")) return e2;
  } catch (t2) {
  }
  return () => false;
})();
function pr(t2, e2, n2) {
  const r2 = t2, i2 = r2.getTextContent().split(/(?=\s)/g), s2 = i2.length;
  let o2 = 0, l2 = 0;
  for (let t3 = 0; t3 < s2; t3++) {
    const r3 = t3 === s2 - 1;
    if (l2 = o2, o2 += i2[t3].length, e2 && o2 === n2 || o2 > n2 || r3) {
      i2.splice(t3, 1), r3 && (l2 = void 0);
      break;
    }
  }
  const c2 = i2.join("").trim();
  "" === c2 ? r2.remove() : (r2.setTextContent(c2), r2.select(l2, l2));
}
function yr(e2, n2, r2, i2) {
  let s2, o2 = n2;
  if (yo(e2)) {
    let l2 = false;
    const c2 = e2.childNodes, a2 = c2.length, u2 = i2._blockCursorElement;
    o2 === a2 && (l2 = true, o2 = a2 - 1);
    let f2 = c2[o2], d2 = false;
    if (f2 === u2) f2 = c2[o2 + 1], d2 = true;
    else if (null !== u2) {
      const t2 = u2.parentNode;
      if (e2 === t2) {
        n2 > Array.prototype.indexOf.call(t2.children, u2) && o2--;
      }
    }
    if (s2 = Ts(f2), Zn(s2)) o2 = Qo(s2, l2 ? "next" : "previous");
    else {
      let c3 = Ts(e2);
      if (null === c3) return null;
      if (hi(c3)) {
        const a3 = i2.getElementByKey(c3.getKey());
        null === a3 && t(214);
        const u3 = c3.getDOMSlot(a3);
        [c3, o2] = u3.resolveChildIndex(c3, a3, e2, n2), hi(c3) || t(215), l2 && o2 >= c3.getChildrenSize() && (o2 = Math.max(0, c3.getChildrenSize() - 1));
        let f3 = c3.getChildAtIndex(o2);
        if (hi(f3) && (function(t2, e3, n3) {
          const r3 = t2.getParent();
          return null === n3 || null === r3 || !r3.canBeEmpty() || r3 !== n3.getNode();
        })(f3, 0, r2)) {
          const t2 = l2 ? f3.getLastDescendant() : f3.getFirstDescendant();
          null === t2 ? c3 = f3 : (f3 = t2, c3 = hi(f3) ? f3 : f3.getParentOrThrow()), o2 = 0;
        }
        Zn(f3) ? (s2 = f3, c3 = null, o2 = Qo(f3, l2 ? "next" : "previous")) : f3 !== c3 && l2 && !d2 && (hi(c3) || t(216), o2 = Math.min(c3.getChildrenSize(), o2 + 1));
      } else {
        const t2 = c3.getIndexWithinParent();
        o2 = 0 === n2 && pi(c3) && Ts(e2) === c3 ? t2 : t2 + 1, c3 = c3.getParentOrThrow();
      }
      if (hi(c3)) return sr(c3.__key, o2, "element");
    }
  } else s2 = Ts(e2);
  return Zn(s2) ? sr(s2.__key, Qo(s2, o2, "clamp"), "text") : null;
}
function mr(t2, e2, n2) {
  const r2 = t2.offset, i2 = t2.getNode();
  if (0 === r2) {
    const r3 = i2.getPreviousSibling(), s2 = i2.getParent();
    if (e2) {
      if ((n2 || !e2) && null === r3 && hi(s2) && s2.isInline()) {
        const e3 = s2.getPreviousSibling();
        Zn(e3) && t2.set(e3.__key, e3.getTextContent().length, "text");
      }
    } else hi(r3) && !n2 && r3.isInline() ? t2.set(r3.__key, r3.getChildrenSize(), "element") : Zn(r3) && t2.set(r3.__key, r3.getTextContent().length, "text");
  } else if (r2 === i2.getTextContent().length) {
    const r3 = i2.getNextSibling(), s2 = i2.getParent();
    if (e2 && hi(r3) && r3.isInline()) t2.set(r3.__key, 0, "element");
    else if ((n2 || e2) && null === r3 && hi(s2) && s2.isInline() && !s2.canInsertTextAfter()) {
      const e3 = s2.getNextSibling();
      Zn(e3) && t2.set(e3.__key, 0, "text");
    }
  }
}
function xr(t2, e2, n2) {
  if ("text" === t2.type && "text" === e2.type) {
    const r2 = t2.isBefore(e2), i2 = t2.is(e2);
    mr(t2, r2, i2), mr(e2, !r2, i2), i2 && e2.set(t2.key, t2.offset, t2.type);
    const s2 = Gr();
    if (s2.isComposing() && s2._compositionKey !== t2.key && ar(n2)) {
      const r3 = n2.anchor, i3 = n2.focus;
      t2.set(r3.key, r3.offset, r3.type, true), e2.set(i3.key, i3.offset, i3.type, true);
    }
  }
}
function Cr(t2, e2, n2, r2, i2, s2) {
  if (null === t2 || null === n2 || !Zi(i2, t2, n2)) return null;
  const o2 = yr(t2, e2, ar(s2) ? s2.anchor : null, i2);
  if (null === o2) return null;
  const l2 = yr(n2, r2, ar(s2) ? s2.focus : null, i2);
  if (null === l2) return null;
  if ("element" === o2.type && "element" === l2.type) {
    const e3 = Ts(t2), r3 = Ts(n2);
    if (pi(e3) && pi(r3)) return null;
  }
  return xr(o2, l2, s2), [o2, l2];
}
function vr(t2, e2, n2, r2, i2, s2) {
  const o2 = qr(), l2 = new ur(sr(t2, e2, i2), sr(n2, r2, s2), 0, "");
  return l2.dirty = true, o2._selection = l2, l2;
}
function kr() {
  const t2 = sr("root", 0, "element"), e2 = sr("root", 0, "element");
  return new ur(t2, e2, 0, "");
}
function Tr() {
  return new cr(/* @__PURE__ */ new Set());
}
function br(t2, e2, n2, r2) {
  const i2 = n2._window;
  if (null === i2) return null;
  const s2 = r2 || i2.event, o2 = s2 ? s2.type : void 0, l2 = "selectionchange" === o2, c2 = !X && (l2 || "beforeinput" === o2 || "compositionstart" === o2 || "compositionend" === o2 || "click" === o2 && s2 && 3 === s2.detail || "drop" === o2 || void 0 === o2);
  let a2, u2, f2, d2;
  if (ar(t2) && !c2) return t2.clone();
  if (null === e2) return null;
  if (a2 = e2.anchorNode, u2 = e2.focusNode, f2 = e2.anchorOffset, d2 = e2.focusOffset, l2 && ar(t2) && !Zi(n2, a2, u2)) return t2.clone();
  const h2 = Cr(a2, f2, u2, d2, n2, t2);
  if (null === h2) return null;
  const [g2, _2] = h2;
  return new ur(g2, _2, ar(t2) ? t2.format : 0, ar(t2) ? t2.style : "");
}
function wr() {
  return qr()._selection;
}
function Er() {
  return Gr()._editorState._selection;
}
function Or(t2, e2, n2, r2 = 1) {
  const i2 = t2.anchor, s2 = t2.focus, o2 = i2.getNode(), l2 = s2.getNode();
  if (!e2.is(o2) && !e2.is(l2)) return;
  const c2 = e2.__key;
  if (t2.isCollapsed()) {
    const e3 = i2.offset;
    if (n2 <= e3 && r2 > 0 || n2 < e3 && r2 < 0) {
      const n3 = Math.max(0, e3 + r2);
      i2.set(c2, n3, "element"), s2.set(c2, n3, "element"), Mr(t2);
    }
  } else {
    const o3 = t2.isBackward(), l3 = o3 ? s2 : i2, a2 = l3.getNode(), u2 = o3 ? i2 : s2, f2 = u2.getNode();
    if (e2.is(a2)) {
      const t3 = l3.offset;
      (n2 <= t3 && r2 > 0 || n2 < t3 && r2 < 0) && l3.set(c2, Math.max(0, t3 + r2), "element");
    }
    if (e2.is(f2)) {
      const t3 = u2.offset;
      (n2 <= t3 && r2 > 0 || n2 < t3 && r2 < 0) && u2.set(c2, Math.max(0, t3 + r2), "element");
    }
  }
  Mr(t2);
}
function Mr(t2) {
  const e2 = t2.anchor, n2 = e2.offset, r2 = t2.focus, i2 = r2.offset, s2 = e2.getNode(), o2 = r2.getNode();
  if (t2.isCollapsed()) {
    if (!hi(s2)) return;
    const t3 = s2.getChildrenSize(), i3 = n2 >= t3, o3 = i3 ? s2.getChildAtIndex(t3 - 1) : s2.getChildAtIndex(n2);
    if (Zn(o3)) {
      let t4 = 0;
      i3 && (t4 = o3.getTextContentSize()), e2.set(o3.__key, t4, "text"), r2.set(o3.__key, t4, "text");
    }
  } else {
    if (hi(s2)) {
      const t3 = s2.getChildrenSize(), r3 = n2 >= t3, i3 = r3 ? s2.getChildAtIndex(t3 - 1) : s2.getChildAtIndex(n2);
      if (Zn(i3)) {
        let t4 = 0;
        r3 && (t4 = i3.getTextContentSize()), e2.set(i3.__key, t4, "text");
      }
    }
    if (hi(o2)) {
      const t3 = o2.getChildrenSize(), e3 = i2 >= t3, n3 = e3 ? o2.getChildAtIndex(t3 - 1) : o2.getChildAtIndex(i2);
      if (Zn(n3)) {
        let t4 = 0;
        e3 && (t4 = n3.getTextContentSize()), r2.set(n3.__key, t4, "text");
      }
    }
  }
}
function Ar(t2, e2, n2, r2, i2) {
  let s2 = null, o2 = 0, l2 = null;
  null !== r2 ? (s2 = r2.__key, Zn(r2) ? (o2 = r2.getTextContentSize(), l2 = "text") : hi(r2) && (o2 = r2.getChildrenSize(), l2 = "element")) : null !== i2 && (s2 = i2.__key, Zn(i2) ? l2 = "text" : hi(i2) && (l2 = "element")), null !== s2 && null !== l2 ? t2.set(s2, o2, l2) : (o2 = e2.getIndexWithinParent(), -1 === o2 && (o2 = n2.getChildrenSize()), t2.set(n2.__key, o2, "element"));
}
function Pr(t2, e2, n2, r2, i2) {
  "text" === t2.type ? t2.set(n2, t2.offset + (e2 ? 0 : i2), "text") : t2.offset > r2.getIndexWithinParent() && t2.set(t2.key, t2.offset - 1, "element");
}
function Dr(t2, e2, n2, r2, i2) {
  try {
    t2.setBaseAndExtent(e2, n2, r2, i2);
  } catch (t3) {
  }
}
function Fr(t2, e2, n2, r2, i2, s2, o2) {
  const l2 = r2.anchorNode, c2 = r2.focusNode, a2 = r2.anchorOffset, u2 = r2.focusOffset, f2 = document.activeElement;
  if (i2.has(wi) && f2 !== s2 || null !== f2 && Qi(f2)) return;
  if (!ar(e2)) return void (null !== t2 && Zi(n2, l2, c2) && r2.removeAllRanges());
  const d2 = e2.anchor, h2 = e2.focus, g2 = d2.key, _2 = h2.key, p2 = Hs(n2, g2), y2 = Hs(n2, _2), m2 = d2.offset, x2 = h2.offset, C2 = e2.format, S2 = e2.style, v2 = e2.isCollapsed();
  let k = p2, T = y2, N2 = false;
  if ("text" === d2.type) {
    k = ls(p2);
    const t3 = d2.getNode();
    N2 = t3.getFormat() !== C2 || t3.getStyle() !== S2;
  } else ar(t2) && "text" === t2.anchor.type && (N2 = true);
  var b2, w, E, O, M2;
  if (("text" === h2.type && (T = ls(y2)), null !== k && null !== T) && (v2 && (null === t2 || N2 || ar(t2) && (t2.format !== C2 || t2.style !== S2)) && (b2 = C2, w = S2, E = m2, O = g2, M2 = performance.now(), gn = [b2, w, E, O, M2]), a2 !== m2 || u2 !== x2 || l2 !== k || c2 !== T || "Range" === r2.type && v2 || (null !== f2 && s2.contains(f2) || s2.focus({ preventScroll: true }), "element" === d2.type))) {
    if (Dr(r2, k, m2, T, x2), !i2.has(Oi) && e2.isCollapsed() && null !== s2 && s2 === document.activeElement) {
      const t3 = ar(e2) && "element" === e2.anchor.type ? k.childNodes[m2] || null : r2.rangeCount > 0 ? r2.getRangeAt(0) : null;
      if (null !== t3) {
        let e3;
        if (t3 instanceof Text) {
          const n3 = document.createRange();
          n3.selectNode(t3), e3 = n3.getBoundingClientRect();
        } else e3 = t3.getBoundingClientRect();
        !(function(t4, e4, n3) {
          const r3 = Gs(n3), i3 = eo(r3);
          if (null === r3 || null === i3) return;
          let { top: s3, bottom: o3 } = e4, l3 = 0, c3 = 0, a3 = n3;
          for (; null !== a3; ) {
            const e5 = a3 === r3.body;
            if (e5) l3 = 0, c3 = no(t4).innerHeight;
            else {
              const t5 = a3.getBoundingClientRect();
              l3 = t5.top, c3 = t5.bottom;
            }
            let n4 = 0;
            if (s3 < l3 ? n4 = -(l3 - s3) : o3 > c3 && (n4 = o3 - c3), 0 !== n4) if (e5) i3.scrollBy(0, n4);
            else {
              const t5 = a3.scrollTop;
              a3.scrollTop += n4;
              const e6 = a3.scrollTop - t5;
              s3 -= e6, o3 -= e6;
            }
            if (e5) break;
            a3 = qs(a3);
          }
        })(n2, e3, s2);
      }
    }
    ln = true;
  }
}
function Kr(e2) {
  let n2 = e2;
  e2.isCollapsed() || n2.removeText();
  const r2 = wr();
  ar(r2) && (n2 = r2), ar(n2) || t(161);
  const i2 = n2.anchor;
  let s2 = i2.getNode(), o2 = i2.offset;
  for (; !vo(s2); ) {
    const t2 = s2;
    if ([s2, o2] = zr(s2, o2), t2.is(s2)) break;
  }
  return o2;
}
function zr(t2, e2) {
  const n2 = t2.getParent();
  if (!n2) {
    const t3 = Fi();
    return Ss().append(t3), t3.select(), [Ss(), 0];
  }
  if (Zn(t2)) {
    const r3 = t2.splitText(e2);
    if (0 === r3.length) return [n2, t2.getIndexWithinParent()];
    const i2 = 0 === e2 ? 0 : 1;
    return [n2, r3[0].getIndexWithinParent() + i2];
  }
  if (!hi(t2) || 0 === e2) return [n2, t2.getIndexWithinParent()];
  const r2 = t2.getChildAtIndex(e2);
  if (r2) {
    const n3 = new ur(sr(t2.__key, e2, "element"), sr(t2.__key, e2, "element"), 0, ""), i2 = t2.insertNewAfter(n3);
    i2 && i2.append(r2, ...r2.getNextSiblings());
  }
  return [n2, t2.getIndexWithinParent() + 1];
}
function Br(t2, e2, n2, r2, i2 = "decorators-and-blocks") {
  if ("move" === e2 && "character" === r2 && !t2.isCollapsed()) {
    const [e3, r3] = n2 === t2.isBackward() ? [t2.focus, t2.anchor] : [t2.anchor, t2.focus];
    return r3.set(e3.key, e3.offset, e3.type), true;
  }
  const s2 = _l(t2.focus, n2 ? "previous" : "next"), o2 = "lineboundary" === r2, l2 = "move" === e2;
  let c2 = s2, a2 = "decorators-and-blocks" === i2;
  if (!Nl(c2)) {
    for (const t3 of c2) {
      a2 = false;
      const { origin: e3 } = t3;
      if (!pi(e3) || e3.isIsolated() || (c2 = t3, !o2 || !e3.isInline())) break;
    }
    if (a2) for (const t3 of ol(s2).iterNodeCarets("extend" === e2 ? "shadowRoot" : "root")) {
      if (Vo(t3)) t3.origin.isInline() || (c2 = t3);
      else {
        if (hi(t3.origin)) continue;
        pi(t3.origin) && !t3.origin.isInline() && (c2 = t3);
      }
      break;
    }
  }
  if (c2 === s2) return false;
  if (l2 && !o2 && pi(c2.origin) && c2.origin.isKeyboardSelectable()) {
    const t3 = Tr();
    return t3.add(c2.origin.getKey()), ks(t3), true;
  }
  return c2 = Tl(c2), l2 && pl(t2.anchor, c2), pl(t2.focus, c2), a2 || !o2;
}
var Rr = null;
var Wr = null;
var Jr = false;
var $r = false;
var jr = 0;
var Ur = { characterData: true, childList: true, subtree: true };
function Vr() {
  return Jr || null !== Rr && Rr._readOnly;
}
function Yr() {
  Jr && t(13);
}
function Hr() {
  jr > 99 && t(14);
}
function qr() {
  return null === Rr && t(195, Xr()), Rr;
}
function Gr() {
  return null === Wr && t(196, Xr()), Wr;
}
function Xr() {
  let t2 = 0;
  const e2 = /* @__PURE__ */ new Set(), n2 = $i.version;
  if ("undefined" != typeof window) for (const r3 of document.querySelectorAll("[contenteditable]")) {
    const i2 = ns(r3);
    if (ts(i2)) t2++;
    else if (i2) {
      let t3 = String(i2.constructor.version || "<0.17.1");
      t3 === n2 && (t3 += " (separately built, likely a bundler configuration issue)"), e2.add(t3);
    }
  }
  let r2 = ` Detected on the page: ${t2} compatible editor(s) with version ${n2}`;
  return e2.size && (r2 += ` and incompatible editors with versions ${Array.from(e2).join(", ")}`), r2;
}
function Zr(t2, e2, n2) {
  const r2 = e2.__type, i2 = Hi(t2, r2);
  let s2 = n2.get(r2);
  void 0 === s2 && (s2 = Array.from(i2.transforms), n2.set(r2, s2));
  const o2 = s2.length;
  for (let t3 = 0; t3 < o2 && (s2[t3](e2), e2.isAttached()); t3++) ;
}
function ti(t2, e2) {
  return void 0 !== t2 && t2.__key !== e2 && t2.isAttached();
}
function ei(t2, e2) {
  if (!e2) return;
  const n2 = t2._updateTags;
  let r2 = e2;
  Array.isArray(e2) || (r2 = [e2]);
  for (const t3 of r2) n2.add(t3);
}
function ri(e2, n2) {
  const r2 = e2.type, i2 = n2.get(r2);
  void 0 === i2 && t(17, r2);
  const s2 = i2.klass;
  e2.type !== s2.getType() && t(18, s2.name);
  const o2 = s2.importJSON(e2), l2 = e2.children;
  if (hi(o2) && Array.isArray(l2)) for (let t2 = 0; t2 < l2.length; t2++) {
    const e3 = ri(l2[t2], n2);
    o2.append(e3);
  }
  return o2;
}
function ii(t2, e2, n2) {
  const r2 = Rr, i2 = Jr, s2 = Wr;
  Rr = e2, Jr = true, Wr = t2;
  try {
    return n2();
  } finally {
    Rr = r2, Jr = i2, Wr = s2;
  }
}
function si(t2, e2) {
  const n2 = t2._pendingEditorState, r2 = t2._rootElement, i2 = t2._headless || null === r2;
  if (null === n2) return;
  const s2 = t2._editorState, o2 = s2._selection, l2 = n2._selection, c2 = t2._dirtyType !== y, a2 = Rr, u2 = Jr, f2 = Wr, h2 = t2._updating, g2 = t2._observer;
  let _2 = null;
  if (t2._pendingEditorState = null, t2._editorState = n2, !i2 && c2 && null !== g2) {
    Wr = t2, Rr = n2, Jr = false, t2._updating = true;
    try {
      const e3 = t2._dirtyType, r3 = t2._dirtyElements, i3 = t2._dirtyLeaves;
      g2.disconnect(), _2 = ie(s2, n2, t2, e3, r3, i3);
    } catch (e3) {
      if (e3 instanceof Error && t2._onError(e3), $r) throw e3;
      return Wi(t2, null, r2, n2), ot(t2), t2._dirtyType = x, $r = true, si(t2, s2), void ($r = false);
    } finally {
      g2.observe(r2, Ur), t2._updating = h2, Rr = a2, Jr = u2, Wr = f2;
    }
  }
  n2._readOnly || (n2._readOnly = true);
  const p2 = t2._dirtyLeaves, m2 = t2._dirtyElements, C2 = t2._normalizedNodes, S2 = t2._updateTags, v2 = t2._deferred;
  c2 && (t2._dirtyType = y, t2._cloneNotNeeded.clear(), t2._dirtyLeaves = /* @__PURE__ */ new Set(), t2._dirtyElements = /* @__PURE__ */ new Map(), t2._normalizedNodes = /* @__PURE__ */ new Set(), t2._updateTags = /* @__PURE__ */ new Set()), (function(t3, e3) {
    const n3 = t3._decorators;
    let r3 = t3._pendingDecorators || n3;
    const i3 = e3._nodeMap;
    let s3;
    for (s3 in r3) i3.has(s3) || (r3 === n3 && (r3 = xs(t3)), delete r3[s3]);
  })(t2, n2);
  const k = i2 ? null : ho(no(t2));
  if (t2._editable && null !== k && (c2 || null === l2 || l2.dirty) && null !== r2 && !S2.has(Mi)) {
    Wr = t2, Rr = n2;
    try {
      if (null !== g2 && g2.disconnect(), c2 || null === l2 || l2.dirty) {
        const e3 = t2._blockCursorElement;
        null !== e3 && fo(e3, t2, r2), Fr(o2, l2, t2, k, S2, r2);
      }
      !(function(t3, e3, n3) {
        let r3 = t3._blockCursorElement;
        if (ar(n3) && n3.isCollapsed() && "element" === n3.anchor.type && e3.contains(document.activeElement)) {
          const i3 = n3.anchor, s3 = i3.getNode(), o3 = i3.offset;
          let l3 = false, c3 = null;
          if (o3 === s3.getChildrenSize()) {
            uo(s3.getChildAtIndex(o3 - 1)) && (l3 = true);
          } else {
            const e4 = s3.getChildAtIndex(o3);
            if (null !== e4 && uo(e4)) {
              const n4 = e4.getPreviousSibling();
              (null === n4 || uo(n4)) && (l3 = true, c3 = t3.getElementByKey(e4.__key));
            }
          }
          if (l3) {
            const n4 = t3.getElementByKey(s3.__key);
            return null === r3 && (t3._blockCursorElement = r3 = (function(t4) {
              const e4 = t4.theme, n5 = document.createElement("div");
              n5.contentEditable = "false", n5.setAttribute("data-lexical-cursor", "true");
              let r4 = e4.blockCursor;
              if (void 0 !== r4) {
                if ("string" == typeof r4) {
                  const t5 = d(r4);
                  r4 = e4.blockCursor = t5;
                }
                void 0 !== r4 && n5.classList.add(...r4);
              }
              return n5;
            })(t3._config)), e3.style.caretColor = "transparent", void (null === c3 ? n4.appendChild(r3) : n4.insertBefore(r3, c3));
          }
        }
        null !== r3 && fo(r3, t3, e3);
      })(t2, r2, l2);
    } finally {
      null !== g2 && g2.observe(r2, Ur), Wr = f2, Rr = a2;
    }
  }
  null !== _2 && (function(t3, e3, n3, r3, i3) {
    const s3 = Array.from(t3._listeners.mutation), o3 = s3.length;
    for (let t4 = 0; t4 < o3; t4++) {
      const [o4, l3] = s3[t4];
      for (const t5 of l3) {
        const s4 = e3.get(t5);
        void 0 !== s4 && o4(s4, { dirtyLeaves: r3, prevEditorState: i3, updateTags: n3 });
      }
    }
  })(t2, _2, S2, p2, s2), ar(l2) || null === l2 || null !== o2 && o2.is(l2) || t2.dispatchCommand(le, void 0);
  const T = t2._pendingDecorators;
  null !== T && (t2._decorators = T, t2._pendingDecorators = null, oi("decorator", t2, true, T)), (function(t3, e3, n3) {
    const r3 = Cs(e3), i3 = Cs(n3);
    r3 !== i3 && oi("textcontent", t3, true, i3);
  })(t2, e2 || s2, n2), oi("update", t2, true, { dirtyElements: m2, dirtyLeaves: p2, editorState: n2, mutatedNodes: _2, normalizedNodes: C2, prevEditorState: e2 || s2, tags: S2 }), (function(t3, e3) {
    if (t3._deferred = [], 0 !== e3.length) {
      const n3 = t3._updating;
      t3._updating = true;
      try {
        for (let t4 = 0; t4 < e3.length; t4++) e3[t4]();
      } finally {
        t3._updating = n3;
      }
    }
  })(t2, v2), (function(t3) {
    const e3 = t3._updates;
    if (0 !== e3.length) {
      const n3 = e3.shift();
      if (n3) {
        const [e4, r3] = n3;
        ci(t3, e4, r3);
      }
    }
  })(t2);
}
function oi(t2, e2, n2, ...r2) {
  const i2 = e2._updating;
  e2._updating = n2;
  try {
    const n3 = Array.from(e2._listeners[t2]);
    for (let t3 = 0; t3 < n3.length; t3++) n3[t3].apply(null, r2);
  } finally {
    e2._updating = i2;
  }
}
function li(e2, n2) {
  const r2 = e2._updates;
  let i2 = n2 || false;
  for (; 0 !== r2.length; ) {
    const n3 = r2.shift();
    if (n3) {
      const [r3, s2] = n3, o2 = e2._pendingEditorState;
      let l2;
      void 0 !== s2 && (l2 = s2.onUpdate, s2.skipTransforms && (i2 = true), s2.discrete && (null === o2 && t(191), o2._flushSync = true), l2 && e2._deferred.push(l2), ei(e2, s2.tag)), null == o2 ? ci(e2, r3, s2) : r3();
    }
  }
  return i2;
}
function ci(e2, n2, r2) {
  const i2 = e2._updateTags;
  let s2, o2 = false, l2 = false;
  void 0 !== r2 && (s2 = r2.onUpdate, ei(e2, r2.tag), o2 = r2.skipTransforms || false, l2 = r2.discrete || false), s2 && e2._deferred.push(s2);
  const c2 = e2._editorState;
  let a2 = e2._pendingEditorState, u2 = false;
  (null === a2 || a2._readOnly) && (a2 = e2._pendingEditorState = xi(a2 || c2), u2 = true), a2._flushSync = l2;
  const f2 = Rr, d2 = Jr, h2 = Wr, g2 = e2._updating;
  Rr = a2, Jr = false, e2._updating = true, Wr = e2;
  const _2 = e2._headless || null === e2.getRootElement();
  Ui(null);
  try {
    u2 && (_2 ? null !== c2._selection && (a2._selection = c2._selection.clone()) : a2._selection = (function(t2, e3) {
      const n3 = t2.getEditorState()._selection, r3 = ho(no(t2));
      return ar(n3) || null == n3 ? br(n3, r3, t2, e3) : n3.clone();
    })(e2, r2 && r2.event || null));
    const i3 = e2._compositionKey;
    n2(), o2 = li(e2, o2), (function(t2, e3) {
      const n3 = e3.getEditorState()._selection, r3 = t2._selection;
      if (ar(r3)) {
        const t3 = r3.anchor, e4 = r3.focus;
        let i4;
        if ("text" === t3.type && (i4 = t3.getNode(), i4.selectionTransform(n3, r3)), "text" === e4.type) {
          const t4 = e4.getNode();
          i4 !== t4 && t4.selectionTransform(n3, r3);
        }
      }
    })(a2, e2), e2._dirtyType !== y && (o2 ? (function(t2, e3) {
      const n3 = e3._dirtyLeaves, r3 = t2._nodeMap;
      for (const t3 of n3) {
        const e4 = r3.get(t3);
        Zn(e4) && e4.isAttached() && e4.isSimpleText() && !e4.isUnmergeable() && kt(e4);
      }
    })(a2, e2) : (function(t2, e3) {
      const n3 = e3._dirtyLeaves, r3 = e3._dirtyElements, i4 = t2._nodeMap, s4 = gs(), o3 = /* @__PURE__ */ new Map();
      let l3 = n3, c3 = l3.size, a3 = r3, u3 = a3.size;
      for (; c3 > 0 || u3 > 0; ) {
        if (c3 > 0) {
          e3._dirtyLeaves = /* @__PURE__ */ new Set();
          for (const t3 of l3) {
            const r4 = i4.get(t3);
            Zn(r4) && r4.isAttached() && r4.isSimpleText() && !r4.isUnmergeable() && kt(r4), void 0 !== r4 && ti(r4, s4) && Zr(e3, r4, o3), n3.add(t3);
          }
          if (l3 = e3._dirtyLeaves, c3 = l3.size, c3 > 0) {
            jr++;
            continue;
          }
        }
        e3._dirtyLeaves = /* @__PURE__ */ new Set(), e3._dirtyElements = /* @__PURE__ */ new Map(), a3.delete("root") && a3.set("root", true);
        for (const t3 of a3) {
          const n4 = t3[0], l4 = t3[1];
          if (r3.set(n4, l4), !l4) continue;
          const c4 = i4.get(n4);
          void 0 !== c4 && ti(c4, s4) && Zr(e3, c4, o3);
        }
        l3 = e3._dirtyLeaves, c3 = l3.size, a3 = e3._dirtyElements, u3 = a3.size, jr++;
      }
      e3._dirtyLeaves = n3, e3._dirtyElements = r3;
    })(a2, e2), li(e2), (function(t2, e3, n3, r3) {
      const i4 = t2._nodeMap, s4 = e3._nodeMap, o3 = [];
      for (const [t3] of r3) {
        const e4 = s4.get(t3);
        void 0 !== e4 && (e4.isAttached() || (hi(e4) && q(e4, t3, i4, s4, o3, r3), i4.has(t3) || r3.delete(t3), o3.push(t3)));
      }
      for (const t3 of o3) s4.delete(t3);
      for (const t3 of n3) {
        const e4 = s4.get(t3);
        void 0 === e4 || e4.isAttached() || (i4.has(t3) || n3.delete(t3), s4.delete(t3));
      }
    })(c2, a2, e2._dirtyLeaves, e2._dirtyElements));
    i3 !== e2._compositionKey && (a2._flushSync = true);
    const s3 = a2._selection;
    if (ar(s3)) {
      const e3 = a2._nodeMap, n3 = s3.anchor.key, r3 = s3.focus.key;
      void 0 !== e3.get(n3) && void 0 !== e3.get(r3) || t(19);
    } else fr(s3) && 0 === s3._nodes.size && (a2._selection = null);
  } catch (t2) {
    return t2 instanceof Error && e2._onError(t2), e2._pendingEditorState = c2, e2._dirtyType = x, e2._cloneNotNeeded.clear(), e2._dirtyLeaves = /* @__PURE__ */ new Set(), e2._dirtyElements.clear(), void si(e2);
  } finally {
    Rr = f2, Jr = d2, Wr = h2, e2._updating = g2, jr = 0;
  }
  const p2 = e2._dirtyType !== y || e2._deferred.length > 0 || (function(t2, e3) {
    const n3 = e3.getEditorState()._selection, r3 = t2._selection;
    if (null !== r3) {
      if (r3.dirty || !r3.is(n3)) return true;
    } else if (null !== n3) return true;
    return false;
  })(a2, e2);
  p2 ? a2._flushSync ? (a2._flushSync = false, si(e2)) : u2 && Gi((() => {
    si(e2);
  })) : (a2._flushSync = false, u2 && (i2.clear(), e2._deferred = [], e2._pendingEditorState = null));
}
function ai(t2, e2, n2) {
  Wr === t2 && void 0 === n2 ? e2() : ci(t2, e2, n2);
}
var ui = class _ui {
  constructor(t2, e2, n2) {
    this.element = t2, this.before = e2 || null, this.after = n2 || null;
  }
  withBefore(t2) {
    return new _ui(this.element, t2, this.after);
  }
  withAfter(t2) {
    return new _ui(this.element, this.before, t2);
  }
  withElement(t2) {
    return this.element === t2 ? this : new _ui(t2, this.before, this.after);
  }
  insertChild(e2) {
    const n2 = this.before || this.getManagedLineBreak();
    return null !== n2 && n2.parentElement !== this.element && t(222), this.element.insertBefore(e2, n2), this;
  }
  removeChild(e2) {
    return e2.parentElement !== this.element && t(223), this.element.removeChild(e2), this;
  }
  replaceChild(e2, n2) {
    return n2.parentElement !== this.element && t(224), this.element.replaceChild(e2, n2), this;
  }
  getFirstChild() {
    const t2 = this.after ? this.after.nextSibling : this.element.firstChild;
    return t2 === this.before || t2 === this.getManagedLineBreak() ? null : t2;
  }
  getManagedLineBreak() {
    return this.element.__lexicalLineBreak || null;
  }
  setManagedLineBreak(t2) {
    if (null === t2) this.removeManagedLineBreak();
    else {
      const e2 = "decorator" === t2 && (l || o);
      this.insertManagedLineBreak(e2);
    }
  }
  removeManagedLineBreak() {
    const t2 = this.getManagedLineBreak();
    if (t2) {
      const e2 = this.element, n2 = "IMG" === t2.nodeName ? t2.nextSibling : null;
      n2 && e2.removeChild(n2), e2.removeChild(t2), e2.__lexicalLineBreak = void 0;
    }
  }
  insertManagedLineBreak(t2) {
    const e2 = this.getManagedLineBreak();
    if (e2) {
      if (t2 === ("IMG" === e2.nodeName)) return;
      this.removeManagedLineBreak();
    }
    const n2 = this.element, r2 = this.before, i2 = document.createElement("br");
    if (n2.insertBefore(i2, r2), t2) {
      const t3 = document.createElement("img");
      t3.setAttribute("data-lexical-linebreak", "true"), t3.style.cssText = "display: inline !important; border: 0px !important; margin: 0px !important;", t3.alt = "", n2.insertBefore(t3, i2), n2.__lexicalLineBreak = t3;
    } else n2.__lexicalLineBreak = i2;
  }
  getFirstChildOffset() {
    let t2 = 0;
    for (let e2 = this.after; null !== e2; e2 = e2.previousSibling) t2++;
    return t2;
  }
  resolveChildIndex(t2, e2, n2, r2) {
    if (n2 === this.element) {
      const e3 = this.getFirstChildOffset();
      return [t2, Math.min(e3 + t2.getChildrenSize(), Math.max(e3, r2))];
    }
    const i2 = fi(e2, n2);
    i2.push(r2);
    const s2 = fi(e2, this.element);
    let o2 = t2.getIndexWithinParent();
    for (let t3 = 0; t3 < s2.length; t3++) {
      const e3 = i2[t3], n3 = s2[t3];
      if (void 0 === e3 || e3 < n3) break;
      if (e3 > n3) {
        o2 += 1;
        break;
      }
    }
    return [t2.getParentOrThrow(), o2];
  }
};
function fi(e2, n2) {
  const r2 = [];
  let i2 = n2;
  for (; i2 !== e2 && null !== i2; i2 = i2.parentNode) {
    let t2 = 0;
    for (let e3 = i2.previousSibling; null !== e3; e3 = e3.previousSibling) t2++;
    r2.push(t2);
  }
  return i2 !== e2 && t(225), r2.reverse();
}
var di = class extends An {
  constructor(t2) {
    super(t2), this.__first = null, this.__last = null, this.__size = 0, this.__format = 0, this.__style = "", this.__indent = 0, this.__dir = null, this.__textFormat = 0, this.__textStyle = "";
  }
  afterCloneFrom(t2) {
    super.afterCloneFrom(t2), this.__key === t2.__key && (this.__first = t2.__first, this.__last = t2.__last, this.__size = t2.__size), this.__indent = t2.__indent, this.__format = t2.__format, this.__style = t2.__style, this.__dir = t2.__dir, this.__textFormat = t2.__textFormat, this.__textStyle = t2.__textStyle;
  }
  getFormat() {
    return this.getLatest().__format;
  }
  getFormatType() {
    const t2 = this.getFormat();
    return j[t2] || "";
  }
  getStyle() {
    return this.getLatest().__style;
  }
  getIndent() {
    return this.getLatest().__indent;
  }
  getChildren() {
    const t2 = [];
    let e2 = this.getFirstChild();
    for (; null !== e2; ) t2.push(e2), e2 = e2.getNextSibling();
    return t2;
  }
  getChildrenKeys() {
    const t2 = [];
    let e2 = this.getFirstChild();
    for (; null !== e2; ) t2.push(e2.__key), e2 = e2.getNextSibling();
    return t2;
  }
  getChildrenSize() {
    return this.getLatest().__size;
  }
  isEmpty() {
    return 0 === this.getChildrenSize();
  }
  isDirty() {
    const t2 = Gr()._dirtyElements;
    return null !== t2 && t2.has(this.__key);
  }
  isLastChild() {
    const t2 = this.getLatest(), e2 = this.getParentOrThrow().getLastChild();
    return null !== e2 && e2.is(t2);
  }
  getAllTextNodes() {
    const t2 = [];
    let e2 = this.getFirstChild();
    for (; null !== e2; ) {
      if (Zn(e2) && t2.push(e2), hi(e2)) {
        const n2 = e2.getAllTextNodes();
        t2.push(...n2);
      }
      e2 = e2.getNextSibling();
    }
    return t2;
  }
  getFirstDescendant() {
    let t2 = this.getFirstChild();
    for (; hi(t2); ) {
      const e2 = t2.getFirstChild();
      if (null === e2) break;
      t2 = e2;
    }
    return t2;
  }
  getLastDescendant() {
    let t2 = this.getLastChild();
    for (; hi(t2); ) {
      const e2 = t2.getLastChild();
      if (null === e2) break;
      t2 = e2;
    }
    return t2;
  }
  getDescendantByIndex(t2) {
    const e2 = this.getChildren(), n2 = e2.length;
    if (t2 >= n2) {
      const t3 = e2[n2 - 1];
      return hi(t3) && t3.getLastDescendant() || t3 || null;
    }
    const r2 = e2[t2];
    return hi(r2) && r2.getFirstDescendant() || r2 || null;
  }
  getFirstChild() {
    const t2 = this.getLatest().__first;
    return null === t2 ? null : _s(t2);
  }
  getFirstChildOrThrow() {
    const e2 = this.getFirstChild();
    return null === e2 && t(45, this.__key), e2;
  }
  getLastChild() {
    const t2 = this.getLatest().__last;
    return null === t2 ? null : _s(t2);
  }
  getLastChildOrThrow() {
    const e2 = this.getLastChild();
    return null === e2 && t(96, this.__key), e2;
  }
  getChildAtIndex(t2) {
    const e2 = this.getChildrenSize();
    let n2, r2;
    if (t2 < e2 / 2) {
      for (n2 = this.getFirstChild(), r2 = 0; null !== n2 && r2 <= t2; ) {
        if (r2 === t2) return n2;
        n2 = n2.getNextSibling(), r2++;
      }
      return null;
    }
    for (n2 = this.getLastChild(), r2 = e2 - 1; null !== n2 && r2 >= t2; ) {
      if (r2 === t2) return n2;
      n2 = n2.getPreviousSibling(), r2--;
    }
    return null;
  }
  getTextContent() {
    let t2 = "";
    const e2 = this.getChildren(), n2 = e2.length;
    for (let r2 = 0; r2 < n2; r2++) {
      const i2 = e2[r2];
      t2 += i2.getTextContent(), hi(i2) && r2 !== n2 - 1 && !i2.isInline() && (t2 += B);
    }
    return t2;
  }
  getTextContentSize() {
    let t2 = 0;
    const e2 = this.getChildren(), n2 = e2.length;
    for (let r2 = 0; r2 < n2; r2++) {
      const i2 = e2[r2];
      t2 += i2.getTextContentSize(), hi(i2) && r2 !== n2 - 1 && !i2.isInline() && (t2 += B.length);
    }
    return t2;
  }
  getDirection() {
    return this.getLatest().__dir;
  }
  getTextFormat() {
    return this.getLatest().__textFormat;
  }
  hasFormat(t2) {
    if ("" !== t2) {
      const e2 = $[t2];
      return !!(this.getFormat() & e2);
    }
    return false;
  }
  hasTextFormat(t2) {
    const e2 = W[t2];
    return !!(this.getTextFormat() & e2);
  }
  getFormatFlags(t2, e2) {
    return cs(this.getLatest().__textFormat, t2, e2);
  }
  getTextStyle() {
    return this.getLatest().__textStyle;
  }
  select(t2, e2) {
    Yr();
    const n2 = wr();
    let r2 = t2, i2 = e2;
    const s2 = this.getChildrenSize();
    if (!this.canBeEmpty()) {
      if (0 === t2 && 0 === e2) {
        const t3 = this.getFirstChild();
        if (Zn(t3) || hi(t3)) return t3.select(0, 0);
      } else if (!(void 0 !== t2 && t2 !== s2 || void 0 !== e2 && e2 !== s2)) {
        const t3 = this.getLastChild();
        if (Zn(t3) || hi(t3)) return t3.select();
      }
    }
    void 0 === r2 && (r2 = s2), void 0 === i2 && (i2 = s2);
    const o2 = this.__key;
    return ar(n2) ? (n2.anchor.set(o2, r2, "element"), n2.focus.set(o2, i2, "element"), n2.dirty = true, n2) : vr(o2, r2, o2, i2, "element", "element");
  }
  selectStart() {
    const t2 = this.getFirstDescendant();
    return t2 ? t2.selectStart() : this.select();
  }
  selectEnd() {
    const t2 = this.getLastDescendant();
    return t2 ? t2.selectEnd() : this.select();
  }
  clear() {
    const t2 = this.getWritable();
    return this.getChildren().forEach(((t3) => t3.remove())), t2;
  }
  append(...t2) {
    return this.splice(this.getChildrenSize(), 0, t2);
  }
  setDirection(t2) {
    const e2 = this.getWritable();
    return e2.__dir = t2, e2;
  }
  setFormat(t2) {
    return this.getWritable().__format = "" !== t2 ? $[t2] : 0, this;
  }
  setStyle(t2) {
    return this.getWritable().__style = t2 || "", this;
  }
  setTextFormat(t2) {
    const e2 = this.getWritable();
    return e2.__textFormat = t2, e2;
  }
  setTextStyle(t2) {
    const e2 = this.getWritable();
    return e2.__textStyle = t2, e2;
  }
  setIndent(t2) {
    return this.getWritable().__indent = t2, this;
  }
  splice(e2, n2, r2) {
    const i2 = r2.length, s2 = this.getChildrenSize(), o2 = this.getWritable();
    e2 + n2 <= s2 || t(226, String(e2), String(n2), String(s2));
    const l2 = o2.__key, c2 = [], a2 = [], u2 = this.getChildAtIndex(e2 + n2);
    let f2 = null, d2 = s2 - n2 + i2;
    if (0 !== e2) if (e2 === s2) f2 = this.getLastChild();
    else {
      const t2 = this.getChildAtIndex(e2);
      null !== t2 && (f2 = t2.getPreviousSibling());
    }
    if (n2 > 0) {
      let e3 = null === f2 ? this.getFirstChild() : f2.getNextSibling();
      for (let r3 = 0; r3 < n2; r3++) {
        null === e3 && t(100);
        const n3 = e3.getNextSibling(), r4 = e3.__key;
        fs(e3.getWritable()), a2.push(r4), e3 = n3;
      }
    }
    let h2 = f2;
    for (let e3 = 0; e3 < i2; e3++) {
      const n3 = r2[e3];
      null !== h2 && n3.is(h2) && (f2 = h2 = h2.getPreviousSibling());
      const i3 = n3.getWritable();
      i3.__parent === l2 && d2--, fs(i3);
      const s3 = n3.__key;
      if (null === h2) o2.__first = s3, i3.__prev = null;
      else {
        const t2 = h2.getWritable();
        t2.__next = s3, i3.__prev = t2.__key;
      }
      n3.__key === l2 && t(76), i3.__parent = l2, c2.push(s3), h2 = n3;
    }
    if (e2 + n2 === s2) {
      if (null !== h2) {
        h2.getWritable().__next = null, o2.__last = h2.__key;
      }
    } else if (null !== u2) {
      const t2 = u2.getWritable();
      if (null !== h2) {
        const e3 = h2.getWritable();
        t2.__prev = h2.__key, e3.__next = u2.__key;
      } else t2.__prev = null;
    }
    if (o2.__size = d2, a2.length) {
      const t2 = wr();
      if (ar(t2)) {
        const e3 = new Set(a2), n3 = new Set(c2), { anchor: r3, focus: i3 } = t2;
        gi(r3, e3, n3) && Ar(r3, r3.getNode(), this, f2, u2), gi(i3, e3, n3) && Ar(i3, i3.getNode(), this, f2, u2), 0 !== d2 || this.canBeEmpty() || so(this) || this.remove();
      }
    }
    return o2;
  }
  getDOMSlot(t2) {
    return new ui(t2);
  }
  exportDOM(t2) {
    const { element: e2 } = super.exportDOM(t2);
    if (yo(e2)) {
      const t3 = this.getIndent();
      t3 > 0 && (e2.style.paddingInlineStart = 40 * t3 + "px");
      const n2 = this.getDirection();
      n2 && (e2.dir = n2);
    }
    return { element: e2 };
  }
  exportJSON() {
    const t2 = { children: [], direction: this.getDirection(), format: this.getFormatType(), indent: this.getIndent(), ...super.exportJSON() }, e2 = this.getTextFormat(), n2 = this.getTextStyle();
    return 0 !== e2 && (t2.textFormat = e2), "" !== n2 && (t2.textStyle = n2), t2;
  }
  updateFromJSON(t2) {
    return super.updateFromJSON(t2).setFormat(t2.format).setIndent(t2.indent).setDirection(t2.direction).setTextFormat(t2.textFormat || 0).setTextStyle(t2.textStyle || "");
  }
  insertNewAfter(t2, e2) {
    return null;
  }
  canIndent() {
    return true;
  }
  collapseAtStart(t2) {
    return false;
  }
  excludeFromCopy(t2) {
    return false;
  }
  canReplaceWith(t2) {
    return true;
  }
  canInsertAfter(t2) {
    return true;
  }
  canBeEmpty() {
    return true;
  }
  canInsertTextBefore() {
    return true;
  }
  canInsertTextAfter() {
    return true;
  }
  isInline() {
    return false;
  }
  isShadowRoot() {
    return false;
  }
  canMergeWith(t2) {
    return false;
  }
  extractWithChild(t2, e2, n2) {
    return false;
  }
  canMergeWhenEmpty() {
    return false;
  }
  reconcileObservedMutation(t2, e2) {
    const n2 = this.getDOMSlot(t2);
    let r2 = n2.getFirstChild();
    for (let t3 = this.getFirstChild(); t3; t3 = t3.getNextSibling()) {
      const i2 = e2.getElementByKey(t3.getKey());
      null !== i2 && (null == r2 ? (n2.insertChild(i2), r2 = i2) : r2 !== i2 && n2.replaceChild(i2, r2), r2 = r2.nextSibling);
    }
  }
};
function hi(t2) {
  return t2 instanceof di;
}
function gi(t2, e2, n2) {
  let r2 = t2.getNode();
  for (; r2; ) {
    const t3 = r2.__key;
    if (e2.has(t3) && !n2.has(t3)) return true;
    r2 = r2.getParent();
  }
  return false;
}
var _i = class extends An {
  decorate(e2, n2) {
    t(47);
  }
  isIsolated() {
    return false;
  }
  isInline() {
    return true;
  }
  isKeyboardSelectable() {
    return true;
  }
};
function pi(t2) {
  return t2 instanceof _i;
}
var yi = class _yi extends di {
  static getType() {
    return "root";
  }
  static clone() {
    return new _yi();
  }
  constructor() {
    super("root"), this.__cachedText = null;
  }
  getTopLevelElementOrThrow() {
    t(51);
  }
  getTextContent() {
    const t2 = this.__cachedText;
    return !Vr() && Gr()._dirtyType !== y || null === t2 ? super.getTextContent() : t2;
  }
  remove() {
    t(52);
  }
  replace(e2) {
    t(53);
  }
  insertBefore(e2) {
    t(54);
  }
  insertAfter(e2) {
    t(55);
  }
  updateDOM(t2, e2) {
    return false;
  }
  splice(e2, n2, r2) {
    for (const e3 of r2) hi(e3) || pi(e3) || t(282);
    return super.splice(e2, n2, r2);
  }
  static importJSON(t2) {
    return Ss().updateFromJSON(t2);
  }
  collapseAtStart() {
    return true;
  }
};
function mi(t2) {
  return t2 instanceof yi;
}
function xi(t2) {
  return new vi(new Map(t2._nodeMap));
}
function Ci() {
  return new vi(/* @__PURE__ */ new Map([["root", new yi()]]));
}
function Si(e2) {
  const n2 = e2.exportJSON(), r2 = e2.constructor;
  if (n2.type !== r2.getType() && t(130, r2.name), hi(e2)) {
    const i2 = n2.children;
    Array.isArray(i2) || t(59, r2.name);
    const s2 = e2.getChildren();
    for (let t2 = 0; t2 < s2.length; t2++) {
      const e3 = Si(s2[t2]);
      i2.push(e3);
    }
  }
  return n2;
}
var vi = class _vi {
  constructor(t2, e2) {
    this._nodeMap = t2, this._selection = e2 || null, this._flushSync = false, this._readOnly = false;
  }
  isEmpty() {
    return 1 === this._nodeMap.size && null === this._selection;
  }
  read(t2, e2) {
    return ii(e2 && e2.editor || null, this, t2);
  }
  clone(t2) {
    const e2 = new _vi(this._nodeMap, void 0 === t2 ? this._selection : t2);
    return e2._readOnly = true, e2;
  }
  toJSON() {
    return ii(null, this, (() => ({ root: Si(Ss()) })));
  }
};
var Ni = "history-merge";
var wi = "collaboration";
var Oi = "skip-scroll-into-view";
var Mi = "skip-dom-selection";
var Pi = class _Pi extends di {
  static getType() {
    return "paragraph";
  }
  static clone(t2) {
    return new _Pi(t2.__key);
  }
  createDOM(t2) {
    const e2 = document.createElement("p"), n2 = Rs(t2.theme, "paragraph");
    if (void 0 !== n2) {
      e2.classList.add(...n2);
    }
    return e2;
  }
  updateDOM(t2, e2, n2) {
    return false;
  }
  static importDOM() {
    return { p: (t2) => ({ conversion: Di, priority: 0 }) };
  }
  exportDOM(t2) {
    const { element: e2 } = super.exportDOM(t2);
    if (yo(e2)) {
      this.isEmpty() && e2.append(document.createElement("br"));
      const t3 = this.getFormatType();
      t3 && (e2.style.textAlign = t3);
    }
    return { element: e2 };
  }
  static importJSON(t2) {
    return Fi().updateFromJSON(t2);
  }
  exportJSON() {
    return { ...super.exportJSON(), textFormat: this.getTextFormat(), textStyle: this.getTextStyle() };
  }
  insertNewAfter(t2, e2) {
    const n2 = Fi();
    n2.setTextFormat(t2.format), n2.setTextStyle(t2.style);
    const r2 = this.getDirection();
    return n2.setDirection(r2), n2.setFormat(this.getFormatType()), n2.setStyle(this.getStyle()), this.insertAfter(n2, e2), n2;
  }
  collapseAtStart() {
    const t2 = this.getChildren();
    if (0 === t2.length || Zn(t2[0]) && "" === t2[0].getTextContent().trim()) {
      if (null !== this.getNextSibling()) return this.selectNext(), this.remove(), true;
      if (null !== this.getPreviousSibling()) return this.selectPrevious(), this.remove(), true;
    }
    return false;
  }
};
function Di(t2) {
  const e2 = Fi();
  return t2.style && (e2.setFormat(t2.style.textAlign), Oo(t2, e2)), { node: e2 };
}
function Fi() {
  return lo(new Pi());
}
function Li(t2) {
  return t2 instanceof Pi;
}
function Wi(t2, e2, n2, r2) {
  const i2 = t2._keyToDOMMap;
  i2.clear(), t2._editorState = Ci(), t2._pendingEditorState = r2, t2._compositionKey = null, t2._dirtyType = y, t2._cloneNotNeeded.clear(), t2._dirtyLeaves = /* @__PURE__ */ new Set(), t2._dirtyElements.clear(), t2._normalizedNodes = /* @__PURE__ */ new Set(), t2._updateTags = /* @__PURE__ */ new Set(), t2._updates = [], t2._blockCursorElement = null;
  const s2 = t2._observer;
  null !== s2 && (s2.disconnect(), t2._observer = null), null !== e2 && (e2.textContent = ""), null !== n2 && (n2.textContent = "", i2.set("root", n2));
}
var $i = class {
  constructor(t2, e2, n2, r2, i2, s2, o2, l2) {
    this._createEditorArgs = l2, this._parentEditor = e2, this._rootElement = null, this._editorState = t2, this._pendingEditorState = null, this._compositionKey = null, this._deferred = [], this._keyToDOMMap = /* @__PURE__ */ new Map(), this._updates = [], this._updating = false, this._listeners = { decorator: /* @__PURE__ */ new Set(), editable: /* @__PURE__ */ new Set(), mutation: /* @__PURE__ */ new Map(), root: /* @__PURE__ */ new Set(), textcontent: /* @__PURE__ */ new Set(), update: /* @__PURE__ */ new Set() }, this._commands = /* @__PURE__ */ new Map(), this._config = r2, this._nodes = n2, this._decorators = {}, this._pendingDecorators = null, this._dirtyType = y, this._cloneNotNeeded = /* @__PURE__ */ new Set(), this._dirtyLeaves = /* @__PURE__ */ new Set(), this._dirtyElements = /* @__PURE__ */ new Map(), this._normalizedNodes = /* @__PURE__ */ new Set(), this._updateTags = /* @__PURE__ */ new Set(), this._observer = null, this._key = ws(), this._onError = i2, this._htmlConversions = s2, this._editable = o2, this._headless = null !== e2 && e2._headless, this._window = null, this._blockCursorElement = null;
  }
  isComposing() {
    return null != this._compositionKey;
  }
  registerUpdateListener(t2) {
    const e2 = this._listeners.update;
    return e2.add(t2), () => {
      e2.delete(t2);
    };
  }
  registerEditableListener(t2) {
    const e2 = this._listeners.editable;
    return e2.add(t2), () => {
      e2.delete(t2);
    };
  }
  registerDecoratorListener(t2) {
    const e2 = this._listeners.decorator;
    return e2.add(t2), () => {
      e2.delete(t2);
    };
  }
  registerTextContentListener(t2) {
    const e2 = this._listeners.textcontent;
    return e2.add(t2), () => {
      e2.delete(t2);
    };
  }
  registerRootListener(t2) {
    const e2 = this._listeners.root;
    return t2(this._rootElement, null), e2.add(t2), () => {
      t2(null, this._rootElement), e2.delete(t2);
    };
  }
  registerCommand(e2, n2, r2) {
    void 0 === r2 && t(35);
    const i2 = this._commands;
    i2.has(e2) || i2.set(e2, [/* @__PURE__ */ new Set(), /* @__PURE__ */ new Set(), /* @__PURE__ */ new Set(), /* @__PURE__ */ new Set(), /* @__PURE__ */ new Set()]);
    const s2 = i2.get(e2);
    void 0 === s2 && t(36, String(e2));
    const o2 = s2[r2];
    return o2.add(n2), () => {
      o2.delete(n2), s2.every(((t2) => 0 === t2.size)) && i2.delete(e2);
    };
  }
  registerMutationListener(t2, e2, n2) {
    const r2 = this.resolveRegisteredNodeAfterReplacements(this.getRegisteredNode(t2)).klass, i2 = this._listeners.mutation;
    let s2 = i2.get(e2);
    void 0 === s2 && (s2 = /* @__PURE__ */ new Set(), i2.set(e2, s2)), s2.add(r2);
    const o2 = n2 && n2.skipInitialization;
    return void 0 !== o2 && o2 || this.initializeMutationListener(e2, r2), () => {
      s2.delete(r2), 0 === s2.size && i2.delete(e2);
    };
  }
  getRegisteredNode(e2) {
    const n2 = this._nodes.get(e2.getType());
    return void 0 === n2 && t(37, e2.name), n2;
  }
  resolveRegisteredNodeAfterReplacements(t2) {
    for (; t2.replaceWithKlass; ) t2 = this.getRegisteredNode(t2.replaceWithKlass);
    return t2;
  }
  initializeMutationListener(t2, e2) {
    const n2 = this._editorState, r2 = wo(n2).get(e2.getType());
    if (!r2) return;
    const i2 = /* @__PURE__ */ new Map();
    for (const t3 of r2.keys()) i2.set(t3, "created");
    i2.size > 0 && t2(i2, { dirtyLeaves: /* @__PURE__ */ new Set(), prevEditorState: n2, updateTags: /* @__PURE__ */ new Set(["registerMutationListener"]) });
  }
  registerNodeTransformToKlass(t2, e2) {
    const n2 = this.getRegisteredNode(t2);
    return n2.transforms.add(e2), n2;
  }
  registerNodeTransform(t2, e2) {
    const n2 = this.registerNodeTransformToKlass(t2, e2), r2 = [n2], i2 = n2.replaceWithKlass;
    if (null != i2) {
      const t3 = this.registerNodeTransformToKlass(i2, e2);
      r2.push(t3);
    }
    return (function(t3, e3) {
      const n3 = wo(t3.getEditorState()), r3 = [];
      for (const t4 of e3) {
        const e4 = n3.get(t4);
        e4 && r3.push(e4);
      }
      if (0 === r3.length) return;
      t3.update((() => {
        for (const t4 of r3) for (const e4 of t4.keys()) {
          const t5 = _s(e4);
          t5 && t5.markDirty();
        }
      }), null === t3._pendingEditorState ? { tag: Ni } : void 0);
    })(this, r2.map(((t3) => t3.klass.getType()))), () => {
      r2.forEach(((t3) => t3.transforms.delete(e2)));
    };
  }
  hasNode(t2) {
    return this._nodes.has(t2.getType());
  }
  hasNodes(t2) {
    return t2.every(this.hasNode.bind(this));
  }
  dispatchCommand(t2, e2) {
    return Vs(this, t2, e2);
  }
  getDecorators() {
    return this._decorators;
  }
  getRootElement() {
    return this._rootElement;
  }
  getKey() {
    return this._key;
  }
  setRootElement(t2) {
    const e2 = this._rootElement;
    if (t2 !== e2) {
      const n2 = Rs(this._config.theme, "root"), r2 = this._pendingEditorState || this._editorState;
      if (this._rootElement = t2, Wi(this, e2, t2, r2), null !== e2 && (this._config.disableEvents || En(e2), null != n2 && e2.classList.remove(...n2)), null !== t2) {
        const e3 = eo(t2), r3 = t2.style;
        r3.userSelect = "text", r3.whiteSpace = "pre-wrap", r3.wordBreak = "break-word", t2.setAttribute("data-lexical-editor", "true"), this._window = e3, this._dirtyType = x, ot(this), this._updateTags.add(Ni), si(this), this._config.disableEvents || (function(t3, e4) {
          const n3 = t3.ownerDocument, r4 = on.get(n3);
          (void 0 === r4 || r4 < 1) && n3.addEventListener("selectionchange", Tn), on.set(n3, (r4 || 0) + 1), t3.__lexicalEditor = e4;
          const i2 = vn(t3);
          for (let n4 = 0; n4 < tn.length; n4++) {
            const [r5, s2] = tn[n4], o2 = "function" == typeof s2 ? (t4) => {
              bn(t4) || (Nn(t4), (e4.isEditable() || "click" === r5) && s2(t4, e4));
            } : (t4) => {
              if (bn(t4)) return;
              Nn(t4);
              const n5 = e4.isEditable();
              switch (r5) {
                case "cut":
                  return n5 && Vs(e4, $e, t4);
                case "copy":
                  return Vs(e4, Je, t4);
                case "paste":
                  return n5 && Vs(e4, ge, t4);
                case "dragstart":
                  return n5 && Vs(e4, Be, t4);
                case "dragover":
                  return n5 && Vs(e4, Re, t4);
                case "dragend":
                  return n5 && Vs(e4, We, t4);
                case "focus":
                  return n5 && Vs(e4, qe, t4);
                case "blur":
                  return n5 && Vs(e4, Ge, t4);
                case "drop":
                  return n5 && Vs(e4, Ke, t4);
              }
            };
            t3.addEventListener(r5, o2), i2.push((() => {
              t3.removeEventListener(r5, o2);
            }));
          }
        })(t2, this), null != n2 && t2.classList.add(...n2);
      } else this._window = null, this._updateTags.add(Ni), si(this);
      oi("root", this, false, t2, e2);
    }
  }
  getElementByKey(t2) {
    return this._keyToDOMMap.get(t2) || null;
  }
  getEditorState() {
    return this._editorState;
  }
  setEditorState(e2, n2) {
    e2.isEmpty() && t(38);
    let r2 = e2;
    r2._readOnly && (r2 = xi(e2), r2._selection = e2._selection ? e2._selection.clone() : null), st(this);
    const i2 = this._pendingEditorState, s2 = this._updateTags, o2 = void 0 !== n2 ? n2.tag : null;
    null === i2 || i2.isEmpty() || (null != o2 && s2.add(o2), si(this)), this._pendingEditorState = r2, this._dirtyType = x, this._dirtyElements.set("root", false), this._compositionKey = null, null != o2 && s2.add(o2), this._updating || si(this);
  }
  parseEditorState(t2, e2) {
    return (function(t3, e3, n2) {
      const r2 = Ci(), i2 = Rr, s2 = Jr, o2 = Wr, l2 = e3._dirtyElements, c2 = e3._dirtyLeaves, a2 = e3._cloneNotNeeded, u2 = e3._dirtyType;
      e3._dirtyElements = /* @__PURE__ */ new Map(), e3._dirtyLeaves = /* @__PURE__ */ new Set(), e3._cloneNotNeeded = /* @__PURE__ */ new Set(), e3._dirtyType = 0, Rr = r2, Jr = false, Wr = e3, Ui(null);
      try {
        const i3 = e3._nodes;
        ri(t3.root, i3), n2 && n2(), r2._readOnly = true;
      } catch (t4) {
        t4 instanceof Error && e3._onError(t4);
      } finally {
        e3._dirtyElements = l2, e3._dirtyLeaves = c2, e3._cloneNotNeeded = a2, e3._dirtyType = u2, Rr = i2, Jr = s2, Wr = o2;
      }
      return r2;
    })("string" == typeof t2 ? JSON.parse(t2) : t2, this, e2);
  }
  read(t2) {
    return si(this), this.getEditorState().read(t2, { editor: this });
  }
  update(t2, e2) {
    !(function(t3, e3, n2) {
      t3._updating ? t3._updates.push([e3, n2]) : ci(t3, e3, n2);
    })(this, t2, e2);
  }
  focus(t2, e2 = {}) {
    const n2 = this._rootElement;
    null !== n2 && (n2.setAttribute("autocapitalize", "off"), ai(this, (() => {
      const r2 = wr(), i2 = Ss();
      null !== r2 ? r2.dirty || ks(r2.clone()) : 0 !== i2.getChildrenSize() && ("rootStart" === e2.defaultSelection ? i2.selectStart() : i2.selectEnd()), Qs("focus"), Zs((() => {
        n2.removeAttribute("autocapitalize"), t2 && t2();
      }));
    })), null === this._pendingEditorState && n2.removeAttribute("autocapitalize"));
  }
  blur() {
    const t2 = this._rootElement;
    null !== t2 && t2.blur();
    const e2 = ho(this._window);
    null !== e2 && e2.removeAllRanges();
  }
  isEditable() {
    return this._editable;
  }
  setEditable(t2) {
    this._editable !== t2 && (this._editable = t2, oi("editable", this, true, t2));
  }
  toJSON() {
    return { editorState: this._editorState.toJSON() };
  }
};
$i.version = "0.35.0+prod.esm";
var ji = null;
function Ui(t2) {
  ji = t2;
}
var Vi = 1;
function Hi(e2, n2) {
  const r2 = qi(e2, n2);
  return void 0 === r2 && t(30, n2), r2;
}
function qi(t2, e2) {
  return t2._nodes.get(e2);
}
var Gi = "function" == typeof queueMicrotask ? queueMicrotask : (t2) => {
  Promise.resolve().then(t2);
};
function Xi(t2) {
  return pi(ms(t2));
}
function Qi(t2) {
  const e2 = document.activeElement;
  if (!yo(e2)) return false;
  const n2 = e2.nodeName;
  return pi(ms(t2)) && ("INPUT" === n2 || "TEXTAREA" === n2 || "true" === e2.contentEditable && null == ns(e2));
}
function Zi(t2, e2, n2) {
  const r2 = t2.getRootElement();
  try {
    return null !== r2 && r2.contains(e2) && r2.contains(n2) && null !== e2 && !Qi(e2) && es(e2) === t2;
  } catch (t3) {
    return false;
  }
}
function ts(t2) {
  return t2 instanceof $i;
}
function es(t2) {
  let e2 = t2;
  for (; null != e2; ) {
    const t3 = ns(e2);
    if (ts(t3)) return t3;
    e2 = qs(e2);
  }
  return null;
}
function ns(t2) {
  return t2 ? t2.__lexicalEditor : null;
}
function rs(t2) {
  return rr(t2) || t2.isToken();
}
function is(t2) {
  return rs(t2) || t2.isSegmented();
}
function ss(t2) {
  return mo(t2) && t2.nodeType === g;
}
function os(t2) {
  return mo(t2) && t2.nodeType === _;
}
function ls(t2) {
  let e2 = t2;
  for (; null != e2; ) {
    if (ss(e2)) return e2;
    e2 = e2.firstChild;
  }
  return null;
}
function cs(t2, e2, n2) {
  const r2 = W[e2];
  if (null !== n2 && (t2 & r2) == (n2 & r2)) return t2;
  let i2 = t2 ^ r2;
  return "subscript" === e2 ? i2 &= ~W.superscript : "superscript" === e2 ? i2 &= ~W.subscript : "lowercase" === e2 ? (i2 &= ~W.uppercase, i2 &= ~W.capitalize) : "uppercase" === e2 ? (i2 &= ~W.lowercase, i2 &= ~W.capitalize) : "capitalize" === e2 && (i2 &= ~W.lowercase, i2 &= ~W.uppercase), i2;
}
function us(t2, e2) {
  const n2 = (function() {
    const t3 = ji;
    return ji = null, t3;
  })();
  if (null != (e2 = e2 || n2 && n2.__key)) return void (t2.__key = e2);
  Yr(), Hr();
  const r2 = Gr(), i2 = qr(), s2 = "" + Vi++;
  i2._nodeMap.set(s2, t2), hi(t2) ? r2._dirtyElements.set(s2, true) : r2._dirtyLeaves.add(s2), r2._cloneNotNeeded.add(s2), r2._dirtyType = m, t2.__key = s2;
}
function fs(t2) {
  const e2 = t2.getParent();
  if (null !== e2) {
    const n2 = t2.getWritable(), r2 = e2.getWritable(), i2 = t2.getPreviousSibling(), s2 = t2.getNextSibling(), o2 = null !== s2 ? s2.__key : null, l2 = null !== i2 ? i2.__key : null, c2 = null !== i2 ? i2.getWritable() : null, a2 = null !== s2 ? s2.getWritable() : null;
    null === i2 && (r2.__first = o2), null === s2 && (r2.__last = l2), null !== c2 && (c2.__next = o2), null !== a2 && (a2.__prev = l2), n2.__prev = null, n2.__next = null, n2.__parent = null, r2.__size--;
  }
}
function ds(t2) {
  Hr();
  const e2 = t2.getLatest(), n2 = e2.__parent, r2 = qr(), i2 = Gr(), s2 = r2._nodeMap, o2 = i2._dirtyElements;
  null !== n2 && (function(t3, e3, n3) {
    let r3 = t3;
    for (; null !== r3; ) {
      if (n3.has(r3)) return;
      const t4 = e3.get(r3);
      if (void 0 === t4) break;
      n3.set(r3, false), r3 = t4.__parent;
    }
  })(n2, s2, o2);
  const l2 = e2.__key;
  i2._dirtyType = m, hi(t2) ? o2.set(l2, true) : i2._dirtyLeaves.add(l2);
}
function hs(t2) {
  Yr();
  const e2 = Gr(), n2 = e2._compositionKey;
  if (t2 !== n2) {
    if (e2._compositionKey = t2, null !== n2) {
      const t3 = _s(n2);
      null !== t3 && t3.getWritable();
    }
    if (null !== t2) {
      const e3 = _s(t2);
      null !== e3 && e3.getWritable();
    }
  }
}
function gs() {
  if (Vr()) return null;
  return Gr()._compositionKey;
}
function _s(t2, e2) {
  const n2 = (e2 || qr())._nodeMap.get(t2);
  return void 0 === n2 ? null : n2;
}
function ps(t2, e2) {
  const n2 = ys(t2, Gr());
  return void 0 !== n2 ? _s(n2, e2) : null;
}
function ys(t2, e2) {
  return t2[`__lexicalKey_${e2._key}`];
}
function ms(t2, e2) {
  let n2 = t2;
  for (; null != n2; ) {
    const t3 = ps(n2, e2);
    if (null !== t3) return t3;
    n2 = qs(n2);
  }
  return null;
}
function xs(t2) {
  const e2 = t2._decorators, n2 = Object.assign({}, e2);
  return t2._pendingDecorators = n2, n2;
}
function Cs(t2) {
  return t2.read((() => Ss().getTextContent()));
}
function Ss() {
  return vs(qr());
}
function vs(t2) {
  return t2._nodeMap.get("root");
}
function ks(t2) {
  Yr();
  const e2 = qr();
  null !== t2 && (t2.dirty = true, t2.setCachedNodes(null)), e2._selection = t2;
}
function Ts(t2) {
  const e2 = Gr(), n2 = (function(t3, e3) {
    let n3 = t3;
    for (; null != n3; ) {
      const t4 = ys(n3, e3);
      if (void 0 !== t4) return t4;
      n3 = qs(n3);
    }
    return null;
  })(t2, e2);
  if (null === n2) {
    return t2 === e2.getRootElement() ? _s("root") : null;
  }
  return _s(n2);
}
function Ns(t2) {
  return /[\uD800-\uDBFF][\uDC00-\uDFFF]/g.test(t2);
}
function bs(t2) {
  const e2 = [];
  let n2 = t2;
  for (; null !== n2; ) e2.push(n2), n2 = n2._parentEditor;
  return e2;
}
function ws() {
  return Math.random().toString(36).replace(/[^a-z]+/g, "").substring(0, 5);
}
function Es(t2) {
  return ss(t2) ? t2.nodeValue : null;
}
function Os(t2, e2, n2) {
  const r2 = ho(no(e2));
  if (null === r2) return;
  const i2 = r2.anchorNode;
  let { anchorOffset: s2, focusOffset: o2 } = r2;
  if (null !== i2) {
    let e3 = Es(i2);
    const r3 = ms(i2);
    if (null !== e3 && Zn(r3)) {
      if (e3 === z && n2) {
        const t3 = n2.length;
        e3 = n2, s2 = t3, o2 = t3;
      }
      null !== e3 && Ms(r3, e3, s2, o2, t2);
    }
  }
}
function Ms(t2, e2, n2, r2, i2) {
  let s2 = t2;
  if (s2.isAttached() && (i2 || !s2.isDirty())) {
    const c2 = s2.isComposing();
    let a2 = e2;
    (c2 || i2) && e2[e2.length - 1] === z && (a2 = e2.slice(0, -1));
    const u2 = s2.getTextContent();
    if (i2 || a2 !== u2) {
      if ("" === a2) {
        if (hs(null), o || l || f) s2.remove();
        else {
          const t3 = Gr();
          setTimeout((() => {
            t3.update((() => {
              s2.isAttached() && s2.remove();
            }));
          }), 20);
        }
        return;
      }
      const e3 = s2.getParent(), i3 = Er(), u3 = s2.getTextContentSize(), d2 = gs(), h2 = s2.getKey();
      if (s2.isToken() || null !== d2 && h2 === d2 && !c2 || ar(i3) && (null !== e3 && !e3.canInsertTextBefore() && 0 === i3.anchor.offset || i3.anchor.key === t2.__key && 0 === i3.anchor.offset && !s2.canInsertTextBefore() && !c2 || i3.focus.key === t2.__key && i3.focus.offset === u3 && !s2.canInsertTextAfter() && !c2)) return void s2.markDirty();
      const g2 = wr();
      if (!ar(g2) || null === n2 || null === r2) return void As(s2, a2, g2);
      if (g2.setTextNodeRange(s2, n2, s2, r2), s2.isSegmented()) {
        const t3 = Qn(s2.getTextContent());
        s2.replace(t3), s2 = t3;
      }
      As(s2, a2, g2);
    }
  }
}
function As(t2, e2, n2) {
  if (t2.setTextContent(e2), ar(n2)) {
    const e3 = t2.getKey();
    for (const r2 of ["anchor", "focus"]) {
      const i2 = n2[r2];
      "text" === i2.type && i2.key === e3 && (i2.offset = Qo(t2, i2.offset, "clamp"));
    }
  }
}
function Ps(t2, e2, n2) {
  const r2 = e2[n2] || false;
  return "any" === r2 || r2 === t2[n2];
}
function Ds(t2, e2) {
  return Ps(t2, e2, "altKey") && Ps(t2, e2, "ctrlKey") && Ps(t2, e2, "shiftKey") && Ps(t2, e2, "metaKey");
}
function Fs(t2, e2, n2) {
  return Ds(t2, n2) && t2.key.toLowerCase() === e2.toLowerCase();
}
var Ls = { ctrlKey: !r, metaKey: r };
var Is = { altKey: r, ctrlKey: !r };
function Ks(t2) {
  return "Backspace" === t2.key;
}
function zs(t2) {
  return Fs(t2, "a", Ls);
}
function Rs(t2, e2) {
  void 0 === t2.__lexicalClassNameCache && (t2.__lexicalClassNameCache = {});
  const n2 = t2.__lexicalClassNameCache, r2 = n2[e2];
  if (void 0 !== r2) return r2;
  const i2 = t2[e2];
  if ("string" == typeof i2) {
    const t3 = d(i2);
    return n2[e2] = t3, t3;
  }
  return i2;
}
function Ws(e2, n2, r2, i2, s2) {
  if (0 === r2.size) return;
  const o2 = i2.__type, l2 = i2.__key, c2 = n2.get(o2);
  void 0 === c2 && t(33, o2);
  const a2 = c2.klass;
  let u2 = e2.get(a2);
  void 0 === u2 && (u2 = /* @__PURE__ */ new Map(), e2.set(a2, u2));
  const f2 = u2.get(l2), d2 = "destroyed" === f2 && "created" === s2;
  (void 0 === f2 || d2) && u2.set(l2, d2 ? "updated" : s2);
}
function $s(t2, e2, n2) {
  const r2 = t2.getParent();
  let i2 = n2, s2 = t2;
  return null !== r2 && (e2 && 0 === n2 ? (i2 = s2.getIndexWithinParent(), s2 = r2) : e2 || n2 !== s2.getChildrenSize() || (i2 = s2.getIndexWithinParent() + 1, s2 = r2)), s2.getChildAtIndex(e2 ? i2 - 1 : i2);
}
function js(t2, e2) {
  const n2 = t2.offset;
  if ("element" === t2.type) {
    return $s(t2.getNode(), e2, n2);
  }
  {
    const r2 = t2.getNode();
    if (e2 && 0 === n2 || !e2 && n2 === r2.getTextContentSize()) {
      const t3 = e2 ? r2.getPreviousSibling() : r2.getNextSibling();
      return null === t3 ? $s(r2.getParentOrThrow(), e2, r2.getIndexWithinParent() + (e2 ? 0 : 1)) : t3;
    }
  }
  return null;
}
function Us(t2) {
  const e2 = no(t2).event, n2 = e2 && e2.inputType;
  return "insertFromPaste" === n2 || "insertFromPasteAsQuotation" === n2;
}
function Vs(t2, e2, n2) {
  return (function(t3, e3, n3) {
    const r2 = bs(t3);
    for (let i2 = 4; i2 >= 0; i2--) for (let s2 = 0; s2 < r2.length; s2++) {
      const o2 = r2[s2], l2 = o2._commands.get(e3);
      if (void 0 !== l2) {
        const e4 = l2[i2];
        if (void 0 !== e4) {
          const r3 = Array.from(e4), i3 = r3.length;
          let s3 = false;
          if (ai(o2, (() => {
            for (let e5 = 0; e5 < i3; e5++) if (r3[e5](n3, t3)) return void (s3 = true);
          })), s3) return s3;
        }
      }
    }
    return false;
  })(t2, e2, n2);
}
function Ys(t2) {
  return !mi(t2) && !t2.isLastChild() && !t2.isInline();
}
function Hs(e2, n2) {
  const r2 = e2._keyToDOMMap.get(n2);
  return void 0 === r2 && t(75, n2), r2;
}
function qs(t2) {
  const e2 = t2.assignedSlot || t2.parentElement;
  return xo(e2) ? e2.host : e2;
}
function Gs(t2) {
  return os(t2) ? t2 : yo(t2) ? t2.ownerDocument : null;
}
function Qs(t2) {
  Yr();
  Gr()._updateTags.add(t2);
}
function Zs(t2) {
  Yr();
  Gr()._deferred.push(t2);
}
function to(t2, e2) {
  let n2 = t2.getParent();
  for (; null !== n2; ) {
    if (n2.is(e2)) return true;
    n2 = n2.getParent();
  }
  return false;
}
function eo(t2) {
  const e2 = Gs(t2);
  return e2 ? e2.defaultView : null;
}
function no(e2) {
  const n2 = e2._window;
  return null === n2 && t(78), n2;
}
function io(t2) {
  let e2 = t2.getParentOrThrow();
  for (; null !== e2; ) {
    if (so(e2)) return e2;
    e2 = e2.getParentOrThrow();
  }
  return e2;
}
function so(t2) {
  return mi(t2) || hi(t2) && t2.isShadowRoot();
}
function lo(e2) {
  const n2 = Gr(), r2 = e2.getType(), i2 = qi(n2, r2);
  void 0 === i2 && t(200, e2.constructor.name, r2);
  const { replace: s2, replaceWithKlass: o2 } = i2;
  if (null !== s2) {
    const n3 = s2(e2), i3 = n3.constructor;
    return null !== o2 ? n3 instanceof o2 || t(201, o2.name, o2.getType(), i3.name, i3.getType(), e2.constructor.name, r2) : n3 instanceof e2.constructor && i3 !== e2.constructor || t(202, i3.name, i3.getType(), e2.constructor.name, r2), n3.__key === e2.__key && t(203, e2.constructor.name, r2, i3.name, i3.getType()), n3;
  }
  return e2;
}
function co(e2, n2) {
  !mi(e2.getParent()) || hi(n2) || pi(n2) || t(99);
}
function ao(e2) {
  const n2 = _s(e2);
  return null === n2 && t(63, e2), n2;
}
function uo(t2) {
  return (pi(t2) || hi(t2) && !t2.canBeEmpty()) && !t2.isInline();
}
function fo(t2, e2, n2) {
  n2.style.removeProperty("caret-color"), e2._blockCursorElement = null;
  const r2 = t2.parentElement;
  null !== r2 && r2.removeChild(t2);
}
function ho(t2) {
  return e ? (t2 || window).getSelection() : null;
}
function go(t2) {
  const e2 = eo(t2);
  return e2 ? e2.getSelection() : null;
}
function yo(t2) {
  return mo(t2) && t2.nodeType === h;
}
function mo(t2) {
  return "object" == typeof t2 && null !== t2 && "nodeType" in t2 && "number" == typeof t2.nodeType;
}
function xo(t2) {
  return mo(t2) && t2.nodeType === p;
}
function Co(t2) {
  const e2 = new RegExp(/^(a|abbr|acronym|b|cite|code|del|em|i|ins|kbd|label|mark|output|q|ruby|s|samp|span|strong|sub|sup|time|u|tt|var|#text)$/, "i");
  return null !== t2.nodeName.match(e2);
}
function So(t2) {
  const e2 = new RegExp(/^(address|article|aside|blockquote|canvas|dd|div|dl|dt|fieldset|figcaption|figure|footer|form|h1|h2|h3|h4|h5|h6|header|hr|li|main|nav|noscript|ol|p|pre|section|table|td|tfoot|ul|video)$/, "i");
  return null !== t2.nodeName.match(e2);
}
function vo(t2) {
  if (pi(t2) && !t2.isInline()) return true;
  if (!hi(t2) || so(t2)) return false;
  const e2 = t2.getFirstChild(), n2 = null === e2 || Ln(e2) || Zn(e2) || e2.isInline();
  return !t2.isInline() && false !== t2.canBeEmpty() && n2;
}
function ko(t2, e2) {
  let n2 = t2;
  for (; null !== n2 && null !== n2.getParent() && !e2(n2); ) n2 = n2.getParentOrThrow();
  return e2(n2) ? n2 : null;
}
function To() {
  return Gr();
}
var No = /* @__PURE__ */ new WeakMap();
var bo = /* @__PURE__ */ new Map();
function wo(e2) {
  if (!e2._readOnly && e2.isEmpty()) return bo;
  e2._readOnly || t(192);
  let n2 = No.get(e2);
  return n2 || (n2 = (function(t2) {
    const e3 = /* @__PURE__ */ new Map();
    for (const [n3, r2] of t2._nodeMap) {
      const t3 = r2.__type;
      let i2 = e3.get(t3);
      i2 || (i2 = /* @__PURE__ */ new Map(), e3.set(t3, i2)), i2.set(n3, r2);
    }
    return e3;
  })(e2), No.set(e2, n2)), n2;
}
function Eo(t2) {
  const e2 = t2.constructor.clone(t2);
  return e2.afterCloneFrom(t2), e2;
}
function Oo(t2, e2) {
  const n2 = parseInt(t2.style.paddingInlineStart, 10) || 0, r2 = Math.round(n2 / 40);
  e2.setIndent(r2);
}
function Ao(t2) {
  return true === t2.__lexicalUnmanaged;
}
function Po(t2, e2) {
  return (function(t3, e3) {
    return Object.prototype.hasOwnProperty.call(t3, e3);
  })(t2, e2) && t2[e2] !== An[e2];
}
function Do(t2) {
  const e2 = H in t2.prototype ? t2.prototype[H]() : void 0, n2 = /* @__PURE__ */ (function(t3) {
    return t3 === _i || t3 === di || t3 === An;
  })(t2), r2 = !n2 && Po(t2, "getType") ? t2.getType() : void 0;
  let i2, s2 = r2;
  if (e2) if (r2) i2 = e2[r2];
  else for (const [t3, n3] of Object.entries(e2)) s2 = t3, i2 = n3;
  if (!n2 && s2 && (Po(t2, "getType") || (t2.getType = () => s2), Po(t2, "clone") || (t2.clone = (e3) => (Ui(e3), new t2())), Po(t2, "importJSON") || (t2.importJSON = i2 && i2.$importJSON || ((e3) => new t2().updateFromJSON(e3))), !Po(t2, "importDOM") && i2)) {
    const { importDOM: e3 } = i2;
    e3 && (t2.importDOM = () => e3);
  }
  return { ownNodeConfig: i2, ownNodeType: s2 };
}
var Lo = { next: "previous", previous: "next" };
var Io = class {
  constructor(t2) {
    this.origin = t2;
  }
  [Symbol.iterator]() {
    return al({ hasNext: Uo, initial: this.getAdjacentCaret(), map: (t2) => t2, step: (t2) => t2.getAdjacentCaret() });
  }
  getAdjacentCaret() {
    return Go(this.getNodeAtCaret(), this.direction);
  }
  getSiblingCaret() {
    return Go(this.origin, this.direction);
  }
  remove() {
    const t2 = this.getNodeAtCaret();
    return t2 && t2.remove(), this;
  }
  replaceOrInsert(t2, e2) {
    const n2 = this.getNodeAtCaret();
    return t2.is(this.origin) || t2.is(n2) || (null === n2 ? this.insert(t2) : n2.replace(t2, e2)), this;
  }
  splice(e2, n2, r2 = "next") {
    const i2 = r2 === this.direction ? n2 : Array.from(n2).reverse();
    let s2 = this;
    const o2 = this.getParentAtCaret(), l2 = /* @__PURE__ */ new Map();
    for (let t2 = s2.getAdjacentCaret(); null !== t2 && l2.size < e2; t2 = t2.getAdjacentCaret()) {
      const e3 = t2.origin.getWritable();
      l2.set(e3.getKey(), e3);
    }
    for (const e3 of i2) {
      if (l2.size > 0) {
        const n3 = s2.getNodeAtCaret();
        if (n3) if (l2.delete(n3.getKey()), l2.delete(e3.getKey()), n3.is(e3) || s2.origin.is(e3)) ;
        else {
          const t2 = e3.getParent();
          t2 && t2.is(o2) && e3.remove(), n3.replace(e3);
        }
        else null === n3 && t(263, Array.from(l2).join(" "));
      } else s2.insert(e3);
      s2 = Go(e3, this.direction);
    }
    for (const t2 of l2.values()) t2.remove();
    return this;
  }
};
var Ko = class _Ko extends Io {
  constructor() {
    super(...arguments);
    __publicField(this, "type", "child");
  }
  getLatest() {
    const t2 = this.origin.getLatest();
    return t2 === this.origin ? this : tl(t2, this.direction);
  }
  getParentCaret(t2 = "root") {
    return Go(Ro(this.getParentAtCaret(), t2), this.direction);
  }
  getFlipped() {
    const t2 = Bo(this.direction);
    return Go(this.getNodeAtCaret(), t2) || tl(this.origin, t2);
  }
  getParentAtCaret() {
    return this.origin;
  }
  getChildCaret() {
    return this;
  }
  isSameNodeCaret(t2) {
    return t2 instanceof _Ko && this.direction === t2.direction && this.origin.is(t2.origin);
  }
  isSamePointCaret(t2) {
    return this.isSameNodeCaret(t2);
  }
};
var zo = { root: mi, shadowRoot: so };
function Bo(t2) {
  return Lo[t2];
}
function Ro(t2, e2 = "root") {
  return zo[e2](t2) ? null : t2;
}
var Wo = class _Wo extends Io {
  constructor() {
    super(...arguments);
    __publicField(this, "type", "sibling");
  }
  getLatest() {
    const t2 = this.origin.getLatest();
    return t2 === this.origin ? this : Go(t2, this.direction);
  }
  getSiblingCaret() {
    return this;
  }
  getParentAtCaret() {
    return this.origin.getParent();
  }
  getChildCaret() {
    return hi(this.origin) ? tl(this.origin, this.direction) : null;
  }
  getParentCaret(t2 = "root") {
    return Go(Ro(this.getParentAtCaret(), t2), this.direction);
  }
  getFlipped() {
    const t2 = Bo(this.direction);
    return Go(this.getNodeAtCaret(), t2) || tl(this.origin.getParentOrThrow(), t2);
  }
  isSamePointCaret(t2) {
    return t2 instanceof _Wo && this.direction === t2.direction && this.origin.is(t2.origin);
  }
  isSameNodeCaret(t2) {
    return (t2 instanceof _Wo || t2 instanceof Jo) && this.direction === t2.direction && this.origin.is(t2.origin);
  }
};
var Jo = class _Jo extends Io {
  constructor(t2, e2) {
    super(t2);
    __publicField(this, "type", "text");
    this.offset = e2;
  }
  getLatest() {
    const t2 = this.origin.getLatest();
    return t2 === this.origin ? this : Xo(t2, this.direction, this.offset);
  }
  getParentAtCaret() {
    return this.origin.getParent();
  }
  getChildCaret() {
    return null;
  }
  getParentCaret(t2 = "root") {
    return Go(Ro(this.getParentAtCaret(), t2), this.direction);
  }
  getFlipped() {
    return Xo(this.origin, Bo(this.direction), this.offset);
  }
  isSamePointCaret(t2) {
    return t2 instanceof _Jo && this.direction === t2.direction && this.origin.is(t2.origin) && this.offset === t2.offset;
  }
  isSameNodeCaret(t2) {
    return (t2 instanceof Wo || t2 instanceof _Jo) && this.direction === t2.direction && this.origin.is(t2.origin);
  }
  getSiblingCaret() {
    return Go(this.origin, this.direction);
  }
};
function $o(t2) {
  return t2 instanceof Jo;
}
function Uo(t2) {
  return t2 instanceof Wo;
}
function Vo(t2) {
  return t2 instanceof Ko;
}
var Yo = { next: class extends Jo {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "next");
  }
  getNodeAtCaret() {
    return this.origin.getNextSibling();
  }
  insert(t2) {
    return this.origin.insertAfter(t2), this;
  }
}, previous: class extends Jo {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "previous");
  }
  getNodeAtCaret() {
    return this.origin.getPreviousSibling();
  }
  insert(t2) {
    return this.origin.insertBefore(t2), this;
  }
} };
var Ho = { next: class extends Wo {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "next");
  }
  getNodeAtCaret() {
    return this.origin.getNextSibling();
  }
  insert(t2) {
    return this.origin.insertAfter(t2), this;
  }
}, previous: class extends Wo {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "previous");
  }
  getNodeAtCaret() {
    return this.origin.getPreviousSibling();
  }
  insert(t2) {
    return this.origin.insertBefore(t2), this;
  }
} };
var qo = { next: class extends Ko {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "next");
  }
  getNodeAtCaret() {
    return this.origin.getFirstChild();
  }
  insert(t2) {
    return this.origin.splice(0, 0, [t2]), this;
  }
}, previous: class extends Ko {
  constructor() {
    super(...arguments);
    __publicField(this, "direction", "previous");
  }
  getNodeAtCaret() {
    return this.origin.getLastChild();
  }
  insert(t2) {
    return this.origin.splice(this.origin.getChildrenSize(), 0, [t2]), this;
  }
} };
function Go(t2, e2) {
  return t2 ? new Ho[e2](t2) : null;
}
function Xo(t2, e2, n2) {
  return t2 ? new Yo[e2](t2, Qo(t2, n2)) : null;
}
function Qo(t2, e2, n2 = "error") {
  const r2 = t2.getTextContentSize();
  let i2 = "next" === e2 ? r2 : "previous" === e2 ? 0 : e2;
  return (i2 < 0 || i2 > r2) && ("clamp" !== n2 && (function(t3, ...e3) {
    const n3 = new URL("https://lexical.dev/docs/error"), r3 = new URLSearchParams();
    r3.append("code", t3);
    for (const t4 of e3) r3.append("v", t4);
    n3.search = r3.toString(), console.warn(`Minified Lexical warning #${t3}; visit ${n3.toString()} for the full message or use the non-minified dev environment for full errors and additional helpful warnings.`);
  })(284, String(e2), String(r2), t2.getKey()), i2 = i2 < 0 ? 0 : r2), i2;
}
function Zo(t2, e2) {
  return new il(t2, e2);
}
function tl(t2, e2) {
  return hi(t2) ? new qo[e2](t2) : null;
}
function el(t2) {
  return t2 && t2.getChildCaret() || t2;
}
function nl(t2) {
  return t2 && el(t2.getAdjacentCaret());
}
var rl = class _rl {
  constructor(t2, e2, n2) {
    __publicField(this, "type", "node-caret-range");
    this.anchor = t2, this.focus = e2, this.direction = n2;
  }
  getLatest() {
    const t2 = this.anchor.getLatest(), e2 = this.focus.getLatest();
    return t2 === this.anchor && e2 === this.focus ? this : new _rl(t2, e2, this.direction);
  }
  isCollapsed() {
    return this.anchor.isSamePointCaret(this.focus);
  }
  getTextSlices() {
    const t2 = (t3) => {
      const e3 = this[t3].getLatest();
      return $o(e3) ? (function(t4, e4) {
        const { direction: n3, origin: r2 } = t4, i2 = Qo(r2, "focus" === e4 ? Bo(n3) : n3);
        return Zo(t4, i2 - t4.offset);
      })(e3, t3) : null;
    }, e2 = t2("anchor"), n2 = t2("focus");
    if (e2 && n2) {
      const { caret: t3 } = e2, { caret: r2 } = n2;
      if (t3.isSameNodeCaret(r2)) return [Zo(t3, r2.offset - t3.offset), null];
    }
    return [e2, n2];
  }
  iterNodeCarets(t2 = "root") {
    const e2 = $o(this.anchor) ? this.anchor.getSiblingCaret() : this.anchor.getLatest(), n2 = this.focus.getLatest(), r2 = $o(n2), i2 = (e3) => e3.isSameNodeCaret(n2) ? null : nl(e3) || e3.getParentCaret(t2);
    return al({ hasNext: (t3) => null !== t3 && !(r2 && n2.isSameNodeCaret(t3)), initial: e2.isSameNodeCaret(n2) ? null : i2(e2), map: (t3) => t3, step: i2 });
  }
  [Symbol.iterator]() {
    return this.iterNodeCarets("root");
  }
};
var il = class {
  constructor(t2, e2) {
    __publicField(this, "type", "slice");
    this.caret = t2, this.distance = e2;
  }
  getSliceIndices() {
    const { distance: t2, caret: { offset: e2 } } = this, n2 = e2 + t2;
    return n2 < e2 ? [n2, e2] : [e2, n2];
  }
  getTextContent() {
    const [t2, e2] = this.getSliceIndices();
    return this.caret.origin.getTextContent().slice(t2, e2);
  }
  getTextContentSize() {
    return Math.abs(this.distance);
  }
  removeTextSlice() {
    const { caret: { origin: t2, direction: e2 } } = this, [n2, r2] = this.getSliceIndices(), i2 = t2.getTextContent();
    return Xo(t2.setTextContent(i2.slice(0, n2) + i2.slice(r2)), e2, n2);
  }
};
function ol(t2) {
  return cl(t2, Go(Ss(), t2.direction));
}
function ll(t2) {
  return cl(t2, t2);
}
function cl(e2, n2) {
  return e2.direction !== n2.direction && t(265), new rl(e2, n2, e2.direction);
}
function al(t2) {
  const { initial: e2, hasNext: n2, step: r2, map: i2 } = t2;
  let s2 = e2;
  return { [Symbol.iterator]() {
    return this;
  }, next() {
    if (!n2(s2)) return { done: true, value: void 0 };
    const t3 = { done: false, value: i2(s2) };
    return s2 = r2(s2), t3;
  } };
}
function ul(e2, n2) {
  const r2 = gl(e2.origin, n2.origin);
  switch (null === r2 && t(275, e2.origin.getKey(), n2.origin.getKey()), r2.type) {
    case "same": {
      const t2 = "text" === e2.type, r3 = "text" === n2.type;
      return t2 && r3 ? (function(t3, e3) {
        return Math.sign(t3 - e3);
      })(e2.offset, n2.offset) : e2.type === n2.type ? 0 : t2 ? -1 : r3 ? 1 : "child" === e2.type ? -1 : 1;
    }
    case "ancestor":
      return "child" === e2.type ? -1 : 1;
    case "descendant":
      return "child" === n2.type ? 1 : -1;
    case "branch":
      return fl(r2);
  }
}
function fl(t2) {
  const { a: e2, b: n2 } = t2, r2 = e2.__key, i2 = n2.__key;
  let s2 = e2, o2 = n2;
  for (; s2 && o2; s2 = s2.getNextSibling(), o2 = o2.getNextSibling()) {
    if (s2.__key === i2) return -1;
    if (o2.__key === r2) return 1;
  }
  return null === s2 ? 1 : -1;
}
function dl(t2, e2) {
  return e2.is(t2);
}
function hl(t2) {
  return hi(t2) ? [t2.getLatest(), null] : [t2.getParent(), t2.getLatest()];
}
function gl(e2, n2) {
  if (e2.is(n2)) return { commonAncestor: e2, type: "same" };
  const r2 = /* @__PURE__ */ new Map();
  for (let [t2, n3] = hl(e2); t2; n3 = t2, t2 = t2.getParent()) r2.set(t2, n3);
  for (let [i2, s2] = hl(n2); i2; s2 = i2, i2 = i2.getParent()) {
    const o2 = r2.get(i2);
    if (void 0 !== o2) return null === o2 ? (dl(e2, i2) || t(276), { commonAncestor: i2, type: "ancestor" }) : null === s2 ? (dl(n2, i2) || t(277), { commonAncestor: i2, type: "descendant" }) : ((hi(o2) || dl(e2, o2)) && (hi(s2) || dl(n2, s2)) && i2.is(o2.getParent()) && i2.is(s2.getParent()) || t(278), { a: o2, b: s2, commonAncestor: i2, type: "branch" });
  }
  return null;
}
function _l(e2, n2) {
  const { type: r2, key: i2, offset: s2 } = e2, o2 = ao(e2.key);
  return "text" === r2 ? (Zn(o2) || t(266, o2.getType(), i2), Xo(o2, n2, s2)) : (hi(o2) || t(267, o2.getType(), i2), El(o2, e2.offset, n2));
}
function pl(e2, n2) {
  const { origin: r2, direction: i2 } = n2, s2 = "next" === i2;
  $o(n2) ? e2.set(r2.getKey(), n2.offset, "text") : Uo(n2) ? Zn(r2) ? e2.set(r2.getKey(), Qo(r2, i2), "text") : e2.set(r2.getParentOrThrow().getKey(), r2.getIndexWithinParent() + (s2 ? 1 : 0), "element") : (Vo(n2) && hi(r2) || t(268), e2.set(r2.getKey(), s2 ? 0 : r2.getChildrenSize(), "element"));
}
function yl(t2) {
  const e2 = wr(), n2 = ar(e2) ? e2 : kr();
  return ml(n2, t2), ks(n2), n2;
}
function ml(t2, e2) {
  pl(t2.anchor, e2.anchor), pl(t2.focus, e2.focus);
}
function xl(t2) {
  const { anchor: e2, focus: n2 } = t2, r2 = _l(e2, "next"), i2 = _l(n2, "next"), s2 = ul(r2, i2) <= 0 ? "next" : "previous";
  return cl(bl(r2, s2), bl(i2, s2));
}
function Cl(t2) {
  const { direction: e2, origin: n2 } = t2, r2 = Go(n2, Bo(e2)).getNodeAtCaret();
  return r2 ? Go(r2, e2) : tl(n2.getParentOrThrow(), e2);
}
function Sl(t2, e2 = "root") {
  const n2 = [t2];
  for (let r2 = Vo(t2) ? t2.getParentCaret(e2) : t2.getSiblingCaret(); null !== r2; r2 = r2.getParentCaret(e2)) n2.push(Cl(r2));
  return n2;
}
function vl(t2) {
  return !!t2 && t2.origin.isAttached();
}
function kl(e2, n2 = "removeEmptySlices") {
  if (e2.isCollapsed()) return e2;
  const r2 = "root", i2 = "next";
  let s2 = n2;
  const o2 = wl(e2, i2), l2 = Sl(o2.anchor, r2), c2 = Sl(o2.focus.getFlipped(), r2), a2 = /* @__PURE__ */ new Set(), u2 = [];
  for (const t2 of o2.iterNodeCarets(r2)) if (Vo(t2)) a2.add(t2.origin.getKey());
  else if (Uo(t2)) {
    const { origin: e3 } = t2;
    hi(e3) && !a2.has(e3.getKey()) || u2.push(e3);
  }
  for (const t2 of u2) t2.remove();
  for (const t2 of o2.getTextSlices()) {
    if (!t2) continue;
    const { origin: e3 } = t2.caret, n3 = e3.getTextContentSize(), r3 = Cl(Go(e3, i2)), o3 = e3.getMode();
    if (Math.abs(t2.distance) === n3 && "removeEmptySlices" === s2 || "token" === o3 && 0 !== t2.distance) r3.remove();
    else if (0 !== t2.distance) {
      s2 = "removeEmptySlices";
      let e4 = t2.removeTextSlice();
      const n4 = t2.caret.origin;
      if ("segmented" === o3) {
        const t3 = e4.origin, n5 = Qn(t3.getTextContent()).setStyle(t3.getStyle()).setFormat(t3.getFormat());
        r3.replaceOrInsert(n5), e4 = Xo(n5, i2, e4.offset);
      }
      n4.is(l2[0].origin) && (l2[0] = e4), n4.is(c2[0].origin) && (c2[0] = e4.getFlipped());
    }
  }
  let f2, d2;
  for (const t2 of l2) if (vl(t2)) {
    f2 = Tl(t2);
    break;
  }
  for (const t2 of c2) if (vl(t2)) {
    d2 = Tl(t2);
    break;
  }
  const h2 = (function(t2, e3, n3) {
    if (!t2 || !e3) return null;
    const r3 = t2.getParentAtCaret(), i3 = e3.getParentAtCaret();
    if (!r3 || !i3) return null;
    const s3 = r3.getParents().reverse();
    s3.push(r3);
    const o3 = i3.getParents().reverse();
    o3.push(i3);
    const l3 = Math.min(s3.length, o3.length);
    let c3;
    for (c3 = 0; c3 < l3 && s3[c3] === o3[c3]; c3++) ;
    const a3 = (t3, e4) => {
      let n4;
      for (let r4 = c3; r4 < t3.length; r4++) {
        const i4 = t3[r4];
        if (so(i4)) return;
        !n4 && e4(i4) && (n4 = i4);
      }
      return n4;
    }, u3 = a3(s3, vo), f3 = u3 && a3(o3, ((t3) => n3.has(t3.getKey()) && vo(t3)));
    return u3 && f3 ? [u3, f3] : null;
  })(f2, d2, a2);
  if (h2) {
    const [t2, e3] = h2;
    tl(t2, "previous").splice(0, e3.getChildren()), e3.remove();
  }
  const g2 = [f2, d2, ...l2, ...c2].find(vl);
  if (g2) {
    return ll(bl(Tl(g2), e2.direction));
  }
  t(269, JSON.stringify(l2.map(((t2) => t2.origin.__key))));
}
function Tl(t2) {
  const e2 = (function(t3) {
    let e3 = t3;
    for (; Vo(e3); ) {
      const t4 = nl(e3);
      if (!Vo(t4)) break;
      e3 = t4;
    }
    return e3;
  })(t2.getLatest()), { direction: n2 } = e2;
  if (Zn(e2.origin)) return $o(e2) ? e2 : Xo(e2.origin, n2, n2);
  const r2 = e2.getAdjacentCaret();
  return Uo(r2) && Zn(r2.origin) ? Xo(r2.origin, n2, Bo(n2)) : e2;
}
function Nl(t2) {
  return $o(t2) && t2.offset !== Qo(t2.origin, t2.direction);
}
function bl(t2, e2) {
  return t2.direction === e2 ? t2 : t2.getFlipped();
}
function wl(t2, e2) {
  return t2.direction === e2 ? t2 : cl(bl(t2.focus, e2), bl(t2.anchor, e2));
}
function El(t2, e2, n2) {
  let r2 = tl(t2, "next");
  for (let t3 = 0; t3 < e2; t3++) {
    const t4 = r2.getAdjacentCaret();
    if (null === t4) break;
    r2 = t4;
  }
  return bl(r2, n2);
}

// ../node_modules/.pnpm/lexical@0.35.0/node_modules/lexical/Lexical.mjs
var mod = true ? Lexical_dev_exports : Lexical_prod_exports;
var $addUpdateTag2 = mod.$addUpdateTag;
var $applyNodeReplacement2 = mod.$applyNodeReplacement;
var $caretFromPoint2 = mod.$caretFromPoint;
var $caretRangeFromSelection2 = mod.$caretRangeFromSelection;
var $cloneWithProperties2 = mod.$cloneWithProperties;
var $comparePointCaretNext2 = mod.$comparePointCaretNext;
var $copyNode2 = mod.$copyNode;
var $create2 = mod.$create;
var $createLineBreakNode2 = mod.$createLineBreakNode;
var $createNodeSelection2 = mod.$createNodeSelection;
var $createParagraphNode2 = mod.$createParagraphNode;
var $createPoint2 = mod.$createPoint;
var $createRangeSelection2 = mod.$createRangeSelection;
var $createRangeSelectionFromDom2 = mod.$createRangeSelectionFromDom;
var $createTabNode2 = mod.$createTabNode;
var $createTextNode2 = mod.$createTextNode;
var $extendCaretToRange2 = mod.$extendCaretToRange;
var $getAdjacentChildCaret2 = mod.$getAdjacentChildCaret;
var $getAdjacentNode2 = mod.$getAdjacentNode;
var $getAdjacentSiblingOrParentSiblingCaret2 = mod.$getAdjacentSiblingOrParentSiblingCaret;
var $getCaretInDirection2 = mod.$getCaretInDirection;
var $getCaretRange2 = mod.$getCaretRange;
var $getCaretRangeInDirection2 = mod.$getCaretRangeInDirection;
var $getCharacterOffsets2 = mod.$getCharacterOffsets;
var $getChildCaret2 = mod.$getChildCaret;
var $getChildCaretAtIndex2 = mod.$getChildCaretAtIndex;
var $getChildCaretOrSelf2 = mod.$getChildCaretOrSelf;
var $getCollapsedCaretRange2 = mod.$getCollapsedCaretRange;
var $getCommonAncestor2 = mod.$getCommonAncestor;
var $getCommonAncestorResultBranchOrder2 = mod.$getCommonAncestorResultBranchOrder;
var $getEditor2 = mod.$getEditor;
var $getNearestNodeFromDOMNode2 = mod.$getNearestNodeFromDOMNode;
var $getNearestRootOrShadowRoot2 = mod.$getNearestRootOrShadowRoot;
var $getNodeByKey2 = mod.$getNodeByKey;
var $getNodeByKeyOrThrow2 = mod.$getNodeByKeyOrThrow;
var $getPreviousSelection2 = mod.$getPreviousSelection;
var $getRoot2 = mod.$getRoot;
var $getSelection2 = mod.$getSelection;
var $getSiblingCaret2 = mod.$getSiblingCaret;
var $getState2 = mod.$getState;
var $getStateChange2 = mod.$getStateChange;
var $getTextContent2 = mod.$getTextContent;
var $getTextNodeOffset2 = mod.$getTextNodeOffset;
var $getTextPointCaret2 = mod.$getTextPointCaret;
var $getTextPointCaretSlice2 = mod.$getTextPointCaretSlice;
var $getWritableNodeState2 = mod.$getWritableNodeState;
var $hasAncestor2 = mod.$hasAncestor;
var $hasUpdateTag2 = mod.$hasUpdateTag;
var $insertNodes2 = mod.$insertNodes;
var $isBlockElementNode2 = mod.$isBlockElementNode;
var $isChildCaret2 = mod.$isChildCaret;
var $isDecoratorNode2 = mod.$isDecoratorNode;
var $isElementNode2 = mod.$isElementNode;
var $isExtendableTextPointCaret2 = mod.$isExtendableTextPointCaret;
var $isInlineElementOrDecoratorNode2 = mod.$isInlineElementOrDecoratorNode;
var $isLeafNode2 = mod.$isLeafNode;
var $isLineBreakNode2 = mod.$isLineBreakNode;
var $isNodeCaret2 = mod.$isNodeCaret;
var $isNodeSelection2 = mod.$isNodeSelection;
var $isParagraphNode2 = mod.$isParagraphNode;
var $isRangeSelection2 = mod.$isRangeSelection;
var $isRootNode2 = mod.$isRootNode;
var $isRootOrShadowRoot2 = mod.$isRootOrShadowRoot;
var $isSiblingCaret2 = mod.$isSiblingCaret;
var $isTabNode2 = mod.$isTabNode;
var $isTextNode2 = mod.$isTextNode;
var $isTextPointCaret2 = mod.$isTextPointCaret;
var $isTextPointCaretSlice2 = mod.$isTextPointCaretSlice;
var $isTokenOrSegmented2 = mod.$isTokenOrSegmented;
var $isTokenOrTab2 = mod.$isTokenOrTab;
var $nodesOfType2 = mod.$nodesOfType;
var $normalizeCaret2 = mod.$normalizeCaret;
var $normalizeSelection__EXPERIMENTAL = mod.$normalizeSelection__EXPERIMENTAL;
var $onUpdate2 = mod.$onUpdate;
var $parseSerializedNode2 = mod.$parseSerializedNode;
var $removeTextFromCaretRange2 = mod.$removeTextFromCaretRange;
var $rewindSiblingCaret2 = mod.$rewindSiblingCaret;
var $selectAll2 = mod.$selectAll;
var $setCompositionKey2 = mod.$setCompositionKey;
var $setPointFromCaret2 = mod.$setPointFromCaret;
var $setSelection2 = mod.$setSelection;
var $setSelectionFromCaretRange2 = mod.$setSelectionFromCaretRange;
var $setState2 = mod.$setState;
var $splitAtPointCaretNext2 = mod.$splitAtPointCaretNext;
var $splitNode2 = mod.$splitNode;
var $updateRangeSelectionFromCaretRange2 = mod.$updateRangeSelectionFromCaretRange;
var ArtificialNode__DO_NOT_USE2 = mod.ArtificialNode__DO_NOT_USE;
var BLUR_COMMAND2 = mod.BLUR_COMMAND;
var CAN_REDO_COMMAND2 = mod.CAN_REDO_COMMAND;
var CAN_UNDO_COMMAND2 = mod.CAN_UNDO_COMMAND;
var CLEAR_EDITOR_COMMAND2 = mod.CLEAR_EDITOR_COMMAND;
var CLEAR_HISTORY_COMMAND2 = mod.CLEAR_HISTORY_COMMAND;
var CLICK_COMMAND2 = mod.CLICK_COMMAND;
var COLLABORATION_TAG2 = mod.COLLABORATION_TAG;
var COMMAND_PRIORITY_CRITICAL2 = mod.COMMAND_PRIORITY_CRITICAL;
var COMMAND_PRIORITY_EDITOR2 = mod.COMMAND_PRIORITY_EDITOR;
var COMMAND_PRIORITY_HIGH2 = mod.COMMAND_PRIORITY_HIGH;
var COMMAND_PRIORITY_LOW2 = mod.COMMAND_PRIORITY_LOW;
var COMMAND_PRIORITY_NORMAL2 = mod.COMMAND_PRIORITY_NORMAL;
var CONTROLLED_TEXT_INSERTION_COMMAND2 = mod.CONTROLLED_TEXT_INSERTION_COMMAND;
var COPY_COMMAND2 = mod.COPY_COMMAND;
var CUT_COMMAND2 = mod.CUT_COMMAND;
var DELETE_CHARACTER_COMMAND2 = mod.DELETE_CHARACTER_COMMAND;
var DELETE_LINE_COMMAND2 = mod.DELETE_LINE_COMMAND;
var DELETE_WORD_COMMAND2 = mod.DELETE_WORD_COMMAND;
var DRAGEND_COMMAND2 = mod.DRAGEND_COMMAND;
var DRAGOVER_COMMAND2 = mod.DRAGOVER_COMMAND;
var DRAGSTART_COMMAND2 = mod.DRAGSTART_COMMAND;
var DROP_COMMAND2 = mod.DROP_COMMAND;
var DecoratorNode2 = mod.DecoratorNode;
var ElementNode2 = mod.ElementNode;
var FOCUS_COMMAND2 = mod.FOCUS_COMMAND;
var FORMAT_ELEMENT_COMMAND2 = mod.FORMAT_ELEMENT_COMMAND;
var FORMAT_TEXT_COMMAND2 = mod.FORMAT_TEXT_COMMAND;
var HISTORIC_TAG2 = mod.HISTORIC_TAG;
var HISTORY_MERGE_TAG2 = mod.HISTORY_MERGE_TAG;
var HISTORY_PUSH_TAG2 = mod.HISTORY_PUSH_TAG;
var INDENT_CONTENT_COMMAND2 = mod.INDENT_CONTENT_COMMAND;
var INSERT_LINE_BREAK_COMMAND2 = mod.INSERT_LINE_BREAK_COMMAND;
var INSERT_PARAGRAPH_COMMAND2 = mod.INSERT_PARAGRAPH_COMMAND;
var INSERT_TAB_COMMAND2 = mod.INSERT_TAB_COMMAND;
var INTERNAL_$isBlock2 = mod.INTERNAL_$isBlock;
var IS_ALL_FORMATTING2 = mod.IS_ALL_FORMATTING;
var IS_BOLD2 = mod.IS_BOLD;
var IS_CODE2 = mod.IS_CODE;
var IS_HIGHLIGHT2 = mod.IS_HIGHLIGHT;
var IS_ITALIC2 = mod.IS_ITALIC;
var IS_STRIKETHROUGH2 = mod.IS_STRIKETHROUGH;
var IS_SUBSCRIPT2 = mod.IS_SUBSCRIPT;
var IS_SUPERSCRIPT2 = mod.IS_SUPERSCRIPT;
var IS_UNDERLINE2 = mod.IS_UNDERLINE;
var KEY_ARROW_DOWN_COMMAND2 = mod.KEY_ARROW_DOWN_COMMAND;
var KEY_ARROW_LEFT_COMMAND2 = mod.KEY_ARROW_LEFT_COMMAND;
var KEY_ARROW_RIGHT_COMMAND2 = mod.KEY_ARROW_RIGHT_COMMAND;
var KEY_ARROW_UP_COMMAND2 = mod.KEY_ARROW_UP_COMMAND;
var KEY_BACKSPACE_COMMAND2 = mod.KEY_BACKSPACE_COMMAND;
var KEY_DELETE_COMMAND2 = mod.KEY_DELETE_COMMAND;
var KEY_DOWN_COMMAND2 = mod.KEY_DOWN_COMMAND;
var KEY_ENTER_COMMAND2 = mod.KEY_ENTER_COMMAND;
var KEY_ESCAPE_COMMAND2 = mod.KEY_ESCAPE_COMMAND;
var KEY_MODIFIER_COMMAND2 = mod.KEY_MODIFIER_COMMAND;
var KEY_SPACE_COMMAND2 = mod.KEY_SPACE_COMMAND;
var KEY_TAB_COMMAND2 = mod.KEY_TAB_COMMAND;
var LineBreakNode2 = mod.LineBreakNode;
var MOVE_TO_END2 = mod.MOVE_TO_END;
var MOVE_TO_START2 = mod.MOVE_TO_START;
var NODE_STATE_KEY2 = mod.NODE_STATE_KEY;
var OUTDENT_CONTENT_COMMAND2 = mod.OUTDENT_CONTENT_COMMAND;
var PASTE_COMMAND2 = mod.PASTE_COMMAND;
var PASTE_TAG2 = mod.PASTE_TAG;
var ParagraphNode2 = mod.ParagraphNode;
var REDO_COMMAND2 = mod.REDO_COMMAND;
var REMOVE_TEXT_COMMAND2 = mod.REMOVE_TEXT_COMMAND;
var RootNode2 = mod.RootNode;
var SELECTION_CHANGE_COMMAND2 = mod.SELECTION_CHANGE_COMMAND;
var SELECTION_INSERT_CLIPBOARD_NODES_COMMAND2 = mod.SELECTION_INSERT_CLIPBOARD_NODES_COMMAND;
var SELECT_ALL_COMMAND2 = mod.SELECT_ALL_COMMAND;
var SKIP_COLLAB_TAG2 = mod.SKIP_COLLAB_TAG;
var SKIP_DOM_SELECTION_TAG2 = mod.SKIP_DOM_SELECTION_TAG;
var SKIP_SCROLL_INTO_VIEW_TAG2 = mod.SKIP_SCROLL_INTO_VIEW_TAG;
var TEXT_TYPE_TO_FORMAT2 = mod.TEXT_TYPE_TO_FORMAT;
var TabNode2 = mod.TabNode;
var TextNode2 = mod.TextNode;
var UNDO_COMMAND2 = mod.UNDO_COMMAND;
var buildImportMap2 = mod.buildImportMap;
var createCommand2 = mod.createCommand;
var createEditor2 = mod.createEditor;
var createSharedNodeState2 = mod.createSharedNodeState;
var createState2 = mod.createState;
var flipDirection2 = mod.flipDirection;
var getDOMOwnerDocument2 = mod.getDOMOwnerDocument;
var getDOMSelection2 = mod.getDOMSelection;
var getDOMSelectionFromTarget2 = mod.getDOMSelectionFromTarget;
var getDOMTextNode2 = mod.getDOMTextNode;
var getEditorPropertyFromDOMNode2 = mod.getEditorPropertyFromDOMNode;
var getNearestEditorFromDOMNode2 = mod.getNearestEditorFromDOMNode;
var getRegisteredNode2 = mod.getRegisteredNode;
var getRegisteredNodeOrThrow2 = mod.getRegisteredNodeOrThrow;
var getStaticNodeConfig2 = mod.getStaticNodeConfig;
var isBlockDomNode2 = mod.isBlockDomNode;
var isCurrentlyReadOnlyMode2 = mod.isCurrentlyReadOnlyMode;
var isDOMDocumentNode2 = mod.isDOMDocumentNode;
var isDOMNode2 = mod.isDOMNode;
var isDOMTextNode2 = mod.isDOMTextNode;
var isDOMUnmanaged2 = mod.isDOMUnmanaged;
var isDocumentFragment2 = mod.isDocumentFragment;
var isExactShortcutMatch2 = mod.isExactShortcutMatch;
var isHTMLAnchorElement2 = mod.isHTMLAnchorElement;
var isHTMLElement2 = mod.isHTMLElement;
var isInlineDomNode2 = mod.isInlineDomNode;
var isLexicalEditor2 = mod.isLexicalEditor;
var isModifierMatch2 = mod.isModifierMatch;
var isSelectionCapturedInDecoratorInput2 = mod.isSelectionCapturedInDecoratorInput;
var isSelectionWithinEditor2 = mod.isSelectionWithinEditor;
var makeStepwiseIterator2 = mod.makeStepwiseIterator;
var removeFromParent2 = mod.removeFromParent;
var resetRandomKey2 = mod.resetRandomKey;
var setDOMUnmanaged2 = mod.setDOMUnmanaged;
var setNodeIndentFromDOM2 = mod.setNodeIndentFromDOM;

export {
  $addUpdateTag2 as $addUpdateTag,
  $applyNodeReplacement2 as $applyNodeReplacement,
  $caretFromPoint2 as $caretFromPoint,
  $caretRangeFromSelection2 as $caretRangeFromSelection,
  $cloneWithProperties2 as $cloneWithProperties,
  $comparePointCaretNext2 as $comparePointCaretNext,
  $copyNode2 as $copyNode,
  $create2 as $create,
  $createLineBreakNode2 as $createLineBreakNode,
  $createNodeSelection2 as $createNodeSelection,
  $createParagraphNode2 as $createParagraphNode,
  $createPoint2 as $createPoint,
  $createRangeSelection2 as $createRangeSelection,
  $createRangeSelectionFromDom2 as $createRangeSelectionFromDom,
  $createTabNode2 as $createTabNode,
  $createTextNode2 as $createTextNode,
  $extendCaretToRange2 as $extendCaretToRange,
  $getAdjacentChildCaret2 as $getAdjacentChildCaret,
  $getAdjacentNode2 as $getAdjacentNode,
  $getAdjacentSiblingOrParentSiblingCaret2 as $getAdjacentSiblingOrParentSiblingCaret,
  $getCaretInDirection2 as $getCaretInDirection,
  $getCaretRange2 as $getCaretRange,
  $getCaretRangeInDirection2 as $getCaretRangeInDirection,
  $getCharacterOffsets2 as $getCharacterOffsets,
  $getChildCaret2 as $getChildCaret,
  $getChildCaretAtIndex2 as $getChildCaretAtIndex,
  $getChildCaretOrSelf2 as $getChildCaretOrSelf,
  $getCollapsedCaretRange2 as $getCollapsedCaretRange,
  $getCommonAncestor2 as $getCommonAncestor,
  $getCommonAncestorResultBranchOrder2 as $getCommonAncestorResultBranchOrder,
  $getEditor2 as $getEditor,
  $getNearestNodeFromDOMNode2 as $getNearestNodeFromDOMNode,
  $getNearestRootOrShadowRoot2 as $getNearestRootOrShadowRoot,
  $getNodeByKey2 as $getNodeByKey,
  $getNodeByKeyOrThrow2 as $getNodeByKeyOrThrow,
  $getPreviousSelection2 as $getPreviousSelection,
  $getRoot2 as $getRoot,
  $getSelection2 as $getSelection,
  $getSiblingCaret2 as $getSiblingCaret,
  $getState2 as $getState,
  $getStateChange2 as $getStateChange,
  $getTextContent2 as $getTextContent,
  $getTextNodeOffset2 as $getTextNodeOffset,
  $getTextPointCaret2 as $getTextPointCaret,
  $getTextPointCaretSlice2 as $getTextPointCaretSlice,
  $getWritableNodeState2 as $getWritableNodeState,
  $hasAncestor2 as $hasAncestor,
  $hasUpdateTag2 as $hasUpdateTag,
  $insertNodes2 as $insertNodes,
  $isBlockElementNode2 as $isBlockElementNode,
  $isChildCaret2 as $isChildCaret,
  $isDecoratorNode2 as $isDecoratorNode,
  $isElementNode2 as $isElementNode,
  $isExtendableTextPointCaret2 as $isExtendableTextPointCaret,
  $isInlineElementOrDecoratorNode2 as $isInlineElementOrDecoratorNode,
  $isLeafNode2 as $isLeafNode,
  $isLineBreakNode2 as $isLineBreakNode,
  $isNodeCaret2 as $isNodeCaret,
  $isNodeSelection2 as $isNodeSelection,
  $isParagraphNode2 as $isParagraphNode,
  $isRangeSelection2 as $isRangeSelection,
  $isRootNode2 as $isRootNode,
  $isRootOrShadowRoot2 as $isRootOrShadowRoot,
  $isSiblingCaret2 as $isSiblingCaret,
  $isTabNode2 as $isTabNode,
  $isTextNode2 as $isTextNode,
  $isTextPointCaret2 as $isTextPointCaret,
  $isTextPointCaretSlice2 as $isTextPointCaretSlice,
  $isTokenOrSegmented2 as $isTokenOrSegmented,
  $isTokenOrTab2 as $isTokenOrTab,
  $nodesOfType2 as $nodesOfType,
  $normalizeCaret2 as $normalizeCaret,
  $normalizeSelection__EXPERIMENTAL,
  $onUpdate2 as $onUpdate,
  $parseSerializedNode2 as $parseSerializedNode,
  $removeTextFromCaretRange2 as $removeTextFromCaretRange,
  $rewindSiblingCaret2 as $rewindSiblingCaret,
  $selectAll2 as $selectAll,
  $setCompositionKey2 as $setCompositionKey,
  $setPointFromCaret2 as $setPointFromCaret,
  $setSelection2 as $setSelection,
  $setSelectionFromCaretRange2 as $setSelectionFromCaretRange,
  $setState2 as $setState,
  $splitAtPointCaretNext2 as $splitAtPointCaretNext,
  $splitNode2 as $splitNode,
  $updateRangeSelectionFromCaretRange2 as $updateRangeSelectionFromCaretRange,
  ArtificialNode__DO_NOT_USE2 as ArtificialNode__DO_NOT_USE,
  BLUR_COMMAND2 as BLUR_COMMAND,
  CAN_REDO_COMMAND2 as CAN_REDO_COMMAND,
  CAN_UNDO_COMMAND2 as CAN_UNDO_COMMAND,
  CLEAR_EDITOR_COMMAND2 as CLEAR_EDITOR_COMMAND,
  CLEAR_HISTORY_COMMAND2 as CLEAR_HISTORY_COMMAND,
  CLICK_COMMAND2 as CLICK_COMMAND,
  COLLABORATION_TAG2 as COLLABORATION_TAG,
  COMMAND_PRIORITY_CRITICAL2 as COMMAND_PRIORITY_CRITICAL,
  COMMAND_PRIORITY_EDITOR2 as COMMAND_PRIORITY_EDITOR,
  COMMAND_PRIORITY_HIGH2 as COMMAND_PRIORITY_HIGH,
  COMMAND_PRIORITY_LOW2 as COMMAND_PRIORITY_LOW,
  COMMAND_PRIORITY_NORMAL2 as COMMAND_PRIORITY_NORMAL,
  CONTROLLED_TEXT_INSERTION_COMMAND2 as CONTROLLED_TEXT_INSERTION_COMMAND,
  COPY_COMMAND2 as COPY_COMMAND,
  CUT_COMMAND2 as CUT_COMMAND,
  DELETE_CHARACTER_COMMAND2 as DELETE_CHARACTER_COMMAND,
  DELETE_LINE_COMMAND2 as DELETE_LINE_COMMAND,
  DELETE_WORD_COMMAND2 as DELETE_WORD_COMMAND,
  DRAGEND_COMMAND2 as DRAGEND_COMMAND,
  DRAGOVER_COMMAND2 as DRAGOVER_COMMAND,
  DRAGSTART_COMMAND2 as DRAGSTART_COMMAND,
  DROP_COMMAND2 as DROP_COMMAND,
  DecoratorNode2 as DecoratorNode,
  ElementNode2 as ElementNode,
  FOCUS_COMMAND2 as FOCUS_COMMAND,
  FORMAT_ELEMENT_COMMAND2 as FORMAT_ELEMENT_COMMAND,
  FORMAT_TEXT_COMMAND2 as FORMAT_TEXT_COMMAND,
  HISTORIC_TAG2 as HISTORIC_TAG,
  HISTORY_MERGE_TAG2 as HISTORY_MERGE_TAG,
  HISTORY_PUSH_TAG2 as HISTORY_PUSH_TAG,
  INDENT_CONTENT_COMMAND2 as INDENT_CONTENT_COMMAND,
  INSERT_LINE_BREAK_COMMAND2 as INSERT_LINE_BREAK_COMMAND,
  INSERT_PARAGRAPH_COMMAND2 as INSERT_PARAGRAPH_COMMAND,
  INSERT_TAB_COMMAND2 as INSERT_TAB_COMMAND,
  INTERNAL_$isBlock2 as INTERNAL_$isBlock,
  IS_ALL_FORMATTING2 as IS_ALL_FORMATTING,
  IS_BOLD2 as IS_BOLD,
  IS_CODE2 as IS_CODE,
  IS_HIGHLIGHT2 as IS_HIGHLIGHT,
  IS_ITALIC2 as IS_ITALIC,
  IS_STRIKETHROUGH2 as IS_STRIKETHROUGH,
  IS_SUBSCRIPT2 as IS_SUBSCRIPT,
  IS_SUPERSCRIPT2 as IS_SUPERSCRIPT,
  IS_UNDERLINE2 as IS_UNDERLINE,
  KEY_ARROW_DOWN_COMMAND2 as KEY_ARROW_DOWN_COMMAND,
  KEY_ARROW_LEFT_COMMAND2 as KEY_ARROW_LEFT_COMMAND,
  KEY_ARROW_RIGHT_COMMAND2 as KEY_ARROW_RIGHT_COMMAND,
  KEY_ARROW_UP_COMMAND2 as KEY_ARROW_UP_COMMAND,
  KEY_BACKSPACE_COMMAND2 as KEY_BACKSPACE_COMMAND,
  KEY_DELETE_COMMAND2 as KEY_DELETE_COMMAND,
  KEY_DOWN_COMMAND2 as KEY_DOWN_COMMAND,
  KEY_ENTER_COMMAND2 as KEY_ENTER_COMMAND,
  KEY_ESCAPE_COMMAND2 as KEY_ESCAPE_COMMAND,
  KEY_MODIFIER_COMMAND2 as KEY_MODIFIER_COMMAND,
  KEY_SPACE_COMMAND2 as KEY_SPACE_COMMAND,
  KEY_TAB_COMMAND2 as KEY_TAB_COMMAND,
  LineBreakNode2 as LineBreakNode,
  MOVE_TO_END2 as MOVE_TO_END,
  MOVE_TO_START2 as MOVE_TO_START,
  NODE_STATE_KEY2 as NODE_STATE_KEY,
  OUTDENT_CONTENT_COMMAND2 as OUTDENT_CONTENT_COMMAND,
  PASTE_COMMAND2 as PASTE_COMMAND,
  PASTE_TAG2 as PASTE_TAG,
  ParagraphNode2 as ParagraphNode,
  REDO_COMMAND2 as REDO_COMMAND,
  REMOVE_TEXT_COMMAND2 as REMOVE_TEXT_COMMAND,
  RootNode2 as RootNode,
  SELECTION_CHANGE_COMMAND2 as SELECTION_CHANGE_COMMAND,
  SELECTION_INSERT_CLIPBOARD_NODES_COMMAND2 as SELECTION_INSERT_CLIPBOARD_NODES_COMMAND,
  SELECT_ALL_COMMAND2 as SELECT_ALL_COMMAND,
  SKIP_COLLAB_TAG2 as SKIP_COLLAB_TAG,
  SKIP_DOM_SELECTION_TAG2 as SKIP_DOM_SELECTION_TAG,
  SKIP_SCROLL_INTO_VIEW_TAG2 as SKIP_SCROLL_INTO_VIEW_TAG,
  TEXT_TYPE_TO_FORMAT2 as TEXT_TYPE_TO_FORMAT,
  TabNode2 as TabNode,
  TextNode2 as TextNode,
  UNDO_COMMAND2 as UNDO_COMMAND,
  buildImportMap2 as buildImportMap,
  createCommand2 as createCommand,
  createEditor2 as createEditor,
  createSharedNodeState2 as createSharedNodeState,
  createState2 as createState,
  flipDirection2 as flipDirection,
  getDOMOwnerDocument2 as getDOMOwnerDocument,
  getDOMSelection2 as getDOMSelection,
  getDOMSelectionFromTarget2 as getDOMSelectionFromTarget,
  getDOMTextNode2 as getDOMTextNode,
  getEditorPropertyFromDOMNode2 as getEditorPropertyFromDOMNode,
  getNearestEditorFromDOMNode2 as getNearestEditorFromDOMNode,
  getRegisteredNode2 as getRegisteredNode,
  getRegisteredNodeOrThrow2 as getRegisteredNodeOrThrow,
  getStaticNodeConfig2 as getStaticNodeConfig,
  isBlockDomNode2 as isBlockDomNode,
  isCurrentlyReadOnlyMode2 as isCurrentlyReadOnlyMode,
  isDOMDocumentNode2 as isDOMDocumentNode,
  isDOMNode2 as isDOMNode,
  isDOMTextNode2 as isDOMTextNode,
  isDOMUnmanaged2 as isDOMUnmanaged,
  isDocumentFragment2 as isDocumentFragment,
  isExactShortcutMatch2 as isExactShortcutMatch,
  isHTMLAnchorElement2 as isHTMLAnchorElement,
  isHTMLElement2 as isHTMLElement,
  isInlineDomNode2 as isInlineDomNode,
  isLexicalEditor2 as isLexicalEditor,
  isModifierMatch2 as isModifierMatch,
  isSelectionCapturedInDecoratorInput2 as isSelectionCapturedInDecoratorInput,
  isSelectionWithinEditor2 as isSelectionWithinEditor,
  makeStepwiseIterator2 as makeStepwiseIterator,
  removeFromParent2 as removeFromParent,
  resetRandomKey2 as resetRandomKey,
  setDOMUnmanaged2 as setDOMUnmanaged,
  setNodeIndentFromDOM2 as setNodeIndentFromDOM,
  Lexical_exports
};
//# sourceMappingURL=chunk-IFCHFTA7.js.map
